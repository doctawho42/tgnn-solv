% THE ARTICLE'S SUBSET of si_tables/external_baselines_rows.tex, split off 2026-08-08.
% That file stays the full transcription and is what the Supporting Information prints
% (Table \ref{tab:si-baselines-full}); this one is the same rows with two blocks' worth
% removed so the article's copy can be set at \footnotesize instead of \scriptsize.
%
% WHAT IS NOT HERE, AND WHY EACH WENT.  Three referees and the supervisor all named this
% table's type size; at 8 pt it filled 82% of a float page carrying no body text, and the
% only way to 10 pt was to take rows out.
%   - the third block, six rows quoted AS PUBLISHED on other corpora, in mol/L, with a
%     dash in three of the four data columns.  No value in it is computed here and no
%     conversion of ours stands behind any of them, so a reader cannot check them against
%     anything else in the table anyway; the claim the article makes against them prints
%     their range in the sentence that makes it (\S\ref{sec:discussion}).
%   - the two RANDOM-PAIR rows.  They are a split control -- the same two arms on a split
%     where the solute is seen in training -- and the article makes no claim from them.
%   - the note block, 489 words in twenty lines, which is the whole of it in the SI copy.
% NO VALUE, NO ROW ORDER AND NO COLUMN IS CHANGED between the two files.  The one textual
% difference is on the surviving FastSolv row: the full file keys it \tnk{a} to the note
% that says what "re-scored" means, and with no note block here the word is set in the
% row instead.  If the numbers move, re-run the generator, re-apply the four typesetting
% edits listed in the full file, and re-cut this one from it.
% THE COLUMN HEADS ARE STACKED HERE AND NOT IN THE FULL FILE, and that is arithmetic rather
% than taste.  The two L{} boxes are fractions of \linewidth, so they do not shrink when the
% type does, while the four numeric columns are set to their widest cell and do.  At 8 pt the
% whole thing measured 485 pt inside a 502.6 pt full-width float; at 10 pt the same specifier
% measures 533 pt and printed 30 pt into the right margin (one Overfull \hbox).  Two heads were
% wider than any number under them -- `n (ln x2/log10 S)' by 10 pt and `RMSE log10 S' by 14 pt --
% so breaking all four over two lines returns 24 pt at no cost to any cell, and \tabcolsep 3 pt
% with the model column at 0.27 returns the rest with about 9 pt to spare.  Anything that widens
% a cell here must be re-measured against 502.6 pt.
\begin{tabular}{@{}L{0.27\linewidth}L{0.23\linewidth}rrrr@{}}
\toprule
Model & Test set & \shortstack[r]{$n$\\($\lnx$/$\log_{10}S$)} & \shortstack[r]{MAE\\$\lnx$} &
\shortstack[r]{RMSE\\$\log_{10}S$} & \shortstack[r]{$R^2$\\$\lnx$} \\
\midrule
\multicolumn{6}{@{}l@{}}{\emph{This work, solute-scaffold split; mean $\pm$ s.d. over seeds, three unless the row says otherwise}}\\
DirectGNN, same encoder, no closure \emph{(control)} & solute scaffold, this corpus & $5608$ / $5440$ & $1.70\pm0.03$ & $1.00\pm0.02$ & $0.42\pm0.03$ \\
TGNN, NRTL closure (physics) & solute scaffold, this corpus & $5608$ / $5440$ & $1.79\pm0.07$ & $1.54\pm0.07$ & $0.34\pm0.03$ \\
TGNN, COSMO-SAC closure, supervised $\hat\sigma$ (physics) & solute scaffold, this corpus & $5608$ / $5440$ & $1.85\pm0.05$ & $1.39\pm0.19$ & $0.33\pm0.05$ \\
TGNN, COSMO-SAC, reference $\sigma^\star$ substituted at evaluation & solute scaffold, this corpus & $5608$ / $5440$ & $2.25\pm0.02$ & $1.60\pm0.14$ & $-0.03\pm0.04$ \\
\addlinespace[2pt]
\multicolumn{6}{@{}l@{}}{\emph{Scale reference on the same rows}}\\
predict the test-set mean of $\lnx$ & solute scaffold, this corpus & $5608$ / $5440$ & $2.32$ & $1.33$ & $0.00$ \\
\addlinespace[2pt]
\multicolumn{6}{@{}l@{}}{\emph{External models re-run on this corpus, one run each---a different split, so not a ranking against the block above}}\\
FastSolv, retrained on this corpus, re-scored & by-solute, this corpus & $10287$ & $1.44$ & $0.84$ & $0.55$ \\
SolProp cycle, released model $+$ 3-parameter recalibration & by-solute, this corpus & $10287$ & $2.34$ & $1.34$ & $-0.18$ \\
SolProp cycle, released model, zero-shot & by-solute, this corpus & $10287$ & $2.36$ & $1.87$ & $-0.47$ \\
SolProp encoder retrained on $\lnx$, cycle discarded & by-solute, this corpus & $10570$ / $10287$ & $1.62$ & $1.07$ & $0.39$ \\
\bottomrule
\end{tabular}
