# Paper plan — "When Does Physics Help?"

_Generated by the `repo-to-paper` skill (Phases 0–2), reviewed at the gate 2026-07-03.
Decision: **complete the pending GPU experiments before writing the full paper.**_

## Venue
- **Target: Digital Discovery (RSC)** — Full paper, no page cap, mandatory Data
  Availability Statement, a dedicated **data reviewer reruns the code** (+2 scientific
  reviewers). Most receptive of the candidates to a rigorous, reproducible, honestly-
  scoped null/conditional result.
- Fallback: **Machine Learning: Science and Technology (MLST, IOP)** — same theory,
  pitched as an ML conceptual advance (harder novelty gate, no data-reviewer ethos).
- Rejected: **JCIM** — a "physics-tax / does-not-beat-the-black-box" headline risks
  desk-reject there (expects a demonstrated positive improvement).
- LaTeX: RSC `article` + RSC `.sty` (twocolumn, 9pt), `rsc.bst`.

## Framing
- **Title:** *When Does Physics Help? Conditional Optimality of Thermodynamic
  Decomposition for Solubility Prediction*
- Theory-first modified IMRAD. The accept-criterion contribution is the
  **identifiability + conditional-optimality theory (T1/T3)** and the reproducible
  diagnostics — **not** accuracy (which is null-leaning and its number is invalidated).
- Structure: Abstract → Intro (+Contributions) → Related Work → Problem Setup →
  **Theory (T1–T5 + A-optimal prop; load-bearing T1/T3/T4)** → Methods → Results
  (nulls flagged inline) → Discussion → Conclusion → Data Availability.

## Figures / tables
- **Fig1** architecture + SLE factorization schematic (concept)
- **Fig2** E1 identifiability: Fisher cond 1.7e3→2.3e5→1.9e16; CRLB sd(dHfus) 19,895→1,689 J/mol
- **Fig3** data-integrity twin: Tm 258 vs 45 K, solubility unchanged (1.835 vs 1.812), same Φ via different (Tm,dH)
- **Fig4** crystal-head parity on corrected labels: Tm MAE 45.0 K vs Joback 189 K
- **Fig5** defensible wins: ranking (Spearman 0.511, top-1 0.477) + temperature extrapolation (van't Hoff 0.368/R²0.887)
- **Fig6** split-conformal reliability: nominal 0.90 → empirical 0.918 (vs native PICP@90 0.127)
- **Tab1** headline controlled comparison (⚠ invalidated pre-bugfix checkpoint footnote)
- **Tab2** σ-grounding pilot (PRELIMINARY, 1 seed) · **Tab3/FigS1** noise-floor + encoder probe (SI)
- All figures: soft-pastel style (`repo-to-paper/assets/softpastel.mplstyle`).

## Blockers → experiment runway (must clear before submission)
GPU experiments (now feasible on Blackwell / ColabPro+):
1. **3-seed σ-grounding** (`run_e5`, already built + CUDA/num_workers-fixed this session; seed 42 cached → 43/44 ≈ 14 GPU-h). Resolves "σ-grounding = 1 seed". **Most ready.**
2. **Corrected-split accuracy re-run** — regen orphaned checkpoints on the +273K-corrected split; full-capacity TGNN + DirectGNN. Replaces the invalidated Tab1 numbers.
3. **E2 crystal grounding = the T3 test**, with a *proper* dose (prior E2 was ~0.46%/step homeopathic and mis-dosed). Headline conditional-optimality test.
4. **E3** (physics-vs-black-box temperature rows), **E4** (ablations), **data-efficiency curve** (the stated strongest scientific claim: physics vs black-box in the low-data regime).

Non-GPU fixes (anytime):
5. **Verify/replace 3 placeholder citations** — osanloo2024 (FastSolv), winter2022 (arxiv/2203.00000), sanchezmedina2023 (arxiv/2304.00000) — fabricated arXiv IDs.
6. **Repo git-hygiene** — main is a partial snapshot (~1900 untracked incl. `tests/conftest.py`); a fresh checkout fails the suite → DD data-reviewer can't rerun. Fix before submission.
7. **Rewrite E0 "learned compensation"** framing → accounting artifact (corr −0.962 reproduced by permutation null; superseded by `results/PAPER_PHASE0_FINDINGS.md`).

_Full structured plan (all evidence handles, 40+ claims): workflow output in the session task log._
