# Numbers this manuscript deliberately stopped printing.
#
# Format: VALUE  <one-line reason>   (write % exactly as the manuscript printed it)
# Read by scripts/analysis/check_number_conservation.py --allowlist.
# Every entry here was checked against the artifact it came from; a number that is
# gone for any other reason is a defect, not an entry.

73%    precision: 73% was the rounded companion-run top-two share, which results/compensation/isolation_gpu.json isolation.A1.top2_cum_evr gives as 0.72899, so the manuscript now prints 72.9%. HEAD already paired that 73% with a separate 72 +- 1% for the three seeds; the three-seed figure is unchanged in substance and now printed at the precision its own artifact carries, 71.8 +- 1.3% from results/sur/surrogate_seeds/surrogate_seeds.json aggregate.top2_evr (mean 0.7181873, sd 0.0126520, n=3), with the 72 +- 1% form still quoted beside it.
-2.25  corrected mis-rounding: results/b_insuff/fidelity_lever_fair2002.json representative.ci90_2010dsp = [-2.2366, +1.3057], so the dispersion arm's 90% margin is restored as [-2.24, +1.31]; HEAD's -2.25 matches the artifact under no rounding rule.
2.69   re-rounded down: results/b_insuff/regime.json bclosure_lb_binning for the m>1.5 stratum is 2.685, and the prose and Table 10's row now both print the conservative 2.68.
1.42   precision: the corner's separation threshold 2 x B_insuff^up is 1.4268, now printed as 1.427 against that set's residual-only total MSE of 1.472 (results/b_insuff/binning_conventions.json sets.corner_60.conventions.res.mse = 1.4722677), so the +0.05 margin can be checked from the two printed figures. The 1.472 is the MSE, not a bound on B_clos; the B_clos lower bound on those rows is 1.472 - 0.713 = 0.759, printed in the same sentence.
1.92   precision: the worst learned-sigma seed is 1.921, now printed at the three decimals of Table 6's per-seed triple 1.803/1.921/1.813 rather than rounded to 1.92.
6%     corrected: the random-fold correctability figure's uncertainty is a standard error over the five fit seeds (the convention the same paragraph states), and results/compensation/local_closure_fix_seeded_random.json ranks.1.reduction_pct_per_seed = [56.512, 28.462, 55.164, 41.754, 48.869] gives mean 46.152 with sd(ddof=1) 11.491, i.e. se 5.14. The manuscript now prints 46 +- 5%. The two grouped-design figures beside it, -5 +- 16% and +2 +- 15% (se 15.91 and 15.24), already matched and are unchanged.
0.089  corrected: the seed-42 logP cell of the representation-variance table printed the raw eta-squared statistic where the caption promises an excess over the permutation null. results/blackbox/study4_2026-07-26.json seeds.directgnn_seed42."logP(solute)" has stat 0.08914 and excess 0.08322, so the cell now prints +0.083. Its share-of-ceiling line below the table (13.6%) was already computed from the excess, 0.0832/0.6127.

# 2026-07-31 page-budget pass. The black-box probe paragraph's per-seed triple moved to App. J,
# which already prints the same three scores unrounded.
-0.51  rounded restatement, moved: the Discussion paragraph printed the seed-42 crystal-decode probe as -0.51, a rounding of the -0.506 that App. J, Table 15 (tab:crystal-decode) prints from results/blackbox/study4_2026-07-26.json together with its permutation p and jackknife range. The paragraph now carries the claim (the probe recovers the model's own prediction at R^2=0.98 but not the ideal-solubility term) and the bound the negative supports (R^2 <~ 0.30, restated in limitation (vi)), and points at App. J for the seed-by-seed scores and the nulls. The two companion values +0.11 and +0.03 still print elsewhere in the document; only the rounded -0.51 has no surviving occurrence, App. J's -0.514 being the RDKit-descriptor control and a different number.

# The four entries below are SUBSECTION NUMERALS, not measurements. The 2026-07-29 readability pass
# dissolved the separate "Notation, and how the claims are graded" section (criterion 7: the notation
# table moved to the end of the theory section, the grading rule to the head of the Results), which
# renumbered Results from 6 to 5. Every "\S6.x" cross-reference is therefore now "\S5.x": the checker
# keeps "6.1" as tier A because it carries a decimal point, and its cross-reference rule drops only
# unsigned integers after a structural noun. Each is confirmed to have had no non-cross-reference
# occurrence in the baseline, and the target prints the 5.x counterpart in the same sentences. If a
# later draft ever prints one of these values as a measurement, delete the corresponding line here.
6.1    section-number churn: the old \S6.1 (the grounding paradox) is now \S5.1; every baseline occurrence was a cross-reference, and the target prints 5.1 in the same sentences (baseline x12 -> target x0, gained 5.1).
6.2    section-number churn: the old \S6.2 (the closure-fidelity axis) is now \S5.2; every baseline occurrence was a cross-reference, and the target prints 5.2 in the same sentences (baseline x66 -> target x0, gained 5.2).
6.4    section-number churn: the old \S6.4 (the latent's departure from its reference) is now \S5.4; every baseline occurrence was a cross-reference, and the target prints 5.4 in the same sentences (baseline x35 -> target x0, gained 5.4).
6.5    section-number churn: the old \S6.5 (the gated output residual) is now \S5.5; every baseline occurrence was a cross-reference, and the target prints 5.5 in the same sentences (baseline x6 -> target x0, gained 5.5).

# The five entries below are the same kind of thing, one pass later. The 2026-08-01 structural pass
# folded Related work into the Introduction, so it stopped being Section 2 and everything after it
# moved down one: Setup and theory 3 -> 2, Methods 4 -> 3, Results 5 -> 4, Discussion 6 -> 5,
# Conclusion 7 -> 6. Every "\S3.x" and "\S5.x" cross-reference is therefore now "\S2.x" and "\S4.x".
# EACH WAS AUDITED, not assumed. Occurrences of these five values in the baseline PDF, counted with
# context: 3.2 -> 19 cross-references plus a subsection heading and the "3.2 x 10^6 J/mol" of the
# Fisher sweep; 3.3 -> 3 plus a heading; 5.2 -> 58 plus a heading; 5.5 -> 5 plus a heading and two
# "5.5%" enrichment figures; 5.6 -> 5 plus a heading and the "5.6 x 10^3" of the same sweep. The
# heading numerals are dropped by the section-number rule, the scientific-notation ones are folded
# into 3.2e6 and 5.6e3, and "5.5%" is a different value because % is part of the identity -- so the
# tier-A count the checker carries for each is exactly its cross-reference count, and the target
# prints the renumbered counterpart the same number of times (gained 2.2 +19, 2.3 +3, 4.2 +58,
# 4.5 +5, 4.6 +5). If a later draft prints one of these as a measurement, delete its line here.
3.2    section-number churn: the old \S3.2 (the closure--insufficiency decomposition) is now \S2.2; all 19 baseline occurrences were cross-references, and the target prints 2.2 in the same sentences (baseline x19 -> target x0, gained 2.2 x1 -> x20).
3.3    section-number churn: the old \S3.3 (what the instrument resolves) is now \S2.3; all 3 baseline occurrences were cross-references, and the target prints 2.3 in the same sentences (baseline x3 -> target x0, gained 2.3 x3 -> x6).
5.2    section-number churn: the old \S5.2 (the closure-fidelity axis) is now \S4.2; all 58 baseline occurrences were cross-references, and the target prints 4.2 in the same sentences (baseline x58 -> target x0, gained 4.2 x0 -> x58).
5.5    section-number churn: the old \S5.5 (the gated output residual) is now \S4.5; all 5 baseline occurrences were cross-references -- the 5.5% enrichment figures of App. H are a different value and still print -- and the target prints 4.5 in the same sentences (baseline x5 -> target x0, gained 4.5 x3 -> x8).
5.6    section-number churn: the old \S5.6 (the second chemical closure) is now \S4.6; all 5 baseline occurrences were cross-references, and the target prints 4.6 in the same sentences (baseline x5 -> target x0, gained 4.6 x0 -> x5).

# NOT ENTRIES, a note. The 2026-07-29 front-matter pass (criteria 2, 3, 9, 10) emptied the abstract,
# the introduction, the contributions and the conclusion of numbers, grades and modalities, and cut
# the grade/scope blocks out of the Figure 1, Figure 2 and Figure 4 captions and the cross-reference
# out of Table 3's. Every value it removed is still printed in the body, so the checker reports them
# all under section 2 ("this statement no longer prints it") and section 1 stays empty; none of them
# belongs in this allowlist, which is only for numbers gone from the document altogether. If a later
# pass makes one of 0.51, 0.99, 2.04, 1.85, 2.25, 1.80, 1.98, 0.05, 44, 60, 477, 185 or 90% show up
# in section 1, that is a real loss and not this pass's doing.

# 2026-07-29 Results cut. The per-arm hyperparameter triple left the Discussion for App. A
# (the plan's 90-word move); every one of its six values still prints in \S\ref{sec:si-methods}.
4.2    rendering, not a loss: the control's weight decay 4.2e-5 still prints in App. A ("weight decay 4.2 x 10^-5 against 5 x 10^-4"), but there pdftotext spaces the mantissa from the "x 10" so the checker folds it into the single number 4.2e-05 and the bare mantissa disappears. In the baseline the Discussion's copy of the same pair rendered without that space, so "4.2" was also counted on its own. The value is in the document, at the same precision, in the paragraph that now owns it; only the unfolded token is gone. ALSO, since the 2026-08-02 four-section reshape, section-number churn: the old \S4.2 (the closure-fidelity axis) is now \S3.2, so the 77 baseline tier-A occurrences of 4.2 are that pointer plus the weight-decay mantissa above, and the target prints 3.2 in the same sentences.

# 2026-08-02 four-section reshape (Introduction / Computational methods / Results and discussion /
# Conclusions). The old Sec. 4 (Results) became Sec. 3 and the old Sec. 5 (Discussion) folded into
# it, so the subsection numerals move again, one link further down the chain the 2026-07-29 entries
# above record. Both values below are cross-reference churn only: every baseline occurrence is a
# "SS4.4"/"SS4.6" pointer, the section headings themselves are dropped by the section-number rule,
# and the target prints the renumbered counterpart in the same sentences. The checker cannot exclude
# them by its cross-reference rule because pdftotext emits "SS4.4" as ONE word, so the section sign
# is not the preceding word and "SS" is not alphabetic. Verified against the baseline PDF before
# adding: the 36 tier-A occurrences of 4.4 are 35 pointers plus one table cell pointer, and no
# measurement in either revision has either value (the separate token "4.4%" is a different key and
# still prints).
4.4    section-number churn: the old \S4.4 (the latent's departure from its reference) is now \S3.3; all 36 baseline occurrences were cross-references, and the target prints 3.3 in the same sentences (baseline x36 -> target x0).
4.6    section-number churn: the old \S4.6 (the second chemical closure, pKa) is now \S3.5; all 5 baseline occurrences were cross-references, and the target prints 3.5 in the same sentences (baseline x5 -> target x0).

# 2026-08-02 REBALANCE (editorial item A3). Two entries, and BOTH ARE THE CHECKER'S OWN
# BIBLIOGRAPHY RULE, not losses. The rebalance moved the article from 26 pages to 28, which moved
# the reference list from pp. 23-24 to pp. 26-27. The bibliography exclusion is a REGION rule -- a
# page qualifies when three or more numbered entries make up at least 40% of its blocks -- and the
# two identifiers below sat, in the baseline, on a page that did not qualify and so were counted as
# printed numbers; in the target they sit on a page that does qualify and are excluded. VERIFIED
# BEFORE ADDING, in the target PDF's own text: `pdftotext paper/grounding_paradox.pdf' prints
# "doi:10.26434/chemrxiv-2024-93qp3" and "arXiv:2606.03469" in the article's reference list, and the
# SI prints one of them too. Nothing left the submission; the exclusion boundary moved.
10.26434    checker artifact, not a loss: the fastsolv ChemRxiv preprint DOI is still printed in the article's reference list (entry for Attia et al.), on a page the bibliography-region rule now excludes because the list starts one page later.
2606.03469  checker artifact, not a loss: the arXiv identifier of Ref. ma2026 (Operator Learning under Model Misspecification) is still printed in the article's reference list, on a page the bibliography-region rule now excludes.

# 2026-08-04 TABLE 5 REPAIR. Two entries, both CORRECTED VALUES, each replaced in the same sentence
# by the figure its own deposited artifact carries. Neither is a deletion: the claim survives, the
# number it prints changed because the old one was wrong. Both are checkable in one command each.
1.35   corrected, arm-dependent exclusion removed: Table 5's log10 S RMSE for the sigma-oracle arm. The column was computed by converting each arm's ln x2 predictions unclipped, which sends every prediction at x2 = 1 to +inf; run_external_baseline_comparison.py's _metrics then dropped those rows, so the row set differed BY ARM -- 12/143 rows for the oracle, 195/119/136 for NRTL, 12/143/135 for COSMO-SAC-grounded, and 0/0/0 for the DirectGNN control, which never saturates. The dropped rows are each arm's most catastrophic predictions, so the defect flattered every physics arm and the control not at all. The generator now clips predictions at x2 = 0.999999 (clip_ln_x2_for_logS, the repo's own prediction-side convention) and scores the column on the arm-independent 5440 rows whose TRUE value converts, printing both n. results/external_baseline_comparison/summary.json tier1_this_work_scaffold_split.oracle.logS_mol_per_L.rmse.mean = 1.5663, so the oracle row now prints 1.57; the same repair moves NRTL 1.07 -> 1.54 and COSMO-SAC-grounded 1.08 -> 1.39 (both of those values still print elsewhere in the submission, so only 1.35 reaches this file) and leaves the control at 1.00. Every physics arm got worse and the control did not. READ THE PER-ARM DROPS OFF THE UNROUNDED VALUES, NOT OFF THIS LINE: the deposit's `_defect_unclipped_arm_dependent' gives 1.3548 -> 1.5663 for the oracle (0.21), 1.0786 -> 1.3871 for COSMO-SAC-grounded (0.31) and 1.0670 -> 1.5437 for NRTL (0.48), which is the "0.21 lower / 0.31 / 0.48" the Table 5 note now prints. Subtracting this entry's rounded 1.35 from the rounded 1.57 gives 0.22 and is a rounding artifact, not a second figure.
1.63   corrected mis-rounding: the SI's "External baselines and ablations" quoted the retrained-SolProp-encoder arm at MAE 1.63, against Table 5's 1.62 for the same arm -- one submission printing two values for one number. results/external_baselines/solute/solprop_native_contract_v2/summary.csv row `test' gives mae = 1.624290219372559, so 1.62 is right and 1.63 was the mis-rounding; the SI now prints 1.62. The same sentence also attached that arm's R^2 (0.3881, on n = 10570) to "the n=10,287 rows FastSolv is scored on"; 10,287 is logS_n_samples_finite, the rows its log10 S MAE of 0.76 is on, and the SI now says which figure is on which n.

# 2026-08-05 RETRACTION OF A LOAD-BEARING EXCLUSION. One entry, and it is a DELETION and not a
# correction: the claim itself is withdrawn, and Table 4 is weaker for it. The manuscript printed
# "excludes a cross-solvent reduction above about 30%" in Table 4, in the article's correctability
# paragraph and in the SI's. No interval in the submission supports it. The only candidate is the
# -5 +- 16% beside it, whose 2-s.e. upper end is -4.687 + 2*15.91 = +27, rounded to "about 30%";
# results/compensation/local_closure_fix_solvent_grouped.json ranks."1".reduction_pct_per_seed =
# [28.309, 15.255, 10.088, -61.604, -15.484] gives mean -4.687 and sd(ddof=1) 35.58, i.e. that
# +-16 is a standard error over five FIT SEEDS and prices optimiser noise alone. The same file's
# ranks."1".heldout_sd_over_folds = 1.0235 (MSE) against baseline_mse = 1.4723 puts the
# unpropagated held-out fold spread at 70% of the very quantity the reduction is measured against
# -- larger than the effect the bound purported to exclude -- and the SI already said in print that
# it "is not propagated into it". A bound cannot rest on an interval its own document calls
# understated, so the exclusion is withdrawn rather than widened: Table 4's cell and both prose
# passages now state the null and say why no upper bound follows. This costs the paper a
# quantitative claim and adds none.
30%    RETRACTED, not lost: "excludes a cross-solvent reduction above about 30%" (Table 4, the article's Sec. 3.2.4, the SI's correctability paragraph) was a 2-standard-error extrapolation of the -5 +- 16% printed beside it, and that +-16 is a standard error over five fit seeds only (local_closure_fix_solvent_grouped.json ranks."1".reduction_pct_per_seed -> mean -4.687, sd(ddof=1) 35.58, se 15.91). The held-out spread across folds, heldout_sd_over_folds = 1.0235 in MSE against a reference-input MSE of 1.4723 (70% of it), is not propagated into that +-, as the SI states in print. No interval the submission carries bounds a cross-solvent reduction, so the claim is withdrawn; all three passages now print the null and the reason there is no bound. The -5, the +-16 and the 46% still print, unchanged.

# 2026-08-05 FASTSOLV ROWS RE-SCORED. Two entries, both DEFECTIVE VALUES WITHDRAWN. The two FastSolv
# retraining rows of Table 5 were read from bundles scored through a path that standardises the
# model's inputs twice (scripts/run_fastsolv.py::_predict_with_model scales, then predict_step scales
# again), which saturated the network's sigmoid input activation on temperature. Those rows are now
# an inference-only re-score of the SAME two checkpoints over the SAME rows with a single scaling
# (scripts/analysis/rescore_fastsolv_single_scaling.py), which also re-runs the doubly scaled path as
# a control and reproduces the withdrawn bundles to every digit they printed. Neither entry below is
# a claim the submission stopped making; each is a number the submission stopped believing.
-0.59  WITHDRAWN, defective: the R^2 in ln x2 of the FastSolv random-pair row as scored through the doubly standardising path. results/external_baselines/fastsolv_rescore_summary.json pair_random."deposited_row_set/single_scaled".r2_ln_x2 = 0.7584 on the same 10,696 rows, so that row now prints 0.76 and the defective -0.59 has no surviving occurrence. Its two companions do survive, in S4 only and labelled as what the defect produced: the MAE 2.91 and the RMSE 1.54. The control that reproduces the withdrawn bundle is the same file's pair_random."deposited_row_set/double_scaled_control" (mae_ln_x2 2.9101541551, rmse_logS 1.5440423429, r2_ln_x2 -0.5937458801), matching results/external_baselines/pair_random/fastsolv_contract_v2/summary.csv exactly.
0.179  SUPERSEDED, spot check replaced by the full re-score: S4's first diagnosis of the double-scaling defect quoted one sampled (solute, solvent) pair -- predicted span 0.135 against a measured 0.179 -- as evidence that a singly scaled forward pass restores a temperature response, together with a 60-pair Pearson comparison (0.99 twice-scaled, 0.85 once-scaled). Both runs have now been forwarded in full, so S4 reports the response over all 1102 by-solute and 1149 random-pair spanning pairs instead: median predicted span 0.500 and 0.631 in log10 S against a measured median 0.623 (results/external_baselines/fastsolv_rescore_summary.json, temperature_response of each "deposited_row_set/single_scaled" block). The sampled pair and its measured 0.179 are therefore gone; the 0.135 beside it still prints elsewhere in the submission, as does 0.62.

# 2026-08-05 SEED-44 SIGMA-ORACLE DEPOSIT REPAIRED. Eleven entries, all SUPERSEDED BY A THIRD SEED.
# The per-row prediction file results/e5_sigma_grounding/seed_44/oracle_predictions.csv had been
# deposited at 295 of its 8103 rows: the transfer off the compute volume stopped mid-record, and the
# deposited file is an exact byte-prefix of the complete one (198,190 bytes of 5,213,388). The
# complete file has been recovered from that volume and is now the deposit. It is the file the run
# scored, not a re-run: every field of seed_44/oracle_predictions.summary.json recomputes from it at
# zero absolute difference in float64, including MAE 2.2419879234438436 on n=5608 and the
# matched-subset block's n=5571 with MAE 2.243727888747464
# (scripts/analysis/verify_perrow_deposit.py runs that check over all twenty-two per-row files).
#
# Nothing aggregated at run time moves -- Fig. 2, Table 6 and the +0.41 were computed from the
# in-memory predictions before any file was written. What moves is everything that was recomputed
# from the deposit afterwards and therefore had to be restricted to the two intact seeds: the
# sigma-oracle row of Table 5 and the clip sweep of S4 were two-seed means and are now three-seed
# means like every other row of that block. The repair also removes a disagreement the old caption
# had to explain: that row's R^2 now reads -0.03 +- 0.04, which is what Table 6 reads over the three
# seeds, where the two-seed deposit gave -0.05 +- 0.02.
#
# Source for every replacement value below: results/external_baseline_comparison/summary.json,
# regenerated by scripts/analysis/run_external_baseline_comparison.py from the repaired deposit;
# tier1_this_work_scaffold_split._clip_sensitivity.by_clip[*].oracle for the sweep and
# ._trails_control_by_more_in_logS_than_in_MAE for the deficits. Not one of these is a claim the
# submission stopped making; each is a two-seed figure the submission can now state at three.
1.423  SUPERSEDED by the third seed: the sigma-oracle's log10 S RMSE at the tightest clip (x2 = 1 - 1e-3), averaged over seeds 42 and 43 because the seed-44 deposit was short. Over all three seeds it is 1.4205, so S4's sweep and its "seed mean" reading both now print 1.421. The sentence is unchanged and still fails at seed 43, whose 1.479 still prints.
1.464  SUPERSEDED by the third seed: the same arm at clip 1 - 1e-4, two-seed 1.4638 against three-seed 1.4711, now printed as 1.471.
1.512  SUPERSEDED by the third seed: the same arm at clip 1 - 1e-5, two-seed 1.5120 against three-seed 1.5308, now printed as 1.531.
1.566  SUPERSEDED by the third seed: the same arm at the deposited clip 1 - 1e-6, two-seed 1.5663 against three-seed 1.5982, now printed as 1.598. This is the value Table 5's sigma-oracle row carries, which moves from 1.57 +- 0.17 to 1.60 +- 0.14.
1.626  SUPERSEDED by the third seed: the same arm at clip 1 - 1e-7, two-seed 1.6260 against three-seed 1.6723, now printed as 1.672. The two remaining clips' values, 1.690 and 1.830, become 1.752 and 1.925; both of those baseline values survive elsewhere in the submission and so are not listed here.
0.517  SUPERSEDED by the third seed: the sigma-oracle's log10 S deficit over the control at clip 1 - 1e-5, two-seed, now 0.536 over three seeds.
0.571  SUPERSEDED by the third seed: the same deficit at the deposited clip, two-seed, now 0.603 over three seeds.
0.695  SUPERSEDED by the third seed: the same deficit at clip 1 - 1e-8, two-seed, now 0.757 over three seeds.
0.835  SUPERSEDED by the third seed: the same deficit at the loosest clip, two-seed, now 0.930 over three seeds. The three remaining entries of that list, 0.428, 0.469 and 0.631, become 0.425, 0.476 and 0.677; all three baseline values survive elsewhere in the submission.
0.554  SUPERSEDED by the third seed: the sigma-oracle's MAE deficit over the control, averaged over seeds 42 and 43. Over three seeds it is 2.2517 - 1.7022 = 0.5495, now printed as 0.549.
0.017  SUPERSEDED by the third seed: the margin by which the sigma-oracle's log10 S deficit exceeded its MAE deficit at the deposited clip, on two seeds. Over three seeds that margin is +0.0537, now printed as 0.054. The reading it qualifies is unchanged -- it still fails at the three tightest clips -- and its per-seed statement gains seed 44, which behaves as seed 43 does.

# 2026-08-06. The orphaned solvent-ranking table (old Table S13, sections/supporting.tex) is
# REPLACED, not annotated. Its four values and its n came from
# results/ranking/tgnn_mpnn_ranking.json, computed over
# results/e0_compensation/tgnn_mpnn_test_predictions.csv -- an early proxy checkpoint on a split
# that no longer exists (826 groups over 5826 rows, against the current 823 over 5608). It matched
# no arm of Fig. 2 and printed one paragraph away from the arms a reader would take it for; the
# scaffold split is documented as unstable across pipeline versions, so it cannot be re-derived on
# the current rows either. The replacement measures the same quantity on the arms this paper is
# about -- learned sigma against reference sigma, one checkpoint per seed, three seeds, cluster
# bootstrap over the 107 solutes -- so nothing the submission claimed about ranking is withdrawn;
# each of the four values below is superseded by its paired three-seed counterpart.
# Working: scratchpad negatives/ranking-final.md; deposits rank_final.json, rank_final_floorci.json.
0.511  SUPERSEDED, and by an arm that exists: the proxy checkpoint's mean per-solute Spearman. The replacement prints the learned-sigma arm at 0.592/0.546/0.598 and the reference-sigma arm at 0.365/0.369/0.410, with the paired gap -0.197 [-0.282, -0.117].
0.436  SUPERSEDED, same table and same reason: the proxy's mean Kendall tau. The replacement prints 0.518/0.469/0.521 against 0.303/0.306/0.333, gap -0.188 [-0.263, -0.118].
0.732  SUPERSEDED, same table and same reason: the proxy's mean nDCG@3. The replacement prints 0.796/0.748/0.792 against 0.697/0.690/0.698, gap -0.084 [-0.124, -0.043]. The rounded restatement of it in the same paragraph, "nDCG@3 ~0.73", also goes; 0.73 survives elsewhere in the submission and so is not listed separately.
826    SUPERSEDED: the proxy split's solute-group count, printed twice (table caption and paragraph). The current labelled test rows give 823 groups over 107 solute clusters under the same grouping rule, and both figures print in the replacement, the 107 being the effective n every interval is bootstrapped over.
# 0.477 (the proxy's top-1 accuracy) is NOT listed: the value survives three times in the map
# tables of S6, so its disappearance from this table cannot be flagged and needs no allowlisting.
# The replacement prints top-1 at 0.586/0.515/0.548 against 0.447/0.428/0.454, gap -0.107
# [-0.192, -0.020], quoted pooled because two of the three per-seed intervals span zero.

# 2026-08-06, referee items 2 and 4. The knife-edge floor cell is now printed at the precision the
# bootstrap deposit carries, in both documents and in the same words: the reference-sigma arm's
# nDCG@3 margin over its own shuffled-profile floor at seed 44 is [-0.00081, +0.07622]
# (rank_final_floorci.json seed_44.oracle_minus_own_donor_floor.ndcg.ci95), and the rounded
# [-0.001, +0.076] the SI printed hid exactly the fact the caption has to state -- that this one
# interval decides the count of intervals excluding zero, 0 or 1, on the bootstrap draw. Nothing is
# withdrawn: the same interval prints, one digit longer, in the same sentence and in the caption.
-0.001 re-printed at deposit precision: the rounded lower end of that cell, now [-0.0008, +0.0762].
0.076  re-printed at deposit precision: the rounded upper end of the same cell, now +0.0762.

# 2026-08-06, referee item 7 (precision). NO ENTRY IS NEEDED for the two endpoints below -- the
# checker does not flag them, because both values still print elsewhere in the submission -- but the
# retirement is deliberate and is recorded here so a later diff does not read it as an accident.
# The pooled out-of-sample affine gap printed [+0.30,+2.17] in both documents. Those two decimals
# are not pinned by the 4000 cluster-bootstrap draws behind them: repeating the draw with
# independent RNG puts the lower endpoint anywhere in [+0.282,+0.311] and the upper in
# [+2.181,+2.252], and raising the count does not rescue the second decimal of the upper endpoint
# (at 200000 draws it still runs [+2.193,+2.202], straddling the 2.195 rounding boundary), because
# the statistic is heavy-tailed -- a leave-one-out affine refit on a three-solvent group. Both
# documents now print [+0.3,+2.2], the precision the draw count does pin, and the SI says so in the
# sentence. The point estimate +1.09 is deterministic and unchanged.

# 2026-08-07, referee items 1-5 (the five sentence-level qualifications). Two entries, and BOTH ARE
# THE CHECKER'S OWN EXCLUSION RULES REACTING TO REFLOW, not losses. Putting each qualification back
# where its claim is made added a few lines to Sec. 2.3, Sec. 3.3, the Conclusions, Data
# availability and S1, which re-broke the lines around two numbers and moved them across an
# exclusion boundary. VERIFIED IN THE TARGET PDFs' OWN TEXT BEFORE ADDING, one command each.
2411.15101  checker artifact, not a loss: the arXiv identifier of Ref. mohan2024 (What You See is Not What You Get) is still printed in the article's reference list -- `pdftotext paper/grounding_paradox.pdf - | grep 15101' returns it on p. 31 -- on a page the bibliography-REGION rule now excludes, the list having reflowed by one entry. The same pass moved 12 tokens in total from counted to bibliography-excluded (302 -> 290).
-16         checker artifact, not a loss: the exponent of the profile-area identity's residual in S1, which still prints as "is at most 7 x 10-16" on S-4 -- `pdftotext -layout paper/grounding_paradox_si.pdf - | grep 'at most 7'' returns it. The S1 edit above it re-broke the line so that pdftotext no longer spaces the mantissa from the "x 10", and the checker now folds the whole token as scientific notation instead of counting the bare exponent (sci-base folded 36 -> 37, sci-exponent folded 43 -> 44, both +1). The number is in the document at the same precision; only the unfolded token is gone.

# Same pass, NO ENTRY NEEDED but recorded so a later diff does not read it as an accident: S1's
# "capacity that the in-sample linear probe (R^2 ~ 0.76) indicates is not the binding constraint"
# was the last surviving use of the withdrawn transfer-vs-expressivity diagnosis, and it cited
# S6.1 -- the section that withdraws exactly that inference and shows isotropic noise features
# reaching a HIGHER train R^2 (0.979) than the trained encoder. The clause now says the question is
# unmeasured there and cites the arm comparisons instead, which is where S6.1 already puts the
# surviving version of the expectation. 0.76 still prints nine times elsewhere, so the checker
# lists it under section 2 (value survives) and not section 1.

# 2026-08-07. ONE STATISTIC PRINTED AT FOUR VALUES: the glycol-ether margin's 90% interval.
# Two referees reported it independently. It was not four quantities and not four roundings.
# The map seeds its bootstrap from the stratum's NAME (run_b_insuff_stratified_map.stable_seed),
# so the strata that select the SAME rows each drew an INDEPENDENT 3000-draw replicate of one
# interval, and run_b_insuff_crossfit_scoring.py composes that seed string differently again for
# a fourth. Verified from the deposit: the glycol-ether stratum is the same 182 rows (19 solutes,
# 4 solvents, 43 pairs, 3 publications) in stratified_map_table.csv and crossfit_map_table.csv,
# with MSE 2.251842, B_insuff^up 0.107708 and margin 2.036427 bit-identical in every printing.
# A 3000-draw replicate carries +-0.020 of Monte-Carlo spread on EACH endpoint over 40 seeds --
# twice the digit they were printed to -- and all four printed values sat within 1.8 sd of one
# mean. results/b_insuff/glycol_ether_ci_converged.json, from
# scripts/analysis/run_b_insuff_glycol_ether_ci.py, is that bootstrap run to convergence:
# [+1.2558, +2.8689], standard error 0.001 per endpoint, printed [+1.26, +2.87] everywhere. It is
# WIDER than the [+1.27, +2.83] the abstract had been quoting, which was the narrowest of the
# four. Row-identical strata now print one interval throughout the map (make_map_table_tex.py),
# which retires the duplicate replicates of five other row sets as well.
1.018   re-rounded, not lost: the cross-fitted arm's interval on the glycol ethers, crossfit_map_table.csv broad_473 solvent_class::glycol_ether ci90_cf = [1.0175, 2.4890]. A 3000-draw bootstrap endpoint carries +-0.02, so both arms of that sentence now print to two decimals, [+1.02,+2.49]; the leave-one-publication-out envelopes beside them are exact deletion extrema and keep their three.
2.489   re-rounded, not lost: the same cross-fitted endpoint, ci90_cf upper 2.4890 -> +2.49. See 1.018 above.
2.873   retired replicate: the coarsening arm's upper endpoint in the cross-fit section, crossfit_map_table.csv ci90_bin = [1.2603, 2.8732] -- one 3000-draw replicate of the interval whose converged value is 2.8689. That sentence now prints the converged [+1.26,+2.87], the same interval the abstract, Table 4, Sec. 3.4 and all three map tables now carry.
3.26    retired replicate: the mono-alcohol interval as drawn under the name solvent_family_fine::mono_alcohol, [+0.29,+3.26]. It is the same 35 rows as solvent_class::mono_alcohol, whose replicate [+0.18,+3.24] all three cells now print. The margin +0.78 and the full-convention +1.09 are unchanged, and the row is inadmissible either way (n < 40).
3.37    retired replicate: the same 35 mono-alcohol rows as drawn under solvent_class_x_solute_role::mono_alcohol | organic_solute, [+0.30,+3.37]. Superseded by [+0.18,+3.24] as above.
8.61    retired replicate: the halogenated non-H-bonding interval as drawn under solvent_class_coarse::halogenated_nonHB, [+1.10,+8.61]. Same 30 rows as solvent_class::halogenated, whose [+0.86,+8.12] both cells now print. Margin +3.39 unchanged; inadmissible either way (n < 40, and one deletion leaves too few rows).
9.5     retired replicate: the water-as-solute interval as drawn under solute_family::water, [+3.39,+9.50]. Same 37 rows as solute_role::water_solute, whose [+3.97,+9.49] both cells now print. Margin +6.33 unchanged; inadmissible either way (n < 40, single source, no deletion to run).

# Same pass, NO ENTRY NEEDED, recorded so a later diff does not read it as an accident: the seven
# tier-B integers in section 3 (1161, 1189, 2003, 2021, 2023, 781, 798, and 71 in section 4) are
# the checker's bibliography-REGION exclusion reacting to reflow, not lost references. The report's
# own header shows bibliography-excluded tokens falling 297 -> 290, exactly those seven. All three
# entries still print -- `pdftotext -layout paper/grounding_paradox.pdf - | grep Econometrica'
# returns "Econometrica 2003, 71, 1161-1189" on p. 33, and Digital Discovery 2023, 2, 781-798 and
# the NeurIPS 2021 entry are on p. 32. The reference list did not change in this pass.

# 2026-08-07 rounding/scope pass. NO ENTRY NEEDED, recorded so a later diff does not read any of
# it as an accident. Section 1 is EMPTY: nothing stopped being printed anywhere.
#
# (a) THE ONE DELIBERATE REMOVAL is +0.10 in S6.5, and it was a wrong claim, not a retired value.
#     The sentence read "the second is taken from the unrounded means, since the rounded column
#     would give +0.10". It does not: Table S2's rounded column is 1.79 and 1.70, a difference of
#     +0.09, which is also what the unrounded means give (1.7949751 - 1.7021766 = 0.0927985). The
#     sentence now prints 0.0928 and says the rounded column agrees. The value 0.10 still prints on
#     eight other pages (report: x13 -> x12), so it is a section-2 entry and not a retirement; it is
#     deliberately NOT allowlisted, because allowlisting 0.1 would blind the checker to all thirteen.
#
# (b) THE OTHER FIVE section-2/4 entries are REFLOW, and their counts prove it: +1.40 x6 -> x6,
#     +0.69 x9 -> x9, 74 x5 -> x5, 846 x7 -> x7, 275 x2 -> x2. Adding four lines to Sec. S3 moved a
#     page folio (S-27, S-56, S-61, S-63) into the middle of each of those sentences, which is what
#     defeats the occurrence matcher. All five sentences were read back out of the built SI and are
#     intact -- e.g. "remain admissible--74 rows at +1.40 deployed [S-27] and +1.69 full, under all
#     three deletions" and "at zero under the deployed rule and +0.69 under the full conven- [S-56]
#     tion". Nothing here is allowlisted either.

# 2026-08-10 generator-audit pass. Three tier-A losses, all three the same event: a printed digit
# that the newly written generators do not reproduce, repaired against the artifact. No sentence
# was removed and no page moved (article 33 pp, SI S-84, 6699 numeric tokens before and after).
37000  corrected basis, not a corrected value: Sec. S1's coupling sentence says "over the three seeds", but its median dH_fus of 3.7e4 was seed 42's alone. Pooled over the deposited seeds 42/43/44 -- results/e5_sigma_grounding/section_s1_substitution_perturbation.json, summary.dH_fus.median_pooled = 34788.38, written by scripts/analysis/run_substitution_perturbation.py off the grounded_a/oracle per-row exports -- it is 3.479e4, and the sentence now prints 3.5e4. The three MAXIMA in the same sentence were genuine three-seed maxima and are unchanged; the T_m median moved 449.65 -> 449.04 and still prints 450 at the two significant figures it is quoted to. --expect-published now checks the medians on the pooled basis and refuses the first-seed one.
4.7    same sentence, same cause: the median |Phi| printed as 4.7 is seed 42's 4.7246; pooled over the three seeds it is 4.5339 (same artifact, summary.Phi.median_pooled), so the sentence now prints 4.5. The value 4.7 still prints elsewhere in the document and this entry retires only this occurrence.
0.21   rounding slip in Table S21's Nitrile cell, repaired to +0.20: the stratum is acetonitrile alone (n=252) and its three-seed mean is +0.20481 -- results/e5_sigma_grounding/chemistry_map.json, "solvent/nitrile".mean, written by scripts/analysis/run_e5_chemistry_map.py -- which misses the 0.205 rounding boundary by 2e-4. The sd 0.19 was right and is unchanged. (Rounding each seed before averaging does give +0.21, but that rule then breaks Water +0.51 vs +0.52 and Amide -0.21 vs -0.22, so it was not the table's rule; tests/test_e5_chemistry_map.py asserts that.) The value 0.21 still prints on six other pages and only this cell is retired.
