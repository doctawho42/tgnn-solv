\section{The pKa closure: full construction, measured sweep, and trained comparison}
\label{sec:si-pka}
% MERGED 2026-08-10.  A one-page \section of its own (sections/pka-domain.tex) used to stand
% immediately before this one.  It opened by saying its result is stated in the article, restated
% that result, deferred to this section nine times, and closed by saying the construction is here --
% three statements of one result in a row, which is what makes an appendix obligatory reading.  The
% precis is gone; its two sentences that printed nowhere else are folded in here (the subscript
% convention on sigma_H, just below, and the placement of the crossover against plug-in efficiency
% and quasi-maximum-likelihood, in "The crossover and its scope boundary"), together with the scope
% clause on the a-priori channel assignment.
% BOTH LABELS SIT ON THIS SECTION so that \S\ref{sec:pka} in the article and \S\ref{sec:si-pka}
% everywhere else still resolve.  Do not remove either.
\label{sec:pka}
This section gives the full construction behind \S\ref{sec:pka-main}: a fixed
Hammett linear-free-energy relationship (LFER) $g(\zstar)=\mathrm pK_{a,0}-\rho\,\hammett^\star$ over
$\zstar=(s,\hammett^\star)$: the reaction series $s$, which fixes the per-scaffold Hansch--Leo--Taft
constants $(\mathrm pK_{a,0},\rho)$, together with the Hammett substituent constant $\hammett$, whose
tabulated Hansch--Leo values are the external reference for the coordinate the network predicts.
That constant carries its subscript throughout, so that it cannot be confused with the
$\sigma$-profile of \S\ref{sec:framework}. The analogy is close: as with the $\sigma$-profile through COSMO-SAC, a
learnable physical descriptor is pushed through a fixed, approximate, differentiable map. The two
classic LFER failure modes fall cleanly onto the two error channels of Lemma~\ref{prop:decomp}:
\emph{resonance saturation}---a curvature in $\hammett$ that the linear closure structurally cannot
represent---is a mis-map of a function of $\zstar$ and lands in $\Bclos$; \emph{ortho/steric} field
effects, absent from the tabulated constants, are information the intermediate lacks and land in
$\Binsuf$. The chemical scope is a fidelity sweep, over the four reaction series alike (benzoic acid,
phenol, anilinium, pyridinium): records whose every substituted ring position is meta or para with a
tabulated substituent are the near-exact ($\Bclos\!\approx\!0$) regime, and records carrying an ortho
substituent or one outside the tabulated list---both of which enter $\hammett^\star$ as zero---the
low-fidelity one. The assignment rule is a function of structure alone and never reads the residual,
and its mechanics are next.

% DE-DUPLICATED 2026-08-10.  This paragraph was the article's Sec. 3.6 rule relocated verbatim on
% 2026-08-07; the article then kept the half of it that defines the two poles -- what makes a record
% clean, what makes it degraded, the 158/846/571/275 counts and the not-benzenoid clause -- so both
% documents printed that half.  What is here now is only the mechanics the article does not carry:
% the fixed trial order, the ring-unambiguity conditions and the ring-bond-distance typing.
\paragraph{The rule that selects a reaction series and types a ring position.}
The reference constant is assigned mechanically, and that rule is also what defines the two poles of
\S\ref{sec:pka-main}.
The four kinds are tried in the fixed order benzoic acid, phenol, anilinium, pyridinium, and a record
enters under the first of them whose ionizing group matches exactly once in the molecule, at a ring
atom lying in exactly one ring and that ring six-membered and aromatic. An ionizing site the entering
kind does not consume does not exclude the record: on the same ring it is typed as an ordinary
substituent, tabulated or not. It enters only if its ring system is also unambiguous: no aromatic nitrogen
anywhere in the molecule for the three carbon-ring series, exactly one and no $N$-oxide for
pyridinium. The kind a record enters under names the
reaction series and with it $(\mathrm pK_{a,0},\rho)$. Ring
positions are typed by shortest ring-bond distance from that ring atom---the one bearing the ionizing
group, which for pyridinium is the ring nitrogen itself---one bond \emph{ortho}, two \emph{meta},
three \emph{para}; and $\hammett^\star$ is the sum over the substituted positions
of the Hansch--Leo constant of that substituent \emph{at that position}, $\sigma_m$ meta and
$\sigma_p$ para, taken from the fixed list of nineteen substituents in
Table~\ref{tab:hammett-sigma}; an unsubstituted parent has
$\hammett^\star=0$. The zeroing of ortho and untabulated positions, and with it the definition of
the clean and degraded poles and their counts, is \S\ref{sec:pka-main}'s.

% RELOCATED FROM THE ARTICLE'S Sec. 3.6 ON 2026-08-07, VERBATIM.  The article keeps the three
% clauses that scope the floors it prints -- not the eight-bin cell, a floor and not a separation
% margin, and conditioning on the series being what Lemma 1 demands rather than the conservative
% choice, with the pooled-series 8.3 against 5.1 -- and points here for the identity, the fold
% design and the argument against Sec. 3.5.1's refusal of fitted estimators on solubility.
% ANCHOR, 2026-08-10.  The article's pKa floor of 0.6 is only a floor if no fold hands a held-out
% row what the closure's argument does not already carry, and that argument lived in this paragraph
% and nowhere in the article, which pointed at this whole section (five printed pages) for it.
% LATER THE SAME DAY the article took the design: \S\ref{sec:pka-main} now gives the folds as five
% within each series, pooled over the series by n, with the leakage argument in the sentence that
% carries the floor.  What stays here is the out-of-fold identity in symbols and the pooled-series
% counterfactual's working.  Keep the two statements of the design in step.
\paragraph{The estimator, its identity, and its fold design.}
\label{par:pka-folds}
\S\ref{sec:pka-main} scopes the two floors it prints: they are not measured with the eight-bin
estimator cell of \S\ref{sec:estimator}, and the functional is a floor on $\Bclos$ and not the
separation margin of Table~\ref{tab:notation}. The estimator's identity and its fold design are
here. The closure reads the reaction series too---$\mathrm pK_{a,0}$
and $\rho$ are per-series---so the argument Lemma~\ref{prop:decomp} requires is the pair
$\zstar=(s,\hammett^\star)$, series label and constant, and the estimand is
$\Ex[\Var(\mathrm pK_a\mid s,\hammett^\star)]$. Neither coordinate needs binning: $s$ takes four values and
$\hammett^\star$ is a scalar, so the conditional variance is estimated directly---a random forest of
$\mathrm pK_a$ on $\hammett^\star$ alone, fitted \emph{within} each series and pooled over series by $n$,
five-fold, its \emph{out-of-fold} residual mean square. That regressor $\hat r$ reads $(s,\hammett^\star)$
and nothing else, and predicts each row from the folds that exclude it, which is what makes the split
exact for it:
$\Ex[(m-\hat r)^2]=\Ex[\Var(m\mid s,\hammett^\star)]+\Ex[(\Ex[m\mid s,\hammett^\star]-\hat r)^2]$, so the estimate
exceeds $\Binsuf$ by the regressor's own approximation error---an upper figure, leaving a lower one.
Conditioning on $s$ is not a free choice and it is not the conservative one: pool the four series and
the same estimator returns $8.3$ against the degraded pole's $\mathrm{MSE}$ of $5.1$, a conditioning
under which $g$ is not measurable, so Lemma~\ref{prop:decomp} does not license it and the closure
floor it leaves would in any case be vacuous. The floor of $0.6$ therefore rests on $s$ belonging in
$\zstar$, which is what the lemma demands of a closure that reads it.
\S\ref{sec:broad} declines fitted estimators on solubility because random folds let a held-out row
borrow strength from a near-twin through the representation; here the regressor's only input is
$\hammett^\star$ within a series, so no fold can hand it what the closure's argument does not
already carry. What the identity needs of the fold design is only that a row be held out of the fit
that predicts it.

The nineteen substituents that rule can see, and the constants it sums, are
Table~\ref{tab:hammett-sigma}. They are standard Hammett $\sigma_m/\sigma_p$ values
\cite{hansch1991survey} to two decimals; they are a constant of the builder, not fitted or refit on
these data, which is the sense in which $g$ is fixed for Lemma~\ref{prop:decomp}. The order matters:
a ring position takes the constant of the first pattern in the list that matches at it, and the list
is ordered specific before generic. A substituent outside this list makes the position untabulated,
and the record degraded.

\begin{table*}[tbp]
\centering
\caption[The fixed substituent list behind the Hammett constant]{The fixed substituent list behind $\hammett^\star$: nineteen groups with the meta and para
Hammett constants summed over the substituted positions (\S\ref{sec:pka-main}). Patterns are tried
in the order shown---down the first block, then the second, then the third---and the first match at a
ring position wins. An ortho position, and any group not in this list, enters the sum as zero.}
\label{tab:hammett-sigma}
{\footnotesize
\begin{tabular}{@{}lrr@{\hspace{3em}}lrr@{\hspace{3em}}lrr@{}}
\toprule
group & $\sigma_m$ & $\sigma_p$ & group & $\sigma_m$ & $\sigma_p$ & group & $\sigma_m$ & $\sigma_p$\\
\midrule
$\mathrm{NO_2}$      & $0.71$ & $0.78$  & $\mathrm{OCH_3}$        & $0.12$  & $-0.27$ & $\mathrm{Br}$          & $0.39$  & $0.23$\\
$\mathrm{CF_3}$      & $0.43$ & $0.54$  & $\mathrm{OH}$           & $0.12$  & $-0.37$ & $\mathrm{I}$           & $0.35$  & $0.18$\\
$\mathrm{CN}$        & $0.56$ & $0.66$  & $\mathrm{N(CH_3)_2}$    & $-0.16$ & $-0.83$ & $\mathrm{C(CH_3)_3}$   & $-0.10$ & $-0.20$\\
$\mathrm{COOH}$      & $0.37$ & $0.45$  & $\mathrm{NHC(O)CH_3}$   & $0.21$  & $0.00$  & $\mathrm{C_2H_5}$      & $-0.07$ & $-0.15$\\
$\mathrm{CO_2CH_3}$  & $0.37$ & $0.45$  & $\mathrm{NH_2}$         & $-0.16$ & $-0.66$ & $\mathrm{CH_3}$        & $-0.07$ & $-0.17$\\
$\mathrm{COCH_3}$    & $0.38$ & $0.50$  & $\mathrm{F}$            & $0.34$  & $0.06$  &                        &         & \\
$\mathrm{CHO}$       & $0.35$ & $0.42$  & $\mathrm{Cl}$           & $0.37$  & $0.23$  &                        &         & \\
\bottomrule
\end{tabular}}
\end{table*}

This decomposition is directly visible before any training. Pushed through the fixed Hammett
closure, $p$-nitrophenol reads $\mathrm pK_a\!\approx\!8.3$ against an experimental $\approx7.1$
(and $p$-nitroaniline overshoots similarly): the linear closure is off by $\sim1$ unit exactly
where through-conjugation ($\sigma_{\mathrm H}^-$) breaks it---$\Bclos$ observed in practice.

A controlled semi-synthetic construction on real tabulated $\hammett$ (known ground-truth
channels) validates the estimator and reproduces the paper's central distinction in this second
closure (Fig.~\ref{fig:pka}). Sweeping closure fidelity with a sufficient intermediate, the
grounding gap $\Ggap$ tracks $\Bclos$ (the plug-in estimate recovers the truth to within a few
percent, e.g.\ $0.20$ vs $0.21$, $0.81$ vs $0.81$), the case where Theorem~\ref{thm:gap}'s
proxy is valid. Sweeping the ortho/steric channel with a \emph{well-specified} closure gives
$\Bclos=0$ at every knob while $\Ggap$ rises with the insufficiency fraction, from $-0.035$ at zero
insufficiency through zero near a fraction of $0.25$ to $+0.34$ at $0.7$: at the three fractions
where it is positive it is positive with $\Bclos$ identically zero, which is the pKa realization of
the conflation that makes $\Ggap$ an indicator rather than a certificate. The two negative points
are the same statement in the other direction---a well-specified closure with a nearly sufficient
intermediate leaves the oracle marginally ahead. The oracle-$\hammett$ pipeline
(scaffold-resolved substituent$\to\hammett$ assignment) and this validation extend directly to real
data. On the OPERA experimental $\mathrm pK_a$ compilation \cite{mansouri2019opera} ($7{,}904$
records under the same net-neutral filter), applying the same decomposition to the fixed Hammett
closure yields a \emph{measured} fidelity sweep. On the clean meta/para series ($n{=}158$) the closure
predicts experimental $\mathrm pK_a$ to RMSE $0.55$. The leakage-immune lower bound
$\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ is $-0.2$ ($95\%$~CI $[-0.4,-0.0]$), below zero, and it is
uninformative here rather than reassuring: the out-of-fold $\Binsuf^{\mathrm{up}}$ is $0.5003$
against an $\mathrm{MSE}$ of $0.3023$, so that difference is negative for every closure whatever,
and what the $158$ rows across four series establish is that the within-scaffold regressor predicts
worse than the LFER does. The second bound is informative and points the other way.
Lemma~\ref{prop:decomp}(b) bounds $\Bclos$ below by the \emph{square} of the per-scaffold systematic
offset, and the deposit's per-scaffold Jensen field is already that square: its entries
$0.0918/0.0011/0.1837/0.0392$ are the squares of the offsets $+0.303$ anilinium, $+0.034$
benzoic acid, $+0.429$ phenol, $-0.198$ pyridinium. The offsets reach $0.429$ in $\mathrm pK_a$, and
the $n$-weighted mean of the squares is $0.083$ against a total of $0.303$, or $27\%$; on phenol
alone it is $0.184$ against that series' own $\mathrm{MSE}$ of $0.371$, or half of it.
% DENOMINATOR REPAIR, 2026-08-11 (round 7).  This read "0.184, or 61%", which is phenol's numerator
% over the POOLED denominator: 0.1837/0.3023.  Phenol's own MSE is 0.609^2 = 0.371, so the share on
% phenol alone is 50%, and phenol carries only 34% of the pooled squared error -- a bound quoted as
% 61% of the pooled total exceeded that series' entire contribution to it.  The 27% is unaffected
% (0.0827/0.3023) and is the load-bearing figure.
At least a quarter of this pole's error is closure error, so the
pole is not shown to be well specified, and neither bound available on it says that it is.
The degraded pole ($n{=}846$)
degrades to RMSE $2.26$, dominated by input insufficiency. Here $\zstar$ is the pair
$(s,\hammett^\star)$---the reaction series, which fixes $(\mathrm pK_{a,0},\rho)$, and the constant---and
neither coordinate needs binning, four labels and a scalar, so---unlike the solubility
regime---$\Binsuf=\Ex[\Var(\mathrm pK_a\mid s,\hammett^\star)]$ is estimated \emph{directly} rather
than through a coarsening of the closure's argument (\S\ref{sec:decomp}): a within-series
out-of-fold regression estimator gives $\Binsuf=4.5$
($95\%$~CI $[4.0,5.1]$; $\approx89\%$ of the error, on the unrounded $4.528$ against
$5.094$---$\hammett^\star$ carries nothing from
the zeroed positions, exactly the a-priori channel assignment, which the measurement is consistent
with rather than establishes), leaving a closure misspecification
$\Bclos=0.6$ ($[0.3,0.8]$, a stratified within-scaffold molecule bootstrap; both intervals are
percentile intervals of $4000$ resamples of molecules within each series, at their sizes). Directly estimated is
not point-identified: the estimator is an out-of-fold residual mean square, which is an upper
estimate of $\Ex[\Var(\mathrm{p}K_a\mid s,\hammett^\star)]$, so $4.5$ is an upper figure and the $0.6$ it
leaves is a lower one---one-sided in the same direction as everywhere else in this paper, and
narrower only because a four-valued label and a scalar need no binning (\S\ref{sec:decomp},
% The pooled-over-series figure (8.3) and the within-series one (4.5) both print from
% scripts/analysis/run_pka_binsuff_conditioning_check.py, which calls the same
% _binsuff_within_scaffold / _oof_mse used by run_pka_real_decomposition.py.
\S\ref{sec:pka-main}). The two poles differ by a factor of $17$ in $\mathrm{MSE}$, and that is the
transition, measured rather than asserted. The clean pole is the milder regime and not the
well-specified one. Run the same Jensen bound on both and the closure floor is $0.083$ there against
$0.654$ on the degraded pole, $27\%$ of the first pole's error and $13\%$ of the second's, so what
grows between the poles is the total and the insufficiency inside it---at most $73\%$ on the clean
pole and at most $87\%$ on the degraded---while a closure error of at least a quarter is present on
the pole the relation was built for.

\paragraph{Data quality control (uniform, closure-independent).} The trained runs use the canonical
full OPERA set ($7904$ records). A small number of entries are supplied as pre-ionized microstates---an
$[\mathrm{NH}^+]$-protonated aminopyridine at $\mathrm pK_a=-7.78$, a pyrylium $[\mathrm O^+]$, among
others---inconsistent with a neutral-scaffold Hammett assignment, and they degrade the clean pole
(pyridinium RMSE $0.63\!\to\!3.83$). We drop them with one rule, \emph{net formal charge} $\neq0$,
applied uniformly to both strata. It is closure-independent (it never references the oracle residual,
so it cannot be circular) and keeps net-neutral substituents such as nitro $[\mathrm N^+][\mathrm O^-]{=}\mathrm O$.
This removes $5$ of $163$ clean-pole rows (restoring that pole to RMSE ${\approx}0.55$) and $352$
of $1198$ degraded-pole rows (leaving $846$; that pole's RMSE is essentially unchanged, so the filter
does not manufacture the sign), for $1004$ Hammett-amenable rows. The clean pole's floor does turn on
it: $\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ is $+0.55$ on the $163$ unfiltered rows against the $-0.2$
reported on the $158$ filtered. That sensitivity decides which of the two bounds carries the pole and
not whether the closure is well specified there, since the Jensen bound puts $\Bclos\ge0.083$ on the
filtered rows regardless.

\paragraph{Evidence for the flip.} We train the physics arm (encoder $\to\hat\sigma_{\mathrm H}\to$ fixed LFER)
on $K{=}20$ stratified $80/20$-within-scaffold splits---a Bemis--Murcko split collapses the meta/para
pole to two ring systems and is unstable---and record the paired per-molecule error against the
deterministic oracle, per stratum. Clean pole: learned MAE $0.48\pm0.09$ vs oracle $0.315\pm0.07$, gap
$-0.165$, oracle better in $19/20$ splits, $90\%$ CI $[-0.28,-0.04]$. Degraded pole: learned $0.83\pm0.07$ vs
oracle $1.62\pm0.08$, gap $+0.79$, oracle worse in $20/20$ splits, $90\%$ CI $[+0.57,+0.95]$ (both
intervals the $5$th and $95$th percentiles of $4000$ draws of the scaffold-cluster bootstrap, one
cluster per reaction series). Both halves certify, and the flip with them.

\paragraph{The same certification on a scaffold split, where it fails.} The run above is
in-distribution, and the companion run at the same $K{=}20$ on a Bemis--Murcko scaffold split is
deposited beside it. There the reference helps on both poles and nothing flips. Clean pole: learned
$0.97\pm0.66$ vs oracle $0.37\pm0.14$, gap $-0.61$, oracle better in $20/20$ splits, cluster CI
$[-1.13,-0.24]$---the ``reference helps'' half certifies. Degraded pole: learned $2.73\pm1.72$ vs
oracle $1.63\pm0.32$, gap $-1.10$, oracle better in $12$ of $20$ splits, cluster CI
$[-1.86,+0.37]$ at $P=0.28$---the ``reference hurts'' half does not certify, and neither does the
reversal. Both arms degrade under the harder protocol and the learned one degrades further, which is
the crossover mechanism of the paragraph below acting on both poles at once; the flip itself is a
within-distribution phenomenon and is reported as one. Every solubility result in this paper is
measured on a scaffold split, so this is the one place the protocol differs from the rest, and the
reason it does is that a scaffold split collapses the meta/para pole to two ring systems.

\paragraph{What the two bootstraps do and do not resolve.} We report the sign statements above rather
than the bootstrap $P$ values, because neither bootstrap here carries the resolution a two-decimal $P$
implies. The cluster unit is the Hammett reaction series, and only $G{=}4$ exist (benzoic acid,
phenol, anilinium, pyridinium): the resampling law then has $\binom{7}{4}{=}35$ distinct outcomes and
a finest atom $(1/4)^4{\approx}0.004$, so the cluster-bootstrap $P(\text{oracle worse}){=}1.00$ on
the degraded pole is exactly the statement that all four series means favour the learned arm, and $P{=}0.004$ on
the clean pole that exactly one of them does, the other three favouring the oracle. Read as a sign test on
four units those are $p{=}1/16$ and $p{\approx}0.31$; the interval endpoints quoted above are
percentiles of the same $35$-outcome law and are no finer. This is the same small-$G$ caution applied
on the solubility side (\S\ref{sec:si-robustness}), where the $P$ values are likewise read as a coarse
band rather than as two-decimal quantities. The split bootstrap adds nothing independent: the $20$
splits resample train/test assignments over the same molecules (each molecule lands in ${\approx}4$
test sets), so it prices split-to-split noise rather than molecule sampling, and with all $20$ degraded-pole
gaps positive it returns $1.000$ by construction---a restatement of the $20/20$, not a second
confirmation. What the flip rests on is therefore the sign consistency across splits and series and
the size of the degraded pole's gap ($+0.79$ against an oracle MAE of $1.62$, ${\approx}49\%$), not on a $P$
value. A leave-one-series-out refit, the estimate that would settle the series-level question at
$G{=}4$, was not run.

\paragraph{The crossover and its scope boundary.} The sign of the grounding gap is by construction
the sign of the learned arm's error against the oracle's. What a competence sweep (train fraction
$\in\{1.0,0.5,0.25,0.12\}$ and, as the low-competence extreme, scaffold extrapolation; six seeds each)
adds is that the oracle side of that comparison is fixed by the fidelity set and not by the budget:
its threshold is the same at each of the four budgets, while the learned side moves.
On the clean pole the learned arm rises from $0.46$ to $0.98$ as data shrinks but never reaches
the $0.31$ oracle threshold, so grounding helps throughout. On the degraded pole the learned arm stays below the
$1.62$ threshold across the whole interpolation range ($0.85$ at full data, $1.31$ at $12\%$), so
grounding hurts throughout; only scaffold extrapolation, where $\hat\sigma_{\mathrm H}$ breaks to $2.6\pm2.0$,
pushes it back above the threshold and reverts the sign. That reversion is a property of the mean
over the six splits and not of every split: three of the six still have grounding hurting, and the
negative mean gap ($-1.04$) is carried by two splits at $-3.2$ and $-3.8$. The flip is therefore a within-distribution,
fidelity-driven crossover, and the extrapolation reversion follows the same comparison. It is the
sharp, two-sided form of the penalty sign rule (Remark~\ref{prop:tax}): the half saying that a fitted
intermediate can beat the true one is known---plug-in efficiency under correct specification
\cite{hirano2003}, pseudo-true fitting under misspecification \cite{white1982}---and what is added
here is the fidelity-set threshold at which the two halves change places. A trained
physics-vs-DirectGNN comparison agrees in direction overall but has weak absolute errors and an
untuned control; the load-bearing second-domain results are the decomposition above and the
sign-consistent flip.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_pka_hammett.pdf}
\caption[pKa through a Hammett LFER on a known-ground-truth construction]{pKa through a Hammett LFER on a construction with known ground-truth
channels. \emph{Left:} closure fidelity varied with a sufficient intermediate; grounding gap
$\Ggap$, closure bias $\Bclos$, and the assumption-light Jensen bound. \emph{Right:} an
ortho/steric insufficiency channel varied with a \emph{well-specified} closure; $\Bclos=0$ at each
knob while $\Ggap$ rises with the insufficiency fraction, crossing zero near $0.25$.}
\label{fig:pka}
\end{figure*}
