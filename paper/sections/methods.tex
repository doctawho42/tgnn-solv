% COMPUTATIONAL METHODS.  Sec. 2 in full: the composed predictor, the error split, the estimator
% that measures it, the data, the model and the evaluation protocol.  JCIM carries no separate
% theory section, so the identity and its proof live here.
%
% SELF-CONTAINMENT (2026-08-02, editorial item A3).  A reader must be able to check the METHOD
% without the Supporting Information.  That is why THIS file now carries, in full: Lemma 1's
% statement, together with the cross-term condition and the three settings of z* that are conditions
% of its use; Proposition 1's statement; the noise-floor derivation with
% both functionals; the closure's validation against a reference implementation AND against a
% published evaluation of the same closure; the terminology table; and the run-family table.  Do
% not send any of those back to the Supporting Information to buy pages.
% WHAT IS DELIBERATELY NOT HERE, from 2026-08-10: the PROOFS.  All five now stand together in
% \S\ref{sec:si-proofs} (referee 3's trade against referee 1's request that Proposition 1's
% statement come back).  A statement is what a reader needs to check a method; the algebra behind it
% is what they consult once.  Do not move a proof back into this file to make it self-contained --
% self-containment is a property of the STATEMENTS and the conditions, and those are all here.
%
% WHAT MAY NOT LEAVE THIS SECTION -- a reader of the article alone must never be able to conclude
% that a control was not run:
%   * the data, the scaffold split, and what the labels are;
%   * the closure implementation AND its validation, including where the validation stops (the
%     dispersion term), because the fidelity result of Sec. 3.2.3 rests on that boundary;
%   * the evaluation protocol: the three oracle applications, which results are three-seed and which
%     single-seed, and on what row set;
%   * the estimator cell, the convention rule, the stratification rule and the admissibility rule,
%     all four fixed here, before any margin is reported;
%   * every disclosure that qualifies a result -- the separately tuned arms, the scaffold-leak guard
%     being a property of the builder and not of these runs, the wrong-split sigma-stream build, and
%     the placeholder repository.
% Construction detail (the solver's damped map, implicit-function gradients, the three phases by
% name, the loss taxonomy and per-phase weights, the architecture, the NRTL parametrisation, the
% COSMO-SAC constants and the 51-bin grid, all hyperparameters) is in the Supporting Information.
\section{Computational methods}
\label{sec:methods}

\subsection{The composed predictor and its error split}
\label{sec:framework}
For a solute~$s$ in solvent~$v$ at temperature~$T$, the target is $y=\lnx$, the log saturated mole
fraction. Dissolving a solid trades breaking the crystal lattice ($\Phicry$) against mixing the
freed molecules into the solvent ($\lng$), and solid--liquid equilibrium (SLE) sets the
log-solubility to their negative sum:
\begin{equation}
  \lnx = -\underbrace{\frac{\dHfus}{\Rgas}\!\Big(\tfrac1T-\tfrac1\Tm\Big)}_{\Phicry(T)\ \text{(crystal)}}
         \;-\;\underbrace{\lng(x_2,T)}_{\text{activity}} ,
  \label{eq:sle}
\end{equation}
with no constant-$\Delta C_p$ correction---$\Delta C_p$ being the difference in heat capacity
between the liquid and the solid. That term is absent from the displayed equation because it is
absent from the deployed model, which fixes $\Delta C_p=0$; the head that would predict it is off in
the pinned configuration. The exposure is one-signed and not small. At the group-contribution value
this corpus carries, $60$\,J\,mol$^{-1}$\,K$^{-1}$, the omitted term is a median $0.32$ and a mean
$0.40$ in \lnx\ over the $1318$ labelled test rows that carry a measured $\Tm$, $31$ solutes, positive on every one
of them, which is the size of the substitution penalty of \S\ref{sec:paradox} and twice the
supervision gain. It enters the physics arms and not the closure-free control, and it cancels
exactly in the evaluation-time substitution, where $\Phicry$ is common to the two arms
(\S\ref{sec:si-methods}). What it does not cancel out of is the training of the $\sigma$ head, which
is fitted through a crystal term carrying a bias of that size and in that direction; \S\ref{sec:surrogate}
reads the consequence. The activity term is produced by a
\emph{differentiable closure} $g$: a graph encoder predicts a $\sigma$-profile---the distribution of
screening charge density over a molecule, the histogram a COSMO model consumes---for each molecule,
and COSMO-SAC \cite{lin2002}, a segment-activity reformulation of COSMO-RS, maps the pair of
profiles to $\lng$. The segment-interaction energy inside that map---the electrostatic-misfit and
hydrogen-bonding terms setting what two surface segments cost each other---is the closure's
\emph{kernel}, the part the published revisions rewrite, so a closure variant is named for the year
of its kernel: the \emph{2002 kernel} this pipeline runs, and the \emph{2010 kernel} that types segments as
hydrogen-bond donors or acceptors instead of treating hydrogen bonding with one parameter. Write
$\zstar$ for the \emph{reference} physical latent, a tabulated $\sigma$-profile, and $\hat z$ for the
head's own prediction. \textbf{Two profile databases supply $\zstar$ below and they are never
interchangeable}: \emph{VT-2005}, the Virginia Tech 2005 tabulation \cite{mullins2006}, is what the
$\sigma$ head is supervised against and what is substituted for $\hat z$ at prediction time, and
\emph{UD}, the University of Delaware successor tabulation, is what the activity-coefficient rows of
\S\ref{sec:broad} are matched to. Every set, margin and figure below names which of the two it is
computed on, and no quantity is read across them; the one place they are deliberately set against
each other is the database-to-database bracket of \S\ref{sec:badclosure}. Feeding $\zstar$ in place
of $\hat z$ (a $\sigma$-oracle) isolates the closure from encoder error: if the closure were well
specified, $g(\zstar)$ should be the best available activity estimate. Each trained configuration,
and each closure variant evaluated, is an \emph{arm}; a \emph{DirectGNN} control arm drops the
closure and emits $\lnx$ directly, and the two were tuned separately (\S\ref{sec:eval}).

Everything below is stated for a general \emph{composed predictor} $\hat y=g(h(x))$ in which a fixed
closure $g$ consumes a learned physical intermediate $z=h(x)$, against a matched free predictor that
shares the encoder and drops $g$; solubility is one instance, and a second is the closure of \S\ref{sec:pka-main} for $\mathrm pK_a$,
the acid dissociation constant on a negative logarithmic scale.

The crystal/activity split of Eq.~\eqref{eq:sle} is not identifiable from solubility alone. At
infinite dilution with $\Delta C_p=0$ the observable is affine in $1/T$, and the activity intercept
and slope already span that plane, so the profiled Fisher information for $\dHfus$ is exactly zero
and the indeterminacy set is two-dimensional; at finite dilution the composition factor $(1-x_2)^2$
leaves an information of order $x_2^2$. The deficiency closes only under a rank-completing external
constraint---a direct crystal label, or a fixed activity slope (Lemma~\ref{prop:ident},
\S\ref{sec:si-proofs}). That is why an under-determined factor needs external single-component data
and why compensation between the factors is expected; it says nothing by itself about whether
grounding helps.

\subsubsection{The closure--insufficiency decomposition}
\label{sec:decomp}
Let $m$ be the closure's target---here the experimental infinite-dilution activity coefficient
(IDAC) $\lngi$, in \S\ref{sec:pka-main} the measured $\mathrm pK_a$---and $g(\zstar)$ the closure
evaluated on the reference latent.

% THE STATEMENT IS LETTERED (a)-(d) FROM 2026-08-10, and the lettering is load-bearing rather than
% cosmetic.  The letters used to be the PROOF's, and \S\ref{sec:estimator} and Table~\ref{tab:notation}
% cite "(c)" and "(d)" from this document three times; with the proof moved to
% \S\ref{sec:si-proofs} those three call sites would have pointed at part labels printed only in the
% other document.  Lettering the statement is what keeps them resolvable here.  Part (d) is stated
% and not merely proved for the same reason: \S\ref{sec:estimator}'s convention rule reads it, and
% limitation (v) reads it again.  The proof in \S\ref{sec:si-proofs} proves (a)-(d) under the same
% four letters; if either is relettered, releter both in the same pass.
\begin{lemma}[Exact closure--insufficiency decomposition and its one-sided bounds]\label{prop:decomp}
Let $g$ be a \emph{fixed} (non-fitted) closure and let $\zstar$ denote its \emph{entire}
argument (the profile pair together with the temperature wherever the closure reads one). Then
\emph{(a)}~the reference-input mean squared error splits exactly,
\begin{equation}
\begin{aligned}
  \underbrace{\Ex\big[(m-g(\zstar))^2\big]}_{\text{ref.-input MSE}}
  &= \underbrace{\Ex\big[\Var(m\mid \zstar)\big]}_{\Binsuf\ (\text{irreducible})} \\
  &\quad + \underbrace{\Ex\big[(\Ex[m\mid\zstar]-g(\zstar))^2\big]}_{\Bclos\ (\text{decoder bias})} ,
\end{aligned}
  \label{eq:decomp}
\end{equation}
the cross term vanishing identically; \emph{(b)}~$\Bclos\ge(\Ex[m]-\Ex[g])^2$;
\emph{(c)}~$\Binsuf\le\Ex[\Var(m\mid\mathrm{bin}(g(\zstar)))]$ for any binning of $g(\zstar)$; and
\emph{(d)}~$\Binsuf$ is a functional of the law of $(m,\zstar)$ alone and does not involve $g$, so
it is identical across closure conventions, while the bound in~(c) is not.
\end{lemma}

% THE PROOF OF LEMMA 1 MOVED TO \S\ref{sec:si-proofs} ON 2026-08-10 (referee 3, the trade against
% referee 1's Proposition 1 request, and made on an in-venue precedent: JCIM articles routinely state
% a result in the body and deposit its derivation in the Supporting Information).  Four of this
% submission's five proofs were already there; this was the odd one out, and it is thirty lines of
% tower-rule algebra in a Computational Methods section.
% WHAT DID NOT GO WITH IT, and why each is METHOD and not proof.  (1) The measurability CONDITION and
% its failure mode: it is what makes the identity applicable or not to a given closure, so a reader
% deciding whether this instrument fits their own composed model needs it before they reach for the
% Supporting Information.  (2) How z* is actually fixed on each of the three sets this paper runs
% the instrument on -- that is an analyst choice made in this work, not a step in a proof, and the
% file's own rule ("the estimator cell, the convention rule, the stratification rule and the
% admissibility rule, all four fixed here, before any margin is reported") covers it.
% Both are below, in two sentences, and both are ALSO inside the proof where they belong to the
% argument.  Do not delete them here as duplicates: one is the condition of use and one is the proof.
\noindent The proof is in \S\ref{sec:si-proofs}. Two of its parts are conditions of use rather than
steps, and both are stated here. \textbf{The identity holds because $g(\zstar)$ is
$\sigma(\zstar)$-measurable, and it fails if any argument of $g$ is left out of the conditioning
variable}: with $g=g(\zstar,T)$ and $T$ excluded, the cross term need not vanish and the binning is
no longer a coarsening of the conditioning $\sigma$-algebra, so neither the identity nor the upper
bound survives. Where $g$ reads a temperature---the broad IDAC set of \S\ref{sec:broad},
$\dim=103$---we accordingly take $\zstar=(\text{profile pair},T)$; on the VT-2005-matched set every
row sits at $298$\,K and the two readings coincide; and the Hammett closure of
\S\ref{sec:pka-main} reads a per-series constant pair as well as the substituent constant, so there
$\zstar=(s,\hammett^\star)$ and the conditioning is on both.

\noindent $\Bclos$ is the systematic discrepancy of a fixed physical map in the sense of
Kennedy--O'Hagan \cite{kennedy2001,brynjarsdottir2014}, and its dominance over $\Binsuf$ is the
empirical content of the claim that the ceiling is set by the closure rather than by the inputs.
\textbf{What $\Bclos$ is the error of is a composite object.} The identity compares $g(\zstar)$
against $\Ex[m\mid\zstar]$, and $\zstar$ is not measured but produced by a procedure---for
$\sigma$-profiles a quantum-chemical calculation on one conformer and one protonation state per
molecule at one level of theory. $\Bclos$ is therefore a joint property of the map and of the
procedure that produced the reference it reads, and not of the kernel's functional form alone. A
displaced reference loads onto $\Bclos$
through the curvature of $g$ even where $g$ is exactly specified (\S\ref{sec:si-repro}). The
deployed pipeline consumes that same composite, so it is the object a practitioner faces, but every
attribution below is to the pair and not to the kernel. Where model-discrepancy calibration
\emph{fits} a correction from held-out observations, here $g$ is fixed and $\zstar$ supplied from
outside the fit, so the composite's error is \emph{attributed} rather than corrected---available
only where an external latent oracle exists. \textbf{Exactness is contingent
on $g$ being fixed}: for a \emph{fitted} decoder the cross term does not vanish and a plug-in point
split is invalid. Because $\Binsuf$ is bounded above and $\Bclos$ below, a positive \emph{separation
margin} $\mathrm{MSE}-2\Binsuf^{\mathrm{up}}$ establishes that the closure's own error exceeds what
input insufficiency can account for, while a non-positive one is a failure to separate and licenses
nothing: a reversal would need a \emph{lower} bound on $\Binsuf$, which this instrument does not
supply. \textbf{Its size is a lower bound and not an estimate.} Since
$\mathrm{MSE}=\Binsuf+\Bclos$ exactly, $\Bclos-\Binsuf=\mathrm{MSE}-2\Binsuf$, and the reported
margin under-states that difference by $2(\Binsuf^{\mathrm{up}}-\Binsuf)\ge0$, an amount this design
cannot measure. Every margin below therefore says the closure's error exceeds its inputs' by
\emph{at least} the number printed, and a bootstrap interval on a margin is an interval on that
bound: its upper endpoint limits nothing.

\paragraph{What population, and what sets the resolution.} The population is the \emph{measurement
superpopulation}: a draw is a nominal compound pair \emph{at a temperature}, together with the
conformer, tautomer and protonation state actually populated there, and an independent determination
of $\lngi$ for it. Neither the conformational state nor the replicate is part of the conditioning
variable, so $\Var(m\mid\zstar)$ is the spread of $\lngi$ across the conformational ensemble and
across replicate determinations at one reference profile and temperature---non-zero for physical
reasons unrelated to sample size. Experimental label noise is a \emph{component} of it and never a
term added beside it, and what the design cannot do is \emph{observe} it, neither set holding two
rows at a common argument. The bounds are therefore conservative in one
direction only---were the superpopulation variance negligible the finding would be the stronger
$\Bclos=\mathrm{MSE}$---so nothing reported can be an artifact of over-estimating $\Binsuf$, no
entry anywhere is the share of the error the inputs contribute, and a low cluster-bootstrap
frequency $P$ is loss of resolution and never evidence for the inputs. Resolution is governed by
$\dim(\zstar)/n$.

\subsubsection{The decomposition and the grounding gap}
\label{sec:gap}
The decomposition is the measurement the conclusions rest on: assumption-light, needing neither a
representable encoder nor a lossless bottleneck. Alongside it we report a second, purely observable
quantity, the \emph{grounding gap} $\Ggap:=R_{\mathrm{orc}}-R_{\mathrm{e2e}}$, the oracle risk minus
the best end-to-end risk; the paradox is $\Ggap>0$. Under a representable encoder (A1) and a
lossless bottleneck (A2) it is sandwiched $0\le\Ggap\le\Bclos$, so $\Ggap>0$ would certify
$\Bclos>0$ (Theorem~\ref{thm:gap}, \S\ref{sec:si-proofs}). A1 is unverified---the linear probe of
\S\ref{sec:supporting} does not bear on it, scoring an untrained encoder of the same architecture
within its own spread---and A2 is unverifiable and
physically dubious; when A2 fails, $\Ggap>0$ conflates
closure misspecification with input insufficiency, a well-specified closure with a lossy latent
giving $\Bclos=0$ yet $\Ggap>0$. We therefore read $\Ggap$ as corroborating evidence and base the
model-dominance conclusion on the decomposition.

% PROPOSITION 1 MOVED TO \S\ref{sec:si-proofs} ON 2026-08-07 (page-reduction pass), where its proof
% and the limiting refinement already were, so the statement now sits with what proves it.
%
% AND THE STATEMENT CAME BACK ON 2026-08-10 (referee 1, must-fix), while the proof stayed there.
% The reason the earlier move was wrong: this paragraph gave the reader T(n) = Delta_infty - D(n)
% and nothing else, and SEVEN symbols in that line -- R_e2e, R_direct_infty, V_phys, V_direct,
% Delta_infty, D, n^star -- appear in no display, no definition and no row of Table 1, while three
% later sections (\S\ref{sec:pka-main}, \S\ref{sec:discussion}, \S\ref{sec:data-efficiency}) cite
% the proposition as an A PRIORI PREDICTION the measurements are then read against.  A prediction
% a reader cannot state cannot be checked against a measurement.
% WHAT COMES BACK AND WHAT DOES NOT.  The STATEMENT is here: the two asymptote-plus-variance
% expansions that define every one of those symbols, and the two parts (i) and (ii).  The PROOF
% stays in \S\ref{sec:si-proofs} together with the limiting refinement (iii) -- referee 3's trade,
% and the same trade Lemma 1 now takes.  The label prop:tax is DEFINED HERE from this date; the
% Supporting Information's proof header refers to it by \ref and must not redefine it, which
% scripts/analysis/check_split_refs.py enforces.
% THE SCOPE SENTENCES STAY WHERE THEY WERE, after the statement -- a penalty at the budget actually
% trained, no optimal-grounding theorem, no out-of-distribution claim -- because
% \S\ref{sec:pka-main} and \S\ref{sec:discussion} both read the rule and would otherwise read it
% unscoped.  Do not move a scope into the proposition body: the proposition is a statement about
% two risk expansions and says nothing about what this paper measured.
\paragraph{The sign rule, and scope.}\label{sec:tax} Whether the \emph{trained} physics model
outperforms the \emph{trained} black box at sample size $n$ follows a sign rule.

\begin{remark}[Sign of the physics penalty]\label{prop:tax}
Write $\Ex[\hat R_{\mathrm{phys}}(n)]\approx R_{\mathrm{e2e}}+V_{\mathrm{phys}}(n)$ and
$\Ex[\hat R_{\mathrm{direct}}(n)]\approx R_{\mathrm{direct},\infty}+V_{\mathrm{direct}}(n)$, with
$R_{\mathrm{e2e}}$ and $R_{\mathrm{direct},\infty}$ the two asymptotic risks, $V(n)\downarrow0$ the
estimation variance of each estimator at budget $n$, so that the \emph{physics penalty at budget
$n$} is
$\mathcal{T}(n)\approx\Delta_\infty+[V_{\mathrm{phys}}(n)-V_{\mathrm{direct}}(n)]$ with
$\Delta_\infty:=R_{\mathrm{e2e}}-R_{\mathrm{direct},\infty}$ its \emph{asymptotic} part and
$D(n):=V_{\mathrm{direct}}(n)-V_{\mathrm{phys}}(n)$ the variance a constrained prior saves. Then
\emph{(i)}~$\mathcal{T}(n)<0$ (physics helps) if and only if $D(n)>\Delta_\infty$: the variance a
constrained prior saves must exceed its asymptotic bias. \emph{(ii)}~If $\Delta_\infty>0$ and the
variance gap vanishes ($D(n)\to0$), there is a critical budget $n^\star$ beyond which
$\mathcal{T}(n)>0$ for all $n>n^\star$.
\end{remark}

\noindent Both clauses are rearrangements of the definitions above, and this is stated as a remark
for that reason: given the two expansions, \emph{(i)} is an identity and \emph{(ii)} its limit.
What it supplies is vocabulary---a name for the asymptotic part, a name for what a constrained prior
saves, and the budget at which they change places---and not a prediction any measurement below is
tested against. The limiting refinement in which $\Delta_\infty=\Bclos-\Ggap$ when the
control is asymptotically Bayes is not a rearrangement and is in \S\ref{sec:si-proofs}, with the
working of both clauses.
The consequence used below is that a prior is predicted to hurt precisely in the mature-data,
low-noise, well-covered regime. Each physics-versus-control difference reported here is a penalty at
the budget actually trained, not an asymptotic one; there is no ``optimal grounding'' theorem, and
the decomposition does not improve out-of-distribution accuracy. Its variance term $D(n)$ is what
carries the semiparametric phenomenon in which an estimated propensity outperforms the known-true
one \cite{hirano2003,robins1997}: a fitted nuisance can be the more \emph{efficient} estimator even
when its bias is no smaller, so a true-valued input need not give the lower total risk at finite
$n$.

\input{sections/notation-table}

\subsection{Data curation: sources, splits, and labels}
\label{sec:data}
Solubility measurements come from BigSolDB~2.0 \cite{krasnov2025bigsoldb}: $127{,}930$ rows over
$15{,}665$ solutes and $221$ solvents between $243$ and $438$\,K. The partition is by Bemis--Murcko
scaffold---a molecule's ring systems together with the linkers joining them, side chains
removed---into $111{,}724$ training, $8{,}103$ validation and $8{,}103$ test rows, so that every test
solute is unseen at training. A melting-point-independent miscibility/structure filter sets row
membership, and crystal $\Tm$, $\dHfus$, Hansen parameters and infinite-dilution activity
coefficients \lngi\ are merged on as per-solute labels where available. A split's rows do not all
carry the same label: of the $8{,}103$ test rows, $5608$ carry a solubility measurement and the
other $2495$ carry a melting point and no solubility, so every accuracy number below is on $5608$
rows and says so (\S\ref{sec:eval}).
% ITEM 4 OF THE 2026-08-11 REFEREE ROUND.  Whether the scored corpus honours the equation the
% article DISPLAYS is a methods fact and printed only in \S\ref{sec:si-lock}.  Both halves are
% exact: the stratum count and composition, and the arithmetic of removing it, which runs against
% this paper (dropping it WIDENS the physics penalty).  The accuracy half of that paragraph is a
% negative with 26 clusters and twelve intervals crossing zero and stays there; do not import it.
% The solid-form sentence names an uncontrolled variable in both label sources; it asserts no size,
% because none is measured here.
$796$ of those $5608$ rows, $14.2\%$ of them, carried by $26$ of the $147$ test solutes, have a
solute of more than one component---salts, two ionic liquids and four hydrates---for which the solid
in equilibrium with the saturated solution is not the one-component crystal Eq.~\ref{eq:sle}'s ideal
term is written for, and not one of those $26$ carries a measured melting point. They stay in:
dropping the stratum moves the control's mean and the grounded arm's the same way and widens the
physics penalty this paper reports, so removing it at this stage would flatter the comparison
(\S\ref{sec:si-lock}). Solid form is uncontrolled on both label sources---the solubility rows
aggregate literature measurements on whatever form each laboratory had, and the $\Tm/\dHfus$ labels
are merged from a separate compilation that need not refer to the same one---so a polymorph
difference enters the ideal term as a systematic and the inter-laboratory disagreement below as
part of its spread; neither document separates it. Crystal supervision is
scarce---the test and validation splits contain essentially no measured $\dHfus$, the quantitative
basis of the identifiability argument (\S\ref{sec:framework}). The external single-component data
used for grounding are an open crystal-property pool ($\sim$15{,}000 melting points), the VT-2005
$\sigma$-profile database, and ThermoML infinite-dilution activity coefficients
\cite{frenkel2011thermoml}. Two hazards the pipeline handles explicitly---melting-point unit
detection, and the instability of the seeded scaffold split across pipeline versions---are recorded
in \S\ref{sec:si-repro}.

\paragraph{The label-noise floor, and the two functionals it is read with.}
Independent determinations of the same system disagree, and that disagreement floors what any
predictor can achieve. On the raw BigSolDB rows, group by (solute, solvent, temperature to $1$\,K)
and keep the groups reporting at least two distinct sources; within a group the disagreement
statistic is the mean absolute deviation of \lnx\ from the group mean. Aggregating that per-group
statistic over groups requires a choice of functional, and the two standard ones differ by a factor
of five, so both are reported: the mean over groups is $0.15$ \lnx\ units for the $3948$ groups
backed by two sources and $0.31$ for the $349$ backed by three, while the median over the
same groups is $0.03$ and $0.09$. Collapsing exact-duplicate values first---the same measurement
re-tabulated across compilations, which would otherwise deflate the floor---leaves the mean at
$0.16$ over $4261$ groups. The scaffold-split MAE reported here ($1.70$ to $1.85$) is above the
larger functional by a factor of five, so label noise does not account for the accuracy ceiling
under either reading. This floor is a mean absolute deviation in \lnx; the aleatoric estimate
published for this task, an inter-laboratory RMSE of $0.75$ in $\log_{10}S$ on $34$ duplicate
solutions \cite{osanloo2024}, is a different functional on a different base, and the two are neither
pooled nor converted into one another anywhere below.

\paragraph{Disclosure: the scaffold guarantee is not certified for these runs.}
The released stream builder excludes any pool molecule whose scaffold appears in validation or test.
One $\sigma$-stream build on disk was nevertheless generated against the wrong split files, and
per-run stream provenance was not logged, so the artifacts cannot certify which build fed which arm
and the guarantee is a property of the builder rather than of these runs. Of the $147$ solutes
spanned by the $5608$ labelled test rows, $7$ carry their own reference profile in that build and
$8$, those $7$ among them, carry a scaffold relative, so the second count is the union and deleting
it deletes both. The ungrounded arm carries no $\sigma$ stream either, so the supervision contrast
is the one a leak can reach and the $0.20$ gain of \S\ref{sec:paradox} is not certified leak-free until the
re-run below. The file evidence, the contrast each fact bounds, and the one molecule the leak
touches in the $n{=}44$ analysis of \S\ref{sec:surrogate} are in \S\ref{sec:si-repro}.

% THE RE-RUN PRE-COMMITMENT LIVES IN \S\ref{sec:si-repro}: the protocol, the three-branch decision
% rule and the display inventory with its named exceptions and its same-pass rule.  What stays here
% is this summary, which must keep three things a reader cannot reconstruct from the disclosure
% above: that the protocol was fixed BEFORE the numbers exist, that the decision is stated in all
% three directions and not only the favourable one, and that the list of displays the five-seed
% values replace is closed.  Do not let this shrink to a bare cross-reference; the promise has to be
% legible without the SI open.  If a display carrying the ungrounded, grounded or sigma-oracle arm
% is added anywhere, it goes into the inventory in \S\ref{sec:si-repro} in the same pass.
% 2026-08-10, READABILITY PASS: the protocol's mechanics -- the certification assertion, the two
% SHA-256s, the intersection rule, the three arms and the boundary in both directions -- are printed
% in \S\ref{sec:si-repro} and were printed twice.  This paragraph now carries the promise and not
% the mechanics.  Every number it dropped is in that section.
\paragraph{The leak-free re-run.} Its protocol is fixed before its numbers exist
(\S\ref{sec:si-repro}): the ungrounded and the grounded arm at five seeds ($42$ through $46$), the
published COSMO-SAC configuration unchanged, on a $\sigma$ stream rebuilt against \emph{this}
split's files and certified inside each training run, read on the same $5608$ labelled test rows and
reported seed by seed. So is the decision it settles, which the supervision contrast alone hangs on,
stated in three branches and not in the favourable one alone---gains of one sign, gains straddling
zero, gains of the opposite sign---and so is the list of displays the five-seed values replace. The
substitution result is unaffected in all three branches: it is one checkpoint evaluated two ways,
and no $\sigma$-stream build can reach it.

\subsection{Model, closure, and training}
\label{sec:model}
A \emph{shared} message-passing encoder embeds solute and solvent, a bipartite cross-attention block
lets them interact, and the resulting pair representation feeds every head. Two further backbones
are implemented and neither carries a number reported here; the physics-channelled one was run
against this encoder and did not beat it, an observation recorded with no set, seed count or
interval attached to it (\S\ref{sec:si-methods}), so no accuracy comparison between backbones is
claimed. A fusion head predicts $\Tm$ and $\dHfus$, giving the
ideal term $\Phicry(T)$ of Eq.~\eqref{eq:sle}. The activity term \lng\ comes from one of three
differentiable closures. Two are controls: the fitted NRTL (non-random two-liquid) excess-Gibbs-energy model, and its
symmetric one-parameter collapse. The third, parameter-free, is the closure under study: a $\sigma$-profile head emitting a
$51$-bin screening-charge-density profile per molecule, and a COSMO-SAC layer mapping the profile
pair to \lng. \textbf{That layer runs COSMO-SAC's residual, segment-activity part alone.} The
Staverman--Guggenheim combinatorial term---the contribution molecular size and shape make to the
entropy of mixing, and the second factor of $\gamma=\gamma^{\mathrm{res}}\gamma^{\mathrm{SG}}$ in
Lin--Sandler---is off in every arm the substitution result is reported on, and the $\sigma$ head
emits a profile shape and a cavity area but no cavity volume, so the term cannot be evaluated from
the $\sigma$ head's own output. That is a model-form choice and not an analyst convention: the kernel
constants the layer uses are the $2002$ values, regressed with the combinatorial term present, so
the residual-only setting runs a fitted kernel outside its parameterisation. Its size on a published
evaluation is a mean shift of $-0.74$ in $\lngi$ over $7889$ records, from $+0.03$ to $-0.71$ in
bias. The two settings are the \emph{full} versus \emph{residual-only} pair on which
\S\ref{sec:estimator} and \S\ref{sec:measure}
turn, and one arm of Fig.~\ref{fig:paradox} restores the term by taking a molar volume from a
separate auxiliary head and passing it detached, so the combinatorial term is active but the
solubility loss cannot train it. Inside the layer, the segment activities are themselves a fixed point, truncated at a count
that is a free hyperparameter and is \emph{not} the same in training and scoring: the COSMO-SAC arms
here train at $16$ segment iterations and are scored at $30$. Sixteen was chosen because it lands
within $6\times10^{-5}$ of $30$ on these weights, so the two agree by construction on this
configuration and not as a property of the count; a checkpoint fitted at a different count is scored
through an operator it was never fitted against. The exposure that carries to the substitution
contrast is not measured. A second, independent truncation sits outside it---the SLE fixed point in
$x_2$, run to a different count in training than in scoring---and only that one can be re-solved from
a deposit: the NRTL arm moves by $+0.0025$ in seed-mean MAE at its training count and a further
$+0.017$ to convergence on the outer loop. That figure prices the outer loop and not the segment one.
The COSMO-SAC arms can be re-solved on neither,
their kernel being a $\sigma$-profile pair no deposit and no retained checkpoint carries
(\S\ref{sec:si-methods}). The saturated mole fraction solves $\lnx=-\Phicry(T)-\lng(x_2,T)$ as a damped fixed point in
$x_2$; a bounded, gated adaptive correction perturbs the physical parameters rather than the output,
so the physical decomposition survives it.

External single-component data enter through \emph{grounding streams}---the training-time sense of
grounding. Each stream is a sidecar of \emph{self-solvent} rows (no solubility label, only the target
factor's mask active) interleaved into training under the guard above, grounding the crystal factor
from the melting-point pool and the activity factor from the $\sigma$-profile database, and so
identifying from external data a factor that solubility alone leaves non-identifiable and that a
black-box predictor cannot use. Training runs a three-stage curriculum under a weighted loss, and
the stages are named by number wherever one of them is referred to below: \emph{P1}, a warm-up on
the auxiliary streams alone; \emph{P2}, joint training against solubility; \emph{P3}, a low-rate
stabilisation stage. The
schedule pinned for the arms of Fig.~\ref{fig:paradox} freezes the $\sigma$ head at the end of P1,
so the $\sigma$ stream enters the warm-up only.

\paragraph{The in-house closures, and where their validation stops.}
% Artifact: results/b_insuff/closure_reference_validation.json (2002, rmse 0.0035) via
% scripts/analysis/run_closure_reference_validation.py. The 2010 figure (rmse 1.4e-4) has no
% deposited artifact -- scripts/analysis/validate_cosmo2010_layer.py printed to stdout on the
% run it comes from -- which the SI states.
% THE 2010 RMSE IS PRINTED HERE, 2026-08-08.  The sentence used to say only "reproduces the
% NIST 2010 reference with dispersion off---that second figure quoted from its validator's
% console", and there was no second figure in it: the 2002 layer printed 0.0035 and the 2010
% layer printed nothing, so "that second figure" referred to a value the article never gave.
% The number is \S\ref{sec:si-methods}'s (SI: "reproduces the NIST COSMO-SAC-2010 reference to
% RMSE 1.4e-4 ln gamma").  Keep the console clause: it is the disclosure that this one value is
% not on the deposited record, and the Data-availability section names it as one of four.
The dispersion-off closures in the fidelity measurement (\S\ref{sec:fidelity}) are our own
differentiable implementations, so those arms share one code path, and each consumes its own
model-prescribed $\sigma$-averaging (Mullins for 2002, Hsieh for 2010). Our 2002 layer---the
\emph{2002, all channels} arm wherever the kernels are compared, so named because it sums the
segment contributions of all three profile channels rather than the nonpolar one
alone---reproduces the NIST reference COSMO-SAC-2002~\cite{bell2020cosmosac} to RMSE
$0.0035\,\ln\gamma$ on identical VT-2005 profiles. Our COSMO-SAC-2010/dsp
layer~\cite{hsieh2010,hsieh2014dsp}, built on the \emph{typed} latent of three donor/acceptor-resolved
$\sigma$-profiles per molecule in place of one, reproduces the NIST 2010 reference with dispersion
\emph{off} to RMSE $1.4\times10^{-4}\,\ln\gamma$. That second figure is not deposited, its
validator having written no artifact where the 2002 one deposits its comparison, so it cannot be
checked against the deposited record
(\S\ref{sec:si-methods}). The dispersion term is \emph{not} validated to that standard, which is why every
dispersion-off contrast is computed on our layers while the \emph{2010, dispersion on} runs are
quoted from \texttt{cCOSMO}, that reference implementation itself.

\paragraph{The same closure against a published evaluation.}
% Artifacts: results/published_idac_check/{summary.json,scored_records.csv,table_closure_anchor.tex}
% via scripts/analysis/run_published_idac_closure_check.py.
% COMPRESSED 2026-08-07 (page-reduction pass).  The scoring mechanics -- the three record files, the
% CAS-to-InChIKey resolution, the 7889-of-8138 count and the 2279 pairs -- moved to
% \S\ref{sec:si-repro}.  WHAT MUST STAY: the published two-database bracket that motivates defining
% B_clos on the composite, the fact that the deployed closure lands INSIDE it, and BOTH open limits
% -- the unmatched profile-database axis, and that the published 6977 points are not proven to be
% the rows scored here.  The conclusion is licensed only "subject to those"; do not print it bare.
% NARROWED 2026-08-07 (referee, round 5).  This paragraph used to claim the deployed closure lands
% inside the bracket "on both statistics and under both conventions, as do this paper's own two
% sets".  The table beside it does not show that.  Published Lin-Sandler brackets are AAD
% [0.6775, 1.7457] and R^2 [0.8780, 0.9599] (results/published_idac_check/summary.json).  On the same
% records ours is AAD 0.7651 res / 0.9777 full -- inside -- and R^2 0.9192 res / 0.8818 full, the
% full one clearing the lower end by 0.004 and printing as 0.88 against 0.88.  This paper's OWN two
% sets are inside on AAD (1.1155, 0.8072) but NOT on R^2 (0.6079, 0.4031), and the table scores them
% in the residual-only convention only.  Hence "in AAD" in the conclusion below.  Do not widen either
% sentence back without re-reading the table.
Reproducing a reference implementation fixes the code; it does not say whether the error this paper
decomposes is the error a published COSMO-SAC has, or something peculiar to this data assembly.
Table~\ref{tab:closure-anchor} answers that. De Souza et al.\ \cite{desouza2025cosmosac} score the
original Lin--Sandler COSMO-SAC on the infinite-dilution activity-coefficient database distributed
with \emph{The Properties of Gases and Liquids}, at AAD $=\Ex|\ln\gamma_{\mathrm{calc}}^\infty
-\ln\gamma_{\mathrm{exp}}^\infty|$ of $1.75$ with HF-TZVP profiles and $0.68$ with BP-TZVPD-FINE:
one closure, two profile databases, a factor of $2.6$ between them. An evaluation of COSMO-SAC is
in that sense an evaluation of the profiles it was handed, which is why $\Bclos$ is defined above on
the composite of map and profile calculation and not on the kernel. We ran our own differentiable layer
over the same records (\S\ref{sec:si-repro}), and on AAD it lands inside the
bracket the published closure spans across profile databases, under both conventions; on $R^2$ it is
inside in the residual-only convention and level with the bracket's lower end in the full one, the two
printing alike at $0.88$. This paper's own two sets are inside on AAD alone, and in the residual-only
convention only---their $R^2$, $0.61$ and $0.40$, is below the published range. Two limits stay open: the profile database is the one axis we
cannot match, VT-2005 being what this paper deploys and the axis that publication shows dominates;
and the publication's $6977$ points are not proven to be the same rows as the $7889$ scored here,
the record selection not being stated file by file. Subject to those, the closure error decomposed
in \S\ref{sec:measure} is, in AAD, the error a published COSMO-SAC has; which half of this paper's chemical
localisation carries across to that set, on $130$ times the data and someone else's compilation, and
which half does not, is in \S\ref{sec:badclosure}.
The same publication scores a later kernel on the same records, and its rows are in the table because
they put the deployed one in its place: COSMO-SAC-HB2, which types segments as donors or acceptors,
reaches AAD $0.46$ to $0.48$ at $R^2$ $0.96$ to $0.97$, about half the deployed closure's $0.77$ and
a third of the $1.75$ that anchors the profile-database bracket. This pipeline runs the $2002$ kernel
because it is the one whose differentiable re-implementation can be checked against a public
reference here, and the results below are substitutions inside a fixed map rather than claims that
the map is the best available. \S\ref{sec:badclosure} runs the $2002\to2010$ step on this paper's own
sets and finds the improvement confined to the smaller set's chemistry. Every ceiling this paper
reports is accordingly this kernel's ceiling and not COSMO-SAC's.

% ---------------------------------------------------------------------------
% GENERATED by scripts/analysis/run_published_idac_closure_check.py; the full-convention row is
% added here from the same summary.json (ours.all_scored_records.full). Do not retype a value.
% This float answers half of the referees' "is your closure normal?" objection -- the data half.
% The other half, the implementation, is the NIST reproduction in the paragraph above.
%
% SET AT \footnotesize ACROSS THE FULL MEASURE, 2026-08-08 (the readability pass).  It used to be a
% one-column `table' at \scriptsize -- 8 pt, two sizes under the body -- because its widest row is
% 227 pt of a 240 pt column and does not fit in one column at 10 pt.  A `table*' has 503 pt, so the
% same seven rows fit at 10 pt with room to spare; the cost is about 7% of a page and the buy is two
% type sizes.  Do NOT put it back in one column: the only way back is \scriptsize.
% `p' IS IN THE SPECIFIER because this is now a starred float (see the float note in preamble.tex):
% a deferred `table*' blocks every starred float after it, and `p' keeps a float page available so
% the queue cannot back up.
% THE NOTE KEEPS ONE CLAUSE.  The worst-cell and 298 K arithmetic that used to run on after it is a
% per-cell reading of the middle block, not something the table cannot say without it; it is in
% sec:si-repro beside the paragraph that scores that block, and the caption says so.
% ---------------------------------------------------------------------------
\begin{table*}[tp]
\centering
% THE SECTION REFERENCE LEFT THIS CAPTION, 2026-08-10.  It read "The middle block's per-cell and
% per-regime values are in \S\ref{sec:si-repro}", which was the only cross-reference in any caption
% of this document; a caption states what is shown.  Nothing is lost by the deletion: the paragraph
% above already says "We ran our own differentiable layer over the same records (\S\ref{sec:si-repro})",
% which is the same destination on the same block, in the sentence that makes the claim.
\caption{The deployed closure against a published evaluation of the same closure, and this paper's
own two sets through it. AAD is the mean absolute error in $\ln\gamma^\infty$, $n$ the number of
scored records.}
\label{tab:closure-anchor}
{\footnotesize
\setlength{\tabcolsep}{8pt}
\renewcommand{\arraystretch}{1.05}
\begin{tabular}{@{}llrrr@{}}
\toprule
COSMO-SAC-2002 on & profiles & $n$ & AAD & $R^2$ \\
\midrule
\multicolumn{5}{@{}l@{}}{\emph{As published \cite{desouza2025cosmosac}}}\\
PGL 6th ed.\ IDAC set & HF-TZVP & $6977$ & $1.75$ & $0.88$ \\
PGL 6th ed.\ IDAC set & BP-TZVPD-FINE & $6977$ & $0.68$ & $0.96$ \\
\addlinespace[2pt]
\multicolumn{5}{@{}l@{}}{\emph{A later kernel on the same evaluation \cite{desouza2025cosmosac}}: COSMO-SAC-HB2\tnk{b}}\\
PGL 6th ed.\ IDAC set & HF-TZVP & $6977$ & $0.48$ & $0.96$ \\
PGL 6th ed.\ IDAC set & BP-TZVP & $6977$ & $0.48$ & $0.97$ \\
PGL 6th ed.\ IDAC set & BP-TZVPD-FINE & $6977$ & $0.46$ & $0.97$ \\
\addlinespace[2pt]
\multicolumn{5}{@{}l@{}}{\emph{The same set, through this paper's closure}}\\
\quad full convention\tnk{a} & VT-2005 & $7889$ & $0.98$ & $0.88$ \\
\quad residual-only & VT-2005 & $7889$ & $0.77$ & $0.92$ \\
\quad\quad non-aqueous rows & VT-2005 & $4347$ & $0.47$ & $0.68$ \\
\quad\quad aqueous rows & VT-2005 & $3542$ & $1.13$ & $0.83$ \\
\addlinespace[2pt]
\multicolumn{5}{@{}l@{}}{\emph{This paper's own sets, residual-only convention}}\\
broad IDAC set & UD & $477$ & $1.12$ & $0.61$ \\
VT-2005-matched set & VT-2005 & $60$ & $0.81$ & $0.40$ \\
\bottomrule
\end{tabular}
\tabnotes{%
\tnk{a}\,The like-for-like row, a published COSMO-SAC carrying its Staverman--Guggenheim term; the
residual row is the setting this paper's $\Bclos$ is measured in.\\
\tnk{b}\,A donor/acceptor-typed hydrogen-bonding kernel, not the kernel this paper deploys. Printed
because it is on the same $6977$ records and beats the deployed closure by a factor of about two.}}
\end{table*}

\subsection{Evaluation protocol, and the DirectGNN control}
\label{sec:eval}
The \emph{oracle substitution} replaces the predicted $\sigma$-profile $\hat z$ with the reference
VT-2005 profile $\zstar$ before the closure, matched by canonical SMILES. What is replaced is one
object and not two: the $\sigma$-head emits a normalised $51$-bin shape and a cavity area whose
product is the area-weighted profile the closure reads, and for a matched molecule that pair is
replaced by the tabulated pair, which stands in the same relation---so the reference area reaches
the residual term's $A_2/a_{\mathrm{eff}}$ prefactor as part of replacing that one object rather
than as a separate choice.

% ADDED 2026-08-11.  A referee of the first round named this and it was not disclosed.  Verified
% here directly rather than taken from a report: the checkpoint's two role adapters are separate
% 64x64 layers whose weight vectors are near-orthogonal (cosine -0.015, max|diff| 0.32), and 40
% self-pairs pushed through the deployed model return the numbers below.  The instrument's own null
% is exact and is deposited: one profile on both sides returns 0.0 to machine precision.
% SCOPE, and keep it: the sizes are from the one trained COSMO-SAC head the tree holds, a retired
% checkpoint.  What is architectural is that the two roles are untied at all.
A property of the head bears on what that substitution does, and it is a defect rather than a
choice. The encoder carries a separate learned adapter for the solute and the solvent role, and the
$\sigma$-head reads whichever the molecule occupies, so one molecule has \emph{two} predicted
profiles. A $\sigma$-profile is a property of a molecule alone, and the closure reads the pair, so
the model can and does violate an identity no activity model may violate: on a self-pair, where
$\ln\gamma_2$ must be exactly zero, the deployed model returns a median $-0.058$, a mean magnitude
of $0.279$ and a maximum of $1.31$ over $40$ solutes, nonzero beyond $0.01$ on $95\%$ of them. With
one profile on both sides the same computation returns zero to machine precision, so the violation
is the role gap and nothing else. The substitution overwrites both roles at once and therefore also
closes that gap, which is a second thing it changes besides the profile values; the two are not
separated here. Tying the two roles is the change this calls for, and it is a retraining.
% ITEM 5 OF THE 2026-08-11 REFEREE ROUND.  The substitution swaps a shape and a SIZE at once, and
% A_2/a_eff is a multiplicative prefactor on the residual term, so a reader is owed which of the
% two objects the fidelity metric of \S\ref{sec:surrogate} is taken on and how far the learned area
% sits from the tabulated one.  The metric is on the NORMALISED shape (both sides normalised to sum
% to 1 in run_compensation_surrogate.py's load_true_shapes/predict_sigma_hat), so it carries no
% information about the area at all.  The area ratio on these arms is unrecoverable -- the tree
% keeps no COSMO-SAC weights -- and on the head where it can be read it is 1.008 solute / 1.579
% solvent (results/softmax_floor/profile_geometry.json).  Do not state the departure without saying
% which object it is on.
The departure this paper reports between the two,
$36\pm1\%$ and $51\pm2\%$ in \S\ref{sec:surrogate}, is a relative $L_2$ distance between
\emph{shapes}, both sides normalised, and prices no part of the area. How far the deployed arms'
areas sit from the tabulated ones is not recoverable from the retained artifacts, which keep none of
their weights; on the one trained COSMO-SAC head the tree keeps, the learned cavity area is a median
$1.01$ times the tabulated one over the solutes it scores and $1.58$ times it over the solvents. That
head reproduces neither the size nor the direction of the substitution penalty reported here, so
those two ratios price the quantity and not the arms.
Solute and solvent are matched independently. Nothing else is replaced,
the crystal head reading a solute-only embedding the substitution does not reach; the bounded
correction does read the closure's own parameters, so the crystal values the solver finally receives
% ITEMS 2 AND 3 OF THE 2026-08-10 REFEREE ROUND, both settled from
% results/e5_sigma_grounding/section_s1_substitution_perturbation.json and
% results/e5_sigma_grounding/ranking/rank_final.json (integrity.phi_max_abs_diff_vs_grounded_a).
% (2) The median magnitude read 4.7 here and 4.5 in \S\ref{sec:si-methods}: 4.7246 is the SEED-42
% median under a sentence that says three seeds, and the pooled three-seed median is 4.5339 over all
% test rows and 4.5370 over the substituted ones -- 4.5 either way.  Both places now print 4.5 and
% both name the set.
% (3) The two maxima are the SAME quantity over DIFFERENT row sets, and each must say which: 7.1e-4
% (seed 43) is over the 8066 substituted rows of all 8103 test rows, 4.2e-4 (seed 43) over the 5608
% labelled rows the results are scored on, which are a subset -- so the smaller figure is not a
% competing value.  Both sets are now named wherever either number prints.
shift by a little: pooled over the three seeds and over the $8066$ of the $8103$ test rows where the
substitution applied, at most $7\times10^{-4}$ in $\Phi$, against a median $|\Phi|$ of $4.5$ on that
same pooled set. Restricted to the $5608$ labelled rows every solubility figure below is scored on,
that maximum is $4.2\times10^{-4}$.
\S\ref{sec:si-methods} lists every replaced and unreplaced quantity, and
gives what the match rule reaches: the reference table covers $99.3\%$ of the labelled test rows on
the solvent side and $5.3\%$ on the solute side, so the substitution contrast below is carried by
the solvent profile.

It is applied three ways
to rule out implementation artifacts: at evaluation only; injected at training time so the model
co-adapts; and as a channel-swap control, injected at training under a coordinate-descent schedule
that freezes the crystal branch during the SLE phase, so that only the activity branch refits
against it. The three differ in when the replacement is made, not in what is replaced. The last two
are at seed $42$ only. An analogous override substitutes reference crystal
targets. The activity target for the decomposition is the infinite-dilution
$\lngi=\lim_{x_2\to0}\lng$, on two sets that differ in their reference database and their regime
(\S\ref{sec:broad}--\S\ref{sec:vt2005}).

DirectGNN shares the encoder, interaction, readout and pair representation, adds a thermometer
(cumulative-threshold) temperature encoding, and maps directly to \lnx\ with no heads, closure or
solver. The two arms therefore share their representation but not their inputs or their tuning: the
control receives an explicit temperature encoding the physics arm does not, seeing $T$ instead
through $\Phi(T)$ and the closure, and the optimisation settings were tuned per arm and differ by up
to an order of magnitude. The TGNNSolv$-$DirectGNN gap is accordingly a comparison of two separately
tuned pipelines that differ in the thermodynamic bottleneck, not an isolation of the bottleneck, and
every physics-penalty comparison below carries that caveat (\S\ref{sec:discussion}).

The controlled comparisons of \S\ref{sec:paradox} are run at three seeds and reported as mean$\pm$sd
on a fixed row set; $5608$ is that lock---the test rows that carry a solubility measurement, $5608$
of the $8103$ (\S\ref{sec:data})---so all arms score identical rows. The $2495$ rows outside it
carry a melting point and no solubility and are unscorable in every arm alike, the control included;
none is dropped for anything an arm predicted. The set is fixed by which
rows carry a label and not by which rows an arm handled---the lock's finite-prediction clause is
inert, removing no row from any arm at any seed---and \S\ref{sec:si-lock} gives the decomposition. The
runs behind every number reported here fall into twelve families, a family being a set of runs
sharing a script, a corpus and a seed policy; Table~\ref{tab:runs} gives each number its family, its
seed count and the artifact it regenerates from.

% THE RUN-FAMILY TABLE MOVED TO THE SUPPORTING INFORMATION ON 2026-08-08 (the readability pass),
% whole, and now sits at the head of \S\ref{sec:si-repro}, which is the section its rows point into.
% It went because it is PROVENANCE and not evidence: no claim in this article is read off it, its
% third column is a list of cross-references to displays printed elsewhere, and at 43% of a page in
% 8 pt it was the second-largest float in the document.  The sentence above is what it left behind,
% and it carries the one fact a reader needs at this point -- that every number has a named family
% with a seed policy -- rather than an instruction to go and look.
% Do NOT bring it back to shorten that sentence.

\subsection{Estimating the split: one cell, one convention, one rule}
\label{sec:estimator}
The instrument this section fixes is a procedure, and it takes three inputs and nothing else: a fixed
closure $g$, a table of reference values $\zstar$ its argument can be read at, and measurements $m$
of its target on rows where both exist. It returns, per chemically defined row set, the closure's own
error $\mathrm{MSE}$, an upper bound on what the inputs cannot resolve, the margin between them, a
cluster interval on that margin, and a verdict of admissible or not with the test that decided it.
Four analyst choices set the binning bound of Lemma~\ref{prop:decomp}(c)---the bin count, where the
equal-count bin edges fall, the within-bin variance convention, and the combinatorial convention---and
all four are fixed here, before any margin is reported. One setting of them, together with the choice
of the row or the molecule pair as the unit, is an \emph{estimator cell}. The four rules below are
the procedure in full; the map of \S\ref{sec:broad} is what running it on this closure returns.

\emph{The estimator cell.} Eight equal-count bins of $g(\zstar)$, the \emph{unbiased}
(Bessel-corrected) within-bin
variance, and the row unit, applied identically to every set and every stratum whatever its size.
The bin count is the choice with the most leverage---it takes the broad set's aggregate margin from
$+0.01$ to $+0.96$---so the cell is not left standing alone anywhere a finding rests on it: the
same sweep is run for the strata the admissibility rule passes, and reported with them.
Enumerating every equal-count contiguous partition, the bin-edge choice is immaterial on the broad
IDAC set and material on the VT-2005-matched set, whose margin changes sign inside the envelope; we
accordingly read that set's margin as consistent with zero and carry the claim on the broad set in
stratified form.

\emph{The convention rule.} The margin depends on the combinatorial convention, so one rule governs
both sets: every margin is read in the \emph{residual-only} convention, pairing
$\mathrm{MSE}$ and the bound computed under that same convention. Taking the residual-only convention on
the smaller set and the full convention on the broad one would be the favourable choice on each set
taken separately;
one rule applied to both costs the broad set's margin about half its value. The Jensen
constant-offset bound is convention-specific---it separates under the full convention and inverts
under the residual-only default---so what is reported rests on the binning
bound, which fits nothing and so lets no held-out row borrow strength from a near-twin. Both bounds
are valid on the one population quantity, by Lemma~\ref{prop:decomp}(d), and so is the smaller.

\emph{The stratification rule.} The broad set is stratifiable where the VT-2005-matched set's $60$
pairs are not, on two axes that are pure functions of molecular structure: an ordered nine-class
cascade of substructure tests over the solvent, hydrogen-bonding role first and dominant functional
group second, and the solute's role as water or as an organic. Every class name is a substructure
test and not a chemical judgement: a \emph{glycol ether} here is a solvent carrying a hydroxyl group
and two or more oxygens, and an \emph{aprotic} solvent one with no O--H or N--H hydrogen to donate.
The classifier reads a SMILES string
and nothing else---never $m$, $g$, the squared error, the source or the bootstrap---so no cell can
have been chosen on its outcome, which is a property of the code rather than an assurance. A stratum
is \emph{boundable} at $n\ge40$, where eight bins hold five rows; the smaller ones are reported at
that same cell, with the adaptive bin count $\max(3,\lfloor n/10\rfloor)$ in the rows $n$ it is
applied to carried beside them in the deposited table, nothing being dropped for being small.

\emph{The admissibility rule.} A stratum is \emph{admissible} only if it is \emph{(a)}~boundable at
that fixed cell and \emph{(b)}~robust to leave-one-source-out---its margin keeps its sign when each
source publication contributing to it is deleted in turn---in all four unit\,$\times$\,convention
cells, since the map reports all four. That last clause is load-bearing and is never dropped:
without it the set taken whole passes too. A stratum drawing on one publication fails~(b) by
non-testability, there being no deletion to perform; a deletion leaving fewer than $16$ rows leaves
the fixed cell undefined, and fails~(b) as well. A third clause reduces the admissible \emph{cells}
to row sets, and it is stated here with the same precision rather than left to prose, because the
one claim this paper carries from the map and the multiplicity null that tests that claim both turn
on it. \emph{(c)}~Admissible cells holding identical rows count once; and an admissible positive row
set $X$ is \emph{demoted} to another, $Y$, when $Y$ is admissible and positive, $Y$ holds more rows
than $X$, more than half of $X$'s rows are $Y$'s, and the residual $X\setminus Y$ is not itself
admissible---so that $X$'s margin does not stand outside $Y$. What decides it is that residual and
not the overlap: a row set most of whose rows lie inside a larger one survives~(c) whenever its own
margin stands outside that larger one, so \emph{(c)} is not containment and no cell is demoted for
overlapping (\S\ref{sec:si-map}).
Clause~(c) takes the six admissible cells of \S\ref{sec:broad} to three distinct row sets;
positivity then removes one of the three and~(c)'s residual test the second, leaving one. It is
rebuilt on every draw of the multiplicity null, on the permuted labels, like the rest of the rule
(\S\ref{sec:si-null}). Everything else is \emph{reported in the map and not
stated}, with its $n$, its sources, its margin, its interval and the reason. The rule governs the
cells that favour us and those that do not alike, and it tests whether a number is stable, not what
it means: an admissible stratum whose margin is not positive, and one that~(c) demotes,
both establish nothing.
