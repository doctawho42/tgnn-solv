% Table 1.  CUT ON 2026-08-02 to the symbols that genuinely collide, at referee request.  The
% previous version was a twelve-row glossary of every symbol and every coined term the Results use;
% a symbol used repeatedly and defined where it appears does not need a row, and the coined terms
% went with the grading vocabulary.  What is left is the two letters this manuscript would otherwise
% use for two different things, because it spans two chemical domains and two literatures: sigma and
% Gamma.  Both renamings (\hammett for the Hammett constant, \Ggap for the grounding gap) are
% defined in preamble.tex and are what the body prints.
% Single-column `table', not `table*': at two rows a full-width float wastes a page-width band.
% Do not grow this back into a glossary.  If a symbol needs explaining, explain it where it is used.
\begin{table}[t]
\centering
\caption{The two symbols that carry more than one meaning in this manuscript, and the renamings
that separate them.}
\label{tab:notation}
{\footnotesize
\begin{tabular}{@{}lp{0.60\linewidth}@{}}
\toprule
Symbol & Meaning here \\
\midrule
$\sigma$ & standing alone, the screening-charge-density profile; $\sigma(\cdot)$ with an argument,
the generated $\sigma$-algebra; \hammett, the Hammett substituent constant of the pKa closure,
renamed from $\sigma$; $\sigma_\eta$, experimental label noise \\
$\Gamma$ & $\Gamma_S$, the COSMO-SAC segment activity coefficient of Eq.~\eqref{eq:cosmo}; the
grounding gap is \Ggap, renamed from $\Gamma$ so that the two do not collide \\
\bottomrule
\end{tabular}}
\end{table}
