% =====================================================================
% THE ARTICLE.  The Supporting Information is a separate document,
% grounding_paradox_si.tex; both \input preamble.tex and neither \inputs the other.
%
% SECTION SHAPE (2026-08-02, referee request): four sections only --
%   1 Introduction            field, related work, the assumption under test, what was done
%   2 Computational methods   the composed predictor, the identity and the conditions of its use
%                             (its proof went to the Supporting Information on 2026-08-10), the estimator,
%                             the data and its noise floor, the model and its two validations,
%                             the evaluation protocol and the run-family table
%   3 Results and discussion  the two senses of grounding; the split on the activity data;
%                             the latent's departure; the second closure; what it licenses;
%                             limitations
%   4 Conclusions
% There is no separate theory section and no separate related-work section: JCIM carries
% neither, and the material is inside 2 and 1 respectively.
%
% THE 2026-08-02 REBALANCE (editorial item A3).  Three referees, calibrating against published
% JCIM papers, reported that the article was not self-contained: every proof, the second chemical
% domain, the only comparison against published solubility models and the noise-floor derivation
% were all outside it.  So THIS document now carries, in full: Lemma 1's statement with the
% cross-term condition and the three settings of z*; Proposition 1's statement; the noise-floor
% derivation with both
% functionals; the external-baseline table; the closure-anchor table; the related-work paragraphs;
% the parity plot; a self-contained Sec. 3.5; the terminology table; and the run-family table.
% WHAT WENT THE OTHER WAY, and why that and nothing else: the map's two boundable-stratum tables
% and the enumeration of the 44 unbounded strata.  Those are DETAIL -- every row of them is
% already printed in the Supporting Information's five map tables, verified value by value -- and
% the FINDING they carry is a sentence, which stays here with its numbers.  A reader of this
% document must still be able to say which chemistry separates and which does not, and Sec. 3.5.1
% is written so that they can.
% AND ON 2026-08-10 THE FIVE PROOFS WENT BACK THE OTHER WAY TOO -- referee 3's trade, made in the
% same round as referee 1's request that Proposition 1's STATEMENT return here.  Self-containment is
% a property of the statements and the conditions of use, both of which stay; the algebra is what a
% reader consults once and it now stands in one place, \S\ref{sec:si-proofs}, instead of one proof
% here and four there.
% THE COMPRESSION CAME OUT OF HEDGING AND POINTER NARRATION, NOT OUT OF EVIDENCE.  No result,
% control, null, interval, limitation or disclosure left the submission.  If a later pass needs
% pages, take them from words; the numbers are all load-bearing and the checker
% (scripts/analysis/check_number_conservation.py) will catch any that are not.
%
% CROSS-REFERENCES: one form.  Every section reference is \S\ref{...}.  An S in the
% resolved number means the Supporting Information (achemso's suppinfo mode numbers it
% S1, S2, ...), so no prefix, sigil or "App." is needed at any call site.  Do not
% reintroduce "Sec.", "App.", "Appendix", a spelled-out "Section N", or \pageref.
% A REFERENCE IS NOT A SENTENCE.  Pointer narration -- a sentence whose content is where to
% look -- was removed in this pass and must not come back.  Put the reference in the sentence
% that makes the claim.
%
% GRADES: there is no grading vocabulary in this manuscript.  Every claim carries its
% uncertainty in the sentence that makes it -- a value, its interval or its per-seed
% spread, its n, and the set it holds on -- and a claim the data do not resolve says so
% in words.  Do not reintroduce "certified", "likely", "open", "no claim" or "retired" as labels.
% THE PROSE IS WHERE THE HEDGING LIVES, restored 2026-08-08.  Between 2026-08-02 and that date the
% claim ledger held it instead: the scope clauses had been lifted out of the sentences and into its
% value cells to shorten the prose, and this comment used to say so and told editors not to
% "re-hedge in prose what the ledger already scopes".  That trade is off -- readability is not
% traded for length -- and the ledger has left this document for \S\ref{sec:si-ledger}.  Every
% scope it was carrying is back in the sentence that makes the claim; the inventory is in the
% comment above \S\ref{sec:paradox}.  A sentence here may now repeat a scope the SI table also
% prints, and that is correct, not duplication.
%
% ONE NAME PER THING, fixed 2026-08-10 across BOTH documents.  Three objects had been running under
% synonyms, and a reader had to infer that the synonyms were one object.  The names are now:
%   * THE BINNING BOUND -- the upper bound on \Binsuf\ of Lemma 1(c), the variance of m inside bins
%     of g(z*).  It was also "the model-free law-of-total-variance bound", "the LOTV bound", "the
%     binning bound" and "the coarsening"; LOTV is gone from both documents and from the
%     \abbreviations list.  "law of total variance" survives ONLY where the LAW is invoked (the
%     proof of Lemma 1(c), and the contribution paragraph), never as the name of this estimator.
%     "coarsening" survives only as the VERB and as the sigma-algebra sense ("a coarsening of the
%     conditioning sigma-algebra"), never as a noun standing for the estimator.
%   * THE ESTIMATOR CELL -- one SETTING of that bound's four analyst choices, plus the row-or-pair
%     unit.  It is a configuration and not a second name for the bound; \S\ref{sec:estimator}
%     defines it in that sentence and Table~\ref{tab:notation} carries both rows so the two cannot
%     be read as synonyms.  It was also "the eight-bin coarsening cell".
%   * THE UNBIASED WITHIN-BIN VARIANCE -- the variance convention, glossed "(Bessel-corrected)" ONCE,
%     at \S\ref{sec:estimator}, and nowhere else.  It was also "the within-bin Bessel correction" and
%     "the Bessel-corrected binning"; its alternative is the maximum-likelihood within-bin variance.
% AND THE COMBINATORIAL CONVENTION IS "RESIDUAL-ONLY", NOT "DEPLOYED".  "Deployed" was carrying three
% senses -- ordinary English in the title and the abstract, a kernel vintage, and this named
% convention -- so the convention was renamed, which is the sense that had a spare name available:
% the pair is now \emph{full} against \emph{residual-only}.  Every remaining "deployed" in either
% document is ordinary English ("as deployed", "the deployed pipeline", "the deployed closure").
% Do not reintroduce "deployed convention", and do not name a kernel "the deployed" one.
%
% NUMBER PROVENANCE: tab:runs names the run family, the seed count, the script and the
% artifact behind every number in this document.  It is in the Supporting Information, at the head
% of sec:si-repro, since 2026-08-08 -- it is provenance and not evidence, and no claim here is read
% off it -- and \ref{tab:runs} resolves to its S-number from either document.  Fig. 2 / map numbers
% regenerate from results/e5_sigma_grounding/ and results/b_insuff/.
%
% LAYOUT.  `layout=twocolumn' is the published-article measure and is what the page
% counts refer to.  ACS expects the double-spaced single-column measure at submission:
% drop `,layout=twocolumn' in BOTH files for that.  Re-measure page counts from the
% build, never from a comment.
% =====================================================================
\documentclass[journal=jcisd8,manuscript=article,layout=twocolumn]{achemso}
% The preamble proper -- packages, macros, theorem environments, float tuning, the
% amsthm/achemso patches and the line-breaking parameters -- is shared verbatim with
% the Supporting Information and lives in preamble.tex.
\input{preamble}
% The other half of the submission. xr-hyper (loaded in preamble.tex) reads
% grounding_paradox_si.aux, so every \ref{} in this file that names a label defined
% in the Supporting Information resolves to its S-number without any change at the
% call site. The first optional argument, the label prefix, is empty because the two
% documents' label names are disjoint; that is checked by
% scripts/analysis/check_split_refs.py, which fails if a name is defined in both.
% THE SECOND OPTIONAL ARGUMENT IS LOAD-BEARING. Without `nocite' xr-hyper also
% imports the other document's \bibcite lines, and since the two bibliographies
% number the same work differently every key cited in both is reported "multiply
% defined". `nocite' stops the import: each document numbers its citations only from
% its own bibliography.
\externaldocument[][nocite]{grounding_paradox_si}
% TITLE, 2026-08-11 (fourth revision).  ONE clause, twelve words.  Keep it identical to the \title
% in grounding_paradox_si.tex.
%
% "SOLVENT" ENTERED 2026-08-11 (referee round 6).  The substitution the paper is named for reaches
% 99.3% of the labelled test rows on the solvent side and 5.3% on the solute side (\S\ref{sec:eval}),
% so what is measured is the substitution of a reference SOLVENT profile; \S\ref{sec:eval} and
% \S\ref{sec:discussion} said so and the title did not.  Twelve words is JCIM's stated preference,
% so the word is affordable and the scope is no longer left to the body.
%
% WHY IT IS ONE CLAUSE NOW.  JCIM's guide for authors asks for "preferably 12 words or fewer"; the
% two-clause form below ran to 22, and all three referees this round stopped on the same structural
% fault rather than on the length: the second clause is a semicolon away from the first, its subject
% is a gerund the reader does not reach until the ninth word of it, and it ends on a negation
% ("Not a Verdict") that tells a reader what the paper did not return.  Twelve words cannot carry
% two findings, so one had to go.
%
% WHICH FINDING THE TITLE KEEPS, AND WHY THAT ONE.  It keeps the substitution.  Three reasons, in
% the order they decided it:
%   (a) It is the finding the article is named for and says so in its own text
%       (\S\ref{sec:paradox}: "that substitution alone is the effect this paper is named for").
%       The map is what the second axis returns; the substitution is what happened.
%   (b) It is the finding that survives compression.  The substitution is three seeds with no
%       per-seed overlap on either arm.  The map's headline is one row set out of fifty-nine, whose
%       margin a chemistry-blind relabelling reaches in a tenth of draws and for which no
%       out-of-sample rows exist (\S\ref{sec:broad}) -- it cannot be stated in a title without the
%       scope clauses a twelve-word limit forbids, and the two forms that tried are struck below.
%   (c) A title clause whose content is what an instrument's OUTPUT is shaped like ("Returns a Map,
%       Not a Verdict") is a methods statement, not a result, and it is the clause the referees
%       reported having to re-read.
% The second finding is not lost to the reader who searches: it is indexed by the keyword
% "infinite-dilution activity coefficients", added in the same pass for exactly that reason, and it
% is the subject of \S\ref{sec:measure}, of the abstract's middle, and of half of Fig. 1.
%
% THE HEDGE IS GONE WITH THE CLAUSE.  "Not a Verdict" was the manuscript's only hedge above the
% abstract.  Nothing replaced it: the surviving clause asserts a measured direction and carries no
% qualifier, and its scope (which substitution, of what, for what) is now IN it rather than trailing
% it -- "for Learned Ones" is the four words the old form left to the reader.
% Do not re-attach a scope, a hedge or a second finding here.  The place a scope belongs is the
% sentence that makes the claim, and every one of them is in the body.
%
% THE THREE CLAIMS THIS TITLE HAS SHED, IN ORDER, AND WHY NONE MAY COME BACK.
%
% (1) "and Separates COSMO-SAC Error from Input Error", the form before 2026-08-05.  Wrong on
%     SCOPE -- the separation holds on one row set of fifty-nine, not on either set whole -- and
%     wrong on GRAMMAR, which here is arithmetic: the subject of "Separates" was the gerund
%     "Substituting reference sigma-profiles", so the sentence said the SUBSTITUTION separates
%     the two error terms.  It does not; the law-of-total-variance decomposition does, on
%     infinite-dilution activity data, a different measurement on a different set.
%
% (2) "and COSMO-SAC Error Exceeds Input Error on One Solvent Class of Activity Data", the form
%     that replaced it.  That fixed the scope error and states a CHEMICAL LOCALISATION: the
%     error is placed on a named class of solvents.  Two of this paper's own results now decline
%     to license it.  The multiplicity null (Sec. S3.1) shows a chemistry-blind relabelling of
%     the same molecules certifies at least one row set in 58% of 2000 draws and returns a
%     certified margin of +2.04 or more in 10.1% -- so the outcome the clause asserts as a fact
%     about chemistry is one a blind search returns more often than not.  And the pre-declared
%     out-of-sample test (Sec. 3.5.1) returned NOT TESTABLE: the only out-of-sample IDAC set that
%     exists carries no glycol-ether solvent with an organic solute at all, and its nearest cell
%     is inadmissible under the paper's own rule.  A title cannot carry "survivor of a 59-fold
%     search", so the localisation leaves the title and lives in Sec. 3.5.1 with its null and the
%     absence of any confirming data attached.  Do not restore it if the abstract prints the 58%:
%     the abstract now prints it, and the clause is still not earned.
%
% (3) "; on Activity Data, Bounding a Deployed COSMO-SAC Against Its Inputs Returns a Map, Not a
%     Verdict", the second clause of the 2026-08-05 form, dropped 2026-08-10.  It was EARNED --
%     that the decomposition's output is a map over chemical strata rather than one verdict is the
%     finding the multiplicity null does not touch, it is the reason an aggregate margin is a
%     composition statistic (Sec. 3.5.1, and the random-partition null in Sec. S3.1: observed MSE
%     range 2.163 against a null median of 0.405, no draw reaching it, while the bound's range is
%     at chance), and the out-of-sample set supports it, its classes running from -2.53 to +1.73 at
%     the same cell.  It goes for length and for structure, not because it stopped being true.
%     Every part of it that a reader needed the clause for is stated where it is measured: "on
%     Activity Data" at the head of Sec. 3.5 and in the abstract's "On infinite-dilution activity
%     data", which is what keeps the cross-regime conflation of limitation (i) out of the reading;
%     and "a Deployed COSMO-SAC" -- B_closure being the error of the fixed kernel together with the
%     one computed conformer it reads, and not of the kernel alone -- in Sec. 2.1.1, Table 1 and
%     Sec. 3.5.3, all three of which say it at more length than a title can.
%     If it is ever restored, it must come back as the WHOLE title and the substitution must leave;
%     two findings do not fit in twelve words and the semicolon form is what the referees rejected.
%
% WHAT THE SURVIVING CLAUSE SAYS, AND WHY EACH WORD IS THERE.  "Substituting Reference
% $\sigma$-Profiles for Learned Ones Degrades COSMO-SAC Solubility Prediction", ten words.
%   "for Learned Ones" is new and is the repair of a real ambiguity: the old form said what was
%       substituted and never what it was substituted FOR, so a reader could take it as substituting
%       reference profiles for a quantum-chemical calculation, which is the pipeline's ordinary
%       operation and not a finding.
%   "COSMO-SAC" names the closure the degradation is measured through, so the title is findable by
%       the mechanistic model and does not read as a claim about solubility prediction at large.
%   The subject is the gerund "Substituting" and the verb "Degrades" takes it, so the grammar bug of
%       form (1) below cannot return.
%   There is no "at Inference".  The two operations differ by when the reference enters, and the
%       title names only the one it is about; adding the timing costs a word and buys a distinction
%       that has no counterpart in the title to be told apart from.  The abstract draws it.
\title{Substituting Reference Solvent $\sigma$-Profiles for Learned Ones Degrades COSMO-SAC
Solubility Prediction}

% NOTE: achemso re-typesets the arguments of \affiliation and \email, so anything outstanding here is
% written as a plain comment rather than through \pending.
% ===================== PRE-SUBMISSION CHECKLIST =====================
% Five items are outstanding. None can be settled from the manuscript; all are author input.
%   1. ORCID for A. V. Rudik. N. L. Polomoshnov is 0009-0001-4342-8539. ORCIDs are entered in the
%      ACS submission form, not typeset here, so this blocks the form and not the PDF.
%   2. Street address and postcode of the Faculty of Bioengineering and Bioinformatics, MSU.
%   3. Street address and postcode of the V. N. Orekhovich Institute of Biomedical Chemistry.
%   4. Whether A. V. Rudik also corresponds; if so, add a second \email under her \author block.
%   5. The AI-use disclosure, removed from the acknowledgement at the author's request and to be
%      rewritten. ACS asks contributors to declare generative-AI use and the manuscript now carries
%      none, so this is the one outstanding item a reviewer would notice.
% CONFIRMED, do not re-flag: the Author contributions block is correct as printed.
% Two more, of a different kind, are in the Data availability statement and are handled by \pending:
% the archive commit hash and the Zenodo DOI.
% ==================================================================================
\author{N.~L. Polomoshnov}
\affiliation[MSU]{Faculty of Bioengineering and Bioinformatics, Lomonosov Moscow State
University, Moscow, Russia}
\alsoaffiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}
\email{nikitapol@fbb.msu.ru}
\author{A.~V. Rudik}
\affiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}

% THE TWO PROFILE DATABASES ARE BOTH LISTED, 2026-08-10 (referee item: they must be
% distinguishable wherever either is named).  UD alone was listed, so the list itself invited the
% reading that there is one database with a name and one without.  Both are now here, each with the
% institution that produced it and the role it plays in this paper, and the same contrast is drawn
% in the sentence that first uses either (Sec. 2.1) and in Table 1.  Do not drop VT-2005 from this
% list to save a line: the abbreviation is used in a table caption, in a figure panel and in five
% sections, and "the successor" only means something beside the thing it succeeds.
\abbreviations{SLE, solid--liquid equilibrium; IDAC, infinite-dilution activity
coefficient; LFER, linear free-energy relationship; NRTL, non-random two-liquid;
VT-2005, Virginia Tech 2005 (the $\sigma$-profile database supervised against and substituted from);
UD, University of Delaware (the successor $\sigma$-profile database, matched to the activity rows)}
% KEYWORDS.  JCIM's submission form takes them and the manuscript has carried five since
% 2026-08-02; nothing was missing.  ONE WAS ADDED on 2026-08-10, in the pass that cut the title to
% one clause: "infinite-dilution activity coefficients".  The title no longer names the second
% axis, so the keyword list is now the only place above the abstract where a search on that data
% type finds this paper -- which is the one thing the shortened title gave up.  Six is inside the
% form's limit.  Do not add a seventh to make up for anything else the title dropped: the rest of
% what it dropped is a scope, and a scope is not a search term.
\keywords{solubility prediction, physics-informed machine learning, COSMO-SAC,
infinite-dilution activity coefficients, model misspecification, representation probing}

\begin{document}

% ACS requires a table-of-contents graphic, and it is the one image the journal places beside
% the article, so it may only carry a claim this paper still makes. It prints the two senses of
% grounding and their opposite signs, which is the title's first clause. Regenerate with
% scripts/analysis/make_toc_figure.py. Do not restore an aggregate here, and do not restore the
% two MAGNITUDES either: the pair -0.20 / +0.41 asserts a two-fold asymmetry that the
% co-adaptation control withdraws, and the pair -0.20 / +0.18 juxtaposes a three-seed number with
% a one-seed one that Sec. S3.1 says cannot be ordered against it. The graphic's own docstring
% carries the argument in full; the signs hold at every seed and are what it may print.
% SIZE. achemso puts the graphic in a \vbox to 1.75in inside a 3.25in minipage. Bounding
% the height as well as the width lets the aspect ratio pick whichever limit binds.
\begin{tocentry}
\includegraphics[width=\linewidth,height=1.7in,keepaspectratio]{figs/fig_toc.pdf}
\end{tocentry}

\maketitle
% ABSTRACT.  THREE RULES, and they are the whole of what governs this block.
% (1) THE ABSTRACT CARRIES THE MAGNITUDES ITS FINDINGS TURN ON.  Reversed 2026-08-10 by the
%     author, and the reversal is recorded here rather than left as a silent edit because the rule
%     it replaces stood for eight days and is quoted in two other files.  What it replaced read
%     "NO NUMERALS.  Not one figure, interval, seed count, sample size or percentage", on the
%     ground that a number here is one a reader carries away without the clause that scopes it.
%     What decided it: the supervising professor's letter objects to an abstract "overloaded with
%     numbers and qualifications" and asks for "only the key qualitative conclusions" -- and
%     OVERLOADED IS NOT CONTAINS.  Against that, three referees independently asked for magnitudes,
%     one of them having measured the venue: five of six recent JCIM Articles print numbers in the
%     abstract, including both whose headline finding is a deflation, which is this paper's genre.
%     The letter's target is the PILE OF QUALIFICATIONS, and that is what this block spends (below).
%     WHAT PRINTS, and nothing else may be added without taking words from connective tissue:
%       the three arm means of \S\ref{sec:paradox}, $2.043$ -> $1.846$ -> $2.252$ in \lnx\ MAE, so
%         that both operations are stated in one unit and the reader can take either difference;
%       the standing margin with the scope a one-sided bound needs -- $2.04$, $n{=}182$ WITH ITS
%         CLUSTER COUNT $43$ pairs, the $90\%$ interval $[+1.25,+2.87]$ -- and the $59$-fold
%         search it is the survivor of.  THE PAIR COUNT IS NOT OPTIONAL BESIDE THE ROW COUNT.
%         This project's standing rule is that the effective n is the number of clusters, not the
%         number of rows, and the two differ here by a factor of four; the SI records that $141$
%         of the $182$ rows are one oligomer family and that the worst single-publication deletion
%         leaves $+1.60$.  A bare $n{=}182$ therefore reads in the paper's favour, which is the
%         direction that has to be guarded.  Print both counts or neither -- and neither is not
%         available, because dropping the row count drops an n;
%       the null frequencies in words ("in most draws", "in a tenth") WITH THE ESTIMATOR NAMED,
%         "under the binning bound".  The estimator is not decoration: the pre-declared cross-fitted
%         bound puts the same frequency at $0.137$ on its $473$ rows against the binning bound's
%         $0.094$ there, so an unattributed "a tenth" names no quantity.  \S\ref{sec:broad} carries
%         the comparison; the abstract carries the attribution.
%     DO NOT print the pair $-0.20$ / $+0.41$ here.  The endpoints give a reader either difference
%     and the sentence after them refuses the ordering in the same breath; the pair asserts the
%     two-fold asymmetry directly, which is what the graphic for the table of contents is forbidden
%     to print and for the same reason.  Do not print $5608$, $8103$, $477$ or $58\%$: the row sets
%     and the certifying frequency are carried in words, and every one of them is in the body.
% (2) NO SECTION REFERENCES.  An abstract that points into the document is not self-contained.
% (3) THE CEILING IS 250 WORDS and it still binds.  Re-count after ANY edit with
%     scripts/analysis/wc_abstract.py and take the number the script prints, not the number a
%     previous comment printed -- carrying a count forward unchecked is how the old note here
%     came to be wrong three times.  The operative rule is the script's third ("as rendered"),
%     because the production editor's tool reads the page and not the source.
% WHAT MUST NOT BE DROPPED TO SAVE WORDS, in order: that supervision's gain is not certified
% leak-free; that the two operations' magnitudes do not resolve against each other, so only the
% signs carry; that the input bound's flatness is an estimator property and not chemistry; that
% the row set is one of many searched and a chemistry-blind relabelling reaches its margin often;
% that the out-of-sample verdict is NOT TESTABLE and not a failed replication; and the four
% directions the separation must not be over-read in -- no other stratum, neither data set whole,
% nothing on the solubility axis, and the deployed model rather than the kernel alone.  If words
% are ever needed, they come out of connective tissue, never out of one of those.
% THAT LIST SURVIVED THE NUMBERS PASS INTACT, clause for clause, and it was the constraint that
% decided what the magnitudes were paid for with.  Every item above is still in the block; two are
% in new words and neither changed what it scopes.  Where a protected clause and a magnitude
% competed for the same word, the clause won -- which is why "interpretable" is gone from the first
% sentence and the $90\%$ interval is not.
%
% THE NUMBERS PASS, 2026-08-10.  WHAT WAS BOUGHT: $2.043$, $1.846$, $2.252$, \lnx\ as the unit
% they are in, $2.04$, $n{=}182$, $90\%$, $[+1.25,+2.87]$, $59$-fold, and "under the binning bound".
% WHAT PAID FOR IT, in order of size, and every one of them is connective tissue or a term this
% document defines only later -- which is where the referees said the words were:
%   -8  "The output is a map, most strata unboundable." and "Grounding names two operations."
%       Both are frames rather than findings.  "map", "strata" and "unboundable" are three of the
%       four terms a referee named as defined only later in the paper, and the frame they set up is
%       now set by the sentences themselves: the two operations are named as they are measured, and
%       "the survivor of a $59$-fold search" says in five words that a search ran and one row set
%       came out of it.  The word "grounding" survives, in the sentence that uses it.
%   -4  "Trained through this closure," on the departure sentence.  Every sentence in the block is
%       about this closure; the participle restated it.
%   -3  "only the signs carry" -> "only their directions hold", and "Neither resolves against the
%       gain" -> "Grounding's two operations cannot be ordered by size".  This is the fourth term
%       the referee named, and the new form is \S\ref{sec:paradox}'s own sentence ("Which of the two
%       operations is larger is therefore unresolved, and only their signs are carried forward")
%       in the abstract's register.  IT IS LOAD-BEARING NOW IN A WAY IT WAS NOT: with the endpoints
%       printed a reader can compute $0.197$ against $0.406$ and order them, so the sentence that
%       refuses the ordering has to sit against numbers rather than against nothing.  It may not be
%       shortened again and it may not move away from the pair.
%   -2  "stratum"/"row set" -> "subset", the last of the four; "certifies some row set" ->
%       "certifies something".
%   -1  "interpretable" in the first sentence.
%   -1  "learned" in "The learned profile departs from its reference".  The contrast is carried by
%       "its reference", and the full form is still where (C) needs it, in the substitution
%       sentence.  This one was spent on a defect in the GATE, not on a word: wc_abstract.py counted
%       a math atom as one word, and $\ln x_2$ reaches the page as TWO tokens ("ln x2"), which was
%       the one thing its third rule did not model.  A plain pdftotext count of the built page read
%       251 while the script read 250.
%       THE GATE NOW MODELS THE ATOM (2026-08-10) and the three notes that described it now say one
%       thing.  `rendered_atom' in wc_abstract.py splits an atom on commas, on TeX's operator names
%       and on the explicit spacing commands, all three of which put a space on the page, so the
%       script's third rule and pdftotext agree at 250.  Two consequences, and both are the
%       opposite of reassuring: the block was AT its ceiling and had been recorded as under it,
%       and the script now prints "AT (no words left)" rather than "under" when the count equals
%       the ceiling, because a block with nothing left behaves like one over budget on the next
%       edit.  The note that read "both under the ceiling" was wrong in the direction that lets a
%       word in; SI \S\ref{sec:si-repro} and the discharge sheet each attributed the 250 to the
%       script, where it was the page's.  Still count the page after any edit that adds a math
%       atom -- pdftotext -f 1 -l 1 on the built article -- because the gate models the spacing it
%       has been taught and no more.
% WHAT WAS NOT SPENT, and the next pass should not go looking there: no result, no control, no
% null, no interval, no limitation and no disclosure left the block, and the do-not-drop list above
% is complete in it.
%
% THE ADVERSARIAL PASS, 2026-08-10, WHICH COST THE NUMBERS THREE WORDS AND BOUGHT BACK THREE.  Two
% reads of the block with the magnitudes in it found three things they had done to the prose.  All
% three repairs are below; the block is 250 as rendered before and after, so nothing was financed
% by a scope.
%   (i)  THE ZEUGMA.  "raises the error to $2.252$, and by less during training at one seed"
%        coordinated a LEVEL and a DELTA under one elided verb.  Before the magnitudes went in,
%        both conjuncts hung off a bare "raises the error" and the ellipsis was clean; "to $2.252$"
%        broke it, and no single-verb form can carry both, because the second conjunct needs a
%        predication of its own.  It now reads "raises the error to $2.252$ at inference; the rise
%        during training is smaller, at one seed, where it co-adapts" (+1 word).  WHAT THE REPAIR
%        HAD TO KEEP, and does: (C)'s shared grammatical subject, so "at inference" left the
%        subject and became an adjunct on the first clause; the two arms told apart by WHEN, "at
%        inference" against "during training", both verbatim; the level on the arm that has one and
%        the comparison on the arm that does not; "at one seed" scoping the training arm ALONE; and
%        "where it co-adapts" as the restrictive relative it has to stay.  Do not restore a single
%        verb here, and do not give the training arm a level -- there is no fourth arm value to
%        print and the pre-commitment forbids adding one.
%   (ii) $n{=}182$ WITHOUT ITS CLUSTER COUNT.  Handled under WHAT PRINTS above; "$43$ pairs" costs
%        two words and is the standing rule's unit.
%   (iii) THE THREE PAGE-1 NUMERALS NO CHECKER COULD REACH.  $2.043$, $1.846$ and $2.252$ were the
%        only printed arm means in either document that nothing verified, because
%        check_hand_transcribed_displays.py locates a display by \label and the achemso abstract
%        environment has none.  The block is now delimited by the %<abstract-numerals> markers
%        below and the three are bound to the same artifact the body's copies use.  KEEP THE
%        MARKERS: deleting either one is a fatal finding, not a silent loss of coverage.
% WHERE THE THREE WORDS CAME FROM, none of them a scope and none on the do-not-drop list:
%   -2  "during training" on the SUPERVISION sentence.  Supervision has no other time to happen in,
%       and the when-axis is set one sentence later by the contrast that needs it.  It was also the
%       one place the same phrase named two different training-time operations -- supervising the
%       profile and substituting for it -- which invited them to be read as one.
%   -1  "expecting accurate references to help" -> "; accurate references should help".  The
%       expectation is the premise under test either way, "We measure that" says so in the next
%       sentence, and the copula is the register the assertion pass asks for.
% THE MAGNITUDES ARE ENROLLED IN THE PRE-COMMITMENT, in the same pass that printed them and before
% the leak-free re-run's numbers exist: the display inventory of \S\ref{sec:si-repro} carried "the
% abstract's seed count and its leak-free clause, WHICH CARRY NO VALUE OF THEIR OWN", which does not
% reach a printed $2.043$.  Its first item now names all three arm means and says which branch
% rewrites each.  If a later edit adds a fourth arm value here, it goes into that item in the same
% pass -- and after the seeds land it cannot go in at all, so it cannot be printed here either.
%
% THREE DISTINCTIONS THE NUMBERS USED TO CARRY, restored in words on 2026-08-07 after all three
% referees found them missing from the numberless draft.  Each is a WRONGNESS and not a looseness,
% so none may be traded back for words.
% (A) "THE SAME REFERENCE" NAMED ONE TABLE WHERE THERE ARE TWO.  The evaluation-time substitution
%     of \S\ref{sec:paradox} is the VT-2005 database; the standing glycol-ether margin is computed
%     on the UD profiles the broad IDAC set is matched to (\S\ref{sec:broad}, and the
%     database-to-database bracket of \S\ref{sec:badclosure}, which exists precisely because the
%     two sides name different databases).  What the two axes share is that a tabulated reference
%     for the intermediate exists on both -- which is what "another such database" now says, at no
%     word cost, "such" reaching back to "a quantum-chemical database tabulates them".  Do not
%     write "the same reference", "the same database" or "that reference" here.  Fig. 1 carried the
%     same conflation in its panel labels and was corrected in the same pass
%     (scripts/analysis/make_overview_figure.py); the two must stay in step.
% (B) "IT SURVIVED A SEARCH" CLAIMED THE OPPOSITE OF \S\ref{sec:broad} AND LIMITATION (vi).  A
%     chemistry-blind relabelling certifies some row set in most draws and reaches the observed
%     margin in a tenth of them, so the row set is the SURVIVOR OF the search, not something that
%     survived a null.  It now reads "the survivor of a $59$-fold search, not an established
%     effect" -- "not ESTABLISHED" and not "not a chemical effect", because
%     the null bounds what was shown and not what is true.  Do not restore any verb that puts the
%     finding on the passing side of a test.  The count came in on 2026-08-10 in place of "across
%     every stratum", which said the same thing in two more words and in one of them; $59$ is
%     \S\ref{sec:broad}'s own figure for the search and is what makes "the survivor of" a size.
% (C) ONE GRAMMATICAL SUBJECT CARRIED TWO ARMS, and mis-described the second.  The co-adaptation
%     control does not remove the test-time swap by declining to substitute: it substitutes the
%     reference DURING TRAINING, so the model co-adapts to it (\S\ref{sec:paradox}).  The subject
%     is therefore now the operation both arms share -- substituting the reference for the learned
%     profile -- and the arms are told apart by WHEN, "at inference" against "during training".
%     "by less, at one seed" is the seed-42 pair (+0.18 co-adapted against +0.43 evaluation-only,
%     read off one comparator); it may not be widened to the three-seed headline.  It reads
%     "the rise during training is smaller, at one seed" since the zeugma repair of the adversarial
%     pass above, which changed the register of the comparison and not one thing about its scope.
% WHERE THE WORDS CAME FROM.  The repairs cost four net; two were paid back out of connective
% tissue, neither of it a scope clause -- "solubility mean absolute error" lost "solubility"
% (\S2's "We measure that for solid--liquid solubility" already fixes the axis, and the switch is
% marked at "On infinite-dilution activity data"), and one "so" became a colon.  248 as rendered.
%
% TWO MORE REPAIRS, 2026-08-07, and the SECOND ONE IS PAID FOR -- read the ledger before touching
% any of the eleven words it spends.  249 as rendered; re-run wc_abstract.py, do not trust this.
% (D) "FIXED COSMO-SAC TURNS TWO INTO A SOLUBILITY" WAS FALSE, and false in the direction that
%     flatters the paper: COSMO-SAC returns an activity coefficient, and the solubility needs the
%     crystal term as well (Eq. 1, \lnx = -\Phicry - \lng).  Attributing the whole prediction to
%     the closure inflates exactly the object the paper is measuring the error OF.  It now reads
%     "fixed COSMO-SAC and a crystal term turn two into a solubility", and the sentence was
%     re-ordered so the database attaches to the profiles it tabulates ("charge-density profiles
%     a quantum-chemical database tabulates"), which pays one of the four words back.  Do not
%     write a sketch in which the closure alone reaches a solubility.
% (E) THE SECOND CLOSURE WAS MISSING while the Introduction's last paragraph promises exactly its
%     generality ("Neither consequence is peculiar to charge distributions").  An abstract that
%     omits it leaves the paper's own generality claim unsupported at the only place most readers
%     reach.  Added as one sentence, eleven words: "A Hammett $\mathrm pK_a$ closure reverses the
%     substitution's sign where its constants suffice."  It carries the substitution consequence
%     and not the drift one, because the drift is measured on solubility only; and "where its
%     constants suffice" is the clean meta/para pole (\S\ref{sec:pka-main}), so the sentence also
%     says, by implication, that the sign does NOT reverse where they do not -- which is the
%     agreement with the solubility result.  Do not widen it to "both consequences".
% WHAT (D) AND (E) COST, AND WHY EACH TRADE IS THE RIGHT ONE.  Thirteen words, none of them a
% scope clause and none of them on the do-not-drop list above:
%   -3  "not one verdict" -- the TITLE ends "Returns a Map, Not a Verdict", and "the output is a
%       map, most strata unboundable" states it positively two words earlier.
%   -2  "across strata" in "The input bound barely moves" -- "the output is a map, most strata
%       unboundable" in the same sentence supplies the unit it barely moves across.
%   -2  "nothing on the solubility axis" -> "nothing on solubility".  The direction survives
%       whole; only the word "axis" goes, and the abstract names no other axis.
%   -1  "opposite-signed" in "two opposite-signed operations" -- "lowers" against "raises" and
%       "only the signs carry" say it twice more in the same breath.
%   -1  "improve accuracy" -> "help", the paper's own verb for this quantity.
%   -1  "the model co-adapts" -> "it co-adapts"; the subject has not changed since "raises it".
%   -1  "so only the signs carry" -> "; only the signs carry".
%   -1  "the glycol-ether solvents" -> "glycol-ether solvents" (apposition, no article).
%   -1  "chemical" in "not an established chemical effect".  What (B) protects is "not
%       ESTABLISHED"; the noun it qualifies is a row set of one chemistry either way.
%   +0  "the observed margin" -> "that margin", whose antecedent is the margin two sentences up.
% What was NOT touched, and may not be: "not certified leak-free", "over three seeds", "at
% inference"/"during training", "only a lower bound", the four over-reading directions, "in most
% draws", \emph{not testable}, "on separate three-seed runs", and "a deployed thermodynamic
% model" -- dropping "thermodynamic" would let "the deployed model" be read as the network.
%
% THE ASSERTION PASS, 2026-08-10.  The supervisor's instruction is that a qualification blurs a
% statement into saying neither yes nor no, and that the main text should be unambiguous.  It
% collides with the do-not-drop list above, which several referees demanded and which is a list of
% SCOPES, not of hedges.  The resolution is register, not deletion: a scope stated as a trailing
% fragment reads as a hedge ("X, though only a lower bound"), and the same scope given a subject and
% a verb reads as a fact ("X is a lower bound").  Eleven clauses were rewritten that way; NOTHING on
% the do-not-drop list left the block, and no clause changed what it scopes.  What moved:
%   "lowers MAE, over three seeds, not certified leak-free"
%       -> "lowers MAE over three seeds; that gain is not certified leak-free"   (own predication)
%   "raises it, at inference and---by less, at one seed---during training"
%       -> "raises the error at inference, and by less during training at one seed"
%       (the em-dash parenthesis is gone; both scopes are now on the main verb, and "the error"
%        replaces "it" because the preceding sentence now ends on "that gain")
%       -> "raises the error to $2.252$ at inference; the rise during training is smaller, at one
%        seed" once the level went in.  Putting both scopes on the main verb is exactly what stopped
%        working when one of them acquired a magnitude, and the second clause is a predication in
%        its own right, which is the direction this pass was pushing anyway.
%   "moves---an estimator property, not chemistry---while the error does"
%       -> "moves. That is an estimator property, not chemistry; the error moves."  ("That" has
%        exactly one antecedent, the sentence before it, which is the flatness and not the contrast)
%   "by a margin itself only a lower bound"   -> "That margin is a lower bound."
%   "---and no other stratum, ..."            -> "no other stratum stands, ..."   (finite verb)
%   "It is the survivor"                      -> "That set is the survivor"  ("one row set" left the
%        sentence before it, so the pronoun would have taken a plural antecedent)
% THREE CLAUSES COULD NOT BECOME ASSERTIONS WITHOUT CHANGING MEANING AND WERE KEPT AS THEY WERE:
%   "where it co-adapts"     -- a restrictive relative naming the mechanism of the training-time
%        arm; predicating it separately ("it co-adapts there") asserts co-adaptation as a measured
%        finding, which is the reading \S\ref{sec:paradox} declines to make.
%   "on separate three-seed runs"  -- a prepositional adjunct on an assertion, already not a hedge;
%        "separate three-seed runs show it" makes the runs the subject and the drift the object,
%        which reverses what was measured on what.
%   "where its constants suffice"  -- a restrictive relative that IS the finding's boundary; any
%        separate predication either widens the sign reversal past the clean pole or states the
%        degraded pole as a second result, and \S\ref{sec:pka-main} reports one.
% THE BUDGET PAID FOR IT out of the two places a scope was being said twice, not out of a scope:
% "one row set standing, glycol-ether solvents" -> "glycol-ether solvents standing" (the count is
% carried by "no other stratum stands" in the next sentence), and "by a margin itself only a lower
% bound" lost "itself only" when the margin got its own copula.  Re-run wc_abstract.py.
% THE NUMBERS PASS LATER THE SAME DAY LEFT EVERY ONE OF THOSE PREDICATIONS STANDING and touched the
% wording of two, neither in the direction of a hedge: "no other stratum stands" is "no other subset
% stands" (the same finite verb, the noun plainer), and "Neither resolves against the gain; only the
% signs carry" is "Grounding's two operations cannot be ordered by size; only their directions hold"
% -- still one assertion and one refusal, both with subjects and finite verbs.  The three clauses
% that could not become assertions are all still as they were.
%<abstract-numerals>  DO NOT DELETE -- see find_abstract() in
% scripts/analysis/check_hand_transcribed_displays.py.  These two comment lines are the only
% handle any checker has on this block: achemso's abstract environment takes no \label, so
% without them the three arm means on page 1 are unverified.  Deleting either one is a fatal
% finding, not a silent loss of coverage.
\begin{abstract}
Physics-informed models route predictions through physical quantities;
accurate references should help. We measure that for solid--liquid
solubility: a graph network predicts the charge-density profiles a
database tabulates; COSMO-SAC's fixed residual term and a crystal term turn two into a solubility.
Supervising the profile against that database
lowers $\ln x_2$ mean absolute error from $2.043$ to $1.846$ over three seeds; that gain is not
certified leak-free, surviving deletion of the scored solutes a leak reaches. Substituting the
reference for the learned one raises the
error to $2.252$ at inference, the training-time rise smaller, at one co-adapted
seed. Grounding's two
operations cannot be ordered by size, only by direction. On infinite-dilution
activity data another database bounds a deployed thermodynamic model's error from below,
its inputs' from above; the input bound barely moves, an
estimator property, not chemistry, while the error does. One admissibility rule leaves
glycol-ether solvents standing, $182$ rows over $4$ solvents and $19$ hydrocarbon and halobenzene solutes, its
error exceeding its
inputs' by $2.04$ ($90\%$ interval $[+1.25,+2.87]$), and $1.76$ under the
pre-declared estimator. Both are lower
bounds; no other subset stands, nor the set whole; solubility admits no such split. That set is a
$59$-fold search's survivor, not an established effect: chemistry-blind relabelling reaches its
margin in a tenth of draws under the first bound, a seventh under the other. The
profile departs from its
reference along a shared direction, on separate three-seed runs. A Hammett $\mathrm pK_a$
closure reverses the substitution's sign in distribution, where its constants suffice.
\end{abstract}
%</abstract-numerals>

% INTRODUCTION.  Field, related work, the assumption under test, what was done and what was
% found, roadmap.  JCIM carries no standalone related-work section, so paragraphs 2-4 are it;
% criterion (5) still holds -- they describe the present work only in the paragraph beginning
% "What this literature lacks".
%
% THE ROADMAP PARAGRAPH IS THE LAST ONE, restored 2026-08-10.  This comment used to read "There is
% deliberately NO roadmap paragraph: with four sections it narrates the document rather than the
% science.  Do not restore one."  That was a refusal of an instruction that had been given
% explicitly, twice, and has since been re-sent, so the record is corrected here rather than left
% standing as a reason: the roadmap was ASKED FOR, and the four-section argument is an editorial
% preference that does not outrank the request.  It is written to the manuscript's one
% cross-reference convention -- \S\ref{...}, never a spelled-out "Section N" -- because the
% alternative would put the only hard-coded section numbers in the document into the paragraph
% most likely to go stale if a section is ever added or merged.
%
% THE PARAGRAPH BEFORE IT was the abstract in prose: 375 words, 36% of the section, seven of the
% abstract's claims restated, and three breaches of the letter's "no technical detail, no
% qualifications" rule -- a run-provenance caveat, a 63-word closing sentence enumerating the
% degraded pole's two substituent kinds, and "the two error terms" a page before \Bclos\ and
% \Binsuf\ are defined.  It now says what was done and what was found, in the register of the four
% paragraphs above it.  Each of the three cut items is printed in full where it is measured, and
% they were checked there before the cut: the run provenance in \S\ref{sec:surrogate} ("a separate
% set, trained with the $\sigma$ head left unfrozen through fine-tuning") and in the abstract's "on
% separate three-seed runs"; the ortho and untabulated substituents, their counts and the
% four-series statement in \S\ref{sec:pka-main}; and the two error terms in \S\ref{sec:decomp},
% which is where a reader first needs them.  Do not restate any of the three here.
%
% "THREE USES" IS DELIBERATELY NOT "THE SAME REFERENCE USED THREE WAYS".  The substitution reads
% VT-2005 and the error split reads the UD profiles the broad IDAC set is matched to, which is why
% the third use is introduced with the data set it is made on rather than as a further use of the
% counterpart just named.  This is the distinction the abstract's ledger protects; keep the two
% in step.
\section{Introduction}
\label{sec:intro}
How much of a solid dissolves in a given liquid decides whether a drug can be formulated as a
tablet or an injection, which solvent a crystallisation is run in, and how a product is purified. It
must be measured one solute, one solvent and one temperature at a time, so most industrially
relevant combinations have never been determined, and independent determinations of the same system
disagree by enough to floor what any predictor can achieve. Dissolving a solid also trades the cost
of breaking up a crystal against the cost of mixing the freed molecules into the solvent, so a
predictor has to be right about two unrelated kinds of chemistry at once, on molecules unlike any it
was shown.

Predictors divide by how much physical structure they impose. Direct models regress solubility from
descriptors or from a representation learned off the molecular graph; FastSolv \cite{osanloo2024}
does so with a deep neural network and leads on extrapolation to new solutes. They are the more
accurate there and they are opaque: the prediction arrives as a single number, with no account of
which part of the chemistry it captured. Physics-informed models instead predict the thermodynamic
quantities the physical picture names---an activity coefficient for the solute--solvent interaction,
a melting temperature and heat of fusion for the crystal---and combine them through a fixed
equation, as SolProp \cite{vermeire2022solprop} does with a thermodynamic cycle of separately learned
components. What that buys is decomposability: each part is a quantity chemists tabulate on its own,
so it can be supervised against reference data a direct regressor has no way to use. On accuracy the
black box leads and the physics-informed cycle trails it. Learned surrogates for the activity
coefficient have advanced quickly, and split the same way. Winter et al.\ \cite{winter2022} predict
\lngi\ from SMILES with a language model, and Sanchez-Medina et al.\ \cite{sanchezmedina2023} and
Wang et al.\ \cite{wang2026ghgeat} embed a
Gibbs--Helmholtz constraint in a graph network for its temperature dependence, all making the
activity coefficient the regression target; a second line instead keeps a mechanistic model and
learns its input. COSMO-SAC \cite{lin2002} is a segment-activity reformulation of Klamt's COSMO-RS,
and the $\sigma$-profile it consumes---screening-charge density over a molecule's cavity---is that
framework's construct, tabulated from quantum chemistry \cite{mullins2006}. Predicting it from
structure removes that calculation from the pipeline; TeNNet-SAC \cite{yang2025tennet} does so while
also learning the segment-activity kernel and the cavity geometry, and reports accuracy better than
COSMO-SAC's after end-to-end fine-tuning---the opposite headline from this one, on the same broad
construction, and \S\ref{sec:surrogate} sets the two side by side.

When a fixed physical structure improves or degrades a learned model is the broader question of
physics-informed machine learning \cite{karniadakis2021}, whose dominant forms impose structure as a
differentiable object: a governing equation enforced through the loss \cite{raissi2019}, gray-box
hybrids composing learned components with fixed mechanistic maps \cite{willard2022}, and operator
learning with a trainable correction to a misspecified equation \cite{ma2026}. The prevailing
expectation is that more physics improves generalisation, yet the same structure has documented
failure modes in which it makes optimisation or accuracy worse \cite{krishnapriyan2021}. In neural
PDE solvers, a learnable term added to a fixed discretised operator absorbs truncation artifacts
rather than physics, so the apparent interpretability is illusory \cite{mohan2024}---though that
error source is numerical and has no external reference. The same concern appears in
\emph{concept-bottleneck models}: networks routed through a layer of named, human-interpretable
concepts, so that the prediction is made from those concepts and nothing else. That is the same
shape as a learned intermediate feeding a fixed physical map, and there the failure is called
\emph{concept leakage}---a learned concept silently encodes information
beyond its nominal meaning \cite{mahinpei2021,margeloiu2021,havasi2022}. Recent work traces one
route to it to the downstream model: a misspecified head forces the encoder to deform the concept
into carrying a dependence the head cannot represent \cite{parisini2025}, and overwriting a frozen
head's leaked concepts with ground truth can then degrade accuracy \cite{espinosa2025leakage}. The
gap between a fixed physical map and reality is the model-discrepancy problem of Kennedy and O'Hagan
\cite{kennedy2001} and its calibration critique \cite{brynjarsdottir2014}; four further
literatures---forecast decomposition, quasi-maximum likelihood, identifiability, and the validity of
composed pipelines---supply pieces used below.

The design rests on an assumption rarely stated because it is taken for self-evident: a model
supplied with physical inputs closer to their true values predicts better. For one class of
intermediate that can be tested outright, because a reference value is tabulated and can be handed
to the model in place of the one it computed. It needs testing, because when a predictor of this
shape is inaccurate either the fixed physical model is wrong or the learned quantities feeding it
are uninformative, and the two call for opposite remedies. What this literature lacks is a way to
\emph{measure}, rather than diagnose, which of the two limits a composed predictor's accuracy, and
whether the learned intermediate has stopped reporting the quantity it is named after; in the
concept-bottleneck case no external reference exists against which either question could be settled.
This work supplies one---a reference $\sigma$-profile for solubility, tabulated substituent
constants for a second chemical domain---and holds the mechanistic model fixed, so the tabulated
value stays available as an external reference for the learned one.

Here a graph neural network predicts the pair of charge-density distributions COSMO-SAC consumes, in
place of the quantum-chemical calculation that ordinarily supplies them. The tabulated counterpart
can enter at either of two points: as a target the network is trained toward, or as a replacement for
what the network computed at the moment of prediction (Fig.~\ref{fig:overview}). Those two entry
points are the two operations the word \emph{grounding} names, and here they have opposite signs:
training the network toward the reference lowers error, and supplying the reference at the moment of
prediction raises it at every seed. Tabulated profiles serve a third purpose on activity-coefficient
data, where they divide the error of a fixed model between the model itself and the inputs handed to
it. That division cannot be made on solubility, because a
solubility measurement constrains the crystal and the mixing contributions jointly and cannot say
which of the two is at fault. What it returns is a map rather than a verdict: one answer
per stratum, a stratum being a chemically defined set of rows. Whether a
fixed model's own error stands above what its inputs fail to carry is a property of the chemistry the
model is asked about, and where it stands above, a more accurate input exposes the model's bias
instead of removing it. The learned distribution meanwhile drifts away from its tabulated counterpart
along a direction shared across molecules, and stops reporting the quantity it is named after.
Tabulated substituent constants behave the same way inside a fixed Hammett relation, the linear
equation that predicts how readily an acid gives up its proton from the constants tabulated for its
substituents.

This article is organised as follows. \S\ref{sec:methods} sets out the composed predictor, the
identity that divides its error between the fixed model and the inputs, the estimator for each part,
the data and the measurement noise that floors what any predictor can reach, and the evaluation
protocol. \S\ref{sec:results} reports the signs of the two grounding operations, the division of the
error on activity-coefficient data and the chemical map it returns, the drift of the learned
distribution away from its reference, the same two measurements on a second fixed model for acid
dissociation, where this work's accuracy stands against published models, and the limitations of all
of it. \S\ref{sec:conclusion} states the design rule the measurements support.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_overview.pdf}
% CAPTION BUDGET 80 words, counting the "Overview." lead-in and the (a)/(b) markers.
% 2026-08-10 THE BUDGET FALLS FOR THE FIRST TIME, 82 -> 80, and it falls because the PANEL got
% simpler rather than because the caption was squeezed.  Two changes, in opposite directions:
%   -4  "the evaluation-time substitution of the reference sigma-star, WHICH MAKES IT WORSE" is
%       a result, and panel (a) no longer draws one -- the panel now says "substituted at
%       prediction time", which is the operation, and the caption says the same thing.  The
%       finding itself is three inches away in the body text of this same page, which is where
%       a result belongs.  Do not put "worse", "degrades" or "fails" back into this caption:
%       the rule it now satisfies is that every clause here names something a reader can point
%       to in the drawing.
%   +2  "the three BOUNDABLE solvent classes" -> "the three solvent classes with a bound",
%       because the panel's own label changed the same way (it said "15 boundable" and the word
%       is first defined on page 7).  The two now say it identically, and the caption is
%       intelligible on page 4 without a forward reference.
% "evaluation-time" also went: the panel and the body both say "at the moment of prediction",
% and one page should not carry two names for one instant.
% The budget rose 60 -> 66 -> 72 -> 82, each step a disclosure panel (b) forced: three solvent
% classes are drawn and only one is established, and THE TWO MARKS SAY DIFFERENT THINGS.
% Water (dagger) loses its sign when one publication is deleted; the acceptor-only aprotics
% (double dagger) survive every deletion in all four cells and are not established because
% the margin is not positive and the bound is one-sided.  One dagger glossed as laboratory
% fragility carried both classes and was FALSE of the second -- do not restore it, and do
% not buy the six words back by deleting "each mark carrying its own reason".  No third
% kind of growth.  This float sets early, so every term in it must be defined by then:
% keep it in plain English, and no section reference.
% THE 72 -> 82 STEP, 2026-08-07, is the strip, and it is the same kind of step as the two before
% it: the panel disclosed something and the caption has to name it.  Two referees, independently,
% read three solvent classes drawn out of fifty-nine strata as a map three classes wide, so the
% panel now draws all fifty-nine and the caption has to say which display is which -- "the strip
% is every stratum searched, the bars the three boundable solvent classes".  Ten words, and they
% buy the honest shape of the finding in the one figure that shows its shape.  The panel carries
% the counts (fifty-nine, fifteen, one), so the caption does not repeat them; if a later edit
% strips a count from the panel, this caption must buy it back in words.
% "STARRED", NOT "UNMARKED".  The standing class used to be the one with no mark, and a referee
% could not tell whether that meant it had passed the rule or had not been put to it -- absence
% of a disqualification is not a mark.  All three classes now carry one and the asterisk is
% glossed inside the panel by the rule it clears.  Do not write "unmarked" here again.
% THE DATABASE NAMES ARE IN THE PANELS AND NOT HERE, 2026-08-07, which is why this caption may
% still say "counterpart" and does not have to spend words distinguishing the two references.
% The panels now read "external reference profile (VT-2005)" in (a) and "Delaware (UD) profiles"
% in (b), because the labels used to say "reference profile" on both sides
% of a figure whose two halves are measured on DIFFERENT tables -- the same conflation the
% abstract's "the same reference" carried, corrected in the same pass.  If a later edit strips
% a name from a panel, this caption must buy it back in words, budget or no budget.
% (2026-08-10: "UD" became "Delaware (UD)" IN THE PANEL, so the caption still owes nothing here.
% The initials alone printed on page 4 and were expanded on page 7.)
\caption{\textbf{Overview.} \textbf{(a)}~A learned encoder predicts a charge-density profile
$\hat\sigma$ that a fixed thermodynamic model turns into a solubility; the arrow marks the
substitution of the reference $\sigma^\star$ for it. \textbf{(b)}~The counterpart error on
infinite-dilution activity data: the strip is every
stratum searched, the bars the three solvent classes with a bound; only the starred class stands,
each mark carrying its own reason, and each block is a one-sided
bound rather than an interval. The right-hand block is the error of the map as deployed, the fixed
kernel read at a computed profile, and the profile calculation's share of it is not bounded here.
Training-time supervision is not drawn.}
\label{fig:overview}
\end{figure*}

\input{sections/methods}

\section{Results and discussion}
\label{sec:results}
A cluster-bootstrap frequency $P$ is the frequency, over two-way (solute\,$\times$\,solvent) cluster
resamples, with which the margin it is attached to stays positive; it is not a $p$-value against any
null. Such frequencies are quoted as bands---rounded to one decimal, with $>0.95$ and
$<0.2$ closing the ends---because with $17$ solvent and $41$ solute clusters on the VT-2005-matched
set they do not carry two-decimal resolution; the two-decimal values are in the deposited artifacts.
The resampling intervals of this paper---the ranking and recalibration intervals of
\S\ref{sec:decision}, the margins of \S\ref{sec:measure} and the
pKa intervals of \S\ref{sec:pka-main}---are \emph{percentile} intervals: their endpoints are order
statistics of a resampling distribution, with no bias correction and no studentisation, over the unit
named beside each. \S\ref{sec:decision} resamples the $107$ solute clusters, $4000$ draws, at the
$95\%$ level; \S\ref{sec:measure} resamples solute and solvent clusters two-way, $3000$ draws;
in \S\ref{sec:pka-main} the decomposition bounds resample molecules within each Hammett reaction
series and the flip gap resamples the four series themselves, $4000$ draws. Not every $\pm$ or
bracketed span below is one of those. Two in particular are not: the kernel-transfer figures of
\S\ref{sec:badclosure} are a standard \emph{error} over five fit seeds, and the
leave-one-publication-out span of \S\ref{sec:broad} is the range of that curve.
Every claim below carries its set, its size and its scope in the sentence that makes it; the same
nineteen are collected side by side as one table in \S\ref{sec:si-ledger}
(Table~\ref{tab:claims}).

% THE CLAIM LEDGER LEFT THIS DOCUMENT ON 2026-08-08 for \S\ref{sec:si-ledger}, whole and with its
% floats, its \ContinuedFloat and its notes a-h intact.  It went for readability: twenty-five
% rows of \scriptsize across two pages of the article, which is what the author asked to be rid of.
% THE MOVE DID NOT FIX THE TYPE SIZE, and both referees this round said so of the relocated table.
% It is now three floats at \footnotesize with its notes at \scriptsize; the arithmetic behind the
% three is in the comment above the first part in sections/SI.tex.  A ledger set small enough to
% fit two floats is the defect coming back, not a saving -- do not re-tighten it here or there.
% WHAT WAS PAID FOR THE MOVE, and what a later editor must not undo.  Between 2026-08-02 and this
% pass the ledger was the manuscript's HEDGING APPARATUS -- the scope clauses that used to trail
% the prose sentences had been moved into its value cells to shorten the prose.  Moving the table
% with those scopes still inside it would have left the article ASSERTING FLATLY what it now
% qualifies.  So the scopes went back into the prose FIRST, claim by claim, and only then did the
% table move.  The restorations are, by ledger block:
%   two senses  -- the 5608-of-8103 row set and its identity across the single-seed controls, said
%                  once at the head of \S\ref{sec:paradox} and governing the block; per-seed
%                  non-overlap on both operations; the 0.59 share with "describes that seed, not a
%                  magnitude" and the two other-seed readings that show why.
%   decision    -- top-1 -0.107 [-0.192,-0.020] with "pooled and only pooled, two of three per-seed
%                  intervals span zero", in its own period, per the note above \S\ref{sec:decision}.
%   closure     -- note b (what a P is) at the head of \S\ref{sec:results}; note g in full, plus
%                  note d, as the reading rule at the head of \S\ref{sec:measure}, where it governs
%                  every margin of \S\ref{sec:broad}--\S\ref{sec:badclosure} at once; P>0.95 on the
%                  four glycol-ether cells; "no stratum but the one named below is stated as a
%                  finding".
%   fidelity    -- "and not a measured equality either" with the two frequencies that show it; the
%                  57 shared pairs' non-independence restated where the claim is made.
%   mechanism   -- in sections/compensation-surrogate.tex: the 51+-2% magnitude's instability
%                  across analysis configurations, "one grounded checkpoint" on the top-two share,
%                  and what the +-0.5 on the 4.1x spans.
%
% SECOND PASS, 2026-08-08, and the reason there had to be one.  Three referees read the moved
% article and all three reported claims flatter than the cells they came from, which is the one
% failure the move was run to avoid.  The first pass had swept the ledger's HEDGE clauses and not
% its SET, n, LEVEL and RESAMPLING-UNIT columns, so what it missed is systematic rather than
% scattered: a scope that lived in the "Set (n)" column, or in a P/interval the value cell carried
% beside the point estimate, had nowhere in the prose to land and was silently dropped.  The seven
% they named and the four the sweep added:
%   named      -- the dispersion-on cell +0.06 at P~0.6 (\S\ref{sec:fidelity}: without it the
%                 section said "not an improvement under either rule" of a step whose sign is not
%                 established in EITHER direction); the two typed-latent floors +1.29 and -0.77
%                 with their P bands, of which only the first stands; the kernel-residual null's
%                 set and size (n=60, folds grouped by solvent); the 18.75-against-1.47's set and n
%                 (in compensation-surrogate.tex); the cross-fitted +1.76's interval [+1.02,+2.49],
%                 which the disclosed deviation from the pre-declaration turns on and which may
%                 never again print as a bare point estimate; the clean pole's sign test p~0.31
%                 beside the degraded pole's 1/16, and the clean pole's gap given the interval
%                 treatment the degraded pole's already had; \S\ref{sec:decision}'s level and unit
%                 (95%, the 107 solute clusters) plus the four parity slopes' seed.
%   swept      -- the 57-shared-pair block's own value and P (1.78->0.77 at P>0.95: the article was
%                 printing the P of the WEAKEST of the three blocks and none of the strongest);
%                 "over the same three seeds" on the 1.85-vs-2.25 pair; the nine solvent classes
%                 the 2000-partition null permutes; the 5608 rows the +0.14 dMAE is measured on.
% THE RULE THIS LEAVES.  A ledger cell has FOUR columns' worth of scope, not one: claim, set, n,
% and the value's own uncertainty.  Check a restoration against all four.
% Do not shorten the prose by moving a scope back into that table.  Do not bring the table back
% into this document.
\subsection{Supervising the intermediate toward the reference, and substituting it at prediction time}
\label{sec:paradox}
The word ``grounding'' names two opposite operations on the same VT-2005 database. Every solubility
figure in this subsection and the next is an \lnx\ MAE over one row set, the $5608$ of the $8103$
scaffold-split test rows that carry a solubility measurement (Table~\ref{tab:notation}); the
single-seed control arms below carry that identical set, so nothing here is compared across row
sets. Supervising the
$\sigma$ head against the database \emph{during training} improves solubility MAE from
$2.043\pm0.040$ (ungrounded) to $1.846\pm0.053$ (grounded), a gain of $0.20$ over three seeds, the
two arms' per-seed values not overlapping.
% ITEM 2 OF THE 2026-08-11 REFEREE ROUND.  The two arms are NOT budget-matched -- the schedule
% gives the grounded arm a sigma warm-up of up to 40 epochs before the shared curriculum and the
% ungrounded arm zero (configs/cosmo_sac.yaml sigma_warmup_epochs: 40; scripts/train.py runs
% run_sigma_warmup_pretraining whenever a sigma stream exists) -- and the article printed the gain
% without it, while \S\ref{sec:si-tables} carried the asymmetry.  The null-target arm that would
% separate the reference values' ACCURACY from the extra pretraining is a training run and was not
% made.  Do not restate this gain as a measurement of supervision toward accurate values.
The two arms are not budget-matched: the grounded arm precedes the shared curriculum with a
$\sigma$ warm-up on the grounding pool of up to $40$ epochs, early-stopped on a held-out tenth,
and the ungrounded arm sets that budget to zero (\S\ref{sec:si-lock}). No arm runs that warm-up
against permuted $\sigma$ targets, so what the gain prices is the intervention entire---extra
pretraining and accurate targets together---and not the accuracy of the reference values on its own.
Those spreads are over initialisation and batch ordering
and price nothing about which solutes fell in the test set; pooling the three seeds and resampling
the $147$ test solute clusters puts the gain at $+0.198$, $95\%$ percentile interval
$[+0.06,+0.33]$. The
ungrounded arm carries no $\sigma$ stream, so this is the one contrast in the paper that the
uncertified stream build of \S\ref{sec:data} could inflate rather than one the arguments there bound
(\S\ref{sec:si-repro}). Deleting every solute that build can reach---the $8$ of the $147$ carrying
their own reference profile or a scaffold relative in it, on $351$ of the $5608$ rows---leaves
$2.036$ against $1.864$ on the remaining $139$ solute clusters, a gain of $+0.172$
$[+0.03,+0.31]$. That bounds
where the leak can act and does not certify the build: it removes the rows a leaked profile scores,
not the effect a leaked profile had on the weights.
Substituting the reference profile for the learned one \emph{at inference} degrades it, and that
substitution alone is the effect this paper is named for. It erases any positive $R^2$
(Fig.~\ref{fig:paradox}): the model predicts solubility better \emph{from its own learned
$\sigma$-profile} than when \emph{given the external reference one} (MAE $1.85$ vs $2.25$, in \lnx\
units, over the same three seeds and with no overlap between the two arms' per-seed values either).
Resampling the same $147$ solute clusters over the pooled three seeds puts that penalty at $+0.406$,
$95\%$ percentile interval $[+0.29,+0.53]$; the median per-row gap is $+0.32$ against the mean's
$+0.41$, so the effect is present at the middle of the distribution and heavier above it.
Of the figure's eight arms two are non-COSMO controls, DirectGNN and a fitted NRTL closure;
five COSMO-SAC arms differ in how the $\sigma$ head is trained---the warm-up asymmetry above
included---and in what it is fed, and the
sixth (\emph{grounded\,$+$comb.}) differs instead in the closure, restoring the
Staverman--Guggenheim combinatorial term. \emph{Ref.\ $\sigma$ (eval)} is the grounded checkpoint
re-evaluated with the reference profile substituted at test time, \emph{ref.\ $\sigma$ (train)} the
co-adaptation control injecting it during training instead.

That $+0.41$ is the evaluation-only substitution, and part of it may be unrelated to the
closure: swapping an input distribution at test time degrades a pipeline on its own. The control that
removes that component injects the reference profile \emph{at training}, so the model co-adapts; at
seed $42$ it costs $+0.18$ ($1.803\to1.981$) where the evaluation-only substitution costs $+0.43$ at
that same seed ($1.803\to2.232$), so part of the $+0.41$ goes with co-adaptation and the
co-adapted arm is by design free of \emph{that} confound. That part is $0.251$ of the seed-$42$ gap
of $0.428$, a share of $0.59$,
% ITEM 4 OF THE 2026-08-10 REFEREE ROUND.  This said "the same numerator", which is not what the
% alternative readings hold fixed: the INJECTED arm is the quantity measured once (1.9806), and both
% the learned baseline and the substituted arm take their own values at seeds 43 and 44, so the
% numerator moves with the substituted arm.  The three shares were and are right; only the account of
% how they were obtained was wrong.  Arithmetic from si_tables/si_arms_table.json, unrounded:
% (2.231698-1.980577)/(2.231698-1.803446)=0.586, (2.281292-1.980577)/(2.281292-1.921140)=0.835,
% (2.241988-1.980577)/(2.241988-1.812583)=0.609.
and we report it as a description of that seed rather than as a magnitude: the injected arm is what was
measured once, and holding it at that value while the learned and substituted arms take their
seed-$43$ and seed-$44$ values---which moves the numerator too, to $0.301$ and $0.261$---gives $0.83$
and $0.61$ (\S\ref{sec:si-tables}). A channel-swap control costs $+0.27$, to
$2.08$. It carries a second confound the co-adapted arm carries too: neither is certified to have
trained on the same $\sigma$-stream build as the seed-$42$ comparator both are read against, and the
file times settle nothing either way (\S\ref{sec:si-repro}).
Both are worse than the learned-$\sigma$ arm, which rules out an implementation artifact. Only the
three-seed arms can be set against one another, and there the evaluation-only substitution costs
about twice what supervision gains ($+0.41$ against $-0.20$). That ratio is between two three-seed
arms and carries the co-adaptation confound with it, so it is not the paradox's co-adaptation-free
magnitude; the arm that is, at $+0.18$, was run at one seed on an uncertified stream build and
cannot be ordered against the
$-0.20$ at all, whose own per-seed values ($0.237$, $0.075$, $0.281$) straddle it. Which of the two
operations is larger is therefore unresolved, and only their signs are carried forward.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_paradox.pdf}
% Caption: what is shown, and nothing else.  The two caveats that used to close it -- that
% the sigma-oracle bar swaps the input at evaluation only, and that DirectGNN was tuned
% separately -- are both in the body within a paragraph of this float.  Do not restore them.
\caption{Controlled comparison on the $5608$ labelled test rows of the scaffold split.
Solid bars: three seeds,
mean $\pm$ a population standard deviation over them. The two hatched bars are the
co-adaptation controls, seed $42$ only, so they carry no error bar and no $R^2$, and keep an empty
slot in the right panel. Dashed in the left panel only, the DirectGNN reference.}
\label{fig:paradox}
\end{figure*}

\subsection{What the substitution costs: level, spread, and the choice of solvent}
\label{sec:decision}
% NEW SECTION NUMBER, 2026-08-07, carved out of the incumbent \S3.1 without rewriting a
% clause of it: the parity/shrinkage paragraph, the ranking result and the solute-blind
% comparison move here whole.  The section carries the phrase "the choice of solvent", which
% to a chemist cashes as top-1 -- the one ranking metric of the three that does not hold per
% seed -- so the sentence that first reports the ranking result states in its own period that
% top-1 is pooled only.  IT DID NOT, until 2026-08-08: the top-1 value and its "pooled only, two of
% three per-seed intervals span zero" were in the ledger's cell and nowhere in this prose, so the
% section named for the choice of solvent never printed the one metric that phrase cashes as.  Both
% are now in the period after nDCG@3, where this comment always said they belonged.
% Do not split that clause off into a later caveat, into
% Table~\ref{tab:claims}, or into the Supporting Information.
Figure~\ref{fig:parity-lnx2} sets the same rows out predicted against measured, for the control and
three of the closure arms. Two things are visible there that the arm ordering is not. First, every
arm is severely shrunk toward the corpus mean. The ordinary least-squares slope of predicted on
measured is $0.515$ for DirectGNN, $0.416$ ungrounded, $0.528$ grounded and $0.690$ with the
reference profile, none of them near $1$, and in every panel the median trace crosses the identity:
sparingly soluble compounds are called more soluble than they are and highly soluble ones less.
All four slopes are seed $42$'s, the figure's seed, with no replicate behind them. That shrinkage
is not the closure's doing: at that seed the closure-free control's slope differs from the grounded closure's by
$0.013$, so whatever produces it lives in the encoder and the data. Second, the reference-profile arm is the
\emph{least} shrunk and the worst. Its slope is the closest of the four to $1$ and its prediction
spread exceeds the measurement's, yet its MAE is the highest and its $R^2$ is not distinguishable
from zero; substituting the reference restores spread that is not aligned with the truth, and its
worst region is the sparingly-soluble tail it appears to reach. That tail is where the closure is
driven hardest: at seed $42$ the substituted arm returns $174$ rows with $\lng>10$ against none in
the learned arm, whose largest is $9.70$ against the substituted arm's $19.5$, and $179$ of its
\lnx\ predictions fall below $-15$, to a minimum of $-22.3$ where the corpus itself stops at
$-17.7$. The worst $500$ of the $5608$ rows carry $0.32$ of that seed's $+0.43$ mean gap.
Neither arm's fixed point is re-solved to convergence there, both being scored at the
$30$ segment iterations of \S\ref{sec:model}, so those rows are the ones on which the truncation is
least certified as well as the ones on which the two profiles differ most.
% ITEM 1 OF THE 2026-08-11 REFEREE ROUND.  The article said the truncation is least certified on
% these rows and left the exposure at that; the Supporting Information said it was "unmeasured".  It is
% unmeasured ON THESE ARMS -- the tree keeps none of their weights -- and it is not small where it
% CAN be measured.  Artifact: results/softmax_floor/{gate,ladder_table}.  The scoping clause is
% load-bearing and this project's own rule requires it: that head does not reproduce the phenomenon,
% so what the two counts price is the lever and not this paper's number.  Do not drop it.
What the count is worth on these arms is not measurable from the retained artifacts, which keep none
of their weights. On the one trained COSMO-SAC head the tree does keep---a retired run whose weights
were fit at $8$ iterations and which scores $2.61$ at $30$ against $1.92$ at $8$ on these same
labelled rows---the substitution penalty moves with the count as well: substituting the reference
profile costs $+4.32$ $[+3.61,+5.25]$ at $8$ against $+6.43$ $[+5.39,+7.68]$ at $30$ over $5571$ rows in $64$ solvent clusters whose solvent carries one.
That head reproduces neither the size nor the direction of the penalty reported here, its own being
fifteen times larger at the count the $+0.41$ is measured at, and moving its predictions the other way, so those two figures price the lever and
not this paper's $+0.41$. That is the misspecified
map made visible on the solubility axis.

% RANKING (2026-08-06).  Added here and not in the SI because it says what the paradox IS about:
% the substitution costs a decision, not a calibration.  Both sides are ONE checkpoint, re-run with
% a different profile injected, max |dPhi| <= 4.2e-4, so it clears the SEPARATE-TUNING confound --
% which is why it sits in the section the confound paragraph of \S\ref{sec:discussion} lists as
% surviving -- and it does NOT clear co-adaptation, because it is still an evaluation-time swap.
% Do not restore "the one place that can be read with no matched control": that overstated it.
% Everything printed here is three-seed with a cluster bootstrap over the 107 solutes; the two
% things that are not (per-seed top-1, and the affine residual at seed 43) are quoted pooled or not
% at all.  The affine recalibration is quoted BOTH in sample and leave-one-row-out, because the
% in-sample fit is two parameters on a median of five solvents and bounds the concession rather
% than measuring it.  Working and deposits: \S\ref{sec:supporting}, Table~\ref{tab:ranking},
% results/e5_sigma_grounding/ranking/.
The substitution also costs the decision the model is used for. The separate-tuning
confound of \S\ref{sec:discussion} does not stand in the way here: the reference-profile arm re-runs
the grounded checkpoint's own weights over the same rows and changes only the profile fed to the
closure, so no second tuning stands between the two sides. They differ in $\Phi$ by at most
$4.2\times10^{-4}$ over these $5608$ rows at the worst of the three seeds.
The confound it does not remove is co-adaptation: like the MAE gap above this is an
input swapped at evaluation, no arm that had trained on the reference profile was ranked, and what
co-adaptation would do to an ordering is unmeasured here.
Group the labelled test rows by solute and temperature and rank the solvents inside each group
($823$ groups over $107$ solutes; Table~\ref{tab:ranking}). The mean per-solute Spearman correlation
then falls from $0.592/0.546/0.598$ to $0.365/0.369/0.410$ across the three seeds, pooled $-0.197$
$[-0.282,-0.117]$, every per-seed interval excluding zero. Each of the three falls is larger than
any difference between seed replicates of a single arm, the largest of which is $0.060$.
Every interval here is a $95\%$ percentile bootstrap over those $107$ solute clusters, the
effective sample rather than the $823$ groups.
Normalised discounted cumulative gain over the top three solvents (nDCG@3) falls by $-0.084$
$[-0.124,-0.043]$, and top-1 accuracy by $-0.107$ $[-0.192,-0.020]$. That
last figure is quoted pooled and only pooled: two of its three per-seed intervals span zero, so it is
the one of the three ranking metrics that does not hold seed by seed. The solvent the
model would select changes in $40$ to $53\%$ of groups, asymmetrically: the learned profile alone
picks the measured best solvent in $20.0/23.2/21.5\%$ of groups, the reference profile alone in
$6.2/14.5/12.2\%$. None of that is a level. Fitting a separate affine recalibration inside every
group---a slope as well as an intercept, the largest concession a reading on which the substitution
merely miscalibrates can ask for---removes $86\%$ of the MAE gap, $+0.431$ $[+0.31,+0.56]$ to
$+0.060$ $[+0.02,+0.10]$, which is the restored, misaligned spread of the paragraph above in
another currency. That map is fitted on the rows it is then scored on, two free parameters against a
median of five solvents, so it bounds what the concession can buy rather than measuring it. Refitted
leaving out the row it scores, it removes none of the gap: $+1.09$ $[+0.3,+2.2]$ pooled. On the
$550$ groups holding at least five solvents, where a refit has something to work with, it gives
$+0.28/{+}0.07/{+}0.04$ by seed, two of the three intervals spanning zero
(\S\ref{sec:supporting}). The MAE penalty is level and scale in the sense that a per-group map
fitted on the answer absorbs most of it; out of sample nothing that sharp can be said in either
direction. The ranking penalty is not, under either fit, and by invariance rather than
by any centring argument---though the invariance is to an \emph{increasing} affine map, and the
fitted map decreases in $10$ to $12\%$ of learned-profile groups and $17$ to $19\%$ of
reference-profile ones, where it reverses that group's order outright. Restricted to the $589$ groups
of $823$ whose map increases in both arms at all three seeds, the ranking gap is $\rho$ $-0.148$
$[-0.218,-0.079]$ and nDCG@3 $-0.068$ $[-0.110,-0.026]$, with top-1 then spanning zero.

What stands in its place cannot be told apart from a solute-blind ordering. Two references know
nothing about the solute: the same arm's own predictions for a randomly drawn other solute over the
same solvents, and a ranking of those solvents by their leave-one-solute-out mean measured
solubility. Set against both, the learned-profile arm is above them on Spearman, Kendall, top-1 and
nDCG@3 at all three seeds, twenty-four intervals of twenty-four excluding zero. The reference-profile arm's own
twenty-four margins are small and all but one positive, and it is their intervals that carry the
reading: twenty-four of the twenty-four include zero, or twenty-three, one cell sitting on the
boundary at $[-0.0008,+0.0762]$ and changing side with the bootstrap draw
(Table~\ref{tab:ranking}). What that measures is a failure to
separate the reference-profile arm from orderings built without the solute, not a demonstration that
it cannot be separated from them. Neither arm is chance, the within-group permutation floor being
$\rho=0.00\pm0.02$ for both. Supplying the true physical intermediate does not leave the model
miscalibrated about solutes; it leaves its ordering of them indistinguishable from one drawn without
them.

\input{sections/fig-parity-lnx2}

\input{sections/compensation-surrogate}

\subsection{What the substitution does not decide, and what would settle it}
\label{sec:notdecided}
% NEW SECTION, 2026-08-07, phenomenon-first restructure (spine/phenomenon.md Sec. 3, slot 3).
% IT IS THE BARRIER between the phenomenon (Secs. 3.1-3.3) and the second axis (Sec. 3.5), and it
% sits BEFORE the second measurement rather than after it, so that it reads as forward scope --
% what the next measurement is and is not for -- and not as a retroactive devaluation of pages the
% reader has just finished.  Placing it after was the reading failure recorded against the rival
% designs.
% WHAT IS DELIBERATELY NOT HERE.  The sentence that used to close \S\ref{sec:paradox} -- "what
% evidence there is for the causal reading is therefore taken from the activity axis ... the
% decomposition and the latent's departure from its reference" -- is NOT carried forward.  It is
% the only sentence in the article that characterises \S\ref{sec:measure} as evidence for a reading
% \S\ref{sec:discussion} declines to assert, and under this numbering it pointed across this
% barrier.  Its surviving clause lives with the latent's departure, at the head of
% \S\ref{sec:surrogate}; what replaces the rest is the impossibility claim below.  The disposition
% is recorded as named exception 1 in the pre-commitment inventory, which moved to
% \S\ref{sec:si-repro} on 2026-08-07; \S\ref{sec:data} keeps only the summary of it.
% Do not restore it.  Do not preview \S\ref{sec:measure}'s parity result here either: a result of
% the second axis stated inside the phenomenon block is the adjacency this section exists to
% prevent, and it used to sit three words from the sentence above.
% Do not cite \S\ref{sec:estimator} from this section or from \S\ref{sec:paradox}--\S\ref{sec:surrogate}:
% the estimator cell is used by \S\ref{sec:measure} alone, and \S\ref{sec:measure} restates it at
% its own head.

Which of the two channels the substitution fails through is not decided here. Drug-like solutes are
essentially absent from the VT-2005 set, so the substitution falls almost entirely on the
\emph{solvent} channel, and the both-channel substitution the attribution would need covers only
${\approx}7$ solutes; enlarging that set would mean computing new reference profiles rather than
looking them up.

Nor does anything above decide \emph{why} the substitution hurts. The reading it invites is that
handing the closure a faithful input surfaces a misspecified map rather than repairing it, so that
the reference profile is the better input only where the map that consumes it is faithful. That
reading is untested here, and for a stronger reason than a test that returned nothing: on the
solubility axis the two terms of the split do not separate at all, and the
solubility axis is where the substitution of \S\ref{sec:paradox} was measured. The quantities the
reading is about---a fixed map's own error, and what its inputs fail to carry---are therefore not
observable on the rows \S\ref{sec:paradox}--\S\ref{sec:surrogate} report.

\S\ref{sec:measure} measures a second axis rather than explaining the first. It uses a
different reference database, a different error functional, no trained model at all, and a row set
that shares none of the rows above; the two are measured in different regimes on largely disjoint
chemistry, and no measurement in this paper connects them. Until the connecting experiment is run,
``the substitution hurts \emph{because} the map is misspecified'' remains untested.

Two experiments would settle it and neither has been run. The first is the
transfer back to the paradox's own regime---finite composition and all $\gamma$, where the
substitution is measured, rather than infinite dilution, where the split is---which is
limitation~(i) of \S\ref{sec:discussion} and the principal open question here.
% SLACK TAKEN, 2026-08-10 (item 5 of the referee round: report the growth, and take slack that costs
% no claim).  This sentence read "The second is a typed sigma-profile head trained end to end
% through a modern kernel, which would show whether the substitution's sign survives a closure of
% different fidelity" -- 24 words that the limitation-(x) discharge below had just restated more
% precisely, and twice: the head appears in "Both runs need the typed head trained end to end
% through the 2010 kernel", and the question appears as "whether the solubility paradox survives a
% 2010/dsp closure is untested", which names the kernel where the deleted clause said "different
% fidelity".  The requirement is kept, at its one remaining occurrence, and the indefinite article
% restored there so it still introduces the object.  Nothing else went.
% LIMITATION (x) PRINTS ITS CONTENT HERE, 2026-08-10 (referee item: the Supporting Information's
% preamble names (ix), (x) and (xiii) as quantities an article sentence rests on and does not print,
% and this is the sentence that rests on (x)).  Before this pass the article cited the numeral twice
% and stated only the second of the limitation's three parts, so a reader could not tell that
% \Ggap\ is unmeasured under a modern closure at all -- which is what stops the fidelity contrast of
% \S\ref{sec:fidelity} from being read as a measurement of the paradox under a better closure.  The
% third part, what each of the two runs needs, is what makes "has not been run" a statement about
% work rather than about willingness.  Do not compress this back to the numeral.
The second is limitation~(x), and it has a part the fidelity
contrast of \S\ref{sec:fidelity} does not supply: that contrast compares \emph{oracle-input}
activity error across kernels, so $\Ggap$ itself is measured under no modern closure on either axis,
and whether the \emph{solubility} paradox survives a 2010/dsp closure is untested. Both runs need a
typed $\sigma$-profile head trained end to end through the 2010 kernel, and the solubility one
additionally needs a UD$\,\cap\,$BigSolDB matched set, which does not exist here.


\subsection{A second axis: bounding a deployed COSMO-SAC against its inputs}
\label{sec:measure}
% Artifacts. VT-2005-matched set (n=60): results/b_insuff/decomposition.json via
% run_b_insuff_decomposition.py; bin-count / variance / convention grid with per-cell bootstrap:
% results/b_insuff/estimator_grid.json; blocked-fold and distinct-pair estimators and the deployed
% adaptive-bin bootstrap: results/b_insuff/keystone_robustness.json.
% Broad IDAC set (n=477): results/b_insuff/representative_decomposition.json via
% run_b_insuff_representative.py; round-2 design/provenance audit: representative_audit.json.
% GUARD 6 (2026-08-07).  \S\ref{sec:estimator} is the last section of Methods and the section it
% abuts is \S\ref{sec:paradox}, not this one, which is seven pages further on.  A pre-declaration
% belongs beside its first number, so this section restates the cell and the four choices fixed in
% advance at its own head.  The restatement may NOT add a choice, and where it and
% \S\ref{sec:estimator} disagree, \S\ref{sec:estimator} governs.
The estimator is the binning bound, read at the one estimator cell fixed in advance in
\S\ref{sec:estimator} and used nowhere else in
this paper: eight equal-count bins of the conditioning variable, the residual-only convention, the
unbiased within-bin variance, and the admissibility clauses~(a)--(c) that decide which strata may be
read at all. All four were fixed before any margin was computed; none of
\S\ref{sec:paradox}--\S\ref{sec:notdecided} is measured with them.

The decomposition of \S\ref{sec:decomp} separates a fixed closure's error into a part in the map and
a part in its inputs. Its output is a map over chemical strata rather than one verdict for a set,
for the reason \S\ref{sec:broad} establishes, and it runs on two data sets that must not be
conflated, the \emph{broad IDAC set} and the \emph{VT-2005-matched set} of
Table~\ref{tab:notation}.

One reading rule governs every margin below, on either set and under either kernel, and it is stated
once here rather than at each of them. The comparison is one-sided in both directions it can be
read: a positive margin establishes $\Bclos>\Binsuf$, while a non-positive one is a failure to
separate the two terms and licenses nothing---it is not a measurement that they are equal, and no
cell of the map can ever return ``better inputs would help here''. The size of a margin is a
\emph{lower bound} on $\Bclos-\Binsuf$ and not an estimate of it (Lemma~\ref{prop:decomp},
\S\ref{sec:decomp}), so an interval quoted on a margin is an interval on that bound and not on the
difference itself. And $\Bclos$ throughout is the error of the fixed map \emph{read at a computed
profile}, so what a positive margin establishes is a property of that composite and not of the
kernel alone (\S\ref{sec:badclosure}). The one estimator cell above serves both sets, both kernels
and every stratum, so no cell below was fixed after the number it returned was seen.

\subsubsection{The broad IDAC set ($n{=}477$)}
\label{sec:broad}
The closure reads a temperature as well as a profile pair and is evaluated at each row's own
temperature, so the conditioning variable is its full argument $(\zstar,T)$: the concatenated UD
profile pair with $T$, $103$ dimensions against $n{=}477$ ($\dim/n\approx0.22$). The target $m$
is the experimental $\lngi$, so floor and closure error are measured on one data set and one profile
database, that database being UD throughout this subsection.
Its chemistry is small: no solute exceeds $12$ heavy atoms (median $7$), against a median
of $14$ and a maximum of $122$ for the drug-like solids of this paper's own corpus, and only
$0.46\%$ of corpus rows carry a solute from this set. \emph{Broad} therefore names the reach of the
$\gamma^\infty$ corpus the kernel is calibrated on and not coverage of the chemistry a solubility
model is deployed against: it covers the solvent side of deployment and not the solute side. It is
matched to UD by InChIKey with a first-block fallback, looser than the VT-2005 set's rule. A
displaced $\zstar$ inflates $\Bclos$ and the binned $\Binsuf^{\mathrm{up}}$ together and its
direction on the margin is unsigned; what carries the point is the re-estimate, the aggregate
margin being unchanged on the $465$ exactly matched rows.

Every $\mathrm{MSE}$, bound and margin on this axis is in squared \lngi\ units, and none of them is
on the \lnx\ axis the MAEs of \S\ref{sec:paradox} are measured on.
Under the convention rule of \S\ref{sec:estimator} the \emph{2002, all channels} arm scores
$\mathrm{MSE}=1.902$ against $\Var(m)=4.85$, and the binning bound gives $\Binsuf\lesssim0.70$ and
hence $\Bclos\gtrsim1.21$. The separation margin for the set taken whole is $+0.51$, against $+0.95$
under the full convention and $+0.27$ collapsed to one row per distinct pair. It is positive in
every cell of the bin-count\,$\times$\,variance\,$\times$\,convention grid, and two of the three
bootstrap clusterings extend just below zero.

That aggregate is not the instrument's output. Two operations reported separately---the pair unit,
and dropping the two chemistries the error concentrates in, the glycol-ether solvents and the rows
carrying water as the solute---compound badly. The chemistry cut alone takes $+0.51$ to $-0.12$ on
the $258$ rows that remain, the pair unit alone gives $+0.27$, and the two together give $-0.38$ on
$136$ pairs; under the full convention the same pair of cuts takes $+0.95$ to $+0.28$ to $+0.01$.
Nor is the aggregate carried by the set: deleting one source publication at a time,
\texttt{10.1021/acs.jced.7b00114}---$37$ rows, six molecule pairs, $32\%$ of the set's squared
error---takes the margin to $+0.18$, while each of the other fourteen deletions leaves it between
$+0.42$ and $+0.87$ (under the full convention that curve is flat, $[+0.82,+1.07]$ against $+0.95$).
That single deletion loses two thirds of the margin and its resolution without reversing its sign;
the sign reverses only under the compounded cut. Two caveats qualify the corroborating estimators
and not the bound---the out-of-fold ones leak under random folds and are uninformative or split
under blocked ones, and the tighter binning-free pair bound samples no between-laboratory component
and is therefore not promoted---so together they say the binning bound is slack rather than that
$\Binsuf$ is negligible (\S\ref{sec:si-tables}). What is reported rests on the binning bound
(\S\ref{sec:estimator}, Table~\ref{tab:bounds}).

% THE BROKEN PRE-DECLARATION KEEPS ITS REASON, 2026-08-10.  The paragraph above states, in print,
% that a binding pre-declaration was not followed, and then sent the reader to
% \S\ref{par:crossfit-deviation} to learn why.  A disclosure whose reason is somewhere else is a
% disclosure a reader cannot weigh, and this one is the paper's own admission against interest, so
% the two sentences that carry the reason are here -- the hazard the rule was written against, the
% hazard it did not anticipate, and what obeying it would have printed.  They are the Supporting
% Information's own sentences, not a paraphrase.  What stays there is the poorer map cell by cell,
% which is the evidence the deviation can be audited against and is a table's worth of cells.
% Do not put the reason back behind the pointer, and do not soften "we did not follow that rule"
% wherever it is stated in either document.
% THE BOLD SENTENCE, 2026-08-05 (round 3, referee 1, must-fix).  It used to read "That row set is
% the one established finding", which asserts a status this paper's own controls decline to give:
% the row set is the survivor of a 59-fold search, its magnitude is at the ninetieth percentile of
% the chemistry-blind null below, and the pre-declared out-of-sample cell is empty.  The Conclusions
% already say "where the instrument answered, not where the model is shown to fail"; this sentence
% now says the same thing, and the null number is INSIDE it rather than one paragraph later, so a
% reader cannot take the claim and leave the caveat.  Do not restore "established finding" here, the
% bold in the map tables (tab:map-*, whose passing row is generated by
% scripts/analysis/make_map_table_tex.py), or "the established stratum" as a name for the row set.
\emph{The map, and how much of it survives the rule.} The instrument runs over $59$ strata of the
broad IDAC set---$477$ rows, $185$ pairs, $78$ solutes, $36$ solvents, $15$ source
publications---on the solvent axis at three granularities and the solute axis at two, plus their
cross-tab and the set as a whole. Fifteen have $n\ge40$ and are boundable, the only ones whose
margins the rule can act on, and Table~\ref{tab:map-article} prints all fifteen with their
$n$, their sources, their margins, their intervals and the condition each is in. The other $44$,
including the $24$ that carry no estimate at all, are
printed with the test each fails in \S\ref{sec:si-map}
(Tables~\ref{tab:map-full}--\ref{tab:map-detail} and Fig.~\ref{fig:decomp}), every stratum with its
error, its bound, its interval and its share of the set's squared error, nothing dropped for
being small. No stratum but the one named below is stated as a finding anywhere in this paper.
Six of the fifteen are \emph{admissible}. Clause~(c) of the rule makes those six three
distinct row sets, of which one is both positive and not demoted by its residual test: the
glycol-ether solvents. \textbf{That row
set is the one the admissibility rule leaves standing}, at a margin a chemistry-blind relabelling of
the same molecules reaches in $10.1\%$ of draws (below). Its clusters are few and its chemistry is
narrow, and both belong in the sentence that states it. The $182$ rows over $43$
pairs carrying $45\%$ of the set's squared error span $4$ solvents---ethane-1,2-diol and the di-,
tri- and tetraethylene glycols, one homologous series---and $19$ solutes, every one of them a
hydrocarbon or a halobenzene ($108$ alkane rows, $48$ aromatic hydrocarbon, $24$ alkene, $2$
halogenated aromatic); $175$ of the $182$ rows are gas chromatography, and two of the three
publications carry $177$ of them.
On those rows the closure's own error exceeds what input insufficiency can
account for by at least $+2.04$, that bound carrying a $90\%$ interval of $[+1.25,+2.87]$ at the
residual-only convention and reaching $+2.94$
at the pair unit under the full one. It is positive in all four cells at $P>0.95$ in each, and under
every single-publication
deletion, the worst leaving $+1.60$ when \texttt{10.1016/j.jct.2016.10.013}, which carries $53\%$ of
that stratum's squared error, is removed. The unit the stratum's chemistry actually varies over is
the solvent, and the deletion that unit calls for is not the publication's: three publications map
onto four molecules, so leaving one out is the sharper test. Deleting each glycol in turn leaves
$+1.72$, $+1.94$, $+2.05$ and $+2.29$, and taken one at a time the four give $+3.17$, $+2.44$,
$+1.71$ and $+1.21$, falling monotonically with chain length on $41$, $36$, $56$ and $49$ rows. The
sign survives every deletion; the magnitude runs over a factor of two and is set by the shortest
oligomer. Read the four one-at-a-time values as a shape and not as four bounds: diethylene glycol's
$36$ rows are below the $n\ge40$ the map's own boundability rule asks of a cell, so that one is
printed here for the trend it belongs to and is not admissible on its own.
That interval is the two-way cluster bootstrap run to
convergence, at $1.2\times10^{8}$ draws against the $3000$ each cell of the map draws. Its
$\pm0.02$ of Monte-Carlo spread at $3000$ is this cell's own and does not carry to the others:
measured per row set over $200$ seeds, the spread of an endpoint any seed moves runs $0.0009$ to
$0.223$, and $42$ of the $46$ endpoints drawn at $3000$ stood inside four standard errors of the
boundary their second decimal turns on. The other four are not the ones that converged: they are
two cells with one cluster on one of their two axes, so the resample is one-way over three
clusters, about ten distinct compositions exist and no seed moves them at all. Each interval in
the map therefore prints at the precision its own
draw count determines, and the eleven that neither two decimals nor one would reach carry their
Monte-Carlo spread beside them (note~\emph{b} of Table~\ref{tab:map-full}). That rule settles the
Monte-Carlo question and not the sampling one, and the two are different: the second decimal of
$[+1.25,+2.87]$ is a property of the draw count, while the interval's solvent axis resamples the
same four molecules the deletions above delete, so how far the endpoints would travel over a
different four glycols is not a question this bootstrap or any other on these rows can answer. The
deletion spread is the honest width on that axis. It is not an
artifact of one solute family: the $74$ rows
left when its alkane solutes are dropped keep the sign at $+1.40$ residual-only and $+1.69$ full, under
all three deletions. Nor is it a property of the estimator cell. Table~\ref{tab:grid-rep}'s
bin-count grid is the analyst choice with the largest leverage here, taking the aggregate from
$+0.01$ to $+0.96$. Swept over it, the stratum is positive in all
$56$ row-unit cells and all $40$ the pair unit defines, the residual-only unbiased column running
$+1.06$ to $+2.19$, and the deletion test passes at every one of the $36$ cells where it can be run
(Table~\ref{tab:grid-glycol}). The occupancy rule that fixed the eight-bin cell would ask for $23$
bins at $n{=}182$, so the cell the map reads is the coarser and the lower of the two. That sweep
varies the binning bound's own settings and not the family of estimator. A pre-declared cross-fitted
regressor, tested after this analysis was fixed, returns a \emph{looser} bound: it moves this
stratum to $+1.76$ $[+1.02,+2.49]$---one $3000$-draw replicate, whose second decimal is seed and
not estimator (\S\ref{sec:si-crossfit})---leaving it the one admissible positive row set but with fewer
cells around it, and moves the chemistry-blind null the \emph{wrong} way. That figure is at the
residual-only convention and row unit the $+2.04$ is read at. Under the declaration's conservative
fold variant, grouped on the source publication rather than the molecule pair, no admissible
positive row set survives at all: this stratum's cross-fitted bound there is $4.92$ against an
$\mathrm{MSE}$ of $2.25$, for a margin of $-7.58$, on $15$ source groups split five ways. That
bound exceeds the stratum's own total error, which $\Binsuf$ itself cannot do, an upper bound on it being free to be arbitrarily loose, and the
regressor behind it scores $R^2=-0.60$ out of fold on those rows: the estimator failed at that fold
count, and the margin is uninformative there rather than a reversal---the same reading
\S\ref{sec:pka-main} gives its own negative floor.
That null is re-run on both sides for
that comparison, on the $473$ rows the cross-fitted estimator scores rather than the $477$ the map
is built on, so its baseline column is not the $10.1\%$ and $17\%$ quoted below. Against a
binning-bound baseline of $0.094$ on those rows the cross-fit gives $p(D)=0.137$, and the share of
certifying draws that reach the observed maximum goes from $16\%$ to $54\%$---the difference between
a margin at the ninetieth percentile of its own search null and one near the median of it. The
declaration required the cross-fitted estimator to be adopted \textsc{unconditionally} if it passed
a validity gate that reads no margin, and it passed; every margin printed in this paper is
nevertheless the binning bound's. That deviation runs in this paper's favour, and it is a choice made
after the two bounds were compared, which is what a declaration exists to prevent. The reason is
that the rule was written against one hazard---taking the smaller bound cell by cell once the cells
are seen, which is biased low---and did not anticipate the other, that one valid bound would be
uniformly \emph{looser} than the other. Obeying the rule there prints a bound that is valid and
strictly less informative: every claim weakens and none becomes more correct. What limits the
deviation is that the number it was made after is a single global comparison of two bounds on one
row set and not a per-cell selection, and that the poorer cross-fitted map is printed cell by cell
in \S\ref{par:crossfit-deviation}, where the declared rule can be held against it. What the sweep establishes is therefore robustness within
the binning bound, not across estimators.

% ===========================================================================================
% THE MAP, PROMOTED INTO THE ARTICLE 2026-08-11 (referee round 6, all three referees).  The
% instrument's output was legible in the article only as the three-bar strip of Fig. 1b, and a
% referee could not check "no other stratum separates" without the Supporting Information.  These
% are the FIFTEEN strata the fixed estimator can bound -- the only rows the admissibility rule can
% act on -- on both axes and at every granularity.  The two bodies are GENERATED by
% scripts/analysis/make_map_table_tex.py from results/b_insuff/admissibility_table.csv, the same
% generator and the same cell as the SI's five map tables; the 44 unboundable strata stay there.
% Do not edit the bodies here.  If a granularity is added to the map, it lands in both places in
% the same pass.
% ===========================================================================================
\begin{table*}[tp]
\centering
\caption[The map: every stratum the estimator can bound]{\textbf{The map: every stratum the fixed estimator can bound}---the $15$ of the $59$ with
$n\ge40$, at the pre-declared cell (eight equal-count bins of $g(\zstar)$, unbiased within-bin
variance, row unit, residual-only convention), applied identically to all of them. \emph{full} is
the same cell under the full combinatorial convention. Every margin, and the $\mathrm{MSE}$ and
bound behind it, is in squared \lngi. The last column states what the row shows or the test it
fails. The other $44$ strata, the $24$ of them carrying no estimate at all included, are in
\S\ref{sec:si-map} at this same cell.}
\label{tab:map-article}
{\scriptsize\linespread{0.96}\selectfont
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{0.7}
\input{si_tables/stratified_map_rows_article}
\tabnotes{\scriptsize%
\tnk{a}\,Number of source publications, then the largest one and its share of that stratum's
squared error. The keys: \input{si_tables/map_source_key}\quad
\tnk{b}\,\input{si_tables/map_ci_note}\quad
\tnk{c}\,In how many of the four unit\,$\times$\,convention cells the stratum is both boundable and
sign-stable under deletion of each contributing publication; the rule of \S\ref{sec:estimator}
requires all four, which six of the $59$ satisfy---three distinct row sets.\quad
\tnk{d}\,The condition the row is in, not a label for it. A one-source cell cannot be tested and a
deletion leaving fewer than $16$ rows leaves the cell undefined, so both read as untested rather
than as passes. A margin is one-sided: positive puts $\Bclos>\Binsuf$ on those rows at that cell,
non-positive is a failure to separate and licenses nothing.\quad
\tnk{e}\,The search behind every row, in the two nulls of the paragraph below. Under the binning
bound of record, a chemistry-blind relabelling of the same molecules certifies at least one row set
in $58\%$ of $2000$ draws, reaches $+2.04$ or more in $10.1\%$ of all draws and in $17\%$ of the
draws that certify anything, and separates the six admissible strata from chance ($p=0.01$) but not
the two admissible positive row sets ($p=0.286$). Under the pre-declared cross-fitted estimator, on
the $473$ rows it scores, the same relabelling reaches the observed maximum in $13.7\%$ of all draws
and in $54\%$ of the certifying ones, against $9.4\%$ and $16\%$ for the binning bound on those same
rows.}}
\end{table*}

% The solute half of the same fifteen.  Two floats and not one: stacked, the two tabulars and the
% note block run 201pt past the page.  The notes are Table~\ref{tab:map-article}'s and are not
% repeated, which is the arrangement the five SI map tables already use.
\begin{table*}[tp]
\centering
\caption[The map, solute axis: the boundable strata]{\textbf{The map, solute axis}---the rest of the fifteen boundable strata, at the same
estimator cell and under the same rule as Table~\ref{tab:map-article}, whose notes apply here
unchanged. The alkane row is the second admissible positive row set, and clause~(c) demotes it to
the glycol ethers.}
\label{tab:map-article-solute}
{\scriptsize\linespread{0.96}\selectfont
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{0.7}
\input{si_tables/stratified_map_rows_article_solute}}
\end{table*}

What the row set does not have is a multiplicity null it clears, and this belongs beside the
finding rather than in a limitation. It is by construction the survivor of a $59$-fold search.
The null relabels the chemistry blindly: each solvent and solute keeps
its rows and takes another's class labels, and the stratum family is rebuilt and put through the
same rule. Over $2000$ such relabellings of the same molecules, on all $477$ rows and at the
estimator cell the map deploys, the map's six admissible strata are above chance ($p=0.01$) and its
two admissible positive row sets are not ($p=0.286$). Such a relabelling certifies at least one row
set in $58\%$ of draws.
The size is the sharper statistic, and it is not sharp enough: a certified margin of $+2.04$ or
more arises in $10.1\%$ of all draws, which is $17\%$ of the draws that certify anything, against a
median of $+1.57$ among those. The separation is what the instrument licenses on the rows it was
computed on; it is not established as a chemistry-specific effect against the search that found it.
% CORRECTED 2026-08-10.  This sentence said the null's two biases "run against each other", which
% \S\ref{sec:si-null} measures and denies: bias (1) is the one with two halves running in opposite
% directions and left un-netted, while bias (2) runs in a single direction and inflates the 58%
% rather than the 10.1%.  The reading the frequencies carry -- unsigned, a bound in neither
% direction -- is unchanged and is what the first clause states.  The pointer also moved from the
% whole of \S\ref{sec:si-tables} to the subsection that measures them.
% AND THE TWO BIASES ARE NOW PRINTED HERE, later the same day (referee item: the Supporting
% Information's own preamble names them as a quantity an article sentence rests on and does not
% itself print, and all three referees returned "the SI is not optional").  The sentence above
% asserted that they were "measured rather than argued" and then printed neither measurement, so a
% reader of the article alone could not tell which way either bias pushes -- which is exactly what
% decides how the 58% and the 10.1% may be read.  Both are here now, each in its own period, with
% the four figures bias (1) is measured by and the direction bias (2) runs in.  What stays in
% \S\ref{sec:si-null} is the draw-by-draw record and the reason the exactly matched null cannot be
% built.  Values: sections/SI.tex, "Two biases in the null".  Do not take the numbers back out and
% leave the pointer; that is the defect this repairs.
Each $p$ here is a frequency and not a bound in either direction, and the reason is two biases in
the null that are measured rather than argued. The first is that provenance survives the
permutation, publications measuring related sets of solvents: a label-permuted map has \emph{more}
boundable cells than the chemical one ($62$ against $50$, median over draws) and each passes the
deletion test \emph{less} often ($0.56$ against $0.70$). Those two halves run in opposite directions
and are not netted, so the $10.1\%$ in particular cannot be read as conservative. The second runs one
way. The set taken whole has a positive margin at this cell, so a randomly assembled stratum
inherits a positive margin on average, and a null frequency near one half for certifying something
is expected rather than evidence that the rule is loose; that bias inflates the $58\%$ and not the
$10.1\%$. The exactly matched null would permute chemistry and provenance jointly, which these data
do not allow, the two being confounded in them (\S\ref{sec:si-null}).

% OUT-OF-SAMPLE TEST, 2026-08-05.  A selection objection is settled by rows the search never saw,
% not by argument, so one test was declared in full -- row set, cell, rule, and what would count
% as confirmation in each direction -- and then run once.  It returned NOT TESTABLE, the third of
% the three verdicts the pre-declaration lists, which it counts as non-confirmation.
% THE VERB, CORRECTED 2026-08-05 (round 3, three referees, must-fix).  This comment used to say
% "do not soften did not confirm into could not be attempted: the test ran".  The script running
% is not the hypothesis being tested.  What happened is that the cell the finding is about has
% ZERO rows out of sample, so no test of it was possible on any public data; "did not confirm"
% credits the design with a replication attempt it never got to make.  The paragraph now says
% the chemistry could not be tested, and says that the emptiness was readable from the row
% inventory that fixed the design, before any margin existed -- so what the pre-registration
% delivered on this cell is the unavailability of confirming data, not a failed replication.
% Do not restore "did not confirm" here or anywhere (abstract, Table 4, limitation (vi),
% Conclusions, Sec. S3.1).  Do not promote the +0.35: the pre-declaration says an
% inadmissible row set licenses nothing, and that rule is the one governing the whole map.
% Artifact: results/b_insuff/glycol_ether_out_of_sample.json, from
% scripts/analysis/run_glycol_ether_out_of_sample.py, whose docstring is the pre-declaration.
% COMPRESSED 2026-08-07 (page-reduction pass).  The full account -- the two rejected candidate
% row sets, the four things fixed in advance and the three verdicts, the 88 non-aqueous solvent
% molecules, the nearest cell's 52 rows and its 50--2 draw, the +0.35 it licenses nothing with,
% the ethylene-glycol overlap and the ten-class ranking -- was ALREADY printed in full at
% \S\ref{sec:si-tables}, so nothing moved: this paragraph shortened to what a reader must have
% here.  WHAT MUST STAY: that the test ran on rows the search never saw, that the design was
% fixed before any margin, the verdict NOT TESTABLE with its status as non-confirmation, that it
% follows from the row inventory and is therefore not a failed replication, that the nearest cell
% is inadmissible under the same rule, and the two-sided out-of-sample reading.  Do not restore
% "did not confirm" here or anywhere.  Do not promote the +0.35.
What settles a selection objection is the same measurement on rows the search never saw, so we ran
one. The out-of-sample set is the PGL 6th-edition IDAC database scored by the same deployed closure
on VT-2005 profiles (Table~\ref{tab:closure-anchor}): another group's compilation, sixteen times the
broad set's rows on a different profile database. The row set, the estimator cell, the admissibility
rule and what would count as confirmation in each direction were written down before any margin was
computed (\S\ref{sec:si-tables}). \textbf{The chemistry could not be tested there}, for a reason
about the data rather than about the number: the cell the finding is about---a glycol-ether solvent
with an \emph{organic} solute---has no rows there. \emph{Not testable} is the third verdict the
pre-declaration lists and counts as non-confirmation, and it follows from the row inventory alone:
what the pre-registration delivers here is the absence of confirming data and not a failed
replication. The nearest cell that database does carry is water in glycol-ether and polyol solvents,
and it is inadmissible under the same rule that governs the map---the rule refuses it out of sample
for the reason it refuses water-as-solute in sample---so its margin licenses nothing and the sign is
never put at risk. Over that database's ten solvent classes, stated as description and not as a test
(Table~\ref{tab:oos-map}), no class both passes the rule and separates the two terms, and every
margin above the one admitted positive class sits on a class the rule refuses. Out of sample the
glycol ethers are neither where this closure fails hardest nor a cell the rule can read. The
chemical localisation is therefore what the instrument licenses on the rows it was computed on, and
the title does not carry it.

It is also the whole of the separation this paper reports. The phrase ``COSMO-SAC error exceeds
input error'' would carry it past five boundaries, and each of them is a measurement here.
No other stratum of the map returns a separation (below). Neither data set taken whole does: the
aggregate is retired above, and the VT-2005-matched set's $+0.05$ does not stand
(\S\ref{sec:vt2005}). Nothing on the solubility axis does, where the two terms
do not separate at all and where the substitution of \S\ref{sec:paradox} was measured. The error
that stands above the bound is COSMO-SAC's \emph{as deployed}, the composite of map and computed
conformer that the pipeline runs on rather than the kernel (\S\ref{sec:badclosure}). And the margin
is one its own chemistry-blind null returns at the ninetieth
percentile, on the one chemistry no out-of-sample rows exist for.

\emph{Which chemistry does not separate.} Three of the six admissible cells are that same row set at
three granularities. The remaining three are two row sets and neither establishes anything: the
alkane solutes are admissible and positive and clause~(c) demotes them to the glycol ethers, which
hold more rows and hold most of theirs, while the acceptor-only aprotics pass the rule and license
nothing, the instrument being one-sided. That is the whole of the uniqueness claimed for the glycol
ethers: the only row set admissible, positive and left standing by clause~(c)---\emph{not} the only
admissible stratum and not the only one sign-stable under every deletion. Which cell each of the
remaining strata fails on, and by how much---too few rows to bound, one publication so that the
deletion cannot be run, or a sign lost under a deletion---is in \S\ref{sec:si-map}, and none may be
used to explain a reversal. The whole-set row passes at the fixed estimator cell and fails once the pair
unit is required, so the rule does not by itself dispose of the aggregate there; the composition
argument above does.

% COMPRESSED 2026-08-07 (page-reduction pass).  The per-class bounds and errors, the mono-alcohol
% cell flagged as an invalid bound on an already-excluded stratum, and the two orderings' details
% are in \S\ref{sec:si-map} and the map tables, every stratum with its error, bound and interval.
% WHAT MUST STAY: the ratio statistics (they are a row of Table~\ref{tab:claims}), the null they are
% read against, the estimator-geometry REASON -- without it "why a map" is asserted, not shown --
% and the closing clause that the bound stays valid in each stratum, which is what stops this
% paragraph from reading as a retraction of the instrument.
\emph{Why the output is a map.} $\Binsuf^{\mathrm{up}}$ is nearly stratum-independent and
$\mathrm{MSE}$ is not. Across the three classes the design can bound---glycol ether, water and the
acceptor-only aprotics---the bound spans a factor of $2.0$ against errors spanning a factor of $25$.
Adding the three that carry an estimate without being boundable gives $6.8$ against $51$; that $6.8$
is set by a mono-alcohol figure the artifact flags as an invalid bound on a stratum already
excluded. The two orderings are unrelated, the glycol ethers carrying the lowest bound
and the second-highest error among those six. Against $2000$ random partitions of the broad set's
$477$ rows
into blocks of the observed sizes of its nine solvent
classes, at the same cell, the chemical partition's range (largest minus smallest) in
$\mathrm{MSE}$ exceeds every draw while its range in the bound is unremarkable ($p=0.26$); their
ratio gives $p=0.02$. Every stratum's figures are in \S\ref{sec:si-map}. That is the
estimator showing through: every stratum is summarised by eight quantile bins of one scalar, so the
bound measures the residual spread of $m$ inside such a bin, a property of the binning rather than of
the chemistry, while nothing constrains the error to follow it. An aggregate margin is therefore a
composition-weighted average of a term free to move $25$-fold offset against one that barely moves.
It will report a binding closure for any set whose composition happens to include a chemistry the
closure fails on---a statement about the aggregate, not about the bound, which stays valid in each
stratum where it is computed.

\subsubsection{The VT-2005-matched set ($n{=}60$)}
\label{sec:vt2005}
% COMPRESSED 2026-08-07 (page-reduction pass).  The full account of why this set is weak -- the
% dim/n ratio, methanol's 25 of 60 rows and the two leverage pairs' 34% share, the six of nine
% solvent classes none reaching n=40, the four separate ways the +0.05 fails, and the label-systematic
% argument -- moved to \S\ref{sec:si-tables}.  WHAT MUST STAY, because the five boundaries of
% \S\ref{sec:broad} name this set: that its aggregate margin DOES NOT STAND, that it supports no
% stratified bound and no stratum of it is stated as a finding, and that its 57 shared pairs make it
% not an independent sample from the broad set -- which is what makes the two sets' agreement a
% consistency check rather than corroboration.
This set carries the paper's other reference-matched analyses, and it is far weaker. All $60$ rows
sit at $298$\,K, so $\zstar$ is the profile pair alone and $\dim(\zstar)/n\approx102/60$ leaves the
split behind one-sided bounds; it is strongly unbalanced, methanol occupying $25$ of the $60$ rows;
and its $17$ solvents fall in six of the nine classes, none reaching the $n{=}40$ a bound needs, so
it supports no stratified bound and no stratum of it is stated as a finding. Its aggregate margin is
$+0.05$, and it does not stand: it fails under deletion of the two leverage pairs, under dropping
methanol, under blocked out-of-fold estimators, and under $11$ of the $60$ single-pair deletions,
where on the broad set no single-pair deletion moves the sign at all (\S\ref{sec:si-tables}).
Zero-mean label noise can understate a separation but not create it (\S\ref{sec:decomp}); what a
margin here would bound is a label systematic tracking $\zstar$, and that is \emph{not excluded} under
the unbiased within-bin variance (\S\ref{sec:si-tables}). Of the
$60$ pairs, $57$ also appear in the broad IDAC set at $298$\,K under UD profiles, so the two are not
independent samples---this is the sub-design VT-2005, and therefore the paradox's own oracle, is
defined on, and the agreement between the two sets' error orderings is a consistency check rather
than corroboration.

\subsubsection{Whether a better closure moves the ceiling, and where this one fails}
\label{sec:fidelity}
\label{sec:badclosure}
% 2026-08-07 FOLD.  These were two subsubsections (the incumbent 3.2.3 and 3.2.4).  The
% phenomenon-first restructure folds them into one closing block and moves the detail to
% \S\ref{sec:si-fidelity}: the input-fixed control and its three limits, the convention and
% dispersion arms, the block split's third block, the floor checks under the typed latent, the
% published-evaluation split of the donor reading, and the Staverman--Guggenheim counter-test.
% BOTH LABELS ARE KEPT HERE on purpose -- \S\ref{sec:fidelity} and \S\ref{sec:badclosure} are cited
% from Table~\ref{tab:claims}, Table~\ref{tab:notation}, the head of \S\ref{sec:results} and six
% places in the Supporting Information, and all of them still mean this block.
If the ceiling is the closure, the direct remedy is a better closure. The standard $2002\to2010$
step delivers one on the VT-2005-matched set and not on the broad IDAC set, and a block split
identifies the factor that carries that reversal. On the smaller set the step more than halves the
error under the full convention ($P>0.95$), the convention under which the two kernels' published
error figures are reported, and cuts it $42\%$ under the residual-only one. On the broad IDAC set
($477$) none of its three cells establishes an improvement, and none measures an equality either.
The step scores
$-0.99$ at $P\approx0.2$ under the residual-only convention and $-0.07$ at $P\approx0.6$ under the full
one; with the dispersion term on it scores $+0.06$ at $P\approx0.6$ under the full convention, a point
estimate on the improving side and no better resolved than the two that are not. The broad set leaves
the sign open in either direction and does not measure it at zero.

The reversal is the \emph{chemistry} and neither the profile database
nor the temperature range. The block split that says so runs with UD profiles, one extraction and
one convention throughout. The \emph{2010, dispersion off} arm wins on the $57$ pairs shared with
the smaller set ($1.78\to0.77$ at $P>0.95$, full convention); it does not win
on the complementary $420$; and it still does not win on the $80$ complementary rows that are
\emph{also} at $298$\,K. That third block is the one the temperature half of the claim rests on and
the weakest of the three, at $P\approx0.3$ and with no interval, and the three blocks were never
tested against one another. The $57$ shared pairs are broad-set rows as well and therefore not an
independent sample of that set. That block split is the whole of the evidence for the reversal being
chemical. We claim only that \textbf{the fidelity lever we can demonstrate is confined to the smaller
set's chemistry}, and that null is additionally a small-molecule and glycol-heavy one, so on
drug-like solids the lever is untested rather than absent.

Under the 2010 kernel's own typed latent,
$306$-dimensional and so several times worse resolved, the floor checks return two aggregates of
opposite sign. They are $+1.29$ on the broad IDAC set ($P>0.95$, ${\approx}0.9$, ${\approx}0.9$ over
pair, two-way and source clusters) against $-0.77$ at $P\approx0.3$ on the VT-2005-matched set, and
only the first of them stands. That is loss of resolution and not a demonstrated flip to the inputs,
a flip needing a \emph{lower} bound on $\Binsuf$. The
input-fixed control and its three limits, the three arms on both sets under both conventions
(Table~\ref{tab:kernels}), and the third block at $298$\,K are in \S\ref{sec:si-fidelity}.

% COMPRESSED 2026-08-07 (page-reduction pass).  The kernel-residual negative in full -- the
% random-fold 46+-5%, why only the grouped design is free of leakage, what the +- prices and what
% it does not, and the reading of B_clos as an error absorbable within a solvent and not shown
% correctable across them -- moved to \S\ref{sec:si-fidelity}.  WHAT MUST STAY: that the ceiling is
% in the map AS DEPLOYED and not the kernel, that this section does not say which half of the
% composite carries the error, that dominance does not say the composite is fixably wrong, and that
% the grouped-fold result is a NULL and not a bound.  A negative quoted without the last clause
% reads as a measured zero.
Where $\Bclos$ dominates---on this set, the one row set the rule leaves standing---the ceiling is in
the map \emph{as it is deployed}: the fixed kernel together with the one computed conformer it
reads, which is the composite $\Bclos$ is the error of (\S\ref{sec:decomp}) and the object the
pipeline runs on. Which of the two carries the error is a question this section does not answer; nor
does dominance say the composite is \emph{fixably} wrong. A small structure-preserving residual was
fitted on the fixed COSMO-SAC exchange-energy kernel and five-fold cross-validated on the $60$ pairs
of the VT-2005-matched set, on their reference VT-2005 profiles. It removes
$46\pm5\%$ of the reference-input error under random folds but $-5\pm16\%$ once folds are grouped so
that no solvent is shared. Only the grouped design is free of leakage, the closure error being
predominantly a between-solvent systematic. It gives a null and not a bound: the $\pm$ prices only
optimiser noise over five fit seeds, the held-out fold spread that dominates it going unpropagated
(\S\ref{sec:si-tables}). $\Bclos$ is therefore an error a handful of kernel parameters can absorb
\emph{within} a solvent and that we have not shown to be correctable across solvents.
Where it fails is chemically placed, and placed differently on the two sets. On the VT-2005-matched
set $90\%$ of the squared error falls on strong hydrogen-bond-donor solvents and the closure is
near-exact on acceptor-only ones---the chemistry COSMO-SAC's documented single-parameter H-bond term
governs, the term the donor/acceptor-typed 2010 closure was built to
replace \cite{hsieh2010,fingerhut2017}. That the $2002$ kernel fails on hydroxylic and aqueous
systems is established, and the localisation restates it rather than discovering it; what is new is
that on one such chemistry the failure is bounded against what the closure's inputs cannot resolve.
Half of that reading
carries to the published evaluation of Table~\ref{tab:closure-anchor} and half does not, and on the
broad set neither it nor its replacement reproduces, so we state neither
(\S\ref{sec:si-fidelity}). It places the error in a \emph{chemistry} and not in one component of the
composite: the reference profile for each of those solvents is one gas-phase conformer too, and
neither set separates a displaced reference from the kernel. One part of that is now bounded rather
than open. Replacing the four glycol-ether solvent references with Boltzmann conformer ensembles
computed at BP86/def2-TZVP with CPCM moves the stratum's margin from $+2.04$ to $+2.27$, $90\%$
interval $[+1.36,+3.29]$, and an equal-norm random displacement leaves $+1.68$---so the separation
is not an artefact of those references' conformations, though displacing the solutes by the same
norm moves it as much. Reference uncertainty at large is not bounded by that, and its scale is
measured: two defensible calculations of the same rigid molecule's profile differ by a median
relative-$L_2$ of $0.282$, four to eight times the conformational displacement, and substituting a
recomputed lowest conformer for the tabulated one moves this stratum's margin by $+5.43$ against the
ensemble's $+0.23$. Every substitution reported in this paper is therefore scoped to the VT-2005
tabulation's level of theory (\S\ref{sec:si-fidelity}).

% COMPRESSED 2026-08-07 (page-reduction pass).  The exposure arithmetic in full -- the single
% reference conformer and protonation state, the successor Delaware database revising about a third
% of the conformations, the curvature route by which a displaced reference loads onto B_clos even
% where g is exact, the ten-fold reduction the glycol-ether stratum's margin would need, and what
% the arithmetic does and does not bound -- moved to \S\ref{sec:si-fidelity}.  WHAT MUST STAY: that
% the low fidelity is the contribution and not a qualification, the consequence for the latent, that
% outside the one row set the map reports cells it cannot resolve rather than sound ones, and the
% fact that the exposure is SIZED and not BOUNDED.  Do not let the last clause go: without it the
% factor of 2.6 reads as a share of the error.
That COSMO-SAC-2002 is a low-fidelity closure is the contribution rather than a qualification of the
result: it is what a practitioner adopts by default, a closure's fidelity is \emph{not knowable in
advance} on a new system, and the field routinely composes an expressive encoder with a fixed
mechanistic map and assumes the map is good enough. What follows when it is not is that the learned
latent silently departs from the quantity it is named after (\S\ref{sec:surrogate}), invisibly
without an external oracle. Outside that one row set the map reports cells it cannot resolve, not
cells in which the closure is sound. One arithmetic sizes that exposure, and it is worth stating
because the ground truth charging $\Bclos$ is quantum chemistry and not experiment. One closure read
on two profile databases---HF-TZVP against BP-TZVPD-FINE---differs by a factor of $2.6$ in AAD
(Table~\ref{tab:closure-anchor}). The glycol-ether stratum it is set against is computed on the UD
profiles the broad set is matched to, and its margin closes only under a more than ten-fold
reduction in its squared error; a profile source better than UD by the whole of that published
bracket would leave that sign standing. The two sides therefore name different databases, and the bracket enters
as the size of a database-to-database effect rather than as a comparison either side is on.
The arithmetic holds the insufficiency bound fixed, which a new profile
source would not, so it sizes the exposure and does not bound the share; the working, and the route
by which a displaced reference loads onto $\Bclos$ through the curvature of $g$ even where $g$ is
exactly specified, are in \S\ref{sec:si-fidelity} and \S\ref{sec:si-repro}.
\subsection{The same split on a second closure: pKa through a fixed Hammett relation}
\label{sec:pka-main}
The instrument is not specific to COSMO-SAC, and a second domain runs it where the sign of grounding
can be predicted before the measurement. The closure is a Hammett linear free-energy relationship
$g(\zstar)=\mathrm pK_{a,0}-\rho\,\hammett^\star$ in a substituent constant, with $\mathrm pK_{a,0}$ and
$\rho$ the tabulated Hansch--Leo--Taft reaction-series constants, one literature pair per
scaffold---$(4.20,1.00)$ benzoic acid, $(9.99,2.23)$ phenol, $(4.60,2.77)$ anilinium,
$(5.25,5.90)$ pyridinium---never fitted or refit on these data, so $g$ is fixed in the sense
Lemma~\ref{prop:decomp} requires and the same $g$ runs inside every split. It reads two things, so
$\zstar$ is the pair $(s,\hammett^\star)$: the series $s$, which selects that literature pair, and
the substituent constant at the tabulated value the rule below assigns. Only the constant is
learned---the learned intermediate $\hat\sigma_{\mathrm H}$ is
that constant predicted from structure; the external reference is the Hansch--Leo table
\cite{hansch1991survey}, printed in full in \S\ref{sec:si-pka}; and the data
are the OPERA experimental compilation \cite{mansouri2019opera} under a uniform,
closure-independent quality filter that drops pre-ionized microstates.

% THE ASSIGNMENT RULE MOVED TO \S\ref{sec:si-pka} ON 2026-08-07 (page-reduction pass), verbatim
% and entire.  WHAT HAD TO STAY IS THE DEFINITION OF THE TWO POLES: the result of this section is a
% sign that reverses between them, so a reader who never opens the Supporting Information must
% still know what makes a record clean and what makes it degraded, what the zeroed positions carry
% -- which is also the a-priori channel assignment the next paragraph turns on -- and that the
% split is not benzenoid against heteroaromatic.  Those are below.  What left is the mechanics: the
% fixed trial order, the ring-unambiguity conditions, the ortho/meta/para typing by ring-bond
% distance, and the nineteen-substituent list.
The reference constant is assigned mechanically, by a rule that reads structure alone and never the
residual (\S\ref{sec:si-pka}), and that rule is also what defines the two poles. A record enters
under one of the four reaction series---benzoic acid, phenol, anilinium, pyridinium---which fixes
its $(\mathrm pK_{a,0},\rho)$; $\hammett^\star$ is then the sum, over the substituted ring positions,
of the Hansch--Leo constant of that substituent \emph{at that position}, $\sigma_m$ meta and
$\sigma_p$ para, taken from a fixed list of nineteen. An ortho position carries no tabulated
constant and enters the sum as zero, and so does a substituent outside the list. The first
pole---the \emph{clean} one below---is the $158$ records whose every substituted position is meta or
para with a tabulated substituent; the \emph{degraded} pole is the $846$ where at least one position
is neither---$571$ of them carrying an ortho substituent, the other $275$ an untabulated one---and
there $\hammett^\star$ is the partial sum over the positions it can see. The split is not benzenoid
against heteroaromatic: all four series occur on both sides, $32$ of the clean pole's records being
pyridinium.

The two classic failure modes of that relation fall onto the two channels of
Lemma~\ref{prop:decomp}: \emph{resonance saturation}, a curvature the linear closure cannot
represent, lands in $\Bclos$, while what the zeroed positions carry---ortho field and steric effects,
and whatever an untabulated substituent contributes---is information the intermediate lacks and lands
in $\Binsuf$. The two poles differ accordingly. On the clean one, the chemistry the relation was
built for ($n{=}158$), the closure predicts to RMSE $0.55$, and the two available bounds on $\Bclos$
disagree about what that leaves. The binning-free floor $\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ is
$-0.2$ ($95\%$~CI $[-0.4,-0.0]$), and it establishes nothing about this closure: the out-of-fold
$\Binsuf^{\mathrm{up}}$ is $0.500$ against an $\mathrm{MSE}$ of $0.302$, so that difference is
negative whatever the closure, and what those $158$ rows across four series show is that a flexible
regressor on the closure's own argument predicts worse than the closure does. The
Lemma~\ref{prop:decomp}(b) bound is informative and runs the other way. The per-scaffold systematic
offsets reach $0.429$ in $\mathrm pK_a$ on phenol ($+0.303$ anilinium, $+0.034$ benzoic acid,
$-0.198$ pyridinium), and the $n$-weighted mean of their squares bounds $\Bclos$ below at $0.083$,
$27\%$ of the $0.303$ total, and at $0.184$ on phenol alone, half of that series' own $0.371$.
At least a quarter of the
clean pole's error is closure error, so the pole is not shown to be well specified.
The quality filter moves the floor and not that conclusion: the rule is
closure-independent---net formal charge $\neq0$, applied uniformly to both poles, never reading a
residual---but it drops five pre-ionized microstates, and on the $163$ rows before it the same
floor is $+0.55$ rather than $-0.2$, positive and establishing $\Bclos>0$ by the other route as well
(\S\ref{sec:si-pka}).
On the degraded pole ($n{=}846$) it degrades to RMSE $2.26$, of which the out-of-fold
within-series regressor places at
most $4.5$ of the $5.1$ total in input insufficiency and hence at least $0.6$ in the closure---both
figures one-sided, in the same direction as the solubility bounds. That pole's RMSE is essentially
unchanged by the filter, so the filter does not manufacture the contrast between the two poles; what
it decides is the sign of the clean pole's floor.

% THE ESTIMATOR'S CONSTRUCTION AND ITS VALIDATION MOVED TO \S\ref{sec:si-pka} ON 2026-08-07
% (page-reduction pass), verbatim: the out-of-fold identity, the fold-design argument against
% \S\ref{sec:broad}'s refusal of fitted estimators, and the pooled-series counterfactual.  WHAT HAD
% TO STAY are the three things that scope the two numbers just printed -- that this is NOT the
% eight-bin cell of \S\ref{sec:estimator}, that the functional is a FLOOR and not the separation
% margin, and that conditioning on the series is what the lemma demands and is not the conservative
% choice.  Drop any of the three and the 0.6 reads as a margin measured with the solubility cell.
% THE FOLD DESIGN CAME BACK 2026-08-10, and it is the fourth thing that scopes the 0.6.  This
% paragraph used to end by DEFERRING it -- "the fold design, and why it escapes the objection
% \S\ref{sec:broad} raises against fitted estimators on solubility, are in \S\ref{sec:si-pka}" --
% which told the reader that the figure they had just been given rests on an argument printed
% somewhere else.  The floor is a floor only if no fold hands a held-out row what the closure's
% argument does not already carry, so that argument is in this paragraph now: five folds within each
% series, pooled over the series by n, and the reason the leakage \S\ref{sec:broad} refuses cannot
% arise on a regressor whose only input is the constant within a series.  \S\ref{sec:si-pka} keeps
% the out-of-fold identity in symbols and the pooled-series counterfactual's working; do not put
% the fold design back behind that pointer.
That estimator is not the eight-bin estimator cell of \S\ref{sec:estimator}, and the functional is
not the same either: the two figures just given are floors $\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ on
$\Bclos$, which when positive establish $\Bclos>0$, and not the separation margin
$\mathrm{MSE}-2\Binsuf^{\mathrm{up}}$ of Table~\ref{tab:notation}, which when positive would
establish the stronger $\Bclos>\Binsuf$. Neither coordinate of $\zstar=(s,\hammett^\star)$ needs
binning---$s$ takes four values and $\hammett^\star$ is a scalar---so the conditional variance is
estimated directly, as the \emph{out-of-fold} residual mean square of a random forest of
$\mathrm pK_a$ on $\hammett^\star$ fitted \emph{within} each series, five-fold and pooled over the
series by $n$, which exceeds $\Binsuf$ by the
regressor's own approximation error and so leaves a lower figure on $\Bclos$. Conditioning on $s$ is
not a free choice and it is not the conservative one: pool the four series and the same estimator
returns $8.3$ against the degraded pole's $\mathrm{MSE}$ of $5.1$, a conditioning under which $g$ is
not measurable, so Lemma~\ref{prop:decomp} does not license it and the closure floor it leaves would
in any case be vacuous. The floor of $0.6$ therefore rests on $s$ belonging in $\zstar$, which is
what the lemma demands of a closure that reads it. Those folds carry no leakage of the kind
\S\ref{sec:broad} refuses a fitted estimator for on solubility, where a held-out row borrows
strength from a near-twin through the representation: here the regressor's only input is
$\hammett^\star$ within a series, so no fold can hand it what the closure's own argument does not
already carry, and the identity asks of the fold design only that a row be held out of the fit that
predicts it.

The decisive test is the same oracle swap, measured \emph{in-distribution} across $K{=}20$
within-scaffold splits: the learned $\hat\sigma_{\mathrm H}$ replaced by the tabulated
$\sigma_{\mathrm H}^\star$ in the same closure---the series label is read from the structure in both
arms, so the swap moves one coordinate of $\zstar$---the sign of the change recorded per stratum. It
flips. On the clean pole ($n{=}158$) the reference \emph{improves} accuracy---MAE $0.48$ learned against $0.315$
with the reference, a gap of $-0.165$ at a $90\%$ CI of $[-0.28,-0.04]$, better in $19$ of $20$
splits and in three of the four reaction series---so the
ceiling there is the noisy learned estimate and not the closure, which is an internal positive
control on real data. It is a control on the swap's sign and not on the closure's specification: the
Jensen bound puts at least a quarter of that pole's error in the closure, so the reference wins there
against a closure carrying error of its own.
On the degraded pole ($n{=}846$) the same swap \emph{degrades} it---$0.83$ against $1.62$,
worse in $20$ of $20$ splits and in all four series, a gap of $+0.79$ at a $90\%$ CI of
$[{+}0.57,{+}0.95]$---the freely
learned constant absorbing what the zeroed positions carry and the tabulated one lacks. The sign of
grounding therefore reverses inside one closure, in the direction the two poles' construction fixes
in advance---which ring positions the Hansch--Leo table can see, complete on one pole and a partial
sum on the other---with Remark~\ref{prop:tax} supplying the vocabulary and not the prediction.
% ITEM 2 OF THE 2026-08-11 REFEREE ROUND.  This sentence rested the degraded pole's attribution on
% "at most 87% in insufficiency", which is an UPPER bound on B_insuff and is equally consistent
% with B_insuff = 0: it fails to refute the attribution and cannot carry it, which is the move
% \S\ref{sec:broad} forbids itself twice.  The attribution is well supported by the flip and by the
% pole's construction, so it rests on those now, and the 87% is demoted to what it is.  Both bounds
% are also NAMED here, the Jensen one and the out-of-fold regressor, which answer at 0.65 and 4.5
% on the same pole.  Do not let "the same estimator" back into either sentence.
That the reversal runs through the insufficiency channel rather than through the closure-error
cancellation of \S\ref{sec:surrogate} is unsupported on the clean side, where the Jensen bound above
leaves at least $27\%$ of the error in the closure. On the degraded side the attribution rests on the
flip itself---the freely learned constant beats the tabulated one there, $0.83$ against $1.62$, in
$20$ of $20$ splits---and on how that pole is built, $\hammett^\star$ being a partial sum over the
positions the table cannot see. What the Jensen bound adds there is that it does not refute that
reading: it leaves at most $87\%$ of the error in insufficiency, and an upper bound is as consistent
with none of it as with all of it. The out-of-fold regressor two paragraphs above is a different
estimator answering the same question on the same pole, at $4.5$ of the $5.1$---and it is an upper
bound too, so the two agree without either of them supporting the attribution.
Both signs are one comparison: the sign of the grounding gap is by
construction that of the learned arm's error against the fidelity-set oracle error, and what a
competence sweep adds is that the oracle side of it does not move with the training budget while the
learned side does. The
clean pole's threshold ($\approx0.315$) is never beaten by the learned arm, so grounding helps at all
four budgets swept, while the degraded pole's threshold ($1.62$) is high enough that the learned
arm beats it across the whole interpolation range, down to $12\%$ of the data, and grounding reverts
to helping only under scaffold \emph{extrapolation}, where the learned constant breaks
($2.6{\pm}2.0$) back above the threshold---in the mean over the six splits, three of which still
have grounding hurting. \textbf{The flip is an in-distribution result and the scaffold protocol does
not certify it.} Re-running the same $K{=}20$ certification on a scaffold split rather than a
stratified one, the reference helps on \emph{both} poles: the clean pole's gap is $-0.61$ and the
degraded pole's $-1.10$, with a series-cluster interval of $[-1.86,+0.37]$ and $P=0.28$ there, so
the degraded pole's ``grounding hurts'' half does not certify and the reversal does not either.
Every solubility result in this paper is measured on a scaffold split, and this is the one place the
protocol changes; what the scaffold run confirms is the mechanism rather than the flip, the learned
constant breaking under extrapolation and rising above the oracle threshold on both poles.
The clustering unit is the Hammett reaction series, of
which only $G{=}4$ exist, so the bootstrap resolves nothing finer than series-level sign agreement,
read as a sign test on four units: $p{=}1/16$ on the degraded pole, where grounding hurts and all
four series agree, and $p\approx0.31$ on the clean one, where three of them do---so that test
carries the degraded pole and not the positive control, which rests on its splits and its gap
instead. The construction,
the estimator validation and the measured fidelity sweep are in \S\ref{sec:pka}.

\subsection{Scope, accuracy, and limitations}
\label{sec:discussion}
The result is a cautionary measurement about one of the two operations the word ``grounding'' names,
and it is two measurements made in different places. On solubility: supervising a learned physical
latent on reference values during training helps, and \emph{supplying} those values at inference
hurts. On infinite-dilution activity data, and there on one class of solvent: the fixed map's own
error stands above the bound on what its inputs fail to carry---the condition under which better
inputs surface a map's bias rather than help, and one the standard route to a better closure did not
clear either. The two are
measured in different regimes on largely disjoint chemistry, no measurement here connects them, and
what would settle it is named in limitation~(i) below and in (x); until it is run, ``the
substitution hurts \emph{because} the map is misspecified'' remains untested. Both sets are scoped by the reference database that defines them
and neither is the deployment regime. The useful form of the finding is one-sided and
narrow: a place where grounding effort is \emph{not} the remedy, on one row set of the map, rather
than an allocation across chemistries.

\input{sections/blackbox-probe}

% THE CONTRIBUTION LIST, REWRITTEN 2026-08-10 against the editorial criterion that every item be an
% assertion carrying no modality and comprehensible without reading the rest of the article.  Three
% items failed the last of those and were unpacked into what they denote, at a cost of about sixty
% words: "the closure--insufficiency split is a law-of-total-variance identity" (the reader is told
% what divides into what before the identity is named), "an identity on a fixed operator, with the
% insufficiency side bounded rather than point-identified" (which side is bounded which way is now
% said in the units of the two error terms, matching the abstract's "from below ... from above"),
% and "an oracle-on-a-fixed-operator affordance the concept-leakage setting lacks" (which is a
% property of the comparison and is now stated as one: nothing tabulated exists to put in a learned
% concept's place).  "Testable \emph{a priori}" became "predicts ... before the substitution is
% performed" for the same reason.
% THE TWO SCOPE CLAUSES IN THE LAST SENTENCE STAY.  They are not modalities on the items above but
% the boundary of the last one: delete them and "the instrument's first application" claims a
% transfer out of the matched regime that no measurement here makes, and the sign reversal claims a
% resolution finer than the four series it is read on.  They also sit inside the Scope-and-
% limitations subsection already, which is where the letter puts a qualification.
\paragraph{The contribution.} The unit of contribution is the measurement, not any single number it
returned. Its two ingredients are individually standard. The error a composed predictor makes when
it is handed reference inputs divides exactly into two parts, what the fixed physical model gets
wrong on those inputs and what the inputs themselves leave undetermined about the measured quantity;
that division is the law of total variance applied to a composition. Whether replacing a learned
input with an accurate one helps or hurts follows from which of the two parts is the larger, which is
the model-discrepancy principle of Kennedy and O'Hagan \cite{kennedy2001,brynjarsdottir2014}.
Combining them gives an instrument. It divides the error of a physical model that is never refitted
to the data, bounds the model's own part from below and the inputs' part from above, and predicts the
sign of grounding's effect before the substitution is made, in both directions. Acid
dissociation supplies that prediction as a sign that reverses inside a single data set, in
distribution: the tabulated
constant helps on the molecules its table describes completely and hurts on the rest. A learned
concept in a concept-bottleneck model admits no such test, because nothing tabulated exists to put in
its place \cite{parisini2025,espinosa2025leakage}.
The solubility results are the instrument's first application. Both scopes are narrow: the sign
reversal is certified on a stratified split and does not certify on a scaffold split, which is the
protocol every solubility result here is measured on, and it
resolves no finer than the four reaction series it is read on; the solubility results' transfer
out of the matched regime is not established.

\paragraph{Where the accuracy stands, and the confound in it.} Table~\ref{tab:baselines} places every
% "three blocks" -> "two", 2026-08-08.  Stale from before the 2026-08-08 cut, when the article's
% copy still carried the as-published block: this work / external / as-published were three that
% may not be read across.  Two of those are left, and the third head in the float is the scale
% reference, which is on block one's own rows and is meant to be read against it -- the next
% sentence but one does exactly that.  The float's caption already said two.
arm of this work beside the external models, in two blocks that may not be read across. The physics
arms trail their own control in the seed mean---the COSMO-SAC arms at every seed, the NRTL arm at
two of three, being ahead at seed $42$ ($1.734$ against $1.749$; Table~\ref{tab:si-arms})---and the
predict-the-mean row shows how little room sits above them.
The physics and control arms were tuned separately and no hyperparameter-matched control was run, so
the global $\Delta\mathrm{MAE}$ of $+0.14$ between them on the $5608$ labelled test rows---from the unrounded means, where
Table~\ref{tab:baselines}'s rounded cells differ by $0.15$---carries no information about the
thermodynamic bottleneck, and neither do the per-solvent-class map, the per-solute-class map, the
novelty inversion or the data-efficiency trend, all of which resolve the \emph{same} difference along
chemical axes. We claim no accuracy result in either direction; those strata are reported in
\S\ref{sec:map} and \S\ref{sec:data-efficiency} as descriptions of where two particular pipelines
differ, and no conclusion rests on them. Running the matched control is the direct repair and we have
not run it. What survives the confound is every \emph{within-checkpoint} contrast, where the two
sides share one trained model and one tuning: \S\ref{sec:paradox}, \S\ref{sec:decision} (the ranking
result included), \S\ref{sec:surrogate} with the output correction folded into it, and
\S\ref{sec:measure}, which uses no trained model at all. Separately, and not as an
accuracy claim about the bottleneck: no configuration we ran attains a lower \emph{seed-mean} error
than the control---at one seed the NRTL arm does---and our one attempt to move the ceiling by grounding the
% ITEM 1 OF THE 2026-08-10 REFEREE ROUND.  This sentence read "tested nothing", which is a null, and
% \S\ref{sec:e2} measures a movement on the only subset that could carry one.  A negative that was
% never established, printed as though it had been, is the failure this project treats as worse than
% a wrong positive, so the claim is now what the measurement supports: the arm is UNDERPOWERED for
% external crystal grounding, and the net-new subset moved.  Do not compress this back to "tested
% nothing" -- the two CIs are the evidence that the word would contradict.
crystal factor on external labels is underpowered rather than null. $97.76\%$ of its pool was melting
points the model was already trained on. Where the pool did carry something new the crystal head
moved: its paired $\Tm$ MAE over the pool itself went $-12.233$~K $[-18.25,-6.27]$ on the $345$
net-new molecules, against $-0.371$~K $[-0.81,+0.05]$ on the $15{,}082$ duplicated ones.
% THE HELD-OUT HALF OF THE SAME ESTIMAND PRINTS HERE.  The sentence above is the POOL side, and it
% is the favourable one; the article printed it alone, so the crystal head's own property appeared
% to have moved one way only.  On the held-out solutes it moves the other way, and the ungrounded
% arm's re-scored per-row file (scripts/analysis/run_e2_held_out_tm_ci.py, whose gate reproduces all
% four scalars of results/e2_with_crystal/e2_comparison.json) gives that half the interval the pool
% half already had.  Do not print one side of this estimand without the other.
On the held-out solutes the same head is the worse of the two: $\Tm$ MAE $45.0\to46.6$~K over the
$2518$ test solutes carrying a label, a paired $+1.59$~K with a solute-clustered $95\%$ interval of
$[+0.31,+2.85]$.
% THE DOWNSTREAM HALF PRINTS ITS INTERVAL HERE, 2026-08-10, and it is the last of the four places the
% Supporting Information's preamble declared itself not optional.  The crystal-head half was repaired
% earlier the same day (the comment above); the solubility half was still standing on the arm
% comparison alone, so the article carried a downstream gap with no uncertainty attached to it while
% \S\ref{sec:e2} carries one that crosses zero.  A gap quoted without its clustering is a row count
% pretending to be a sample: 5608 rows, 147 solutes.  Do not restore the bare gap.
The downstream solubility half is weaker still than its row count suggests: the $+0.065$ \lnx\ MAE
gap against the grounded arm is $5608$ rows from $147$ test solutes, and clustered by solute it is $+0.088$ with a $95\%$
interval of $[-0.127,+0.284]$, crossing zero
(\S\ref{sec:e2}).
% THE TEMPERATURE CLAIM PRINTS ITS SET AND ITS TWO VALUES HERE, 2026-08-10.  It rested on a
% paragraph of \S\ref{sec:supporting} that the article did not carry: the sentence asserted that the
% physical structure helps at temperature extrapolation and named no split, no n, no magnitude and
% no uncertainty, so a reader could neither size it nor see that the split withholds TEMPERATURE and
% not structure -- the same pairs stand on both sides, which is what makes it a statement about the
% functional form and not about generalisation to new chemistry.  It was the fifth sentence to fail
% the Supporting Information's claim-by-claim audit, and the promise in that document's preamble is
% narrowed in the same pass.  Do not compress this back to "helps as a property of the hypothesis
% class": the clause is the claim, and the two MAEs are what sizes it.
The one place the physical \emph{structure} helps is temperature
extrapolation, and it helps there as a property of the hypothesis class rather than of any arm
trained here. Fitting below $T\le309.75$~K and scoring at $T\ge330$~K---the same $1751$
solute--solvent pairs on both sides, so the split withholds temperature and not structure---a
structured van't Hoff class, log-solubility affine in $1/T$, reaches an MAE of $0.37$ over the
$3343$ high-temperature test rows against $1.29$ for a random forest over Morgan fingerprints and
temperature, both in \lnx\ units. Neither figure carries an interval: each is one fit on one fixed
temperature cut, with no replicate behind it. Whether a trained arm
inherits that form we do not know: the only checkpoint ever scored on the temperature-withheld split
is one whose predictions are near-constant, so it is untested rather than answered
(\S\ref{sec:supporting}, \S\ref{sec:si-mech}).

% THE ONLY EXTERNAL-BENCHMARK SENTENCE IN THIS DOCUMENT NOW CARRIES ITS ATTRIBUTION, 2026-08-10.
% It quoted "0.83 to 0.99" and "1.43 to 2.16" bare: the models, the corpora and the works those six
% figures are published in printed only in the third block of Table \ref{tab:si-baselines-full},
% whose citation markers are the Supporting Information's own S-numbers and cannot be resolved from
% here.  A comparison a reader cannot source is not a comparison.  Both keys are already in THIS
% document's bibliography (osanloo2024 and vermeire2022solprop are cited in the Introduction), so
% the attribution costs no new reference.  The Leeds set is named and not cited: no verified entry
% for Boobier et al. exists in references_verified.bib, and the evaluation of record for all six
% figures is Attia et al., which is cited (the key osanloo2024 is a historical key for that entry).  "None of the six carries a published uncertainty" is
% the clause that keeps a range from being read as a spread; do not drop it to save words.
The external block is not like-for-like and the table says so in its own block heads. FastSolv and SolProp are deposited here on a \emph{by-solute} split, this work's arms on the harder solute-scaffold split, and the published figures are as published, on other corpora and in another unit; scoring released FastSolv weights on our test rows is invalid because FastSolv is trained on this corpus. So there is no like-for-like external comparison in this paper. That is a budget and not a barrier: the FastSolv harness deposited here retrains from a train/validation/test triple and accepts this split's own files, and the run was not made, so the one number that would place these arms against published work on the split they are argued on does not exist. % ITEM 5 OF THE 2026-08-11 REFEREE ROUND.  This sentence drew a cross-block reading -- "the same
% order as published new-solute extrapolation" -- from published numbers on other corpora, in
% another unit, with no shared rows, in a paper whose own table says the blocks may not be read
% across.  The run that would license it was not made, so the INFERENCE goes and the levels stay:
% every figure it printed still prints, side by side, with what may not be concluded from them.
% Do not restore an ordering here without the harness run.
What remains is a scale and not a placement: this work's control reaches $\log_{10}S$ RMSE $1.00$ on held-out scaffolds and its physics arms $1.39$ to $1.60$, against published figures of $0.83$ to $0.99$ for that evaluation's leaders and $1.43$ to $2.16$ for the physics-informed cycle. Those are on other corpora and on splits that are not this one, and no row is shared with them, so no ordering follows from setting them beside each other. Those six published figures are $\log_{10}S$ RMSE in mol\,L$^{-1}$ from a single evaluation, Attia et al.\ \cite{osanloo2024}, on two corpora: the SolProp test set, whose solutes are disjoint from its training corpus, and the near-ambient Leeds set of Boobier et al.\ \cite{boobier2020}. The leaders are that work's two direct models---the solution-FASTPROP ensemble at $0.83$ and $0.95$ and its solution-CHEMPROP companion at $0.83$ and $0.99$---and the physics-informed cycle is SolProp \cite{vermeire2022solprop}, scored in the same evaluation at $1.43$ and $2.16$. Each range is two point values on two corpora rather than a spread, and none of the six carries a published uncertainty. What may be read from those figures is what survives the clip sweep and not the two decimals; the sweep, the random-pair rows and the conversion behind the $\log_{10}S$ column are in \S\ref{sec:si-baselines}.

% ---------------------------------------------------------------------------
% EXTERNAL BASELINES, IN THE ARTICLE (2026-08-02, editorial item A3).  The only comparison against
% published solubility models used to be Supporting Information, which is what three referees
% objected to.  EACH BLOCK IS LABELLED WITH WHAT IT MAY BE COMPARED TO, and the table refuses the
% flattering comparison in both directions: the physics arms trail their own control in block one,
% and block two is on an easier split than block one so it is not a ranking.
%
% CUT TO 10 PT, 2026-08-08.  This float used to be the whole transcription -- seventeen rows in
% three blocks, six columns, and a 489-word note in twenty lines -- set at \scriptsize, which is
% 8 pt against a 12 pt body, and filling 82% of a float page that carried no body text.  All three
% referees named the type size.  Nothing about the measure fixes it: this is already a full-width
% `table*', so at 10 pt the only lever left is rows, and these went, in this order:
%   (1) the note block, whole;
%   (2) the third block, six rows quoted as published on other corpora in mol/L, three of whose
%       four data columns are a dash;
%   (3) the two random-pair rows, a split control the article makes no claim from.
% Every one of them is printed in Table \ref{tab:si-baselines-full}, from the same source file,
% and the caption says so.  What had to STAY is the comparison \S\ref{sec:discussion} makes
% explicitly -- this work's arms against external models re-run on this corpus -- so blocks one
% and two and the scale reference are here in full.  The published leaders' range is not a table
% row any more, so the sentence that compares against it now prints it (2026-08-08); do not take
% it back out on the grounds that a table has it.
% Rows: si_tables/external_baselines_rows_article.tex, cut from the full transcription in
% si_tables/external_baselines_rows.tex; see that file's header for the four typesetting edits
% and the regeneration command.
% The n column carries TWO sizes on purpose: the log10 S column is not computed on the rows the MAE
% column is, and the rows it loses used to be ARM-DEPENDENT (2026-08-04 repair; see the generator's
% `THE COMPARISON THIS TABLE MUST NOT FLATTER' block).
% THE `Test set' COLUMN IS GONE, 2026-08-08.  The column printed the same two strings nine times
% and both are in the block heads, so it said nothing and cost 116 pt -- which is why every model
% name wrapped onto two or three lines and the block contrast, the one thing this table is for,
% was the hardest thing in it to see.
% THERE IS NO \tabcolsep HERE ANY MORE, 2026-08-09.  Every one of the five columns' boundaries is
% an explicit @{\hspace{}} in the rows file, because the spacing this table needs is NOT uniform:
% the model column is ragged-right prose and the four numeric columns are right-aligned numbers
% that abut.  Setting \tabcolsep here would change nothing and mislead the next editor.  The
% arithmetic, the printed gutters and how to re-measure them are in the rows file's header; the
% five-column tabular measures 498.75 pt against a 504.47 pt \textwidth, and the LINE it is
% centred in measures 502.08 pt because the newline after \end{tabular} leaves an interword space
% behind it, so a widened cell has 2.4 pt and not 5.7 pt before the line overflows -- and an
% over-wide tabular in a centred float does NOT reliably raise an Overfull \hbox.
% ---------------------------------------------------------------------------
\begin{table*}[tp]
\centering
% THE POINTER AT A SUPPORTING-INFORMATION TABLE LEFT THIS CAPTION, 2026-08-10 -- "The published
% figures, the random-pair rows, and the conversion and saturation clip behind the log10 S column
% are in Table \ref{tab:si-baselines-full}."  A caption states what is shown.  All three
% destinations are in the body of \S\ref{sec:discussion} on the same page: the published figures
% now print there in full with their models, corpora and citations, and the sentence that closes
% that paragraph names the sweep, the random-pair rows and the conversion.  Do not restore a
% cross-reference here; if a destination is ever needed again it goes in the body sentence.
% NO NUMERAL MAY ENTER THIS CAPTION.  It is one of the nine displays
% scripts/analysis/check_hand_transcribed_displays.py binds to artifacts, and a numeral that is
% neither bound nor declared there is a fatal UNREGISTERED finding.
\caption{Solubility accuracy in two blocks that may not be read across: this work on the
solute-scaffold split, external models re-run here on an easier by-solute split. Two sample sizes
appear where a row's two metric columns are not scored on the same rows, one where they coincide.}
\label{tab:baselines}
{\footnotesize
\renewcommand{\arraystretch}{0.95}
\input{si_tables/external_baselines_rows_article}}
\end{table*}

\paragraph{The sign rule's own prediction.} Remark~\ref{prop:tax} predicts \emph{when} to expect
these negatives: a prior hurts precisely in the mature-data, low-noise, well-covered regime. The
synthetic sweep (Fig.~\ref{fig:dial}) is true by construction there, so it verifies the instrument
rather than the hypothesis, while the pKa closure exercises the sufficiency channel on real data.
Whether grounding helps is shaped by \emph{two} axes, closure fidelity and how the latent is trained;
that plane is not characterised on real systems here. The chemical strata form a pattern in the
direction the sign rule would give, but separately tuned pipelines can produce it and there is no
multiplicity control, so \S\ref{sec:map} records it as the experiment a matched control should run
and the diagnostic is \emph{proposed} rather than validated.

% Limitation (i) enlarged-set numbers: run_b_insuff_decomposition.{match_pairs,evaluate_closure,
% lotv_binning} (res convention, 8 equal-count bins) on notebooks/data/raw/idac_expanded{,_raw}.csv
% against results/sigma_profile_artifact/sigma_profiles.csv; the same call reproduces the deployed
% n=60 set (MSE 1.4723, B_insuff 0.6180, margin +0.2362) exactly. Expanded: n=115, MSE 1.2553,
% B_insuff 0.4545, margin +0.3463; gas-chromatography-only subset n=100, MSE 0.9250, B_insuff 0.5274,
% margin -0.1298. Provenance of the n=60 pairs: notebooks/data/raw/idac.csv DOI counts 33/20/7.
% Limitation (vi) scaffold geometry: Murcko scaffolds over the 147 solutes of the has_solubility
% test rows -> 124 scaffolds, 11 multi-member holding 34 solutes and 492 of 5608 rows (8.8%).
\paragraph{Limitations: what the sets can carry.} (i)~The phenomenon and its causal attribution lie
in \emph{different regimes}: the paradox is measured where the model operates (finite composition,
all $\gamma$, $n{=}5608$), the attribution and the latent-departure result only on infinite-dilution
activity data ($n{=}477$ across temperatures, and the $298$\,K, low-$\gamma$, VT-2005-matched $n{=}60$
and $n{=}44$ sets). Transfer back to the paradox's own regime is a conjecture and is the principal
open question here. The $n{=}60$ set is narrowly sourced---all by gas chromatography and all from
three publications, $33$ pairs from one---so neither a method-stratified nor a source-clustered
re-estimate is available on it, and its margin is extraction-sensitive: a broader ThermoML pull at
$298$\,K widens it but \emph{inverts} it on the gas-chromatography subset, a sensitivity we leave
unresolved. The broad set does not move that way under the same cut and is not the set the paradox's
own oracle is defined on; the arithmetic and the enlarged-pull values are in \S\ref{sec:si-repro}.

\paragraph{Limitations: what the estimator can deliver.} (iv)~$\Binsuf$ is upper-bounded, not
point-identified (no replicates at fixed $\zstar$); the smoothness-free Jensen bound assumes $m$ and
$g$ share the $\lngi$ reference state, established by the closure validation of \S\ref{sec:model}, so
the mean offset is decoder bias and not a units artifact. $\Bclos$ is the error of a
composite---the fixed map together with the calculation that produced $\zstar$---and no measurement
here divides it between the two; \S\ref{sec:badclosure} sizes that exposure and leaves the share
unbounded. (vi)~The stratification reads
structure alone, so no cell was selected on its outcome, and it is exhaustive and disjoint
\emph{within} an axis but not across the two axes or their granularities: $362$ of the $1711$ stratum
pairs share rows, all $37$ water-as-solute rows lie inside three solvent classes, and $108$ of the
$129$ alkane-solute rows are glycol-ether rows, so cells from different axes are not independent
evidence and those rows are counted once. Granularity is an analyst choice, which is why three
solvent levels and two solute levels are all reported. There is no multiplicity control on the
per-cell bootstrap frequencies, so one cell's $P$ describes that cell alone, and the admissibility
rule is a stability filter and not a correction for the search; six of the nine solvent classes are
not boundable here and three carry no estimate at all. What the search survives is measured rather
than assumed, and neither test establishes the finding as a chemical effect: under the
chemistry-blind relabelling null of \S\ref{sec:broad} the shape of the finding actually claimed---the
number of admissible \emph{positive row sets}, and the certified magnitude---is not above chance, and
the pre-declared out-of-sample test of the chemistry returned \emph{not testable}. The single finding is therefore stated at the level the
instrument licenses on its own rows and \emph{not} as a multiplicity-controlled chemical effect,
and the same holds a fortiori for every cell the map reports and does not state.
The rule is also one-sided in what it can deliver: with $\Binsuf$ bounded
only from above, no cell can ever return ``better inputs would help here'', so the map only ever
withholds a chemistry from an allocation of grounding effort.

\paragraph{Limitations: what is untested, and what is not auditable.} (xii)~The scaffold-leak guard is a property of the released stream builder and not a certified
property of these runs, so the $0.20$ supervision gain is the one reported number a leak could
inflate and is uncertified until the leak-free re-run (\S\ref{sec:data}); deleting the eight
reachable solutes leaves $+0.17$ $[+0.03,+0.31]$, which bounds where a leak can act and does not
restore auditability, and neither do the arguments that bound the other contrasts.
(xiv)~Two terms the thermodynamics asks for are set to zero in the deployed model, and both are
absorbed at training time by the same learned $\sigma$ head. $\Delta C_p$ is zero in the ideal term,
one-signed and worth a median $0.32$ in \lnx\ where a melting point is measured
(\S\ref{sec:framework}). The Staverman--Guggenheim combinatorial term is off in every arm the
paradox is reported on, and the $\sigma$ head emits a profile shape and a cavity area but no cavity
volume, so evaluating that term needs a volume from elsewhere, as the one arm that restores it takes
one. Both cancel in the
evaluation-time substitution and neither cancels in the training that produced the drift of
\S\ref{sec:surrogate}, so the mechanism there is not isolated to the activity kernel.
% THE INDEX WAS RE-CUT, 2026-08-10, in the pass that brought (ix), (x) and (xiii) into the article.
% It used to be one 101-word sentence listing nine numerals under one verb, and three of its entries
% became wrong the moment their content moved here: a reader sent to \S\ref{sec:si-limitations} for
% (ix), (x) or (xiii) would find the same words they had just read in the body.  The list is now
% grouped by WHERE THE CONTENT IS -- stated where it bites, printed in this document, or in the
% Supporting Information alone -- which is the only thing a reader wants from an index of this kind.
% If a limitation ever moves between the two documents, move its line between these groups in
% the same pass.
%
% AND THE RE-CUT MISFILED TWO OF THE NINE -- audited item by item against the compiled article,
% 2026-08-10, which is what the re-cut itself was made to repair and did not do.  BOTH entries of
% the third group were wrong: (vii)'s content is \S\ref{sec:data}'s label-noise paragraph ("The
% scaffold-split MAE reported here (1.70 to 1.85) is above the larger functional by a factor of
% five, so label noise does not account for the accuracy ceiling under either reading"), and (viii)
% is \S\ref{sec:surrogate} throughout -- the 51+-2% with its instability across analysis
% configurations, the 72+-1%, the 4.1+-0.5x with what the +- spans, the two nulls that do not clear,
% and "measured on runs of its own".  With those two moved the third group is EMPTY, so the index
% now says two things and not three: all nine are in the article, and what the Supporting
% Information adds is the qualifying detail.  A third group is not kept alive for symmetry.
% (iii)'S DESCRIPTOR WAS ALSO WRONG, in the other direction: it read "which other cells of the map
% keep their sign", and the article does NOT name them -- \S\ref{sec:broad} states only that the
% glycol ethers are "not the only one sign-stable under every deletion", the halogenated solvents
% and the mono-alcohols being named in \S\ref{sec:si-limitations}.  The descriptor now says what
% this document prints.  Check the descriptors, not only the groups, on the next pass: a line that
% files a limitation correctly and describes it wrongly sends the reader just as far astray.
The nine remaining limitations are in \S\ref{sec:si-limitations}, keeping their numerals. Every one
of them is also stated in this document, and this index gives the place rather than the
kind. Five are stated where they bite: (ii)~what the convention rule costs the aggregate and
(v)~the constant-offset bound's convention-specificity in \S\ref{sec:estimator}, (iii)~that the
glycol ethers are not the only cell of the map to keep their sign in \S\ref{sec:broad},
(vii)~the accuracy ceiling standing a factor of five above the label-noise floor in
\S\ref{sec:data}, and (xi)~the separate-tuning confound above. Four print their content in the
section that carries the result: (viii)~what the three-seed latent-departure result establishes and
what it does not, in \S\ref{sec:surrogate}; (ix)~what bounds the black-box probe, in the probe
paragraph above; (x)~what the closure-fidelity contrast does not measure and what would settle it,
in \S\ref{sec:notdecided}; and (xiii)~what the deposit does not make checkable, in the
data-availability statement. What \S\ref{sec:si-limitations} adds to all nine is the detail each is
qualified by---which two cells, which magnitudes, which of the probe's tests---and none of the nine
is there alone.

% CONCLUSIONS.  Two paragraphs, no numbers, no section references, and NOT a restatement of the
% abstract: the consequence and the design rule, touching a finding only to carry the "therefore".
%
% CUT FROM 451 WORDS TO 208, 2026-08-10, and the cut is of RESTATED FINDINGS, not of scope.  Five of
% the abstract's seven findings were being told again here, two of them in nearly the abstract's own
% words -- the supervision gain, the inference-time loss, the error split and its glycol-ether cell,
% the multiplicity search, the latent's departure -- which is what a conclusion is asked not to do.
% Four went; the fifth, the latent's departure, is the "therefore" this section exists to carry and
% stays.  The first paragraph's opening clause is the one other touch, and it is the premise the
% rest of the section is a consequence of.
% WHERE EACH CUT FINDING NOW LIVES, checked before it was cut, because a caveat travels with the
% sentence it qualifies and both have to survive somewhere: the supervision gain and its leak
% certification in \S\ref{sec:paradox}, limitation (xii) and the abstract; the inference-time loss
% at every seed in \S\ref{sec:paradox}, the Introduction's fifth paragraph and the abstract; the
% error split, its one class of solvent, the composite scope ("the model as deployed ... not its
% kernel alone") and the reading-not-a-test clause in \S\ref{sec:decomp}, \S\ref{sec:broad},
% \S\ref{sec:discussion} and the abstract; the search, the null it does not clear and the empty
% out-of-sample cell in \S\ref{sec:broad}, limitation (vi) and the abstract; and "a single figure
% for a whole data set is an average over that set's composition" in \S\ref{sec:broad}, which is
% where it is measured.  NOTHING WAS DROPPED THAT IS NOT PRINTED IN AT LEAST TWO OTHER PLACES.
% THREE SCOPE CLAUSES REMAIN AND MAY NOT GO.  They are not restatements and they are not decoration;
% each is the boundary of a sentence that is still here, and deleting one makes that sentence claim
% more than the body supports.  (1) "measured on runs of its own" -- without it the departure reads
% as measured on the checkpoints whose paradox it is offered to explain, which is the one thing
% \S\ref{sec:surrogate} says it is not.  (2) "wherever a reference existed to hold it against" --
% the departure is only observable where a tabulated value exists, and without the clause it becomes
% a claim about the latent everywhere.  (3) "it comes one-sided, so what the measurement returns is
% the chemistry on which better inputs are not the remedy" -- with the insufficiency side bounded
% from above only, no cell can ever return "better inputs would help here", and the design rule
% overclaims in exactly that direction without it.
\section{Conclusions}
\label{sec:conclusion}
Grounding a learned physical intermediate in reference values is two operations, not one, and here
they had opposite signs. The operation
that failed is the one a practitioner reaches for, because it is the one that needs no retraining.
The intermediate paid for the arrangement.
Trained through a model it cannot correct, and measured on runs of its own, it stopped reporting the
physical quantity it is named after wherever a reference existed to hold it against---which is the
price of reading a grounded latent as a measurement.

The general statement is a design rule with a test attached. Wherever a predictor composes a fixed
physical model over a learned intermediate for which an external reference exists, that reference
measures whether the fixed model itself limits accuracy, and where it answers, the answer decides the
remedy: a more faithful physical model---the calculation that supplies its reference values included.
% `---or better inputs' DELETED 2026-08-09.  Two referees stopped at the colon and took away a
% two-sided instrument, which the next clause but one then has to take back and \S\ref{sec:discussion}
% flatly denies: with \Binsuf\ bounded only from above, no cell can ever return "better inputs would
% help here".  The remedy this rule names is the only one it can name.  Do not restore the phrase to
% balance the sentence; the sentence is not balanced because the instrument is not.
% AND THE PREMISE FOLLOWED IT, 2026-08-09.  Deleting the second item left "which of the two limits
% accuracy" standing in front of a colon that now opens a one-item list -- the sentence still asked
% the two-sided question and then answered only half of it, which is the reading the deletion was
% made to prevent.  The question is now scoped to the side the bound can answer.  THE FIX IS THE
% PREMISE, NOT THE LIST: do not re-broaden this clause to make the colon look balanced.
% WHAT IS DELIBERATELY STILL MISSING, so a later reader does not repair it by accident.  The pKa
% oracle swap of \S\ref{sec:pka-main} IS two-sided and measured -- it returns "the reference input
% helps here" on the clean pole and "it hurts here" on the degraded -- and this section names the
% second closure nowhere, so this general statement is narrower than the paper's own result.  That
% is a gap in what the Conclusions cover, not an error in this sentence, and closing it means adding
% the swap, NOT widening the bound's claim.  The two are different instruments: the swap compares
% two measured accuracies, the bound compares an error against a one-sided bound on another.
The answer comes chemistry by chemistry rather than once for a field, and it comes
one-sided, so what the measurement returns is the chemistry on which better inputs are not the
remedy. What this work establishes about the instrument is the map itself: which cells
it can answer in, and at what size. Whether a latent trained through an imperfect model becomes, in
general, a surrogate for that model's error is the prediction this work leaves testable.

\section*{Author contributions}
\noindent
%% PROVISIONAL, for both authors to confirm before submission: the roles below are inferred from the
%% work as recorded and are not an agreed statement.
N.~L. Polomoshnov: Conceptualization, Methodology, Software, Formal analysis, Investigation,
Data curation, Visualization, Writing -- original draft.
A.~V. Rudik: Writing -- review \& editing.
%% CRediT vocabulary (use only these 14 names): Conceptualization; Data curation; Formal analysis;
%% Funding acquisition; Investigation; Methodology; Project administration; Resources; Software;
%% Supervision; Validation; Visualization; Writing -- original draft; Writing -- review \& editing.

\section*{Conflicts of interest}
\noindent There are no conflicts to declare.

\section*{Data and Software Availability}
The analysis code, and those intermediate artifacts small enough to version, are available at
\url{https://github.com/doctawho42/tgnn-solv} (commit
\pending[hash to be supplied at submission]) under the MIT licence, that commit being the one
the reported numbers were produced from; the model checkpoints, the processed split, the full
per-arm prediction files and the larger analysis artifacts will be deposited in a Zenodo archive,
\pending[whose DOI is registered at submission]. The data sources are public: BigSolDB~2.0
\cite{krasnov2025bigsoldb} for solubility, the VT-2005 database for the reference
$\sigma$-profiles, Ref.~\cite{fingerhut2017} as redistributed with Ref.~\cite{bell2020cosmosac}
for the UD profiles, ThermoML for the infinite-dilution activity coefficients, the
PGL 6th-edition IDAC database for Table~\ref{tab:closure-anchor}, and the OPERA
compilation \cite{mansouri2019opera} for the pKa data. Four disclosed defects sit on the deposited
record, in \S\ref{sec:si-methods}, \S\ref{sec:si-repro} and \S\ref{sec:si-tables}: the
three-seed surrogate run's per-run split provenance was not logged, one $\sigma$-stream build on disk
was written against the wrong split files, the two
VT-2005-matched audit artifacts disagree on one out-of-fold estimate, and the COSMO-SAC-2010 layer's
reference reproduction (\S\ref{sec:model}) is not deposited, its validator having written no
artifact. Two further defects are
repaired rather than disclosed, each described where its numbers are printed: the two FastSolv
retraining bundles were re-scored (Tables~\ref{tab:baselines} and~\ref{tab:si-baselines-full}, with
the diagnosis, the withdrawn
values and the control that reproduces them in \S\ref{sec:si-baselines}) and the
withdrawn bundle is kept beside the re-scored one, and the truncated seed-$44$ $\sigma$-oracle
per-row file has been recovered complete and now stands in the deposit
(Table~\ref{tab:runs}, \S\ref{sec:si-repro}).
% LIMITATION (xiii) PRINTS ITS CONTENT HERE, 2026-08-10 (same referee item as (ix) and (x)).  This
% is the sentence that rests on it: a data-availability statement that lists what is deposited and
% not what a reader can therefore check is the half of the disclosure that flatters.  What stays in
% \S\ref{sec:si-limitations} is the numeral and the same words; what a reader needs is here, where
% the deposit is described.  Do not delete this on the ground that the SI carries (xiii).
What that deposit does and does not make checkable is limitation~(xiii). Several of the numbers
the verdict rests on are checkable only by re-running the released code, whose input artifacts are
larger than the repository carries. The two decomposition sets are therefore deposited as row-level
tables (Data Sets~S1 and~S2), so those sets and their margins can be recomputed with no code at all;
that does not deliver the provenance of the closure evaluations inside them, nor of the trained arms.

\begin{acknowledgement}
This research received no external funding.
Computations were carried out on commercial cloud services (Google Colab Pro+, Modal, and Google Cloud Platform) using NVIDIA T4 and L4 GPUs.
Generative-AI use: \pending[the tools used, and at which stages, to be stated by the authors].
% The disclosure ACS requires names the tools and the stages they were used at, and that is author
% knowledge, not manuscript content -- it cannot be written here without being invented.  It is now
% a \pending so that it prints in red on every draft instead of sitting in a comment: item 5 of the
% pre-submission checklist above, and the last of the five to be settled.
\end{acknowledgement}

% The Supporting Information is a separate document, grounding_paradox_si.tex. This paragraph is what
% ACS prints as the "Supporting Information Available" note, and it is also where this article tells
% its reader how to read an S-number, since every pointer from here into that document resolves to one.
\begin{suppinfo}
The Supporting Information is a separate document. Its sections, tables, figures and equations are
numbered S1, S2, \ldots, and every reference above that carries an S is to it. It contains
supplementary methods; the proofs of Lemma~\ref{prop:decomp} and Remark~\ref{prop:tax} and
three further results; per-arm tables and decomposition robustness; the map's
$59$ strata in full, including the $44$ the estimator cannot bound; further external ablations;
supplementary mechanism results; supplementary diagnostics and broad negatives; the second-domain
pKa construction; data provenance and reproducibility; the black-box probe tables; the synthetic
closure-fidelity family; and the positioning against adjacent literatures. Two machine-readable
tables are deposited with it: the $477$-row broad IDAC set (Data Set~S1) and the $60$-row
VT-2005-matched set (Data Set~S2); their columns are listed in \S\ref{sec:si-repro}.
\end{suppinfo}

% DO NOT \input sections/SI here. It is the SI document's body now, it declares S-numbered
% floats, and pulling it back in would restore the reading-order defect (the reference list
% printing forty pages after the text that cites it) and give this document a second,
% conflicting set of table and figure numbers.

% achemso selects achemso.bst automatically; no \bibliographystyle is given.
\bibliography{references_verified}

\end{document}
