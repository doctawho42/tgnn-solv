% =====================================================================
% GROUNDING-PARADOX PAPER — reframed submission draft (2026-07-08)
% Supersedes conditional_optimality_skeleton.tex (theory-first, headlined the
% REFUTED Thm3 "conditional optimality iff"). That file is kept for reference /
% reusable Fisher+T-extrap numbers, NOT the thesis.
%
% THESIS PIVOT: from "physics helps iff (conditions)" -> the GROUNDING PARADOX:
%   feeding the TRUE external physical latent (VT-2005 sigma-profiles) through the
%   differentiable COSMO-SAC closure makes activity/solubility prediction WORSE,
%   and we MEASURE why: B = B_closure + B_insuff, closure dominates.
%
% SECTION MAP (general-theory reframe, 2026-07-11):
%   Abstract + Contributions ...... general composed-predictor method (paradox + decomposition
%                                   instrument + Gamma-symptom + physics-tax) ; solubility = instance
%   S2 Setup ...................... SLE eq + sigma-profile closure + z* external latent   [reuse eq:sle]
%   S3 Framework (general g(h(x))). Lemmas 1-3 (non-identifiability/efficient-info/B-decomposition);
%                                   Theorem 1 grounding-gap sandwich 0<=Gamma<=B_clos (thm:gap, sec:gap);
%                                   Proposition penalty sign rule (prop:tax, sec:tax);
%                                   proofs in SI. Gamma = SYMPTOM, decomposition = certificate.
%   S4 Results .................... R1 paradox; R2 B measurement (keystone); R3 Gate B null;
%                                   synthetic dial (sec:dial) + pKa second closure (sec:pka); data-eff; maps
%   S5 Discussion ................. phase picture / when a physics prior must lose + 4 hedges + scope
%   S6 Conclusion
%
% NUMBER PROVENANCE: Fig-paradox / chemistry-map numbers regenerate from
% results/e5_sigma_grounding/ via scripts/analysis/run_e5_comparison.py
% (3-seed aggregate: results/e5_sigma_grounding/THREE_SEED_SUMMARY.md).
% Table-2 B-decomposition numbers regenerate from
% scripts/analysis/run_b_insuff_decomposition.py -> results/b_insuff/decomposition.json.
% BIB: murphy1973 / degroot1983 (calibration refinement) and white1982 (QMLE under
% misspecification) are present in references_verified.bib and cited.
% =====================================================================
% ---------------------------------------------------------------------
% FORMAT: J. Chem. Inf. Model. (ACS). The journal code jcisd8 and the
% permitted manuscript types (article, suppinfo) come from
% achemso-jcisd8.cfg; the class supplies geometry (2.54 cm margins) and
% natbib itself, so neither is loaded here.
% The JCIM configuration calls \SectionNumbersOff. We restore numbering
% with the class's own \SectionNumbersOn because the manuscript carries
% 135 numbered cross-references to its own sections.
%
% LAYOUT. `layout=twocolumn' is the published-article measure and is what the
% main-text page count refers to. Measured on the current build (2026-07-29): 28 pages of main
% text, the Supporting Information beginning on p. 29 (it shares that page with the back matter),
% out of 71 total. Re-measure from the build, not from this comment, after any pass. ACS expects the double-spaced
% single-column measure at submission: drop `,layout=twocolumn' for that (the
% same content then runs to roughly twice that in main text, which is normal for
% the submission format and is not a content change).
% ---------------------------------------------------------------------
\documentclass[journal=jcisd8,manuscript=article,layout=twocolumn]{achemso}
\SectionNumbersOn
\usepackage{amsmath,amssymb,amsthm}
\usepackage{array}
\usepackage{booktabs}
% Ragged-right paragraph column. Narrow justified p-columns in the claim and
% decomposition tables stretched a two-word line across the whole column; L{} sets the
% same text without the stretch, which is what let the column widths be rebalanced
% against actual content instead of against the worst justified line.
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\usepackage{graphicx}
\usepackage{xcolor}
% hidelinks: without it hyperref's default pdfborder={0 0 1} draws a rectangle around every
% cross-reference and citation. The boxes are invisible on screen in some viewers but print,
% and ACS production receives the PDF as-is.
\usepackage[hidelinks]{hyperref}

% ---------------------------------------------------------------------
% FLOAT PLACEMENT. In the two-column measure a deferred `table*'/`figure*' blocks every
% starred float after it, and stock LaTeX flushes that queue only at \clearpage -- which
% here is \end{document}. Left alone, every float from Table 2 onward printed after the
% bibliography, tens of pages from the text that cites it. dblfloatfix repairs the ordering
% and lets a double float reach the bottom of a column; the fractions below let a tall
% starred float be a top float instead of being sent to a float page. The two floats that sit
% at that height limit, Tables 2 and 3, additionally carry `p' in their own placement
% specifiers, so a float page is available to them and the queue never backs up; the note at
% each of them says so. Do NOT force the queue out with \afterpage{\clearpage} before Sec. 6.5
% or before the Discussion: in two-column mode that fires at a column boundary rather than a
% page boundary, so it ends the page with the right-hand column empty -- a blank half page
% before the Discussion -- and it buys no placement that `p' does not.
% Do not tighten these back without checking where the floats land.
% ---------------------------------------------------------------------
\usepackage{dblfloatfix}
\renewcommand{\topfraction}{0.92}
\renewcommand{\dbltopfraction}{0.92}
\renewcommand{\bottomfraction}{0.6}
\renewcommand{\textfraction}{0.06}
\renewcommand{\floatpagefraction}{0.6}
\renewcommand{\dblfloatpagefraction}{0.6}
\setcounter{topnumber}{3}
\setcounter{dbltopnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{5}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\newtheorem{assumption}{Assumption}
\newtheorem{definition}{Definition}

% amsthm sets the optional argument of `proof' as a \trivlist label, i.e. one unbreakable
% \hbox. Four of the SI proof headers are longer than the two-column measure and ran into the
% neighbouring column. This redefinition is amsthm's own, with the head moved out of the label
% and into the paragraph so it wraps; \pushQED/\popQED and the spacing are unchanged.
\makeatletter
\renewenvironment{proof}[1][\proofname]{\par
  \pushQED{\qed}%
  \normalfont \topsep6\p@\@plus6\p@\relax
  \trivlist
  \item[]\hskip\labelsep{\itshape #1\@addpunct{.}}\enspace\ignorespaces
}{\popQED\endtrivlist\@endpefalse}
\makeatother

% ---------------------------------------------------------------------
% ACS-style lettered table footnotes. \tnk{a} sets the superscript key in the row or
% column head it qualifies; \tabnotes{...} sets the note block directly under the
% \bottomrule. THIS IS THE DESTINATION FOR CAPTION MATERIAL. A caption says what the
% table shows; anything that qualifies one row, one column or one cell is keyed to it
% here instead of being appended to the caption. Budget: <=40 words for a table
% caption, <=60 for a figure caption. Captions on this manuscript have been cut twice
% and grown back twice because each repair pass appended a sentence; if a finding
% needs an answer, put the answer in a note, in the body, or in the SI, and leave the
% caption alone.
% ---------------------------------------------------------------------
\newcommand{\tnk}[1]{\textsuperscript{\textit{#1}}}
\newcommand{\tabnotes}[1]{\par\vspace{2.5pt}\begingroup\footnotesize
  \setlength{\parindent}{0pt}\raggedright #1\par\endgroup}

% achemso sets the corresponding author's address with \url, which hyperref turns into a URI
% action with no scheme -- a viewer resolves "nikitapol@fbb.msu.ru" as a relative URL and the
% one external link in the PDF does not open a mail client. This is the class's own
% \acs@email@list@aux with \url replaced by an \href{mailto:...}; nothing else changes, and the
% printed form (\nolinkurl, in the class's \UrlFont) is identical.
\makeatletter
\newcommand*\acs@mailto[1]{\href{mailto:#1}{\nolinkurl{#1}}}
\renewcommand*\acs@email@list@aux{%
  \advance\@tempcnta\@ne\relax
  \ifnum\@tempcnta>\acs@author@cnt\relax\else
    \acs@ifundefined{@email@\@roman\@tempcnta}{}{%
      \advance\@tempcntb\@ne\relax
      \ifnum\@tempcntb>\@ne\relax
        ;
      \fi
      \expandafter\expandafter\expandafter\acs@mailto\expandafter
        \expandafter\expandafter{%
           \csname @email@\@roman\@tempcnta\endcsname
      }%
    }%
    \expandafter\acs@email@list@aux
  \fi
}
\makeatother

\newcommand{\pending}[1][?]{{\color{red}\texttt{[#1]}}}
\newcommand{\lnx}{\ensuremath{\ln x_2}}
\newcommand{\Phicry}{\ensuremath{\Phi}}
\newcommand{\lng}{\ensuremath{\ln\gamma_2}}
\newcommand{\lngi}{\ensuremath{\ln\gamma_2^\infty}}
\newcommand{\Tm}{\ensuremath{T_m}}
\newcommand{\dHfus}{\ensuremath{\Delta H_{\mathrm{fus}}}}
\newcommand{\Rgas}{\ensuremath{R}}
\newcommand{\zstar}{\ensuremath{z^\star}}
\newcommand{\Bclos}{\ensuremath{B_{\mathrm{closure}}}}
\newcommand{\Binsuf}{\ensuremath{B_{\mathrm{insuff}}}}
\newcommand{\Ex}{\ensuremath{\mathbb{E}}}
\newcommand{\Var}{\ensuremath{\mathrm{Var}}}
% The grounding gap. Renamed from \Gamma (2026-07-27) so that it no longer collides with the
% COSMO-SAC segment activity coefficient \Gamma_S of Eq.~(4).
\newcommand{\Ggap}{\ensuremath{\mathcal{G}}}
% Hammett substituent constant, renamed from sigma so that it no longer collides with the
% charge-density profile sigma of the main text.
\newcommand{\hammett}{\ensuremath{\sigma_{\mathrm H}}}

% Previous title (kept for reference): When Reference Physical Inputs Hurt a Learned Solubility Model: The Latent Becomes a Compensating Surrogate
% Previous title (kept for reference): ... Separating Closure Misspecification from Input
% Insufficiency ... -- retitled 2026-07-27 to name the axis on which the separation is
% performed and the strength actually reached.
\title{To Thine Own $\sigma$ Be False: Bounding Closure Misspecification against
Input Insufficiency on the Activity Axis When Reference $\sigma$-Profiles Hurt
COSMO-SAC Solubility Prediction}

% NOTE: achemso re-typesets the arguments of \affiliation and \email, which breaks the
% \pending macro (optional argument + \color), so anything outstanding here is plain text.
% ORCID (entered in the ACS submission form, not typeset here):
%   N. L. Polomoshnov  0009-0001-4342-8539
%   A. V. Rudik        [outstanding]
% STILL OUTSTANDING: street address and postcode of each institution. N. L. Polomoshnov is
% the corresponding author; add a second \email under A. V. Rudik if she corresponds too.
\author{N.~L. Polomoshnov}
\affiliation[MSU]{Faculty of Bioengineering and Bioinformatics, Lomonosov Moscow State
University, Moscow, Russia}
\alsoaffiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}
\email{nikitapol@fbb.msu.ru}
\author{A.~V. Rudik}
\affiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}

\abbreviations{SLE, solid--liquid equilibrium; IDAC, infinite-dilution activity
coefficient; LFER, linear free-energy relationship; LOTV, law of total variance;
MAD, mean absolute deviation; UD, University of Delaware (the successor
$\sigma$-profile database)}
\keywords{solubility prediction, physics-informed machine learning, COSMO-SAC,
model misspecification, representation probing}

\setlength{\emergencystretch}{2.5em}
\begin{document}

% ACS requires a table-of-contents graphic. Panel (a) of the overview figure is
% the self-contained one: it shows the pipeline and the substitution that makes
% the prediction worse, which is the paper in one image.
\begin{tocentry}
\includegraphics[width=\linewidth]{figs/fig_toc.pdf}
\end{tocentry}

\maketitle
\begin{abstract}
Physics-informed models route a prediction through interpretable physical quantities, on the expectation
that grounding those quantities in accurate reference values improves accuracy. Whether it does depends on
the fixed model that consumes them, and on which sense of grounding is meant. We make that dependence
measurable for solid--liquid solubility: with COSMO-SAC implemented as differentiable
activity-coefficient layers, an external reference for the learned input gives an instrument that splits
the prediction error into a misspecification term and an insufficiency term. The split is an exact
identity; its misspecification bound needs no model fit, but the insufficiency term is estimated by
binning and enters as a one-sided bound. Grounding at \emph{training} time, as supervision from the same
reference database, helps: solubility mean absolute error improves from $2.04$ to $1.85$ in units of
\lnx, the natural logarithm of the saturated mole fraction. Grounding at \emph{inference} time, by
substituting the reference profile for the learned one, hurts: $1.85$ to $2.25$ over three seeds when
the substitution is made at evaluation. A co-adapted arm run at one seed costs less, which points to
part of the evaluation-only cost being ordinary test-time distribution shift; one seed establishes
neither that nor its share. That substitution falls almost entirely on the solvent channel,
few drug-like solutes carrying a reference profile. The activity axis is matched instead: solute and
solvent alike carry a reference profile. There the misspecification term of the deployed
COSMO-SAC-2002 kernel is \emph{likely} the larger---a grade this paper defines as measured but
not resolved, the bounds being one-sided and the design not resolving the ordering. On
$477$ infinite-dilution measurements over $185$ molecule pairs it is bounded below by $1.21$ against
an insufficiency term bounded above by $0.70$---both in MSE of \lngi---a margin of $+0.51$ whose
$90\%$ interval spans zero
under two of three clusterings; on the $60$-pair subset for which the paradox's own reference
database is matchable the margin falls to $+0.05$. Both margins use the combinatorial convention the
pipeline deploys, applied to the two sets alike; the literature-standard convention widens
them. Under the modern 2010 kernel the ordering is lost on the small set. Trained end
to end through the misspecified model, the learned profile departs from the reference along a direction
shared across molecules and no longer reports the physical quantity---likely, on $44$ matched
molecules over three seeds. That the departure cancels part of the model's error is unmeasured, and
the one direct check available argues against it but is too weak to exclude it. Raising the
fixed model's fidelity more than halves its error on the matched corner's hydrogen-bonding chemistry
under the literature-standard convention, and is not an improvement on the broader infinite-dilution
set under either convention, where the same step is unresolved in all three cells measured, from
$-0.99$ with dispersion off to $+0.06$ with it on. That bounds
the lever rather than excluding it. The sign reversal
appears again in a synthetic family of models, where it is true by construction and so checks the
instrument rather than testing the claim, and, in a second chemical domain---acid dissociation
constants---in at least $19$ of $20$ resampled splits, there through the insufficiency channel rather
than through this mechanism. The instrument applies wherever an external reference for the intermediate exists: grounded latents are not
reliably interpretable when the model consuming them is misspecified.
\end{abstract}

\section{Introduction}
\label{sec:intro}
The solubility of a solid in a liquid solvent decides how a drug is formulated and how a product is
crystallised and purified, yet it remains hard to predict from
molecular structure alone. Measurements of the same system disagree substantially between laboratories,
and the property depends jointly on how a molecule interacts with the solvent and on the energetics of
its crystalline solid, so first-principles calculation is costly and any data-driven predictor must
generalise to molecules unlike those it was trained on.

Purely data-driven models regress solubility directly from molecular
descriptors or learned representations, and are at present the most accurate on unseen molecules, but
they offer no account of why a given prediction succeeds or fails. Physics-informed models
instead route the prediction through interpretable thermodynamic quantities---an activity coefficient
describing the solute--solvent interaction, and the melting properties of the crystalline solid---each
calibratable against abundant single-component reference data a black-box regressor cannot
exploit. The appeal of this second strategy rests on a widely held expectation: that supplying a model
with physical inputs closer to their true values should make its predictions more accurate.

This expectation can fail, and which sense of ``grounding'' is meant decides the sign. We study a
physics-informed solubility model whose learned intermediate is a molecular charge-density profile (a
$\sigma$-profile) consumed by a fixed thermodynamic model, COSMO-SAC. Such a fixed, non-fitted map from a
learned intermediate to a prediction is what we call a \emph{closure}. Supervising the learned profile
against an external quantum-chemical database during training \emph{helps}: it is the difference between
the ungrounded and the $\sigma$-supervised training configurations ($\mathrm{MAE}\ 2.04\to1.85$, three
seeds; SI, Table~\ref{tab:si-arms}). Substituting the reference profile for the
learned one at inference \emph{hurts},
and by about twice as much ($1.85\to2.25$ evaluation-only, three seeds;
Fig.~\ref{fig:overview}). A co-adapted arm run at one seed hurts less, which points to part of that
cost being the ordinary degradation any pipeline suffers when an input distribution is swapped at
test time; one seed establishes neither that nor its share.
One seed fixes the sign of that arm and not its size, so neither the share nor the two-to-one
comparison above rests on it (\S\ref{sec:paradox}). Those are opposite operations on the same
database, and only the second is the paradox. The closure is misspecified, and the learned profile,
trained end to end through it, no longer reports the physical quantity it nominally
represents---\emph{likely}, on $44$ matched molecules over three seeds (\S\ref{sec:surrogate}); its
departure from the reference has the form a compensation for the closure's error would take, though we do
not measure that the compensation occurs.

Diagnosing such a failure is difficult because the error of a composed predictor confounds two causes:
the closure may be wrong, or the learned inputs may carry too little information. Our central
methodological result is that an external reference for the learned intermediate separates the two
exactly. The identity fits no further model, and neither does the bound it places on the misspecification
term; the insufficiency term is estimated by binning, so the separation is one-sided in either
direction it can fail. On the activity axis it places the accuracy ceiling of the deployed kernel in the
closure rather than in the inputs. Grounding a learned latent on reference values, and reading it as the
physical quantity, is therefore safe only when the model that consumes it is faithful.

\paragraph{Contributions.} This article makes the following contributions.
\begin{enumerate}
\renewcommand{\labelenumi}{(\alph{enumi})}
  \item We implement the COSMO-SAC family of activity-coefficient models as differentiable layers and
  build an instrument that, using an external reference for a model's learned input, splits the
  prediction error into a model-misspecification term and an input-insufficiency term. The bound it
  places on misspecification fits no further model; the insufficiency term is estimated by binning. The
  instrument applies to a predictor that composes a fixed physical model over a learned intermediate
  for which an external reference exists.
  \item A graph neural network predicts solubility more accurately from its own learned
  $\sigma$-profiles than from external quantum-chemical reference profiles supplied to the same fixed
  COSMO-SAC closure---predominantly a solvent-channel substitution, few drug-like solutes carrying a
  reference profile. On the activity axis, where solute and solvent alike carry one, the
  misspecification term of the deployed COSMO-SAC-2002 kernel is likely the larger---by $+0.51$ on the
  $n{=}477$ broad infinite-dilution activity coefficient (IDAC) set and by $+0.05$ on the $n{=}60$
  matched corner, both under the combinatorial convention the pipeline deploys, applied to the two
  sets alike. Neither set is the deployment regime, and how each is built is what scopes that
  (\S\ref{sec:measure}, App.~\ref{sec:si-repro}). Transfer to the solubility regime is argued, not
  measured.
  \item Raising the closure's fidelity is a lever with a boundary, and we locate it. The standard
  $2002\to2010$ step cuts the error on the hydrogen-bond-dominated matched corner by more than
  half under the literature-standard convention and by $42\%$ under the deployed one, and is not an
  improvement on the broad IDAC set under either convention: $-0.99$ deployed and $-0.07$
  literature-standard with the dispersion term off, $+0.06$ with it on, none of the three
  resolved; a within-database, within-temperature
  control attributes the reversal to chemistry. That null is a
  small-molecule one, so on drug-like solids the lever is untested rather than absent, and the one
  downstream correction we test---an additive gated residual on the closure's output, at seed
  $42$---restores calibration but not accuracy. These are findings about
  \emph{routes out of} the ceiling, not further evidence for where it sits, which is what (b)
  measures.
  \item Trained end to end through the misspecified closure, the learned latent departs from the
  reference along a direction shared across molecules, and so no longer reports the physical
  quantity---\emph{likely}, on $44$ matched molecules over three seeds. Whether that shared
  direction \emph{transfers} to molecules outside the fit is measured only against a zero-drift
  null, on a run for which no constant-offset control exists, and is graded \emph{open}
  (Table~\ref{tab:claims}). Whether the departure \emph{cancels} closure error---which would
  explain why more accurate inputs reduce accuracy---is not established: the only direct check we can
  run on retained checkpoints runs against it, on the wrong axis and with too little power to exclude
  partial cancellation (\S\ref{sec:surrogate}).
  \item The same reversal appears in a controlled synthetic family of models---where the penalty
  \emph{equals} the misspecification term by construction, so that sweep checks the instrument and
  does not test the hypothesis---and
  in a second chemical domain, the prediction of acid dissociation constants, where an in-distribution
  test reproduces both signs of the effect in at least 19 of 20 resampled splits---there through the
  input-insufficiency channel, so it replicates the sign rule and not the mechanism of (d).
\end{enumerate}

Table~\ref{tab:claims} gives set, value and grade in one place for the claims of
\S\ref{sec:paradox}, \S\ref{sec:measure} and \S\ref{sec:surrogate} and for the accuracy
comparison (\S\ref{sec:grading}, p.~\pageref{tab:claims}); \S\ref{sec:gateb}'s
downstream-correction result is graded where it is stated, and the second-domain and synthetic
results of App.~\ref{sec:pka} and App.~\ref{sec:dial} are supporting, and carry their scope in the
text instead of a grade. Sections~\ref{sec:framework}--\ref{sec:grading}
develop the instrument and the grading, Section~\ref{sec:results} reports the measurements, and
Section~\ref{sec:discussion} the limitations.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_overview.pdf}
% The grade and the two scope statements below are not caption ornament: a referee meets this
% figure before Table 2, and the art asserts "the model is misspecified" over the split. They are
% exempt from the caption word budget and must not be cut again.
\caption{\textbf{Overview.} \textbf{(a)}~A learned encoder predicts a charge-density profile
$\hat\sigma$; a fixed thermodynamic model turns it into a solubility, and the arrow marks the
evaluation-time substitution of the reference $\sigma^\star$, which makes the prediction
worse. \textbf{(b)}~The counterpart error on the
activity axis, as contiguous one-sided bounds, not intervals; broad IDAC set, deployed convention
(\S\ref{sec:measure}). \emph{Grade:} the separation margin that orders the two blocks has an
interval spanning zero under two of the three clusterings, so the ordering here is \emph{likely}
rather than resolved. \emph{Scope:} on the $n{=}60$ corner of (a)'s own reference database it is
weaker again; transfer of either to (a)'s finite-composition regime is argued, not measured; and the
training-time supervision that \emph{helps} is a separate result, not drawn in (a)
(\S\ref{sec:paradox}).}
\label{fig:overview}
\end{figure*}

\input{sections/related-work}

\section{Setup and theory}
\label{sec:framework}
For a solute~$s$ in solvent~$v$ at temperature~$T$, the target is $y=\lnx$, the
log saturated mole fraction. Dissolving a solid trades two costs against each
other---breaking the crystal lattice ($\Phicry$) and mixing the freed molecules
against non-ideal solute--solvent interactions ($\lng$)---and solid--liquid
equilibrium (SLE) sets the log-solubility to their negative sum:
\begin{equation}
  \lnx = -\underbrace{\frac{\dHfus}{\Rgas}\!\Big(\tfrac1T-\tfrac1\Tm\Big)}_{\Phicry(T)\ \text{(crystal)}}
         \;-\;\underbrace{\lng(x_2,T)}_{\text{activity}} ,
  \label{eq:sle}
\end{equation}
with the constant-$\Delta C_p$ (heat-capacity-of-fusion) correction omitted for
clarity. The activity term is produced by a \emph{differentiable closure} $g$:
a graph encoder predicts a $\sigma$-profile---the distribution of screening
(surface) charge density over a molecule, the histogram a COSMO model
consumes---for each molecule, and COSMO-SAC (the COnductor-like Screening
Model--Segment Activity Coefficient \cite{lin2002}, a reformulation of COSMO-RS)
maps the pair of profiles to $\lng$. Write $\zstar$ for the \emph{reference} physical latent, the externally referenced
$\sigma$-profile from the VT-2005 database \cite{mullins2006}, and $g(\zstar)$ for the closure output when fed the
reference profile. The model's own head instead predicts a \emph{learned} profile
$\hat z$ and evaluates $g(\hat z)$. We call each trained configuration, and each
closure variant we evaluate, an \emph{arm}; a \emph{DirectGNN} control arm shares the
encoder, interaction, readout, capacity and epoch budget but emits $\lnx$ with no
closure, and is the reference against which the cost of the physics bottleneck is
measured (schematic in SI, Fig.~\ref{fig:arch}). Their optimisation settings---dropout,
learning rate, gradient clipping, weight decay---were tuned per arm and differ, so the
gap is not attributable to the bottleneck alone.

\paragraph{The oracle substitution.} Feeding $\zstar$ in place of $\hat z$
(a $\sigma$-oracle) isolates the closure from encoder error: if the closure were
well specified, $g(\zstar)$ should be the best available activity estimate. The
paradox is that it is not.

\paragraph{Data.} Solubility measurements are a Bemis--Murcko scaffold split of
BigSolDB \cite{krasnov2025bigsoldb}, so test solutes share no scaffold with training; the activity
target $m$ is the experimental infinite-dilution activity coefficient ($\lngi$, IDAC) compiled from
ThermoML \cite{frenkel2011thermoml}, and the reference latent $\zstar$ is the VT-2005
$\sigma$-profile. Sources, the scaffold split and the matching rules are \S\ref{sec:methods} and
App.~\ref{sec:si-repro}.


The results below are stated for a general \emph{composed predictor} $\hat y=g(h(x))$
in which a fixed closure $g$ consumes a learned physical
intermediate $z=h(x)$, and are compared against a matched free predictor that shares the
encoder but drops $g$. Solubility, with $g$ the COSMO-SAC closure and $z$ the
$\sigma$-profile, is one instance; App.~\ref{sec:pka} gives a second (pKa through a fixed
Hammett closure), and App.~\ref{sec:dial} a controlled synthetic family. Nothing in the decomposition
of Lemma~\ref{prop:decomp}, in the identifiability and grounding-gap results stated in
App.~\ref{sec:si-proofs}, or in Proposition~\ref{prop:tax} is specific to COSMO-SAC. Two quantities
organise the section:
the closure/insufficiency split $B=\Bclos+\Binsuf$, which is the assumption-light
\emph{instrument}, and the observable grounding gap $\Ggap$, which is an \emph{indicator} of
closure misspecification whose validity we characterise.

\subsection{Why a free head can compensate: structural non-identifiability}
The crystal/activity split is not identifiable from solubility data alone, which is precisely why an
unconstrained head can silently absorb crystal error into the activity term. Under a symmetric
two-suffix activity class the crystal parameters $(\dHfus,\Tm)$ are unidentified from solubility
alone, and the deficiency is closed only by a rank-completing external constraint---a direct crystal
label, or a fixed activity slope (Lemma~\ref{prop:ident}, App.~\ref{sec:si-proofs}, which also gives
the two-temperature design argument, the zero profiled Fisher information and the finite-dilution
remainder).
This is the standard structural-identifiability picture for a collinear design
\cite{raue2009,villaverde2016}. What is specific to our setting is that this indeterminate
component is not projected out by construction but is left free under \emph{end-to-end} training of
the SLE-coupled latent (proved in App.~\ref{sec:si-proofs}, audited in \S\ref{sec:ident}). It is
\emph{enabling background} rather than a principal claim: it explains why grounding an
under-determined factor on external single-component data is necessary, and why compensation between
the factors is expected, and by itself says nothing about whether grounding helps. That depends on
whether the closure consuming the grounded inputs is correctly specified, which is the question we
make measurable next. How near-degenerate the remaining information is depends on the temperature
design rather than being a constant, and the non-identifiability is graded by the activity class
rather than binary: constraining the activity slope leaves $\dHfus$ finite but still unusable
against what external crystal labels give (Lemma~\ref{prop:semiparam}, App.~\ref{sec:si-proofs}; the
sweep, the noise levels and the operating point that fix it are \S\ref{sec:ident}).

\subsection{The closure--insufficiency decomposition: the instrument}
\label{sec:decomp}
Let $m$ be the activity target (experimental $\lngi$) and $g(\zstar)$ the closure
evaluated on the true latent. The risk of the reference-input path---its mean squared error
$\Ex[(m-g(\zstar))^2]$---decomposes exactly into a part the intermediate cannot resolve (the
irreducible Bayes term) and a part attributable to error in the fixed map; the excess over the Bayes predictor
$\Ex[m\mid\cdot]$ is the second term alone.

\begin{lemma}[Exact closure--insufficiency decomposition and its one-sided bounds]\label{prop:decomp}
For a \emph{fixed} (non-fitted) closure $g$, with $\zstar$ denoting the closure's \emph{entire}
argument (on the broad IDAC set of \S\ref{sec:measure} the profile pair together with the
temperature the closure is evaluated at),
\begin{equation}
\begin{aligned}
  \underbrace{\Ex\big[(m-g(\zstar))^2\big]}_{\text{ref.-input MSE}}
  &= \underbrace{\Ex\big[\Var(m\mid \zstar)\big]}_{\Binsuf\ (\text{irreducible})} \\
  &\quad + \underbrace{\Ex\big[(\Ex[m\mid\zstar]-g(\zstar))^2\big]}_{\Bclos\ (\text{decoder bias})} .
\end{aligned}
  \label{eq:decomp}
\end{equation}
The cross term vanishes identically because $\Ex[m\mid\zstar]-g(\zstar)$ is
$\zstar$-measurable and $\Ex[(m-\Ex[m\mid\zstar])\mid\zstar]=0$---which is why $\zstar$ must be the
closure's whole argument and not a part of it. The split is exact with no fitting of the closure
$g$---the cross term vanishes because $g$ is
fixed and $\zstar$ is externally observed---so the total and $\Bclos$'s Jensen lower bound are
fit-free; $\Binsuf=\Ex[\Var(m\mid\zstar)]$, however, must be estimated with a conditional-variance
smoother whose choice affects the verdict (\S\ref{sec:measure}).
\end{lemma}
\noindent Equation~\eqref{eq:decomp} is a bias--variance split of model
discrepancy in the sense of Kennedy--O'Hagan \cite{kennedy2001,brynjarsdottir2014}:
$\Bclos$ is the systematic discrepancy of a fixed physical map, and its dominance over
$\Binsuf$ is the empirical content of the claim that the accuracy ceiling is set by the closure
rather than by the inputs. The split itself is standard; what an externally grounded
$\zstar$ adds is that it can be \emph{measured} rather than assumed. Model-discrepancy
calibration \emph{fits} a correction from held-out observations, whereas here $g$ is fixed and
$\zstar$ externally observed, so the map's error is
\emph{attributed}, not corrected---available only where an external latent oracle exists, not in
gray-box hybrids. \textbf{Exactness is contingent on $g$ being fixed}: for a \emph{fitted}
decoder the cross term does not vanish and a plug-in point split is invalid, a spurious
cross term appearing as soon as $\Ex[m\mid\zstar]$ is replaced by a fitted estimate. We
therefore report \emph{one-sided bounds}, not a point split (proved in App.~\ref{sec:si-proofs}):
\begin{itemize}
  \item $\Bclos \ge (\Ex[m]-\Ex[g])^2$ by Jensen: a constant offset between the
  decoder's mean output and the target is misspecification, not input insufficiency, and
  this bound needs \emph{no smoothness assumption}. Its one requirement is that $m$ and $g$
  share the $\lngi$ reference state, which we check directly (\S\ref{sec:measure}).
  \item $\Binsuf \le \Ex[\Var(m\mid \mathrm{bin}(g(\zstar)))]$, a valid
  \emph{upper} bound (law of total variance, since a binning of $g(\zstar)$
  coarsens $\zstar$).
\end{itemize}
\emph{Bin edges.} We take interpolated (\texttt{numpy} default
\texttt{linear}) quantile edges of $g(\zstar,T)$ at $j/8$, $j=1,\dots,7$, assigning a value falling
exactly on an edge to the upper bin. Neither $n$ is divisible by eight, so standard implementations of
the same phrase disagree; the tie rule is a fourth analyst choice, beside the three of
\S\ref{sec:measure}. Enumerating by dynamic
programming every partition of the $g$-ordered rows into eight contiguous bins of sizes in
$\{\lfloor n/8\rfloor,\lceil n/8\rceil\}$, the broad set's margin lies in $[+0.499,+0.528]$ and the
corner's in $[-0.012,+0.232]$; ours ($0.697$ and $0.713$ for the bound) is an extreme of neither. The
choice is immaterial on the broad set and material on the corner, whose margin changes sign inside the
envelope; we accordingly read the corner's $+0.05$ as consistent with zero and carry the claim on the
broad set (App.~\ref{sec:si-tables}).
The Jensen constant-offset bound is convention-specific---it separates under the full combinatorial
convention but inverts under the deployed residual-only default---so the separation reported in
\S\ref{sec:measure} rests on the leakage-immune (that is, model-free) law-of-total-variance (LOTV)
bound, and the Jensen numbers are reported in App.~\ref{sec:si-tables}.
$\Binsuf$ is one population quantity while the two conventions coarsen two different statistics of
it, so both bounds are valid on it, and so is the smaller (App.~\ref{sec:si-proofs});
\S\ref{sec:measure}(i) fixes which we headline and \S\ref{sec:measure}(iii) states why the
2002-vs-2010 contrast is not a fixed-input decoder difference.

\paragraph{What population, what is conditioned on, and what sets the instrument's resolution.}
$\Binsuf$ is well posed only once the population over which the conditional variance is taken and the
variable conditioned on are named. The first decides whether it can be non-zero at all; the second
decides whether the identity and the bound hold.

The population is the \emph{measurement superpopulation}: a draw is a nominal compound pair
\emph{at a temperature}, together with the conformer, tautomer and protonation state actually
populated under those measurement conditions, and an independent experimental determination of
$\lngi$ for it. The conditioning variable is whatever the closure reads---its full argument. Where
$g$ consumes a temperature as well as a profile pair, as on the broad IDAC set of
\S\ref{sec:measure}, that argument is $(\zstar,T)$ and every statement below is read against
$\sigma(\zstar,T)$; conditioning on $\zstar$ alone would leave $g$ non-measurable with respect to the
conditioning variable, and neither the cross term's vanishing nor the coarsening bound would follow.
We write $\zstar$ for the argument throughout and name $T$ where it enters.
Neither the conformational state nor the replicate determination is part of that argument, so
$\Var(m\mid\zstar,T)$ is the spread of $\lngi$ across the conformational ensemble and across
replicate determinations that share a reference profile and a temperature, non-zero for physical
reasons unrelated to sample size. Experimental label noise $\sigma_\eta^2$ is a
\emph{component} of it, not a term beside it: an independent zero-mean error on $m$ enters
$\Var(m\mid\zstar,T)$ directly, so it is already inside every bound we report and is never added on.
What the deployed design cannot do is \emph{observe} it, neither set holding two rows at a common
argument (\S\ref{sec:si-repro}), so on these two sets $\Binsuf$ is reached only as an \emph{upper
bound}---by coarsening in the law-of-total-variance cells the headline reads, and out of fold in the
fitted estimators listed beside them (App.~\ref{sec:si-tables},
Table~\ref{tab:bounds})---and $\Bclos$ only lower-bounded.

Two consequences reconcile the bounding apparatus with
the injectivity of the design. The bounds are conservative in one direction only: were the
superpopulation variance in fact negligible, the finding would be the stronger
$\Bclos=\mathrm{MSE}$, so nothing we report can be an artifact of having over-estimated $\Binsuf$,
and no entry is the share of the error the inputs contribute. And the resampling frequencies below
are statements about the \emph{instrument}, not the superpopulation: the clustered-bootstrap
frequency $P_{\mathrm{boot}}$ is the fraction of cluster resamples in which the \emph{measured bound}
still falls below half the total error, $\mathrm{MSE}/2$ being the threshold that orders the two
terms (\S\ref{sec:measure}). It measures whether this design resolves the ordering, not the
probability that an inequality between
two population quantities holds. A low $P_{\mathrm{boot}}$ is loss of resolution, never evidence for
the inputs.

Resolution is governed by $\dim(\zstar)/n$, a conditional variance in $\dim(\zstar)$
dimensions being a nonparametric problem: the VT-2005 corner supports one-sided bounds only, and the
broad IDAC set is several times better resolved (\S\ref{sec:measure}(i), App.~\ref{sec:si-repro}).
Where the latent is a scalar the headline estimate is itself the out-of-fold residual rather than a
coarsening---an upper estimate too, with $\Bclos$ again only
lower-bounded (App.~\ref{sec:pka}).

\subsection{What the instrument resolves, and what it does not}
\label{sec:gap}
The decomposition (\S\ref{sec:decomp}) is the measurement the conclusions rest on---assumption-light, needing
neither a representable encoder nor a lossless bottleneck. Alongside it we report a cheaper, purely
observable quantity: the \emph{grounding gap} $\Ggap:=R_{\mathrm{orc}}-R_{\mathrm{e2e}}$, the
oracle risk minus the best end-to-end risk; the paradox is $\Ggap>0$. Under a representable encoder
(A1) and a lossless bottleneck (A2) it is sandwiched $0\le\Ggap\le\Bclos$, so an observed $\Ggap>0$
would certify $\Bclos>0$ (Theorem~\ref{thm:gap}, App.~\ref{sec:si-proofs}). But A1 is empirically
doubtful (the encoder is transfer-limited: a linear probe drops from train $R^2\approx0.76$ to test
$R^2\approx0.45$) and A2 is unverifiable and physically dubious; when A2 fails, $\Ggap>0$ conflates
closure misspecification with input insufficiency---a well-specified closure with a lossy latent gives
$\Bclos=0$ yet $\Ggap>0$ (App.~\ref{sec:dial}, and the pKa ortho sweep of App.~\ref{sec:pka}). We therefore
read $\Ggap$ as corroborating \emph{evidence} and base the model-dominance conclusion on the
decomposition: \emph{the decomposition is the instrument; $\Ggap$ is the indicator that motivates it}.

\paragraph{When a fixed prior loses; and scope.}\label{sec:tax} The deployed question---does the
\emph{trained} physics model outperform the \emph{trained} black box at sample size $n$?---follows a sign
rule. Writing each trained risk as an asymptote plus estimation variance,
$\Ex[\hat R_{\mathrm{phys}}(n)]\approx R_{\mathrm{e2e}}+V_{\mathrm{phys}}(n)$ and
$\Ex[\hat R_{\mathrm{direct}}(n)]\approx R_{\mathrm{direct},\infty}+V_{\mathrm{direct}}(n)$ with
$V(n)\downarrow0$, the \emph{physics penalty at budget $n$} is
$\mathcal{T}(n)\approx\Delta_\infty+[V_{\mathrm{phys}}(n)-V_{\mathrm{direct}}(n)]$ with
$\Delta_\infty:=R_{\mathrm{e2e}}-R_{\mathrm{direct},\infty}$ its \emph{asymptotic} part. Each physics-versus-control
difference reported below is a value of $\mathcal{T}(n)$ at the budget actually trained,
not of $\Delta_\infty$.
\begin{proposition}[Sign of the physics penalty]\label{prop:tax}
(i)~$\mathcal{T}(n)<0$ (physics helps) iff $V_{\mathrm{direct}}(n)-V_{\mathrm{phys}}(n)>\Delta_\infty$:
the variance a constrained prior saves must exceed its asymptotic bias. (ii)~If
$\Delta_\infty>0$ and the variance gap vanishes ($V_{\mathrm{direct}}-V_{\mathrm{phys}}\to0$),
there is a critical budget $n^\star$ beyond which $\mathcal{T}(n)>0$ for all $n>n^\star$.
\end{proposition}
\noindent The consequence for grounding is~(ii): a fixed-closure prior with strictly positive asymptotic
bias always loses once the budget passes $n^\star$ (this and a limiting refinement,
App.~\ref{sec:si-proofs}). There is no
``optimal grounding'' theorem, and the decomposition does not improve out-of-distribution accuracy
(\S\ref{sec:results}). Everything here is measured at infinite dilution, on two sets that differ in
regime (\S\ref{sec:measure}), and transfer of either to the finite-composition
regime in which the paradox is observed is a conjecture (\S\ref{sec:discussion}). We read these
results against existing work on model discrepancy \cite{kennedy2001,brynjarsdottir2014}, parameter sloppiness
\cite{transtrum2015,gutenkunst2007}, identifiability \cite{raue2009,villaverde2016}, misspecification
\cite{white1982}, underspecification \cite{damour2022}, and regularisation and null-space priors
\cite{engl1996,hansen1998,schwab2019}.

\input{sections/methods}

\section{Notation, and how the claims are graded}
\label{sec:grading}
Table~\ref{tab:notation} fixes the instrument's symbols and the coined vocabulary before the
Results use them; the thermodynamic
symbols of Eq.~\eqref{eq:sle} are defined there and not repeated. Where a paper spanning
two chemical domains would collide on one symbol, the table gives the renaming that resolves it.

\input{sections/notation-table}

Table~\ref{tab:claims} collects, as set, value and grade, the claims of \S\ref{sec:paradox},
\S\ref{sec:measure}(i)--(iii), \S\ref{sec:surrogate} and the accuracy comparison of this section;
the downstream-correction result of \S\ref{sec:gateb} is graded where it is stated, and the
supporting results of App.~\ref{sec:pka} and App.~\ref{sec:dial} carry their scope in the text
instead.
\emph{Certified} means it is exactly proved, is
bootstrap-certified across resamples of many clusters, or is a three-seed contrast whose per-seed
values do not overlap. \emph{Likely} means measured but short of that: a clustered-bootstrap
frequency that does not clear $0.95$ under every clustering, a quantity reached only through a
one-sided bound, a single-seed contrast, or a result resolved only on the matched
$n{=}60$/$n{=}44$ sets. \emph{Open} means
unestablished, directional, or conjectural; it is annotated with the direction of the evidence where
there is one (\emph{fails}, \emph{negative}, \emph{null}), and \emph{no claim} marks a difference we
decline to interpret because the arms were tuned separately. We make no accuracy claim in either direction: the two
arms were tuned separately (\S\ref{sec:methods}), so no difference between them is attributable to the
thermodynamic bottleneck, and that $\Delta\mathrm{MAE}$ is a difference between
pipelines. Bootstrap frequencies are quoted as bands---rounded to one decimal, with $>0.95$ and
$<0.2$ closing the ends---because with $17$ solvent and $41$ solute clusters on the corner they do not
carry two-decimal resolution (App.~\ref{sec:si-tables}); the two-decimal values are in the deposited
artifacts. The rule is a rounding rule, not a fixed vocabulary, so every frequency in the paper and in
Table~\ref{tab:claims} is quotable within it.

\section{Results}
\label{sec:results}

The instrument (\S\ref{sec:measure}) and the latent's departure from its reference (\S\ref{sec:surrogate})
are the two results the paper's conclusions rest on. On the two activity sets it is measured on, the
decomposition locates the ceiling of the deployed closure in the map rather than in its inputs---
\emph{likely}, in the grading of \S\ref{sec:grading}, and not resolved. Held against its
reference on $44$ matched molecules over three seeds, the
end-to-end latent no longer reports the physical quantity---\emph{likely} in that same grading
(Table~\ref{tab:claims}). The grounding paradox (\S\ref{sec:paradox})
is the observable effect that makes the failure visible but does not by itself carry the attribution;
whether a higher-fidelity closure (COSMO-SAC-2010/dsp) moves the ceiling we could not establish on
the broad IDAC set (\S\ref{sec:measure}). A controlled synthetic family sweeps closure fidelity
directly, where the response is true by construction and so checks the instrument rather than the
hypothesis (App.~\ref{sec:dial}); further diagnostics are in the SI.

% ---------------------------------------------------------------------------
% Claim ledger: one row per load-bearing claim, so a reader can see data set, n,
% estimator, value and grade without reconstructing them from the prose.
% `p' is in the specifier because this table sits within a few lines of the
% top-float height limit: without it, one extra wrapped row makes it unplaceable
% as a top float and it is deferred past the Discussion, taking every later
% starred float with it.
% ---------------------------------------------------------------------------
\begin{table*}[tp]
\centering
\caption{What is claimed, where it is measured, and how strongly. Grades are defined in
\S\ref{sec:grading}; the symbols, the two data sets and the conventions are Table~\ref{tab:notation}.
Notes \emph{a}--\emph{l} carry what qualifies a particular row or column.}
\label{tab:claims}
{\footnotesize
\begin{tabular}{@{}L{0.30\linewidth}L{0.21\linewidth}L{0.25\linewidth}l@{}}
\toprule
Claim & Set ($n$)\tnk{a} & Value\tnk{b} & Grade \\
\midrule
\multicolumn{4}{@{}l@{}}{\emph{The grounding paradox} (\S\ref{sec:paradox})}\\
Reference profiles hurt solubility at evaluation (MAE) & test split ($5608$), 3 seeds & $1.85\to2.25$ & certified \\
\ldots and still hurt when injected at training & cross-arm rows\tnk{c}, seed $42$ & $1.80\to1.98$ (own baseline $1.80\to2.23$) & likely \\
\ldots so part of that gap is test-time distribution shift & cross-arm rows\tnk{c}, seed $42$ & share $0.59$, single seed & open \\
Training-time $\sigma$ supervision \emph{helps} (MAE) & cross-arm rows\tnk{c}, 3 seeds & $2.04\to1.85$ & certified \\
\addlinespace[2pt]
\multicolumn{4}{@{}l@{}}{\emph{Closure against inputs on the activity axis} (\S\ref{sec:measure}(i)--(ii))}\\
$\Bclos>\Binsuf$, fair 2002 (LOTV\tnk{d}) & broad IDAC ($477$), UD & margin $+0.51$; $P>0.95/{\approx}0.9/{\approx}0.9$\tnk{e} & likely \\
\ldots\ same rows, full convention\tnk{d} & as above & margin $+0.95$; $P>0.95$ (all three) & likely \\
$\Bclos>\Binsuf$, fair 2002 (LOTV\tnk{d}) & corner ($60$), VT-2005 & margin $+0.05$, $P\approx0.7$ & likely \\
$\Bclos>\Binsuf$, 2010 on its \emph{own} typed latent & broad IDAC ($477$), UD & margin $+1.29$; $P>0.95/{\approx}0.9/{\approx}0.9$\tnk{e} & likely \\
$\Bclos>\Binsuf$, 2010 on its \emph{own} typed latent & corner ($60$), UD & margin $-0.77$, $P\approx0.3$ & open (fails) \\
\addlinespace[2pt]
\multicolumn{4}{@{}l@{}}{\emph{Whether a better closure moves the ceiling} (\S\ref{sec:measure}(iii))}\\
The $2002\to2010$ step lowers the error & broad IDAC ($477$) & $-0.99$, $P\approx0.2$\tnk{f} & open (negative) \\
\ldots but does on the corner's chemistry\tnk{g} & $57$ shared pairs, UD & $1.78\to0.77$, $P>0.95$ & likely \\
A fitted kernel residual transfers across solvents & corner ($60$), grouped folds & $-5\pm16\%$ & open (null) \\
\addlinespace[2pt]
\multicolumn{4}{@{}l@{}}{\emph{Mechanism} (\S\ref{sec:surrogate})}\\
The latent departs from its reference along a shared direction & matched molecules ($44$), 3 seeds & $51\pm2\%$\tnk{h} & likely \\
\ldots and that departure's top-two mode share exceeds a phase-randomised null & one grounded checkpoint, same $44$ & $0.40$ against $0.22\pm0.01$\tnk{i} & likely \\
\ldots and that direction \emph{transfers} to molecules outside the fit & matched molecules ($44$), 3 seeds & $4.1\pm0.5\times$ a zero-drift null\tnk{l} & open \\
That departure \emph{cancels} closure error & not measured\tnk{j} & --- & open \\
\addlinespace[2pt]
\multicolumn{4}{@{}l@{}}{\emph{Accuracy} (\S\ref{sec:grading})}\\
Physics arm vs.\ DirectGNN accuracy ($\Delta$MAE) & test split ($5608$)\tnk{k} & $+0.14$ & no claim \\
\bottomrule
\end{tabular}
\tabnotes{%
\tnk{a}\,Sets and profile databases: Table~\ref{tab:notation}; \emph{corner}, the $n{=}60$
VT-2005-matchable pairs at $298$\,K, \emph{broad IDAC}, the $477$ IDAC\,$\cap$\,UD measurements
over $185$ distinct pairs at all temperatures, UD the University of Delaware $\sigma$-profile
database. Resolution $\dim(\zstar,T)/n$ is given per set, per row and per distinct pair in
\S\ref{sec:measure}(i)--(ii) and App.~\ref{sec:si-repro}.\quad
\tnk{b}\,\emph{Margin} is $\mathrm{MSE}-2\Binsuf^{\mathrm{up}}$, positive when $\Bclos$ exceeds
$\Binsuf$; $P$ is a two-way (solute\,$\times$\,solvent) cluster-bootstrap frequency, reported as a
band (\S\ref{sec:grading}).\quad
\tnk{c}\,The cross-arm row set common to every arm of Table~\ref{tab:si-arms}.\quad
\tnk{d}\,LOTV rows use eight equal-count bins, the unbiased within-bin variance, and the
\emph{deployed residual-only} combinatorial convention on both sets and both kernels
(\S\ref{sec:measure}(i)); rows naming the full convention are comparisons.\quad
\tnk{e}\,Bootstrap band over pair\,/\,two-way\,/\,source clusters.\quad
\tnk{f}\,Deployed convention, dispersion off; under the full convention $-0.07$, $P\approx0.6$,
and $+0.06$, $P\approx0.6$ with dispersion on (Table~\ref{tab:kernels}).\quad
\tnk{g}\,At fixed profile database and temperature, full convention.\quad
\tnk{h}\,Magnitude not stable across analysis configurations (\S\ref{sec:surrogate}).\quad
\tnk{i}\,$\pm$ is the null's spread; the fine-tuning drift has no matched null of its own
(\S\ref{sec:discussion}, limitation~(v)).\quad
\tnk{j}\,The direct check is under-powered and runs against it (\S\ref{sec:surrogate}).\quad
\tnk{k}\,Separately tuned arms (\S\ref{sec:grading}).\quad
\tnk{l}\,Zero-drift null, with no constant-offset control on these seeds; the $\pm$ spans seeds at
one partition (\S\ref{sec:surrogate}).}}
\end{table*}


\subsection{The grounding paradox}
\label{sec:paradox}
% Artifacts: results/e5_sigma_grounding/seed_{42,43,44}/comparison.json (3-seed e5 lock, n=5608); aggregate THREE_SEED_SUMMARY.md via run_e5_comparison.py.
% Triple confirmation: eval-oracle / true-at-train / channel-swap.
The word ``grounding'' names two opposite operations on the same VT-2005 database, and they carry
opposite signs. Supervising the $\sigma$ head against the database \emph{during training} improves
solubility MAE from $2.043\pm0.040$ (ungrounded) to $1.846\pm0.053$ (grounded), a gain of $0.20$ over
three seeds (Table~\ref{tab:si-arms}). Substituting the reference profile for the learned one
\emph{at inference} degrades it, and that substitution alone is the paradox; which of the two causes
it fails from is not decided here but on the activity axis below. The effect erases any positive
$R^2$ (Fig.~\ref{fig:paradox}, p.~\pageref{fig:paradox}): the model predicts solubility better \emph{from its own learned
$\sigma$-profile} than when \emph{given the external reference one} (MAE $1.85$ vs $2.25$, in $\lnx$
units). The figure's eight arms are two non-COSMO controls---DirectGNN, which shares the encoder but
has no thermodynamic model, and NRTL, a more expressive fitted closure---and six COSMO-SAC arms:
five differing only in how the $\sigma$ head is trained and in what it is fed, and one
(\emph{grounded\,$+$comb.}) differing instead in the closure, which restores the
Staverman--Guggenheim combinatorial term (\S\ref{sec:methods}). Its tick
labels abbreviate the rest: \emph{grounded} is the learned-$\sigma$ arm;
\emph{ref.\ $\sigma$ (eval)} is the
$\sigma$-oracle, that checkpoint re-evaluated with the external VT-2005 profile substituted for the
learned one at test time, and \emph{ref.\ $\sigma$ (train)} is the co-adaptation control, the same
reference profile injected during training. The physics arms trail DirectGNN, whose optimisation settings
were tuned separately from theirs, so that gap is not attributable to the bottleneck
(\S\ref{sec:grading}); the two remaining bars are the co-adaptation controls below.

The headline $+0.41$ is the evaluation-only substitution, and part of it may have nothing
to do with the closure: swapping an input distribution at test time degrades a pipeline on its own. The
control that would remove that component injects the reference profile \emph{at training}, so the model
co-adapts; at seed $42$ it costs $+0.18$ ($1.803\to1.981$) where the evaluation-only substitution
costs $+0.43$ at that same seed ($1.803\to2.232$). One seed does not establish it, but the direction
points to part of the headline gap going with co-adaptation; the co-adapted arm is by design the
confound-free one. A channel-swap control costs $+0.27$, to $2.08$.
Both are worse than the learned-$\sigma$ arm, which rules out an implementation artifact. Both are
single-seed, so they fix a sign and not an effect size---and the same holds for the share they
imply. That share is $0.59$ on the matched seed-$42$ pair, and we report it as a
description of that seed rather than as a magnitude: its numerator was measured once, and a
between-seed spread the size of the learned arm's own would move it by about $\pm0.27$
(\S\ref{sec:si-tables}). Only the three-seed arms can therefore
be set against one another as magnitudes, and there the substitution costs about twice what
supervision gains ($+0.41$ against $-0.20$, both over the same three seeds). The co-adapted arm cannot
enter that comparison: its $+0.18$ is one and a half times the learned arm's own between-seed range,
and the supervision gain it would have to be ordered against is $0.24/0.08/0.28$ across the three
seeds of Table~\ref{tab:si-arms}, so the ordering reverses at one of them.\footnote{The three oracle-application
controls (evaluation-only, training-time injection, channel-swap; \S\ref{sec:methods}) are computed on
the same three-seed cross-arm row set as Table~\ref{tab:si-arms}; only the first is three-seed
(\S\ref{sec:si-tables}). Their per-arm predictions are released with the code.} That the
fault is a wrong \emph{map} rather than a missing input is measured on the activity axis, where the
reference input is clean (\S\ref{sec:measure}); there the fixed reference-input closure sits well
above the bound on what its inputs can fail to resolve, under-predicting $\lngi$ with a systematic
bias (SI, Fig.~\ref{fig:parity}).

\paragraph{Why the solubility paradox does not by itself carry the attribution.} The solubility effect is real and
certified (three seeds on the evaluation-only substitution, plus two single-seed controls), but it
cannot carry the closure-versus-input attribution, for a
data reason. Drug-like solutes are essentially absent from the VT-2005 reference set, so the reference
substitution falls almost entirely on the \emph{solvent} channel, and the both-channel substitution the
attribution would need covers only ${\approx}7$ solutes---too few to separate the cause either way.
Enlarging that set would mean computing new reference profiles rather than looking them up.
We therefore carry the causal claim on the activity axis, where the reference
input is clean and matched on both channels: the decomposition (\S\ref{sec:measure}) and the latent's
departure from its reference ($n{=}44$, \S\ref{sec:surrogate}). The title names that axis for this
reason.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_paradox.pdf}
\caption{Controlled comparison on the scaffold split ($n{=}5608$). Solid bars: three seeds,
mean $\pm$ a population standard deviation (\texttt{ddof}$=0$). The two hatched bars are the
co-adaptation controls, seed $42$ only, so they carry no error bar and no $R^2$, and keep an empty
slot in the right panel. Dashed in the left panel only, the DirectGNN reference. Arms:
\S\ref{sec:paradox}, Table~\ref{tab:si-arms}. \emph{Caveats:} the $\sigma$-oracle bar swaps the
input at evaluation only, so it carries an ordinary test-time distribution-shift component as well
as the closure effect---which is what the co-adaptation controls beside it remove; and the
DirectGNN control's optimisation settings were tuned separately from the physics arms'
(\S\ref{sec:framework}).}
\label{fig:paradox}
\end{figure*}

\subsection{The closure-fidelity axis: locating the ceiling, and moving it}
\label{sec:measure}
% Artifacts. Corner (n=60): results/b_insuff/decomposition.json via run_b_insuff_decomposition.py;
% bin-count / Bessel / convention grid with per-cell bootstrap: results/b_insuff/estimator_grid.json
% via scripts/analysis/run_b_insuff_estimator_grid.py; blocked-fold and distinct-pair estimators and
% the deployed adaptive-bin bootstrap: results/b_insuff/keystone_robustness.json via
% run_b_insuff_keystone_robustness.py; design composition: results/b_insuff/matched_pairs.csv and
% leverage_robustness.json.
% Broad IDAC set (n=477): results/b_insuff/representative_decomposition.json via
% scripts/analysis/run_b_insuff_representative.py (construction ladder, LOTV grid, kernel-contrast
% bootstrap); kernel MSEs cross-checked against results/b_insuff/fidelity_lever_fair2002.json.
% Round-2 design/provenance audit: results/b_insuff/representative_audit.json via
% scripts/analysis/run_b_insuff_representative_audit.py -- distinct-pair counts, pair-blocked
% out-of-fold estimators, within-pair variance across T, collapse to 185 design points, method and
% source-DOI composition, GC-only margin, DOI- and pair-clustered bootstraps, the 2002-vs-2010
% contrast split into the shared 57 and complementary 420 rows, the corner LOTV bound recomputed
% with z* = the UD profile pair (so the corner floor check is same-database), and both sets' floors
% under the 2010 kernel's own typed latent.
The externally-referenced decomposition of \S\ref{sec:decomp} is an \emph{instrument}: it separates a
fixed closure's error into a part in the map and a part in its inputs. This subsection runs it on two
data sets that must not be conflated---\emph{(i)}~the \emph{broad IDAC set}, $n{=}477$
infinite-dilution activity measurements over $185$ molecule pairs at all temperatures on the UD
profiles the
deployed COSMO-SAC-2002 kernel consumes there, and \emph{(ii)}~the \emph{corner}, the $n{=}60$ pairs
at $298$\,K for which the paradox's own reference database, VT-2005, supplies both profiles---and then
turns \emph{(iii)} to whether a better closure moves the ceiling. A better-resolved counterpart in
another domain is the scalar-latent pKa closure (App.~\ref{sec:pka}), where the same instrument
recovers the a-priori channel assignment (there, insufficiency)---evidence the instrument works, not
a re-confirmation of this verdict.

\paragraph{(i) The decomposition on the broad IDAC set ($n{=}477$).} The closure reads a
temperature as well as a profile pair, and is evaluated here at each row's own temperature, so the
conditioning variable is the closure's full argument $(\zstar,T)$---the concatenated UD
solute/solvent profile pair together with $T$, $103$ dimensions against $n{=}477$
($\dim/n\approx0.22$). Both the identity of Lemma~\ref{prop:decomp} and the LOTV bound are read
against \emph{that} $\sigma$-field, under which $\mathrm{bin}(g(\zstar,T))$ is a coarsening and the
cross term vanishes; conditioning on the profile pair alone would license neither, since $g$ is then
not measurable with respect to the conditioning variable. The target $m$ is the experimental $\lngi$,
so floor and closure error are measured on one data set and one profile database. Its chemistry is
small. No solute exceeds $12$ heavy atoms (median $7$),
against a median of $14$ and a maximum of $122$ for the drug-like solids of this paper's own
solubility corpus, and only $0.46\%$ of corpus rows carry a solute from this set. So \emph{broad}
names the reach of the $\gamma^\infty$ corpus the kernel
is calibrated and benchmarked on, not coverage of the chemistry a solubility model is deployed
against: the set covers the solvent side of deployment and not the solute side. Its construction
ladder, composition and provenance audit are in \S\ref{sec:si-repro}, and it is deposited as
\texttt{broad\_idac\_set\_477.csv} (Table~S1). Some analysis artifacts label the same table
\texttt{representative\_set\_477.csv}.

This set is matched to UD by InChIKey with a first-block fallback, looser than the corner's
canonical-SMILES rule, and a wrong $\zstar$ inflates $\mathrm{MSE}$ and so $\Bclos$: the asymmetry
runs against the ordering we claim, and it is measured rather than argued. On the $465$ exactly
matched rows the verdict is unchanged (App.~\ref{sec:si-tables}).

\emph{One convention rule, applied to both sets.} The margin depends on the combinatorial convention,
so we fix the rule before the numbers: headline the \emph{deployed} residual-only convention, pairing
$\mathrm{MSE}$ and the LOTV bound computed under that same convention, on the broad IDAC set and
on the corner alike. Headlining the deployed convention on the corner and the full convention here
would be the favourable choice on each set taken separately; one rule applied to both costs the
broad set's margin about half its value.

Under that rule the fair 2002 arm (Table~\ref{tab:kernels}) scores $\mathrm{MSE}=1.902$ against $\Var(m)=4.85$, the
LOTV bound gives $\Binsuf\lesssim0.70$ and hence $\Bclos\gtrsim1.21$, at eight equal-count bins under
the \emph{unbiased} within-bin variance: a separation margin of $\mathbf{+0.51}$. The full convention
on the same rows gives a margin of $+0.95$, reported throughout as the comparison rather than the
headline (Table~\ref{tab:decomp}). The margin is positive in all $56$ cells of the bin-count
$\times$ variance $\times$ convention grid, its smallest values at the coarsest conditioning where
the bound is valid but weakest (Table~\ref{tab:grid-rep}). Two of the three bootstrap clusterings
graze zero, so on the deployed convention the broad set's ordering is \emph{likely}, not
resolved---the grade Table~\ref{tab:claims} carries for it; under the full convention all three
clear. Collapsing to one row per distinct pair---$185$ design points, each the row nearest
$298$\,K---leaves the sign standing but costs the margin more than half its size and a band of its
bootstrap support: $+0.51$ at $P\approx0.9$ becomes $+0.27$ at $P\approx0.8$ on the deployed
convention ($+0.95$ at $P>0.95$ becomes $+0.69$, still $P>0.95$, on the full one). The verdict is
therefore not an artifact of counting repeated measurements of one pair as independent draws, and
neither is it sharpened by removing them (App.~\ref{sec:si-tables}). The tighter-of-the-two-conventions reading is reported in
Table~\ref{tab:decomp} but not headlined, a minimum over two finite-sample estimates being
anti-conservative even when each is valid in population.

Two caveats qualify the corroborating estimators, not the bound. Under random folds they leak---a
held-out row's own molecule pair sits in the training fold at a neighbouring temperature---and
blocking the folds on molecules costs them, though not equally on the two sets. On the corner it
leaves nothing informative: blocked on both molecules the forest reaches $3.65$ and the ridge
diverges, and the $k$NN variant taking its neighbour from a distinct pair reaches $1.83$---all past
that set's own $\mathrm{MSE}=1.47$. On the broad set, blocking on the pair moves the
forest from $0.15$ to $0.88$ and the ridge from $0.80$ to $1.47$, so the forest still clears the
$\mathrm{MSE}/2=0.951$ threshold and the ridge does not; blocking on the solute instead takes the
forest to $2.04$, past that set's $\mathrm{MSE}=1.90$ and so vacuous. The verdict rests on
the LOTV bound, which has no fold design to leak through, and not on the agreement of three
estimators. And the binning-free pair bound is not promoted, though it is thirteen times tighter
than the binning bound on the $359$ rows that support it, because its
multi-row pairs each come from a \emph{single} publication, so it samples no between-laboratory
component. Read together the two say the binning bound is slack rather than that $\Binsuf$ is
negligible. SI, Fig.~\ref{fig:decomp} plots every out-of-fold estimator either set's audit artifact
records---in every fold design it records, vacuous designs included---against that set's
$\mathrm{MSE}/2$ threshold, beside a selection of LOTV cells; Table~\ref{tab:bounds} names, values and
scopes those marks.

The labels are also stratifiable, which the corner's $60$ pairs were not: the chromatographic-only
cut that \emph{inverts} the margin on the enlarged $298$\,K corner (\S\ref{sec:discussion},
limitation~(i)) does not invert here under either convention, though it does not uniformly widen the
margin either. On the chemistry the deployed kernel has been most
widely exercised on, the ceiling is in the map---a statement about small-molecule $\gamma^\infty$
chemistry, and not about the drug-like solids of
the solubility corpus, which this set does not reach. The method and source composition, the
per-stratum values, the squared-error composition audit and the DOI- and pair-clustered bootstraps
are in \S\ref{sec:si-repro}.

\paragraph{(ii) The decomposition on the corner ($n{=}60$).} The corner is where the paper's other
reference-matched analyses live, and it is far weaker: all $60$ rows sit at $298$\,K, so $\zstar$ is
the profile pair alone and $\dim(\zstar)/n\approx102/60$ leaves the split behind one-sided bounds. It
spans $41$ solutes and $17$ solvents and is strongly unbalanced---methanol occupies $25$ of the $60$
rows, $13$ of them as the solvent and $12$ as the solute;
pyridine is the largest solvent stratum; and the two worst-fit pairs, $1$-hexanamine and
cyclohexylamine in methanol, carry $34\%$ of the squared error. Both rest on a single record and the
first sits far off its own homologous series, so we do not read that concentration as established
chemistry. The separation does not survive their deletion at the headline cell (margin $-0.002$); it
survives at coarser bins, under the maximum-likelihood variance and under the full convention
(App.~\ref{sec:si-tables}), but a corner margin of $+0.05$ cannot absorb
$34\%$ of the squared error leaving the sample, and we report the deletion as a failure of the
headline cell rather than as a robustness pass. These are not $60$ exchangeable observations, and the folds below hinge on the same
molecules. Of the $60$ pairs, $57$ also appear in the broad IDAC set at $298$\,K under UD profiles,
so the two are not independent samples---the corner is the sub-design on which VT-2005, and
therefore the paradox's own oracle, is defined (App.~\ref{sec:si-tables}).

% Shortening the caption and moving the corroborating-estimator rows to Table~\ref{tab:bounds}
% cut this table to about half a page, so it now sets as a two-column top float with body
% text beneath it. It previously carried `p' as well and took a float page of its own;
% restore `!tp' if a later edit regrows it past the top-float height limit.
\begin{table*}[!t]
\centering
\caption{Closure--insufficiency decomposition on the reference-input activity path, as bounds rather
than a point split. \textbf{The headline columns are the deployed residual-only ones on \emph{both}
sets}, one rule applied throughout (\S\ref{sec:measure}(i)); the full-convention columns are the
comparison.}
\label{tab:decomp}
% Width: the tabular must fit \textwidth (504.5 pt) inside table*. The stub is 0.34\linewidth
% and the body \footnotesize; the qualifications that used to sit in the stub (the corner's
% single bootstrap clustering, the estimator list, the label-noise statement) are lettered
% notes under the rule. Put a phrase back in the stub and the last column leaves the page.
{\footnotesize
\setlength{\tabcolsep}{6pt}
\begin{tabular}{@{}L{0.34\linewidth}rrrr@{}}
\toprule
 & \multicolumn{2}{c}{broad IDAC ($477$)\tnk{a}} & \multicolumn{2}{c}{corner ($60$)\tnk{a}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Quantity & \textbf{res}\tnk{b} & full\tnk{b} & \textbf{res}\tnk{b} & full\tnk{b} \\
\midrule
MSE$_{\text{total}}=\Bclos+\Binsuf$ (exact)      & $1.90$ & $1.91$ & $1.47$ & $1.80$ \\
$\Binsuf$ upper bound (LOTV)\tnk{c} & $0.70$ & $0.48$ & $0.71$ & $0.61$ \\
$\Bclos$ lower bound\tnk{d} & $1.21$ & $1.43$ & $0.76$ & $1.19$ \\
$\Bclos$ lower, constant-offset (Jensen)\tnk{e} & $0.18$ & $0.71$ & $0.43$ & $0.72$ \\
\quad separation margin\tnk{f} & $\mathbf{+0.51}$ & $+0.95$ & $\mathbf{+0.05}$ & $+0.58$ \\
\quad \dots\ bootstrap band\tnk{g} & $>\!0.95/{\approx}0.9/{\approx}0.9$ & $>\!0.95$ (all three) & ${\approx}0.7$ & ${\approx}0.9$ \\
\quad \dots\ maximum-likelihood variance\tnk{h} & $0.68/{+}0.53$ & $0.47/{+}0.96$ & $0.62/{+}0.24$ & $0.53/{+}0.74$ \\
\quad \dots\ tighter of the two conventions\tnk{i} & \multicolumn{2}{c}{$0.48/{+}0.94$} & \multicolumn{2}{c}{$0.61/{+}0.26$} \\
Further $\Binsuf$ upper bounds\tnk{j} & \multicolumn{4}{c}{Table~\ref{tab:bounds}} \\
Label noise $\sigma_\eta^2$ (IDAC) & \multicolumn{4}{l}{already inside every row above\tnk{k}} \\
\bottomrule
\end{tabular}
\tabnotes{%
\tnk{a}\,Broad IDAC, $n{=}477$ on UD profiles; corner, $n{=}60$ on VT-2005 profiles throughout
(\S\ref{sec:measure}(iii) scores the same pairs on UD).\quad
\tnk{b}\,\emph{full}, with the Staverman--Guggenheim combinatorial term; \emph{res},
residual-only.\quad
\tnk{c}\,Law of total variance, at eight equal-count bins of $g(\zstar,T)$ under the unbiased
(Bessel-corrected) within-bin variance. It fits no model to the data.\quad
\tnk{d}\,$\mathrm{MSE}-\Binsuf^{\mathrm{up}}$; this row decides the ordering.\quad
\tnk{e}\,$(\Ex[m]-\Ex[g])^2$.\quad
\tnk{f}\,$\mathrm{MSE}-2\Binsuf^{\mathrm{up}}$, the quantity that orders the two terms.\quad
\tnk{g}\,Pair\,/\,two-way\,/\,source clusters. The corner supports the two-way clustering only, so it
carries one entry per column.\quad
\tnk{h}\,Same rows and bins, maximum-likelihood within-bin variance, as $\Binsuf$ upper\,/\,margin.\quad
\tnk{i}\,Charged against the deployed MSE, as $\Binsuf$ upper\,/\,margin. Not headlined.\quad
\tnk{j}\,The marks of Fig.~\ref{fig:decomp}: out-of-fold RF and ridge (random and blocked folds),
$k$NN-in-$\zstar$, and the binning-free $\Ex[\Var(m\mid\text{pair})]$. Not a paper-wide inventory:
the bin grids behind the LOTV selection are Tables~\ref{tab:grid} and~\ref{tab:grid-rep}, and the
$\lngi$-stratified bounds, computed on corner subsets, are Table~\ref{tab:regime}.\quad
\tnk{k}\,A \emph{component} of $\Binsuf$, not a term beside it (\S\ref{sec:decomp}).}}
\end{table*}

\paragraph{The separation margin, and the choices it depends on.} Since the two terms sum to the total,
$\Bclos>\Binsuf$ follows whenever $\mathrm{MSE}$ exceeds the \emph{separation threshold}
$2\Binsuf^{\text{up}}$; the excess $\mathrm{MSE}-2\Binsuf^{\text{up}}$ is the \emph{separation margin},
and since the threshold uses an upper bound, a negative margin loses the separation rather than
reversing it. On the corner under the deployed residual-only convention and the \emph{unbiased}
within-bin variance, the LOTV bound gives $\Binsuf\lesssim0.713$ and $\Bclos\gtrsim0.759$---$1.472$
against a threshold of $1.427$, a margin of $\mathbf{+0.05}$. We headline that rather than the
maximum-likelihood variant, which biases the within-bin variance downward and so the margin upward
($+0.24$); on the corner the honest statement is that the ordering holds by a margin the design
cannot resolve. Four analyst choices set it: bin count and variance convention, swept in
Tables~\ref{tab:grid} and~\ref{tab:grid-rep} with a bootstrap frequency per cell; the combinatorial
convention, fixed by the rule of~(i) on both sets; and the bin-edge tie rule of \S\ref{sec:decomp}.
On the corner the margin is positive in $66$ of the $72$ cells and non-positive only at the coarsest
conditioning, where the bound is valid but weakest (App.~\ref{sec:si-tables}).

% Fold and leverage stress-tests: results/b_insuff/keystone_robustness.json and
% results/b_insuff/leverage_robustness.json via scripts/analysis/run_b_insuff_keystone_robustness.py
% and scripts/analysis/run_b_insuff_leverage.py.
% The both-roles methanol deletion (n=35) is in neither file; it is recomputed from
% results/b_insuff/matched_pairs.csv with lotv_binning() of run_b_insuff_decomposition.py
% (res convention): margin MSE-2*B_insuff = 0.346 -> 0.106 at 6 bins, 0.236 -> 0.003 at 8.
\emph{Leakage-immune} means here only that the bound fits no model; it does not mean the bound is
unbiased, and
the out-of-fold estimators control the remaining bias on neither set: blocking folds on both corner
molecules leaves nothing informative (RF $3.65$, above $\Var(m){=}2.47$; ridge divergent), and on the
broad set the leak is the near-twin row costed in paragraph~(i). They blunt the instrument rather
than refute the separation, and none supports it either---what eight times the data buys is a sharper
\emph{binning} bound, not a working out-of-fold one. On the corner the separation is accordingly
stable but \emph{not resolved}: at the headline cell all $17$ leave-one-solvent folds hold, four of
the $60$ leave-one-pair folds fail (minimum margin $-0.03$) and five of the $41$ leave-one-solute
folds fail (minimum $-0.22$, under the worse of the two within-bin variance conventions), while deletion of the eight
highest-error pairs holds where deletion of the two worst-fit pairs alone does not
(App.~\ref{sec:si-tables}; those two pairs are
named above). The separation rests on the maximum-leverage solute:
dropping methanol loses it, and deleting methanol from both roles holds only marginally and inverts
under the Bessel-corrected variance. A two-way cluster bootstrap places the margin above zero in
${\approx}0.7$ of resamples under the unbiased variance ($90\%$ margin spanning zero) and
${\approx}0.8$ under the maximum-likelihood one, and a deliberately aggressive out-of-fold ridge
loses the ordering. On the corner, then, ``the larger'' is estimator-conditional: the ordering holds
by the grade \emph{likely} and is not certain. The verdict rests on the broad IDAC set, whose
$78$ solute and $36$ solvent clusters hold the margin above zero in ${\approx}0.9$ to $>\!0.95$ of
resamples where the corner's reach ${\approx}0.7$--${\approx}0.8$. That margin is not resolved
either: at $+0.51$ it is $1.1$ times its own $90\%$ bootstrap half-width under pair clustering and
$0.46$ times it under the two-way one, which is why the broad set too carries the ordering at the
grade \emph{likely} (paragraph~(i)). What the corner adds is that the ordering is not overturned on
the sub-design where VT-2005 is defined.

Zero-mean label noise raises $\mathrm{MSE}$ and $\Binsuf^{\text{up}}$ alike, so it can understate
the separation but not create it (\S\ref{sec:decomp}). What the margin does bound is a label
systematic tracking $\zstar$, which enters
$\Bclos$; that threat is implausible on the maximum-likelihood binning and not excluded on the
Bessel-corrected one. Leverage folds, the noise arithmetic, the systematic's size and the convention,
leakage and $\gamma$-stratification stress-tests are in App.~\ref{sec:si-tables}.

\paragraph{Whether the closure defect is correctable, and where it fails chemically.} That $\Bclos$
dominates locates the ceiling in the map; it does not say the map is \emph{fixably} wrong. A small
structure-preserving residual on the fixed COSMO-SAC exchange-energy kernel, fit on the reference
VT-2005 profiles, removes $46\pm5\%$ of the reference-input error under random folds but $-5\pm16\%$
once folds are grouped so that no solvent is shared, and only the grouped design is free of leakage,
the closure error being predominantly a between-solvent systematic; stated as a bound rather than a
null, that design excludes a cross-solvent reduction above about $30\%$. $\Bclos$ is therefore a
defect a handful of kernel parameters can absorb \emph{within} a solvent and that we have not shown
to be correctable across solvents---weaker than a general repair---and the fitted correction buys
nothing over the free $2002\to2010$ step below (fold design, rank ordering and the standard
errors' scope in App.~\ref{sec:si-tables}). Nor is the residual error an implementation artifact: our
differentiable layer reproduces the NIST benchmark COSMO-SAC-2002~\cite{bell2020cosmosac} to RMSE
$0.0035\,\ln\gamma$ on identical VT-2005 profiles, with self-solvation exactly zero. Yet $90\%$ of the
squared error falls on strong hydrogen-bond-donor solvents ($84\%$ once the two doubted
amine/methanol pairs are deleted; acceptor-only near-exact), placing $\Bclos$ in COSMO-SAC's
documented single-parameter H-bond kernel---the specific defect the donor/acceptor-typed 2010 closure
was built to fix---and adding the physically \emph{more} complete Staverman--Guggenheim combinatorial
term \emph{increases} error ($1.47\to1.80$) on size-asymmetric pairs, which is what a misspecified map
does and not what missing information would.

% Artifacts: results/b_insuff/fidelity_lever_fair2002.json via scripts/analysis/run_fidelity_lever_fair2002.py; results/b_insuff/closure_flip_ci.json via scripts/analysis/run_closure_flip_ci.py.
% Round-2: block split of the kernel contrast (shared 57 / complementary 420) and both sets' floors
% under the 2010 kernel's own typed latent, all in results/b_insuff/representative_audit.json.
\paragraph{(iii) The closure-fidelity lever: present on the corner's chemistry, bounded on the
broad set's chemistry.} If the ceiling is the closure, the obvious remedy is a better closure. The
standard $2002\to2010$ step delivers one on the corner ($n{=}60$) and not on the broad IDAC set
($n{=}477$), and we can now say which factor carries that reversal.

The two kernels have no \emph{prescribed} common input: $\sigma$-profile averaging is part of each
model's definition (Mullins for 2002, Hsieh for 2010~\cite{bell2020cosmosac}), so evaluating each on
its native profile is the specified use of the model. An input-fixed \emph{control} nonetheless
exists (App.~\ref{sec:si-tables}): holding database, cavity area, volume and averaging fixed and
varying only the kernel gives $2.047\to0.765$ on the corner ($P>0.95$), so the $2002\to2010$ step survives
with the input held fixed, though that control cannot separate the donor/acceptor typing, which the
2002 kernel has no channel to consume and which is therefore charged to the kernel by construction.
Its $2002$ endpoint is $2.047$ and not the $1.757$ of the \emph{fair 2002} arm below because
re-averaging the 2002 input onto the Hsieh profile costs 2002 $+0.29$ on its own; the input-fixed
step ($1.28$) is accordingly \emph{larger} than the native-input step ($0.99$), so the control is
favourable to the conclusion it supports, and that averaging step is itself unresolved
(App.~\ref{sec:si-tables}).
One implementation detail fixes the 2002 arm: the NIST reference implementation, handed a typed
three-profile database, silently reads the nonpolar channel only (MSE $1.26$ on the matched corner),
which is not the reference closure, so the \emph{fair 2002} arm is $1.757$ from our NIST-validated
layer (\S\ref{sec:si-methods}).

Table~\ref{tab:kernels} gives the three arms---\emph{fair 2002}, \emph{2010, dispersion off} and
\emph{2010, dispersion on}---on both sets under both conventions. The standard
$2002\to2010$ step more than halves the corner's error under the full convention ($P>0.95$) and cuts
it $42\%$ under the deployed one, so ``more than halves'' is a full-convention statement; on the
broad IDAC set the same step is not an improvement under either rule, and the $3\%$ the dispersion
term recovers there is a null whose interval is an order of magnitude wider than the effect, not a
measured equality. The \emph{2010, dispersion on} arm is quoted from \texttt{cCOSMO} and its corner run is on
$n{=}57$, three bromine- or iodine-containing pairs having no tabulated dispersion value: a coverage
limit of the model offered as the lever, and the broad set's construction excludes the same three
pairs (\S\ref{sec:si-methods}). Because the two dispersion arms therefore sit on different row
counts in the corner column, the $n$-matched control belongs beside them: on those common $57$ the
\emph{dispersion off} arm scores $0.767$, so the $0.765$-versus-$0.785$ ordering is not an artifact
of the differing $n$. No claim rests on that ordering. We run the kernel comparison under the full convention because that
is where the two kernels' published error figures live, and record the deployed-convention reading
rather than leave the two rules mixed.

The reversal is the \emph{chemistry} and neither the profile database nor the temperature range:
with UD profiles, one extraction and one convention throughout, the \emph{2010, dispersion off} arm wins on the $57$
pairs shared with the corner, does not win on the complementary $420$, and still does not win on the
$80$ complementary rows that are \emph{also} at $298$\,K, so the temperature range does not account
for it; that block carries $P\approx0.3$ and no interval, and the three blocks were not tested against
one another (Table~\ref{tab:kernels}). The corner is a low-$\gamma$, hydrogen-bond-dominated
sub-design---methanol and pyridine touch most of it---which is exactly the regime the
donor/acceptor-typed 2010 kernel was built for, and where the error localises above. The lever is
therefore bounded rather than absent: on the corner's chemistry a
free $2002\to2010$ step removes most of $\Bclos$, direct evidence that $\Bclos$ \emph{is} reducible
by a better closure there. What we cannot demonstrate is that the same step reduces it on the
broad set's chemistry, where it does not.

Table~\ref{tab:kernels} runs on UD profiles on both sets, the corner included, while
Table~\ref{tab:decomp} scores the corner on VT-2005: the profile database is the whole difference
between that table's corner/full $1.80$ and the $1.757$ here, on the same $60$ pairs. The floor
checks are accordingly same-database, same-latent \emph{and} same-convention, each stated under the
deployed rule of~(i) with the full-convention reading beside it: $+0.51$ at $P\approx0.9$ on the
broad IDAC set, and $+0.05$ on the corner scored VT-2005 against VT-2005 (Table~\ref{tab:decomp});
the corner's \emph{UD-native} floor check, the one commensurable with the broad set, is under the
rule we deploy at zero, and charged against a UD-native floor the $1.757$ clears at $+0.69$ under
the full convention (App.~\ref{sec:si-tables}). Earlier drafts paired the UD value with the VT-2005
floor; that splice is removed. Under the 2010 kernel's own typed latent, which
is $306$-dimensional and so several times worse resolved, the ordering is \emph{lost} on the corner
(margin $-0.77$) and holds on the broad set ($+1.29$; \emph{likely}, not certified). Because
$\Bclos>\Binsuf$ is one-sided in the direction ``the closure error is large'', degrading the closure
makes the test easier to pass and the grade does not move; losing the ordering on the corner is loss
of resolution and not a certified flip to the inputs, a flip needing a \emph{lower} bound on
$\Binsuf$, which an upper bound that has risen past $\mathrm{MSE}/2$ does not supply. The full arithmetic, under both conventions and all
three clusterings, is in App.~\ref{sec:si-tables}.

We know of no large-scale infinite-dilution benchmark of the $2002\to2010$ step (search scope in
App.~\ref{sec:si-tables}), and our search was not exhaustive, so this is an absence of evidence
rather than evidence of absence. The broad-set null lies within the range of magnitudes the
comprehensive assessment~\cite{fingerhut2017} does report (App.~\ref{sec:si-tables}), and that
family's \emph{best} infinite-dilution
accuracy---a mean absolute deviation of $86\%$, achieved by its own developers---is itself external
evidence that the closure
is the low-fidelity component. We claim only that \textbf{the fidelity lever we can demonstrate is
confined to the corner's chemistry}; the broad set's null is additionally a small-molecule and
glycol-heavy one (composition audit, \S\ref{sec:si-repro}), so on drug-like solids the lever is
untested rather than absent.

% Relocated from prose (move 12 of the 2026-07-28 relocation plan): nine multi-value comparisons
% across three kernels, two sets and two conventions. The sweeps behind it stay in App. C.
\begin{table*}[t]
\centering
\caption{Activity error (MSE, $\lngi$ units) of the three COSMO-SAC arms, and the block split that
reads the $2002\to2010$ reversal against profile database and temperature held fixed.}
\label{tab:kernels}
{\footnotesize
\setlength{\tabcolsep}{6pt}
\begin{tabular}{@{}lcccc@{}}
\toprule
 & \multicolumn{2}{c}{corner ($60$), UD\tnk{b}} & \multicolumn{2}{c}{broad IDAC ($477$), UD\tnk{b}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
(a) Arm\tnk{a} & full\tnk{b} & res\tnk{b} & full\tnk{b} & res\tnk{b} \\
\midrule
fair 2002                    & $1.757$ & $1.436$ & $1.911$ & $1.902$ \\
2010, dispersion off         & $0.765$ & $0.831$ & $1.978$ & $2.893$ \\
2010, dispersion on          & $0.785$\tnk{e} & ---\tnk{d}     & $1.855$ & ---     \\
\addlinespace[2pt]
$P$ ($2010$ better)\tnk{c}, dispersion off & $>\!0.95$ & --- & ${\approx}0.6$ & ${\approx}0.2$ \\
\quad $90\%$ margin\tnk{c}          & $[+0.06,+2.18]$ & --- & $[-1.95,+0.93]$ & $[-3.06,+0.70]$ \\
$P$ ($2010$ better)\tnk{c}, dispersion on & ---      & --- & ${\approx}0.6$ & --- \\
\quad $90\%$ margin\tnk{c}          & ---       & --- & $[-2.24,+1.31]$ & --- \\
\midrule
(b) Block (UD profiles, full convention) & \multicolumn{2}{c}{$2002\to2010$, dispersion off} & $P$\tnk{c} & $90\%$ margin\tnk{c} \\
\midrule
shared with the corner ($57$)      & \multicolumn{2}{c}{$1.783\to0.767$} & $>\!0.95$ & $[+0.06,+2.36]$ \\
complementary ($420$)              & \multicolumn{2}{c}{$1.928\to2.143$} & ${\approx}0.5$ & $[-2.41,+0.87]$ \\
\quad \dots\ of these, at $298$\,K ($80$) & \multicolumn{2}{c}{$0.796\to1.284$} & ${\approx}0.3$ & --- \\
\bottomrule
\end{tabular}
\tabnotes{%
\tnk{a}\,The three arms, named here and in the text: \emph{fair 2002}, COSMO-SAC-2002 in our
NIST-validated layer reading all three profile channels; \emph{2010, dispersion off} and \emph{2010,
dispersion on}, COSMO-SAC-2010 without and with the dispersion term (\S\ref{sec:measure}(iii)).\quad
\tnk{b}\,\emph{full}\,/\,\emph{res} as in Table~\ref{tab:decomp}. \emph{Both} sets are scored on UD
profiles here, the corner included---Table~\ref{tab:decomp} scores the corner on VT-2005 instead,
and that database difference is the whole difference between its corner/full $1.80$ and the $1.757$
here (\S\ref{sec:measure}(iii)). Within a set, each arm uses the $\sigma$-profile averaging its own
model prescribes (Mullins for 2002, Hsieh for 2010).\quad
\tnk{c}\,$P$ is the two-way cluster-bootstrap frequency that the $2010$ arm is the better one, with
the $90\%$ margin on the paired squared-error difference beneath it.\quad
\tnk{d}\,A dash marks a cell the deposited artifacts do not carry.\quad
\tnk{e}\,This one cell is $n{=}57$, not $60$: \texttt{cCOSMO} tabulates no dispersion $\epsilon$ for
the three bromine- or iodine-containing corner pairs. On those common $57$ the \emph{dispersion off}
arm scores $0.767$, so the $0.765$-versus-$0.785$ ordering is not an artifact of the differing $n$,
and no claim rests on that ordering (\S\ref{sec:measure}(iii)).}}
\end{table*}

\paragraph{The reference is itself imperfect, and what that makes $\Bclos$ a property of.} The
external ground truth used to charge $\Bclos$ against $\Binsuf$ is quantum chemistry, not experiment:
the VT-2005 profiles are computed for a single reference conformer and protonation state
(\S\ref{sec:si-repro}), and the successor Delaware database revises those conformations for about a
third of its compounds~\cite{fingerhut2017}. That is the gap between $\zstar$ and the state actually
populated under the measurement, which is why the superpopulation of \S\ref{sec:decomp} has a
non-degenerate $\Binsuf$ at all---but the mismatch does not load on $\Binsuf$ alone. If the reference
is displaced from the state actually populated, even a perfectly specified $g$ carries a curvature
term in $\Bclos$ (\S\ref{sec:si-repro}); we do not bound that share, which scopes what the attribution is
\emph{about}: $\Bclos$ as measured here is the misspecification of the composite object, the kernel
applied to one fixed computed conformer, not of the kernel at the true conformational ensemble. The
deployed pipeline is that same composite, so this is the quantity a practitioner faces; it is not
a statement about the kernel in isolation. The corner measurement is
additionally a low-$\gamma$/$298$\,K one, and neither set transfers to the finite-composition regime
the paradox lives in (\S\ref{sec:discussion}).

\subsection{The status of the closure's known defects}
\label{sec:badclosure}
That COSMO-SAC-2002 is a low-fidelity closure is the contribution rather than a qualification of the
result. The 2002 kernel is what a practitioner adopts by default,
and a closure's fidelity is \emph{not knowable in advance} on a new system. The field routinely
composes an expressive encoder with a fixed mechanistic map and assumes the map is good enough. This
paper reports what follows when it is not: the learned latent silently departs from the quantity it
is named after (\S\ref{sec:surrogate}), and neither that departure nor its cause is visible without
an external oracle.

\input{sections/compensation-surrogate}

\subsection{One downstream correction: a gated output residual restores calibration, not accuracy}
\label{sec:gateb}
% Artifacts: results/e5_sigma_grounding/seed_42/grounded_a_truetrain_residual{,_v2}_predictions.summary.json (Gate B v1 gated-shut / v2 forced-open); model.py correction_force_open_gate.
If the closure binding were merely a bad point estimate, a learned output-space correction on top of
the physics prediction should recover accuracy. It does not: an additive bounded residual with a
learned confidence gate leaves the gate effectively shut, and forcing it open removes the systematic
\emph{bias} ($+0.45\to+0.06$) but recovers little \emph{accuracy} ($R^2$ $0.236\to0.274$ on the same
rows). This is the classical calibration-versus-accuracy separation---a post-hoc map can fix
reliability but not resolution when the upstream map is misspecified---as expected if the closure,
not the estimate, is the binding constraint. Both arms are seed $42$ only, so in the grading of
\S\ref{sec:grading} this is \emph{likely}, and it is a statement about the one correction tested:
an additive bounded residual on the output, gated, on this closure and this corpus. Other
downstream corrections, and this one at other seeds, are untested here.

\section{Discussion}
\label{sec:discussion}
The result is a cautionary measurement for physics-informed modeling, about one of the two operations
the word ``grounding'' names. Supervising a learned physical latent on reference values during
training helps ($2.04\to1.85$ in $\lnx$ MAE). \emph{Supplying} those reference values as an input at
inference hurts ($1.85\to2.25$ substituted at evaluation, three seeds; $1.80\to1.98$ with
co-adaptation, at one seed and against that seed's own baseline), and it
hurts because the fixed map that consumes them is misspecified: when that map is of unknown
fidelity---and the standard route does not improve it---better inputs surface its bias rather than
help. Used as an instrument, the decomposition places the ceiling of the deployed kernel in the
closure rather than its inputs on $477$ activity measurements over $185$ molecule pairs, scored
against the profile database that kernel actually consumes, by a margin of $+0.51$ under the
convention the pipeline deploys---positive in all $56$ cells of the bin-count $\times$ variance
$\times$ convention grid, but with an interval
that spans zero under two of three clusterings, so the ordering there is likely and not resolved.
Whether that ceiling is a lever depends on the chemistry: the $2002\to2010$ step cuts the error on
the hydrogen-bonding corner by more than half under the literature-standard convention and by $42\%$
under the deployed one, where the decomposition also localises the defect, and is not an
improvement on the broad set's chemistry under either convention---the step measures $-0.99$
deployed and $-0.07$ literature-standard with dispersion off, $+0.06$ with it on, none of the three
resolved---while the step does win on the $57$ pairs the two sets share.
Both statements are scoped by what these two sets are. The corner is the sub-design on which the
paradox's own reference database, and hence its oracle, exists; the broad set is the $\gamma^\infty$
chemistry the deployed kernel has been most widely exercised on---no solute above $12$ heavy
atoms, and glycol-heavy (composition audit, \S\ref{sec:si-repro}).
Neither is the deployment regime, so the fidelity null is a null on small-molecule chemistry,
the lever remains untested on drug-like solids, and the attribution is an infinite-dilution one whose
transfer to the finite-composition regime the paradox spans is a conjecture supported by in-regime
signs, not a measured fact.

\input{sections/blackbox-probe}

\paragraph{What the contribution is.} The unit of contribution is the \emph{instrument}, not any single
measurement. Its two ingredients are individually standard---the closure--insufficiency split is a
law-of-total-variance identity, and the sign of grounding's effect follows the model-discrepancy principle
\cite{kennedy2001,brynjarsdottir2014}---and what is new is their combination into a usable measurement.
Because a learned physical latent admits an \emph{external oracle}, the two error terms separate on a
\emph{fixed} operator by an identity, with the insufficiency side bounded rather than
point-identified, and the resulting sign rule can be tested \emph{a priori} and
\emph{bidirectionally}; the pKa domain supplies that test as an in-distribution sign flip, consistent
across splits ($20/20$, $19/20$) and reaction series ($4/4$, $3/4$; App.~\ref{sec:pka})---with only
four series the bootstrap resolves nothing finer than that agreement, a sign test at $p{=}1/16$ on
the pole where grounding hurts. This oracle-on-a-fixed-operator affordance is what the
concept-leakage literature lacks \cite{parisini2025,espinosa2025leakage}, where the intermediate has
no ground truth and leakage is inferred only indirectly. The solubility results are the instrument's
first application, graded in Table~\ref{tab:claims}; their transfer out of the matched regime is not
established.

\paragraph{Why we make no accuracy claim, and what remains after that.} The physics and control arms
were tuned separately, the DirectGNN control's value standing first in each pair---dropout $0.174$
against $0.012$, phase-2 learning rate $8.6\times10^{-4}$
against $8.5\times10^{-5}$, weight decay $4.2\times10^{-5}$ against $5\times10^{-4}$---and no
hyperparameter-matched control was run (\S\ref{sec:si-methods}). A global $\Delta\mathrm{MAE}$ of
$+0.14$ between pipelines whose learning rates differ by an order of magnitude carries no information
about the thermodynamic bottleneck, and neither do the per-solvent-class map, the per-solute-class
map, the novelty inversion or the data-efficiency trend, all of which resolve the \emph{same}
difference along chemical axes. We therefore claim no accuracy result in either direction; those
strata are reported in the SI (\S\ref{sec:map}, \S\ref{sec:data-efficiency}) as hypothesis-generating
descriptions of where two particular pipelines differ, each carrying that caveat, and no conclusion in
this paper rests on them. Running the matched control is the obvious repair and we have not run it.
What survives the confound is every \emph{within-checkpoint} contrast, where the two sides
share one trained model and one tuning: the $\sigma$-oracle substitution (\S\ref{sec:paradox}), the
closure/insufficiency decomposition, which uses no trained model at all (\S\ref{sec:measure}), the
kernel-fidelity and fitted-residual comparisons, the latent's departure from its reference
(\S\ref{sec:surrogate}), and the output-residual control (\S\ref{sec:gateb}). Separately, and not as
an accuracy claim about the bottleneck: we did not find a configuration in which the physics path
attains a lower error than the control---external state-of-the-art models (FastSolv, SolProp) do no
better on a by-solute split, which bounds the task's difficulty and does not rank the models against
one another (App.~\ref{sec:si-baselines}), and grounding the
crystal factor on external labels did not move the ceiling (\S\ref{sec:e2}). The one place the
physical \emph{structure} helps is temperature extrapolation, as a hypothesis-class property the
trained models do not exploit (\S\ref{sec:supporting}). Two structural facts are consistent with
that: the crystal/activity split is non-identifiable from solubility alone (Lemma~\ref{prop:ident}),
and the activity closure is misspecified where we can measure it (\S\ref{sec:measure}).

\paragraph{When a fixed-closure prior helps versus hurts.} The penalty sign rule (Prop.~\ref{prop:tax})
predicts \emph{when} to expect these negatives: a fixed-closure prior helps only when the estimation
variance it saves exceeds its asymptotic bias $\Delta_\infty$, both measurable. The ``grounding helps
iff the closure is well specified'' reading is thus a statement about the \emph{bias} channel; even a
true-valued input need not give the lower total error, because a fitted nuisance can be the more
\emph{efficient} estimator, which Prop.~\ref{prop:tax} carries in
its variance term (App.~\ref{sec:si-proofs}). That benefit is largest at small $n$ and vanishes as data accumulate,
whereas $\Delta_\infty$ is set by the closure, so a prior is predicted to hurt precisely in the
mature-data, low-noise, well-covered regime. The fidelity axis can be swept directly outside
chemistry: in a synthetic sweep composing a teacher of known form with a closure of controlled
fidelity $F$, reference inputs hurt monotonically more as $F$ falls (Fig.~\ref{fig:dial};
App.~\ref{sec:dial})---which is true by construction there, the penalty and the misspecification
term being the same quantity, so the sweep verifies the instrument rather than the
hypothesis---while the pKa closure (App.~\ref{sec:pka}) exercises the sufficiency channel on
real data. Whether grounding helps is shaped by \emph{two} axes, closure fidelity and how the latent
is trained; characterising that plane on real systems is future work. Where the chemical strata do
form a pattern---the closure heaviest on the most \emph{familiar} solutes and in strongly
hydrogen-bonding solvents, reversing sign in the acceptor-dominated amides it was built for---it is
the direction the sign rule would give, but separately tuned pipelines can produce it for reasons
that have nothing to do with the prior, and there is no multiplicity control across strata families;
we record it in \S\ref{sec:map} as the experiment a matched control should run, not as evidence, and
the diagnostic is \emph{proposed} rather than validated.

% Limitation (i) enlarged-corner numbers: run_b_insuff_decomposition.{match_pairs,evaluate_closure,
% lotv_binning} (res convention, 8 equal-count bins) on notebooks/data/raw/idac_expanded{,_raw}.csv
% against results/sigma_profile_artifact/sigma_profiles.csv; the same call reproduces the deployed
% n=60 corner (MSE 1.4723, B_insuff 0.6180, margin +0.2362) exactly. Expanded: n=115, MSE 1.2553,
% B_insuff 0.4545, margin +0.3463; gas-chromatography-only subset n=100, MSE 0.9250, B_insuff 0.5274,
% margin -0.1298. Provenance of the n=60 pairs: notebooks/data/raw/idac.csv DOI counts 33/20/7 (all
% Chromatography). Corner size audit: results/idac_expansion_audit/summary.json.
% Limitation (i) InChIKey alternative: results/b_insuff/decomposition.json records
% n_pairs_inchikey_match=85, n_solutes_inchikey=46, n_solvents_inchikey=19, computed by
% run_b_insuff_decomposition.py on the same 298 K +-1 K window as the deployed n=60 match.
% Limitation (vi) scaffold geometry: Murcko scaffolds (blackbox_study4.murcko) over the 147 solutes
% of the has_solubility test rows -> 124 scaffolds, 11 multi-member (sizes 2x7, 3, 4, 5, 8) holding
% 34 solutes and 492 of 5608 rows (8.8%).
\paragraph{Limitations.} (i)~The phenomenon and its causal attribution lie in \emph{different
regimes}: the paradox is measured where the model operates (finite composition, all $\gamma$,
$n{=}5608$), the closure--insufficiency attribution and the latent-departure result only on
infinite-dilution activity data ($n{=}477$ across temperatures, and the $298$\,K, low-$\gamma$,
VT-2005-matched $n{=}60$ and $n{=}44$ sets). Transfer back to the paradox's own regime, argued above
from the in-regime signs, is a conjecture rather than a measured fact and is the principal open
question here. The matched $n{=}60$ set is also narrowly sourced: all by gas chromatography and all
from three publications, $33$ pairs from one, so neither a method-stratified nor a source-clustered
re-estimate is available on it. The corner margin is also extraction-sensitive: a broader ThermoML
pull at $298$\,K widens it, but \emph{inverts} it on the gas-chromatography subset, so we do not
adopt that pull \emph{as a replacement corner} and we leave the sensitivity unresolved. It is not a
verdict on the extraction, which matters because the broad IDAC set is built from the same pull: it
does not move the same way under the same cut, and its margin survives clustering on sources and on
molecule pairs (\S\ref{sec:measure}(i)). What differs is resolution and not provenance, so we use
the enlarged pull where it is stratifiable and keep the strict $n{=}60$ corner where the paradox's
own oracle is defined; the matching-rule alternatives and the full arithmetic are in
\S\ref{sec:si-repro}. Because
few drug-like solutes carry a VT-2005 profile, the solubility substitution also falls almost entirely
on the solvent channel; the clean both-channel evidence is on the activity axis. Under the deployed
convention the margin is not resolved on either set---$+0.05$ with clustered-bootstrap support
${\approx}0.7$ on the corner, $+0.51$ with two of three clusterings grazing zero on the broad IDAC
set; only the literature-standard convention resolves it ($+0.95$), and the headline convention is
the deployed one (Table~\ref{tab:claims}; \S\ref{sec:measure}(i)). The broad IDAC
set is in any case not the set on which the paradox's own oracle is defined. (ii)~$\Binsuf$ is upper-bounded, not point-identified (no
replicates at fixed $\zstar$); the smoothness-free Jensen bound assumes $m$ and $g$ share the $\lngi$
reference state, which we verify (\S\ref{sec:measure}), so the mean offset is genuine decoder bias, not
a units artifact. (iii)~The constant-offset margin is convention-specific and inverts under the
deployed residual-only default, on both sets; there the separation rests on the law-of-total-variance
bound and the more-physics-worse effect. That bound is not itself convention-independent---only the
estimand $\Binsuf$ is, the two conventions coarsening $\zstar$ along two different statistics
(\S\ref{sec:decomp}, App.~\ref{sec:si-proofs})---so the deployed claim rests on the
law-of-total-variance bound \emph{computed under the deployed convention}, not on a quantity
insensitive to the choice. (iv)~Absolute scaffold
accuracy is encoder-bound but well \emph{above} the inter-lab noise floor ($0.15$--$0.31$ in $\lnx$;
\S\ref{sec:supporting}), so label noise does not explain the ceiling. (v)~The latent-departure
result is three-seed mean$\pm$sd ($n{=}44$; \S\ref{sec:surrogate}); what it establishes is the
\emph{sign} and the presence of a transferable shared direction, with the magnitudes
($51\pm2\%$/$72\pm1\%$/$4.1\pm0.5\times$) reported but not stable across analysis
configurations. The two top-two shares in play are distinct quantities: the learned-versus-reference
deviation concentrates $0.399$ in its first two modes---$1.8\times$ a smoothness-matched
phase-randomised null ($0.221$), though below a wrong-molecule null built from real reference profiles
($0.772$; both in \S\ref{sec:surrogate}), and measured on one grounded checkpoint rather than three
seeds---whereas the $72\pm1\%$ isolation drift has no matched null of its own.
(vi)~The probe result on the unconstrained control is bounded by the same
reference scarcity: its crystal-term and temperature tests rest on $13$ held-out solutes and cannot be
enlarged, so the negative is an upper bound at $R^{2}\approx0.30$ rather than a demonstration that no
crystal information is present, and the crystal-slope test is additionally limited by the corpus,
whose empirical slope carries $-\Delta H_{\text{fus}}/R$ at $r=+0.24$. The scaffold-organisation
result needs no external reference, but its two partitions nearly coincide by construction---only
$11$ of the $124$ test scaffolds hold more than one solute---so a ratio near one is weak evidence of
within-scaffold indistinguishability (App.~\ref{sec:si-blackbox}). (vii)~The closure-fidelity contrast compares \emph{oracle-input} activity error across kernels, so
$\Ggap$ itself is not measured under a modern closure on either axis, and whether the
\emph{solubility} paradox survives a $2010$/dsp closure is untested. Both require a typed
(donor/acceptor) $\sigma$-profile head trained end to end through the 2010 kernel, and the
solubility test a UD$\,\cap\,$BigSolDB matched set; we have neither.
(viii)~No difference in accuracy between the physics and control arms is attributable to the
thermodynamic bottleneck; the confound and what survives it are stated above.
(ix)~The scaffold-leak guard is a property of the released stream builder and not a certified property
of these runs: one $\sigma$-stream build on disk carries $91$ held-out solutes, and per-run stream
provenance was not logged (\S\ref{sec:si-repro}). The paradox contrast is one checkpoint evaluated two
ways and so cannot be generated by such a leak, and DirectGNN uses no stream, but auditability is not
restored by those arguments. (x)~The repository URL, archive DOI, licence and commit hash are
placeholders, so the reproduction commands the
Data availability statement describes cannot be run; several of the numbers the verdict rests
on---the broad IDAC set with its
design and provenance audit, the 2010 kernel's own-latent floors, the fitted kernel residual, the
estimator grid---are therefore checkable only through code we have not yet been able to release. The
objection stands in full for those artifacts, and it reaches much of the verdict, which rests on
evaluations that cannot be opened without the code. One partial remedy is in place: the two
decomposition sets are deposited as row-level tables (Tables~S1 and~S2, the latter printed in full
as Table~\ref{tab:corner-rows}), so the sets and the margins computed on them can be recomputed
without code. What that does not deliver is the provenance of the closure evaluations in those
tables, or of the trained arms anywhere else.

\section{Conclusion}
\label{sec:conclusion}
Physics-informed models are attractive because they promise both accuracy and interpretability, on the
assumption that grounding their internal quantities in reference physics can only help. That
assumption conflates two operations. Grounding as \emph{training-time supervision} from a reference
database helps here, by $0.20$ in MAE over three seeds. Grounding as \emph{inference-time
substitution} of reference values for the model's own hurts, and hurts in the way a misspecified
consuming model predicts: by $0.41$ over the same three seeds when the substitution is made at
evaluation only---about twice what supervision gains---and by $0.18$ at one seed when it is made at
training, so that the model co-adapts. That single-seed arm fixes the sign of the co-adapted cost and
not its size, so the two-to-one comparison rests on the three-seed arms alone. The learned intermediate does not converge to the physical quantity it
represents: it departs from the reference along a direction shared across molecules---\emph{likely},
on the $44$ matched molecules over three seeds on which that is measured. Whether that
departure is a compensation for the model's error---which would explain the sign directly---we could
not measure; the one direct check available on retained checkpoints argues against it, but on the
infinite-dilution axis rather than the one that produces the drift and with too little power to
exclude a partial cancellation, so we leave it open. The failure is nonetheless diagnosable: an
external reference for the intermediate separates model misspecification from input insufficiency, and
on $477$ activity measurements over $185$ molecule pairs it places the accuracy ceiling of the
deployed COSMO-SAC-2002 kernel in the fixed model rather than in its inputs, by a margin of $+0.51$
under the convention that kernel is deployed with---likely rather than resolved, with the estimator
grid, the clusterings and the alternative convention in \S\ref{sec:measure}(i) and
\S\ref{sec:discussion}. Those $477$ measurements
are the $\gamma^\infty$ chemistry the kernel has been most widely exercised on and not the chemistry
it is deployed on: no solute above $12$ heavy atoms, against a corpus of drug-like solids whose
median solute has $14$. What is placed in the map is that
kernel's ceiling on that chemistry.

Reading a grounded latent as the physical quantity, and supplying reference values in its place at
inference, is therefore justified only to the extent that the downstream physical model is faithful.
When it is not, more accurate inputs do not lift the ceiling, and the one downstream correction we
test---an additive gated residual on the output, at seed $42$---restores calibration without
recovering accuracy; the
remedy must therefore be sought in the model rather than in its inputs, and the standard $2002\to2010$ step
supplies it only on the hydrogen-bonding chemistry it was built for, not on the broad IDAC
set---which, that set being a small-molecule one, leaves the step untested on drug-like solids
rather than shown not to pay. Supervising the latent on the same reference data during training
remains worthwhile, and is the sense in which grounding paid here. The instrument makes the
distinction measurable for a predictor that composes a fixed physical model over a learned
intermediate for which an external reference exists; whether a latent trained end to end through an imperfect model generally becomes a
compensating surrogate is a testable prediction we state rather than a result we report.

\section*{Author contributions}
\noindent
%% DRAFT, for both authors to confirm before submission: the roles below are inferred from the
%% work as recorded and are not an agreed statement.
N.~L. Polomoshnov: Conceptualization, Methodology, Software, Formal analysis, Investigation,
Data curation, Visualization, Writing -- original draft.
A.~V. Rudik: Writing -- review \& editing.
%% CRediT vocabulary (use only these 14 names): Conceptualization; Data curation; Formal analysis;
%% Funding acquisition; Investigation; Methodology; Project administration; Resources; Software;
%% Supervision; Validation; Visualization; Writing -- original draft; Writing -- review \& editing.

\section*{Conflicts of interest}
\noindent There are no conflicts to declare.

\section*{Data availability}
The analysis code, and those intermediate artifacts small enough to version, are
available at \url{https://github.com/doctawho42/tgnn-solv} (commit \pending[hash]) under the MIT
licence; the model checkpoints, the processed split, the full per-arm prediction files and the
larger analysis artifacts---among them the two audit files \S\ref{sec:si-repro} names---are
deposited at
\pending[Zenodo DOI]. \textbf{The repository URL, archive DOI and commit hash above are
placeholders.} An anonymised repository
and an archived snapshot at the commit the reported numbers were produced from will be supplied with
the manuscript; until then the reproduction commands cannot be run (limitation~(x)). The data
sources are public: BigSolDB~2.0 \cite{krasnov2025bigsoldb} for solubility,
the VT-2005 database for the reference $\sigma$-profiles, Ref.~\cite{fingerhut2017} as redistributed
with Ref.~\cite{bell2020cosmosac} for the UD profiles, ThermoML for the infinite-dilution activity
coefficients, and the OPERA compilation \cite{mansouri2019opera} for the second-domain pKa data.
Most analysis scripts carry their run commands in their docstrings, and the two audits behind
\S\ref{sec:measure} are named with the script and the artifact file of each in the compute ledger
(App.~\ref{sec:si-repro}). Several deposited artifacts
carry disclosed caveats, recorded in App.~\ref{sec:si-repro} and App.~\ref{sec:si-tables}: the
seed-$44$ oracle per-row file is truncated, the three-seed surrogate run's per-run split provenance
was not logged, one $\sigma$-stream build on disk was written against the wrong split files, and the
two corner audit artifacts disagree on one out-of-fold estimate.

\begin{acknowledgement}
This research received no external funding.
Computations were carried out on commercial cloud services (Google Colab Pro+, Modal, and Google Cloud Platform) using NVIDIA T4 and L4 GPUs.
During the preparation of this work the authors used generative artificial-intelligence
tools (large language models, including Anthropic's Claude) to assist with literature search, drafting
and editing of the text, writing and debugging of analysis code, and generating figures. All content
was reviewed, verified, and edited by the authors, who take full responsibility for the manuscript.
\end{acknowledgement}

\begin{suppinfo}
The Supporting Information contains supplementary methods, including the
architecture and the loss weights; proofs of the lemmas and of the grounding-gap theorem; per-arm
tables and the robustness of the decomposition, including its estimator grids on both sets and a
per-mark table of the input-insufficiency bounds the decomposition figure draws; external
baselines and ablations; supplementary mechanism results; supplementary diagnostics and broad
negatives; the second-domain pKa analysis; data provenance and reproducibility; the
full tables of the black-box probe programme; the controlled synthetic closure-fidelity family;
and the positioning against adjacent literatures.
(The notation table, Table~\ref{tab:notation}, is in the main text.) Two machine-readable tables are deposited with it: the
$477$-row broad IDAC set (Table~S1, \texttt{broad\_idac\_set\_477.csv}) and the $60$-row
corner (Table~S2, \texttt{corner\_set\_60.csv}); their columns are listed in App.~\ref{sec:si-repro}.
\end{suppinfo}

\input{sections/SI}

% achemso selects achemso.bst automatically; no \bibliographystyle is given.
\bibliography{references_verified}

\end{document}
