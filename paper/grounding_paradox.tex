% =====================================================================
% GROUNDING-PARADOX PAPER — reframed submission draft (2026-07-08)
% Supersedes conditional_optimality_skeleton.tex (theory-first, headlined the
% REFUTED Thm3 "conditional optimality iff"). That file is kept for reference /
% reusable Fisher+T-extrap numbers, NOT the thesis.
%
% THESIS PIVOT: from "physics helps iff (conditions)" -> the GROUNDING PARADOX:
%   feeding the TRUE external physical latent (VT-2005 sigma-profiles) through the
%   differentiable COSMO-SAC closure makes activity/solubility prediction WORSE,
%   and we MEASURE why: B = B_closure + B_insuff, closure dominates.
%
% SECTION MAP (what survives / demoted / new):
%   Abstract + Contributions ...... reframed (paradox + B measurement + Gate B null)
%   S2 Setup ...................... SLE eq + sigma-profile closure + z* external latent   [reuse eq:sle]
%   S3 Framework .................. Prop1 non-identifiability (ENABLING; was T1, verified)
%                                   Prop2 B=B_closure+B_insuff decomposition (NEW keystone)
%                                   theory DEMOTED to cited props (Kennedy-O'Hagan, Brynjarsdottir,
%                                   sloppy-models, White, Murphy/DeGroot-Fienberg)
%   S4 Results .................... R1 the paradox (learned head ~4x better; more physics = worse)
%                                   R2 the B measurement (Tier1 convention-independent bound + Tier2 combinatorial + 4 hedges)  <- keystone
%                                   R3 Gate B negative (recalibration -> calibration not accuracy)
%                                   R4 supporting: Fisher audit, defensible wins, conformal  [transcribe+verify]
%   S5 Discussion/Limitations ..... the 4 verified hedges + scope
%   S6 Conclusion
%
% NUMBER PROVENANCE: Fig-paradox / chemistry-map numbers regenerate from
% results/e5_sigma_grounding/ via scripts/analysis/run_e5_comparison.py
% (3-seed aggregate: results/e5_sigma_grounding/THREE_SEED_SUMMARY.md).
% Table-2 B-decomposition numbers regenerate from
% scripts/analysis/run_b_insuff_decomposition.py -> results/b_insuff/decomposition.json.
% BIB: murphy1973 / degroot1983 (calibration refinement) and white1982 (QMLE under
% misspecification) are present in references_verified.bib and cited.
% =====================================================================
\documentclass[11pt]{article}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{hyperref}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\newtheorem{assumption}{Assumption}
\newtheorem{definition}{Definition}

\newcommand{\pending}[1][?]{{\color{red}\texttt{[#1]}}}
\newcommand{\lnx}{\ensuremath{\ln x_2}}
\newcommand{\Phicry}{\ensuremath{\Phi}}
\newcommand{\lng}{\ensuremath{\ln\gamma_2}}
\newcommand{\lngi}{\ensuremath{\ln\gamma_2^\infty}}
\newcommand{\Tm}{\ensuremath{T_m}}
\newcommand{\dHfus}{\ensuremath{\Delta H_{\mathrm{fus}}}}
\newcommand{\Rgas}{\ensuremath{R}}
\newcommand{\zstar}{\ensuremath{z^\star}}
\newcommand{\Bclos}{\ensuremath{B_{\mathrm{closure}}}}
\newcommand{\Binsuf}{\ensuremath{B_{\mathrm{insuff}}}}
\newcommand{\Ex}{\ensuremath{\mathbb{E}}}
\newcommand{\Var}{\ensuremath{\mathrm{Var}}}

\title{The Grounding Paradox: Why True Physical Inputs Make a\\
Solubility Model Worse, and How to Measure It}
\author{N.~L. Polomoshnov \and \pending[co-authors]}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Physics-informed models for solid--liquid solubility factor the target
$\lnx=-\Phicry(T)-\lng$ into a pure-component crystal term and a pair activity term,
and increasingly compute the activity term with a \emph{differentiable thermodynamic
closure}---here COSMO-SAC over a network-predicted $\sigma$-profile---on the premise
that grounding the closure on true physical inputs improves it. It does the opposite:
replacing the model's learned $\sigma$-profiles with the true, externally measured ones
(VT-2005) makes both activity and solubility prediction \emph{worse} than a free
learned head, an effect we confirm three ways. We turn this paradox into a measurement.
Because the physical input has external ground truth, the true-input error splits---with
no model fit---into a part the inputs cannot resolve and a part the fixed closure gets
wrong; on infinite-dilution activity data the closure is the binding term, and a
downstream recalibration recovers \emph{calibration} but not \emph{accuracy}. The
failure is not incidental. The physics-informed model does not beat a matched black box
on accuracy at \emph{any} training-data budget---including the low-data regime where an
inductive bias should help most---nor on temperature extrapolation, and we prove why:
the crystal/activity split is non-identifiable from solubility, and the closure is
misspecified where we can measure it. The paper's contribution is therefore not
accuracy but a reproducible, proved method for locating where a physics prior's ceiling
lies, and---for this system---an account of why more physics does not help; what the
prior does buy is an interpretable decomposition and calibrated uncertainty, not lower
error.
\end{abstract}

\section{Introduction}
\label{sec:intro}
% Frame: the field's default is "add physics / ground on real inputs -> better OOD".
% We show a clean counterexample and a measurement that says why.
Coupling a graph encoder to an explicit thermodynamic bottleneck is a common
recipe for out-of-distribution solubility prediction: the model must route its
prediction through interpretable physical factors, and each factor can in
principle be grounded on abundant single-component data that a black-box
predictor cannot exploit. The expectation is that the closer the model's physical
inputs are to the truth, the better it should predict. On the activity branch
this expectation fails. We characterize the failure quantitatively, and it is the
characterization, more than the failure itself, that this paper is about. That true
$\sigma$-profiles can hurt a COSMO-SAC pipeline will not surprise a thermodynamics
specialist who knows the model's limits; the contribution is not the failure but an
externally grounded procedure---demonstrated here on one closure and one low-$\gamma$
regime---that \emph{measures} where such a physics prior's ceiling lies and attributes
it to the closure rather than the inputs. Whether the finding of closure-dominance
generalizes beyond this regime we leave as a conjecture (\S\ref{sec:discussion}).

\paragraph{Contributions.}
\begin{itemize}
  \item \textbf{The grounding paradox (empirical, triple-confirmed).}
  Substituting true VT-2005 $\sigma$-profiles into the differentiable COSMO-SAC
  closure degrades activity ($\lngi$) and solubility ($\lnx$) prediction relative
  to a free learned activity head, the opposite of the intended benefit
  (\S\ref{sec:paradox}).
  \item \textbf{A measurement affordance, not a theorem.} Because the physical
  latent $\zstar$ (the true $\sigma$-profile) has external ground truth, the
  true-input excess risk splits \emph{exactly} as $B=\Bclos+\Binsuf$ with no model
  fit (Lemma~\ref{prop:decomp}). The decomposition and its estimators are textbook;
  the new element is exploiting an externally measured latent to bound the split
  empirically. On the convention-robust evidence---a convention-independent bound
  and the more-physics-worse effect---$\Bclos$ (misspecified decoder) exceeds
  $\Binsuf$ (input insufficiency): the ceiling is the closure
  (\S\ref{sec:measure}).
  \item \textbf{A negative actionability result (Gate B).} A post-hoc
  output-space residual placed on the physics prediction recovers calibration but
  not accuracy; the closure binding cannot be undone downstream
  (\S\ref{sec:gateb}).
  \item \textbf{An exhaustive, explained negative.} The physics prior does not beat a
  matched black box on accuracy at any training-data budget---including the low-data
  regime where an inductive bias should help most (\S\ref{sec:data-efficiency})---nor on
  temperature, nor via crystal grounding; read against the lemmas and the closure
  measurement, this locates the ceiling as \emph{structural}, not incidental
  (\S\ref{sec:discussion}). We make no accuracy claim.
  \item \textbf{Framework and honest scoping.} We prove three elementary structural
  lemmas (non-identifiability, class-dependent information, and the closure/insufficiency
  split; Lemmas~\ref{prop:ident}--\ref{prop:decomp}, App.~\ref{sec:si-proofs}), read the
  deeper theory against cited results on model discrepancy and calibration, and ship
  reproducible diagnostics.
\end{itemize}

\input{sections/related-work}

\section{Problem setup and notation}
\label{sec:setup}
For a solute~$s$ in solvent~$v$ at temperature~$T$, the target is $y=\lnx$, the
log saturated mole fraction. Solid--liquid equilibrium gives
\begin{equation}
  \lnx = -\underbrace{\frac{\dHfus}{\Rgas}\!\Big(\tfrac1T-\tfrac1\Tm\Big)}_{\Phicry(T)\ \text{(crystal, depends on $s$)}}
         \;-\;\underbrace{\lng(x_2,T)}_{\text{activity, depends on }(s,v)} ,
  \label{eq:sle}
\end{equation}
with the constant-$\Delta C_p$ correction omitted for clarity. The activity term
is produced by a \emph{differentiable closure} $g$: a graph encoder predicts a
$\sigma$-profile for each molecule, and COSMO-SAC maps the pair of profiles to
$\lng$. Write $\zstar$ for the \emph{true} physical latent, the externally
measured $\sigma$-profile (VT-2005 database), and $g(\zstar)$ for the closure's
output when fed the true profile. The model's own head instead predicts a
\emph{learned} profile $\hat z$ and evaluates $g(\hat z)$; a matched
\emph{DirectGNN} control shares the encoder but emits $\lnx$ with no closure and
is the reference for ``what the physics bottleneck costs'' (Fig.~\ref{fig:arch}).

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_architecture.pdf}
\caption{The model and the grounding intervention. Solute and solvent graphs pass
through a shared encoder to a crystal head (giving the ideal term $\Phi(T)$) and a
$\sigma$-profile head; the predicted profile $\hat z$ feeds the COSMO-SAC closure
$g$, which returns $\ln\gamma_2$, and an SLE solver combines the two into $\lnx$.
The $\sigma$-oracle replaces $\hat z$ with the true VT-2005 profile $\zstar$
(salmon). DirectGNN shares the encoder but bypasses the closure.}
\label{fig:arch}
\end{figure}

\paragraph{The grounding intervention.} Feeding $\zstar$ in place of $\hat z$
(a $\sigma$-oracle) isolates the closure from encoder error: if the closure were
well specified, $g(\zstar)$ should be the best available activity estimate. The
paradox is that it is not.

\paragraph{Data.} Solubility measurements are a Bemis--Murcko scaffold split of
BigSolDB \cite{krasnov2025bigsoldb}, so test solutes are unseen at training; the
activity labels $m$ are experimental infinite-dilution coefficients $\lngi$
compiled from ThermoML, and the true $\sigma$-profiles $\zstar$ are the VT-2005
database.

\section{Theory: locating a physics prior's ceiling}
\label{sec:framework}

\subsection{Why a free head can compensate: structural non-identifiability}
The crystal/activity split is not identifiable from solubility data alone, which
is precisely why an unconstrained head can silently absorb crystal error into the
activity term.
\begin{lemma}[Structural non-identifiability of the SLE split]\label{prop:ident}
Consider the infinite-dilution SLE observable $\mu(T)=-\Phicry(T)-\lngi(T)$ with
$\Delta C_p=0$, so that $\Phicry(T)=(\dHfus/\Rgas)(1/T-1/\Tm)$ is affine in $u=1/T$,
and a symmetric two-suffix activity class ($\lng=\lngi\,(1-x_2)^2$) in which
$\lngi(T)$ is affine in $u$. Then, for any design with at least two distinct
temperatures, $\mu$ depends on the parameters only through the two-dimensional
statistic (its slope and intercept in $u$), and the activity intercept and slope
already span that plane;
hence the crystal parameters $(\dHfus,\Tm)$ are unidentified, the profiled Fisher
information for $\dHfus$ is exactly zero, and the indeterminacy set is
two-dimensional. The deficiency is closed only by a rank-completing external
constraint (a direct crystal label, or a fixed activity slope). At finite dilution
the composition factor $(1-x_2)^2$ breaks the exact affinity, leaving a non-zero but
near-degenerate information of order $x_2^2$ (numerically, condition number
$2.3\times10^{5}$; \S\ref{sec:ident}).
\end{lemma}
\noindent This is the standard structural-identifiability picture for a
collinear design \cite{raue2009,villaverde2016}; we prove it in the SLE form
(App.~\ref{sec:si-proofs}) and audit it numerically in \S\ref{sec:ident}. It is
\emph{enabling background},
not our headline: it explains why grounding an under-determined factor on
external single-component data is necessary, and why compensation between the
factors is expected. By itself it says nothing about whether grounding helps.
That depends on whether the closure that consumes the grounded inputs is
correctly specified, which is the question we make measurable next.

The non-identifiability is not binary but graded by how much of the crystal
direction the activity class can absorb.
\begin{lemma}[Class-dependent efficient information]\label{prop:semiparam}
Let the activity nuisance lie in a class $\mathcal H$ with tangent projection
$\Pi_{\mathcal H}$, and let $v=\partial\mu/\partial\dHfus$ be the crystal score.
The efficient information for $\dHfus$ is
$\mathcal I_{\mathrm{eff}}(\dHfus)=\sigma^{-2}\,\lVert v-\Pi_{\mathcal H}v\rVert^2$.
If $\mathcal H$ contains the crystal score's span $\{1,1/T\}$ (a free intercept and slope) then
$\Pi_{\mathcal H}v=v$ and $\mathcal I_{\mathrm{eff}}=0$, recovering
Lemma~\ref{prop:ident}; if $\mathcal H$ is restricted (the slope is known or
constrained) then $\mathcal I_{\mathrm{eff}}>0$ and the Cram\'er--Rao bound on
$\dHfus$ is finite.
\end{lemma}
\noindent The proof is the efficient-score construction (App.~\ref{sec:si-proofs}):
project the crystal score onto the orthogonal complement of the nuisance tangent
space; the squared norm of the residual is the efficient information. The numerical
audit of \S\ref{sec:ident} quantifies both regimes, and shows the honest corollary:
merely \emph{restricting} the activity slope leaves $\mathrm{sd}(\dHfus)\approx8{,}251$\,J/mol
--- finite but unusable --- whereas an \emph{external} crystal label, which
contributes a score direction outside the activity tangent space, closes it to
$\approx1{,}689$\,J/mol.

\subsection{The closure--insufficiency decomposition}
\label{sec:decomp}
Let $m$ be the activity target (experimental $\lngi$) and $g(\zstar)$ the closure
evaluated on the true latent. The excess risk of the true-input path over the
Bayes predictor decomposes exactly.
\begin{lemma}[Exact closure--insufficiency decomposition and its one-sided bounds]\label{prop:decomp}
For a \emph{fixed} (non-fitted) closure $g$,
\begin{equation}
  \underbrace{\Ex\big[(m-g(\zstar))^2\big]}_{\text{MSE of the true-input path}}
  \;=\;
  \underbrace{\Ex\big[\Var(m\mid \zstar)\big]}_{\Binsuf\ \text{(inputs cannot resolve)}}
  \;+\;
  \underbrace{\Ex\big[(\Ex[m\mid\zstar]-g(\zstar))^2\big]}_{\Bclos\ \text{(decoder mis-map)}} .
  \label{eq:decomp}
\end{equation}
The cross term vanishes identically because $\Ex[m\mid\zstar]-g(\zstar)$ is
$\zstar$-measurable and $\Ex[(m-\Ex[m\mid\zstar])\mid\zstar]=0$. Both components
are separately \emph{estimable from data with no model fit}, because $\zstar$ is
externally observed.
\end{lemma}
\noindent Equation~\eqref{eq:decomp} is a bias--variance split of model
discrepancy in the sense of Kennedy--O'Hagan \cite{kennedy2001} and
Brynjarsd\'ottir--O'Hagan \cite{brynjarsdottir2014}: $\Bclos$ is the systematic
discrepancy of a structurally fixed physical map, and its dominance over
$\Binsuf$ is the empirical content of ``the ceiling is the closure, not the
inputs.'' We do not claim \eqref{eq:decomp} as new; the affordance is that the
external groundedness of $\zstar$ lets us \emph{measure} the split rather than
assume it. \textbf{The exactness is contingent on $g$ being fixed}: for a
\emph{fitted} decoder the cross term does not vanish and a plug-in point
decomposition is invalid (we observe a spurious $-0.36$ cross term when
$\Ex[m\mid\zstar]$ is replaced by a fitted estimate). We therefore report
\emph{one-sided bounds}, not a point split (each proved in App.~\ref{sec:si-proofs}):
\begin{itemize}
  \item $\Bclos \ge (\Ex[m]-\Ex[g])^2$, \emph{assumption-free in smoothness}
  (Jensen; a constant decoder offset, which cannot be input insufficiency)---it does
  assume $m$ and $g$ are on the same $\lngi$ reference state, which we check directly
  (\S\ref{sec:measure}).
  \item $\Binsuf \le \Ex[\Var(m\mid \mathrm{bin}(g(\zstar)))]$, a valid
  \emph{upper} bound (law of total variance, since a binning of $g(\zstar)$
  coarsens $\zstar$).
\end{itemize}
Because $\Binsuf=\Ex[\Var(m\mid\zstar)]$ depends only on $(m,\zstar)$, it is
\emph{independent of the closure convention}: any disagreement between closure
variants is decoder disagreement and lands in $\Bclos$.

\subsection{Supporting structural facts}
Three further consequences of the same collinear geometry are used informally and
are not stated as theorems. \emph{(i)~Temperature extrapolation.} Within the
affine-in-$1/T$ activity family, same-pair prediction at a temperature $T^\star$
outside the training range is an affine extrapolation whose error is controlled by
the fitted-slope variance times $|1/T^\star-\overline{1/T}|$, whereas an
unstructured temperature encoder is unconstrained beyond its support. This is the
one place the physical \emph{structure} is expected to help; \S\ref{sec:supporting}
reports the class-level margin, which is a property of the hypothesis class and not
an accuracy claim for the trained model (indeed the trained models trail the
van't~Hoff baseline). \emph{(ii)~Geometry of compensation.} As the training window
$\Delta T\to0$, $1/T$ becomes nearly constant, the crystal and activity design
columns become collinear, and the smallest Fisher eigenvalue tends to zero (the
$\Delta T$ sweep of \S\ref{sec:ident} reaches conditioning $10^{16}$ at
$\Delta T=10$\,K), so an optimizer prefers to split error between the factors rather
than fit either correctly. \emph{(iii)~Grounding design.} Each single-component
stream rank-completes a different deficient direction of Lemma~\ref{prop:ident};
this is the design rule behind the auxiliary streams, though (as \S\ref{sec:e2}
shows for a dose-limited crystal run) it does not by itself promise an end-task gain.

\subsection{What we prove, and what we do not claim}
We prove the three structural lemmas the measurement rests on --- the split's
non-identifiability (Lemma~\ref{prop:ident}), its class-dependence
(Lemma~\ref{prop:semiparam}), and the exact closure/insufficiency decomposition with
its one-sided bounds (Lemma~\ref{prop:decomp}); the proofs are elementary and
collected in App.~\ref{sec:si-proofs}. We claim no more. In particular there is no
``optimal grounding'' theorem and no necessary-and-sufficient condition for the
decomposition to improve out-of-distribution accuracy --- the honest empirical
answer (\S\ref{sec:results}) is that it does not, and an earlier draft's
conditional-optimality ``iff'' is refuted by exactly that. The lemmas are read
against, not in competition with, known results on model discrepancy
\cite{kennedy2001,brynjarsdottir2014}, parameter sloppiness
\cite{transtrum2015,gutenkunst2007}, structural and practical identifiability
\cite{raue2009,villaverde2016}, estimation under misspecification \cite{white1982},
and underspecification \cite{damour2022}; the calibration-versus-accuracy separation
of \S\ref{sec:gateb} is the classical refinement decomposition of a probability
forecast \cite{murphy1973,degroot1983}.

\input{sections/methods}

\section{Results}
\label{sec:results}

\subsection{The grounding paradox}
\label{sec:paradox}
% Artifacts: results/e5_sigma_grounding/seed_{42,43,44}/comparison.json (3-seed e5 lock, n=5608); aggregate THREE_SEED_SUMMARY.md via run_e5_comparison.py.
% Triple confirmation: eval-oracle / true-at-train / channel-swap.
Feeding true $\sigma$-profiles into the closure degrades it rather than improving it,
in both the activity and the solubility target, and it fails the way a \emph{misspecified
map} does---not the way missing information would. In the solubility target the effect
erases any positive $R^2$ (Fig.~\ref{fig:paradox}; three seeds, $n{=}5608$): the model
predicts solubility better \emph{from its own learned $\sigma$-profile} than when
\emph{given the true measured one} (MAE $1.85$ vs $2.25$; the $\sigma$-oracle's
$R^2\approx0$ is no better than predicting the mean). Two facts pin the cause on the map.
Restoring the physically \emph{more} complete Staverman--Guggenheim combinatorial term
\emph{increases} error---what a wrong map does, not what a missing input would; and on
the activity target ($\lngi$, $n{=}60$ matched pairs) the fixed true-input closure sits
about $4\times$ above the irreducible-variance floor that a flexible regressor reaches
(\S\ref{sec:measure}), under-predicting $\lngi$ with a systematic bias
(Fig.~\ref{fig:parity}). The paradox survives three controls---the evaluation-time
oracle, injection of the true profile \emph{at training} (MAE $1.98$), and a channel-swap
($2.08$), all worse than the learned-$\sigma$ arm---ruling out a wiring artifact.
Per-arm values are in \S\ref{sec:si-tables}.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_paradox.pdf}
\caption{Controlled comparison on the scaffold split (3 seeds, mean$\pm$sd,
$n{=}5608$; metrics are the 3-seed mean of the cross-arm intersection lock --- rows
supervised and finite in every arm --- from \texttt{run\_e5\_comparison.py}).
DirectGNN shares the encoder but has no thermodynamic closure; the
physics closures trail it (the ``physics tax''). Replacing the model's learned
$\sigma$-profile with the \emph{true} VT-2005 profile (the $\sigma$-oracle,
salmon) gives the \emph{worst} arm, collapsing $R^2$ to $\approx0$ ($-0.03\pm0.04$):
by MAE the model predicts solubility distinctly better from its invented $\sigma$
($1.85$) than from the measured one ($2.25$). Per-arm exact values are transcribed
in the SI.}
\label{fig:paradox}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.72\linewidth]{figs/fig_parity.pdf}
\caption{Activity-coefficient parity on the $n{=}60$ matched pairs. The true-$\sigma$
COSMO-SAC closure $g(\zstar)$ (salmon) falls systematically below the diagonal at
larger $\lngi$, under-predicting the activity coefficient (MSE $1.80$, full
convention). This systematic downward bias is the visible form of the decoder
misspecification $\Bclos$; for reference a flexible regressor of $\lngi$ on the same
true profiles reaches the irreducible-variance floor (MSE $\approx0.40$, our $\Binsuf$
estimator; \S\ref{sec:measure}), so the closure's excess over that floor is $\Bclos$.}
\label{fig:parity}
\end{figure}

\subsection{Measuring the ceiling: the closure dominates}
\label{sec:measure}
% Artifact: results/b_insuff/decomposition.json via scripts/analysis/run_b_insuff_decomposition.py.
We apply the decomposition of \S\ref{sec:decomp} to the $n{=}60$ matchable pairs
(41 solutes, 17 solvents; the two formamide solvents that the deployed
canonical-SMILES matcher drops on a tautomer mismatch are excluded), with $\zstar$ the concatenated true
solute/solvent $\sigma$-profiles ($102$ dimensions) and $m$ the experimental
$\lngi$ (298\,K, IDAC). Table~\ref{tab:decomp} reports the bounds.

\begin{table}[t]
\centering
\caption{Closure--insufficiency decomposition on the true-input activity path.
Bounds, not a point split (Lemma~\ref{prop:decomp}). Convention ``full'' =
COSMO-SAC with the combinatorial term (size factor $r_i=V_i/r_0$ on the true
COSMO cavity volume, VT-2005); ``res'' = residual-only (the deployed default).
$\Binsuf$ upper bounds are convention-independent estimates of
$\Ex[\Var(m\mid\zstar)]$; ``k\textsc{nn}'' is a direct high-dimensional estimate
(biased upward), the others are law-of-total-variance / out-of-fold upper bounds.
The \emph{load-bearing} separation is the convention-independent
$\Bclos\ge\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ row, which exceeds $\Binsuf$ under
\emph{both} conventions ($1.24$, $0.91>0.62$); the assumption-free constant-offset
(Jensen) bound separates only under ``full'' ($0.72$) and inverts under the
deployed ``res'' ($0.43<0.62$).}
\label{tab:decomp}
\begin{tabular}{lrr}
\toprule
Quantity & full & res \\
\midrule
MSE$_{\text{total}}=\Bclos+\Binsuf$ (exact)      & $1.80$ & $1.47$ \\
$\Bclos$ lower via MSE$-\Binsuf^{\text{up}}$ (RF; \emph{$\Binsuf$ conv.-indep., load-bearing}) & $1.24$ & $0.91$ \\
$\Bclos$ lower bound (Jensen, offset$^2$; assumption-free; \emph{full-only}) & $0.72$ & $0.43$ \\
$\Binsuf$ upper (bin $g$; law of total variance)  & $0.53$ & $0.62$ \\
$\Binsuf$ upper (RF / Ridge out-of-fold)          & $0.56/0.62$ & same \\
$\Binsuf$ (k\textsc{nn}-in-$\zstar$, convention-indep., biased up) & $0.40$ & $0.40$ \\
label noise $\sigma_\eta^2$ (IDAC, unestimable here) & \multicolumn{2}{c}{assumed $0$ (no replicates in matched set)} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_decomposition.pdf}
\caption{One-sided bounds on the closure--insufficiency split (full COSMO-SAC
convention). The assumption-free Jensen lower bound on the decoder
misspecification, $\Bclos\ge0.72$, exceeds every valid upper bound on the input
insufficiency $\Binsuf\le0.62$ (four independent estimators cluster in the salmon
region); the white gap is a separation that holds with no smoothness assumption.
The teal region is the closure share of the total error (MSE $=1.80$). This
assumption-free separation is convention-specific; under the deployed residual-only
default it rests instead on the convention-independent
$\Bclos\ge\mathrm{MSE}-\Binsuf^{\mathrm{up}}\approx0.9>0.6$ bound (\S\ref{sec:measure}).}
\label{fig:decomp}
\end{figure}

\paragraph{The load-bearing separation (convention-independent, Tier~1).} Because
$\Binsuf=\Ex[\Var(m\mid\zstar)]$ depends only on $(m,\zstar)$, its upper bound is the
same in every closure convention; subtracting it from the total therefore lower-bounds
$\Bclos$ even for the model's \emph{deployed} residual-only convention:
$\Bclos\ge\mathrm{MSE}_{\mathrm{res}}-\Binsuf^{\mathrm{up}}\approx1.47-0.62=0.85$ (and
$0.91$ against the RF bound), well above $\Binsuf\le0.62$. This is the claim we lean
on, and it needs no smoothness assumption beyond the validity of the $\Binsuf$ upper
bound. Under the full COSMO-SAC convention the separation \emph{sharpens} to an
assumption-free constant-offset bound: a constant decoder offset alone accounts for
$\Bclos\ge0.72$ (Fig.~\ref{fig:decomp}), exceeding every $\Binsuf$ upper bound
($\le0.62$), and subtracting the insufficiency bound gives $\Bclos\ge1.24>0.62$
($\sim2\times$). That constant-offset bound is convention-specific and \emph{inverts}
under the deployed residual-only default ($0.43<0.62$; hedge~1), which is exactly why
we headline the convention-independent bound above rather than the constant offset.
Label noise is unestimable in this replicate-free set and assumed zero; the bounds we
rely on are invariant to zero-mean noise, so the ceiling is not label noise either.

\paragraph{Convention-robust corroboration (Tier~2).} The genuinely
convention-robust fact is that adding the physically \emph{more} complete
Staverman--Guggenheim combinatorial term \emph{increases} error (activity MSE
$1.47\to1.80$)---what a misspecified map does, not what missing information would.
This is robust to the size-factor's volume basis: recomputing the combinatorial term
on the true COSMO cavity volumes (VT-2005 $V_{\mathrm{cosmo}}$) rather than an RDKit
molar volume leaves it essentially unchanged (Jensen $0.714\to0.717$, MSE
$1.793\to1.800$), because the term depends on molecular-size \emph{ratios} in which a
near-uniform volume rescaling cancels. A second observation is consistent but is
\emph{not} independent of the decomposition: a flexible regressor of $m$ on $\zstar$
fits the activity target $\sim$4$\times$ better than the fixed true-input closure---but
that regressor is exactly our convention-independent $\Binsuf$ estimator
($\approx0.4$), so ``$4\times$ better'' restates that the closure sits far above the
irreducible-variance floor (that $\Bclos$ is large) rather than corroborating it by an
independent route. We therefore lean on the dimension-robust random-forest / ridge
out-of-fold and binning bounds and on the combinatorial effect, which agree that
$\Bclos$ exceeds $\Binsuf$.

\paragraph{Scope and hedges.} An adversarial audit sharpened the scope without
inverting the verdict. The constant-offset bound is convention-specific---it inverts
under the deployed residual-only default, where the convention-independent bound
($\Bclos\ge0.91>0.62$) and the combinatorial fact carry the claim instead; the closure
error is chiefly a between-solvent bias (a solvent-conditional lower bound
$\Bclos\ge0.89$); the solvent-clustered confidence is $P(\Bclos{>}\Binsuf)\approx0.85$;
and the measurement is confined to the low-$\gamma$, $298$\,K, VT-2005-matchable corner,
so the numeric ceiling is not claimed to transfer to the finite-composition regime. The
four hedges are stated in full in App.~\ref{sec:si-tables}.

\input{sections/synthetic-dial}

\begin{figure}[t]
\centering
\includegraphics[width=0.74\linewidth]{figs/fig_dial.pdf}
\caption{Synthetic closure-fidelity dial. As the closure is made more
misspecified (fidelity $F$ decreasing from $1$), the true-input penalty
$\Delta R^2=R^2_{\text{true}}-R^2_{\text{learned}}$ grows monotonically, and the
same ordering holds across five misspecification functional forms (grey). The
real COSMO-SAC closure sits at $F\approx0.49$ (salmon), well inside the
misspecified regime.}
\label{fig:dial}
\end{figure}

\subsection{The ceiling is not fixable downstream (Gate B)}
\label{sec:gateb}
% Artifacts: results/e5_sigma_grounding/seed_42/grounded_a_truetrain_residual{,_v2}_predictions.summary.json (Gate B v1 gated-shut / v2 forced-open); model.py correction_force_open_gate.
If the closure binding were merely a bad point estimate, a learned output-space
correction on top of the physics prediction should recover accuracy. It does not.
An additive bounded residual with a learned confidence gate
($\hat y=\hat y_{\text{phys}}+(1-c)\,r$, with $y=\lnx$) leaves the valve
effectively shut (applied
residual $\approx0$, $R^2=0.236$). Forcing the gate open removes the systematic
\emph{bias} (from $+0.45$ to $+0.06$) but does \emph{not} recover \emph{accuracy}
($R^2$ $0.283\!\to\!0.274$). This is the classical calibration-versus-accuracy
separation: when the upstream map is misspecified, a monotone or affine post-hoc
map can fix reliability but not resolution. Recalibration buys calibration
without buying accuracy, which is what we expect if the closure, rather than the
estimate, is the binding constraint.

\input{sections/ident-compensation}

\input{sections/crystal-null}

\input{sections/data-efficiency}

\input{sections/chemistry-map}

\input{sections/supporting}

\section{Discussion}
\label{sec:discussion}
The result is a cautionary measurement for physics-informed modeling: grounding a
model on truer physical inputs is only as good as the closure that consumes them,
and when that closure is a fixed, mildly misspecified map, better inputs surface
its bias rather than help. The external latent does not make the physics path win. What it buys is
\emph{attribution}: on the infinite-dilution, low-$\gamma$ subset we can measure, the
ceiling sits in the decoder ($\Bclos$), with the inputs ($\Binsuf$) ruled out as its
source and label noise unestimable but assumed zero (and unable to manufacture the
constant offset the bounds rest on). We do not claim this numeric attribution transfers
unchanged to the finite-composition, high-$\gamma$ regime the solubility paradox itself
spans; that the solubility paradox shares the same closure-misspecification cause is a
conjecture supported by in-regime signs---the more-physics-worse effect on the full
$n{=}5608$ set and the chemistry map---not a measured fact. For practice this relocates the lever. Within this closure family, effort
spent on richer inputs or a better encoder is mis-targeted; the payoff is in the
closure itself, for example a corrected or learned residual on the map rather
than on its output (which \S\ref{sec:gateb} shows is inert).

\paragraph{The accuracy case is closed, exhaustively.} That the physics prior does not
improve accuracy is not a single observation but a pattern with no exception we could
find: the grounded model trails the matched black box on the full corpus, the
structured temperature advantage is a property of the van't~Hoff hypothesis class
rather than of the trained model, grounding the crystal factor on external labels does
not move the ceiling (\S\ref{sec:e2}), and---in the case built to favour a physics
prior---it loses at \emph{every} training-data fraction, including the low-data regime
where an inductive bias should help most (\S\ref{sec:data-efficiency}). Read together
these are not scattered nulls but evidence that the ceiling is \emph{structural}: the
crystal/activity split is non-identifiable from solubility alone
(Lemma~\ref{prop:ident}), and the activity closure is misspecified where we can measure
it (\S\ref{sec:measure}), so no amount of better inputs, more data, or auxiliary
grounding within this family recovers an accuracy the factorization cannot express. The
contribution is accordingly not an accuracy result but a method---the externally
grounded decomposition---for locating such a ceiling, and a proved account of why it
sits where it does.

\paragraph{Limitations.} (i)~$\Binsuf$ is bounded, never point-identified: there
are no true replicate measurements at fixed $\zstar$, so the insufficiency estimates
lean on a smoothness assumption. The constant-offset (Jensen) bound is smoothness-free
but assumes $m$ and $g$ share the $\lngi$ (mole-fraction, Lewis--Randall) reference
state; we check this two ways---the closure is nearly exact within the largest
single-solvent stratum (pyridine, MSE $0.07$), which a uniform reference-state offset
could not produce, and an independent recomputation reproduces the closure output---so
the mean offset is genuine decoder bias, not a units artifact. We headline the
convention-independent bound (\S\ref{sec:measure}) as the load-bearing claim.
(ii)~The assumption-free constant-offset margin is convention-specific (full COSMO-SAC)
and \emph{inverts} under the deployed residual-only default; there the separation rests
on the convention-independent bound $\Bclos\ge\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ and
the more-physics-worse effect (which we verified is robust to the molecular-volume
basis). (iii)~The measurement is at infinite
dilution and 298\,K on the VT-2005-matchable, low-$\gamma$ subset; the non-ideal
amide-solvent regime is untested. (iv)~Absolute scaffold $\lnx$ accuracy is
encoder-bound and near the aleatoric label-noise floor; this paper is not an
accuracy claim.

\section{Conclusion}
\label{sec:conclusion}
Feeding a physics-informed solubility model its true physical inputs made it
worse. What the paper contributes is a way to measure that failure: with an
externally grounded latent, the true-input excess risk splits into a
decoder-misspecification term and an input-insufficiency term, and on the
infinite-dilution, low-$\gamma$ subset we can measure, the ceiling turns out to be
the closure rather than the inputs (label noise unestimable and assumed zero). The
practical implication is to fix the map itself, since within this closure truer
inputs surface its bias and an output-space correction is inert.

\section*{Data and code availability}
All code and the intermediate result artifacts needed to regenerate every figure and
table are available at \pending[repository URL]\ (commit \pending[hash]), released
under \pending[license]. The solubility corpus is BigSolDB~2.0
\cite{krasnov2025bigsoldb}; the true $\sigma$-profiles are the VT-2005 database,
ingested to \texttt{results/sigma\_profile\_artifact/sigma\_profiles.csv}; the
experimental infinite-dilution activity coefficients are compiled from ThermoML
(\texttt{notebooks/data/raw/idac.csv}). The Bemis--Murcko scaffold split is produced
by \texttt{scripts/prepare\_data.py}. The controlled comparison
(Fig.~\ref{fig:paradox}, \S\ref{sec:map}) is produced by
\texttt{scripts/experiments/run\_e5\_sigma\_grounding.sh} and aggregated by
\texttt{scripts/analysis/run\_e5\_comparison.py} over
\texttt{results/e5\_sigma\_grounding/seed\_\{42,43,44\}/}; the 3-seed aggregate is
tabulated in \texttt{results/e5\_sigma\_grounding/THREE\_SEED\_SUMMARY.md}. The
closure--insufficiency decomposition (Table~\ref{tab:decomp},
Figs~\ref{fig:parity} and~\ref{fig:decomp}) is regenerated deterministically by
\texttt{scripts/analysis/run\_b\_insuff\_decomposition.py} $\rightarrow$
\texttt{results/b\_insuff/decomposition.json} from the committed matched-pair table
\texttt{results/b\_insuff/matched\_pairs.csv} (the $n{=}60$ VT-2005-matchable pairs).
The model checkpoints and the full per-arm prediction CSVs are large and are deposited
at \pending[Zenodo DOI] rather than in git; the named scripts regenerate them from the
raw sources. The manuscript compiles with \texttt{make} in \texttt{paper/}.

\input{sections/SI}

\bibliographystyle{plain}
\bibliography{references_verified}

\end{document}
