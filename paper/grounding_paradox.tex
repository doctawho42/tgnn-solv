% =====================================================================
% THE ARTICLE.  The Supporting Information is a separate document,
% grounding_paradox_si.tex; both \input preamble.tex and neither \inputs the other.
%
% SECTION SHAPE (2026-08-02, referee request): four sections only --
%   1 Introduction            field, related work, the assumption under test, what was done
%   2 Computational methods   the composed predictor, the identity and the conditions of its use
%                             (its proof went to the Supporting Information on 2026-08-10), the estimator,
%                             the data and its noise floor, the model and its two validations,
%                             the evaluation protocol and the run-family table
%   3 Results and discussion  the two senses of grounding; the split on the activity data;
%                             the latent's departure; the second closure; what it licenses;
%                             limitations
%   4 Conclusions
% There is no separate theory section and no separate related-work section: JCIM carries
% neither, and the material is inside 2 and 1 respectively.
%
% THE 2026-08-02 REBALANCE (editorial item A3).  Three referees, calibrating against published
% JCIM papers, reported that the article was not self-contained: every proof, the second chemical
% domain, the only comparison against published solubility models and the noise-floor derivation
% were all outside it.  So THIS document now carries, in full: Lemma 1's statement with the
% cross-term condition and the three settings of z*; Proposition 1's statement; the noise-floor
% derivation with both
% functionals; the external-baseline table; the closure-anchor table; the related-work paragraphs;
% the parity plot; a self-contained Sec. 3.5; the terminology table; and the run-family table.
% WHAT WENT THE OTHER WAY, and why that and nothing else: the map's two boundable-stratum tables
% and the enumeration of the 44 unbounded strata.  Those are DETAIL -- every row of them is
% already printed in the Supporting Information's five map tables, verified value by value -- and
% the FINDING they carry is a sentence, which stays here with its numbers.  A reader of this
% document must still be able to say which chemistry separates and which does not, and Sec. 3.5.1
% is written so that they can.
% AND ON 2026-08-10 THE FIVE PROOFS WENT BACK THE OTHER WAY TOO -- referee 3's trade, made in the
% same round as referee 1's request that Proposition 1's STATEMENT return here.  Self-containment is
% a property of the statements and the conditions of use, both of which stay; the algebra is what a
% reader consults once and it now stands in one place, \S\ref{sec:si-proofs}, instead of one proof
% here and four there.
% THE COMPRESSION CAME OUT OF HEDGING AND POINTER NARRATION, NOT OUT OF EVIDENCE.  No result,
% control, null, interval, limitation or disclosure left the submission.  If a later pass needs
% pages, take them from words; the numbers are all load-bearing and the checker
% (scripts/analysis/check_number_conservation.py) will catch any that are not.
%
% CROSS-REFERENCES: one form.  Every section reference is \S\ref{...}.  An S in the
% resolved number means the Supporting Information (achemso's suppinfo mode numbers it
% S1, S2, ...), so no prefix, sigil or "App." is needed at any call site.  Do not
% reintroduce "Sec.", "App.", "Appendix", a spelled-out "Section N", or \pageref.
% A REFERENCE IS NOT A SENTENCE.  Pointer narration -- a sentence whose content is where to
% look -- was removed in this pass and must not come back.  Put the reference in the sentence
% that makes the claim.
%
% GRADES: there is no grading vocabulary in this manuscript.  Every claim carries its
% uncertainty in the sentence that makes it -- a value, its interval or its per-seed
% spread, its n, and the set it holds on -- and a claim the data do not resolve says so
% in words.  Do not reintroduce "certified", "likely", "open", "no claim" or "retired" as labels.
% THE PROSE IS WHERE THE HEDGING LIVES, restored 2026-08-08.  Between 2026-08-02 and that date the
% claim ledger held it instead: the scope clauses had been lifted out of the sentences and into its
% value cells to shorten the prose, and this comment used to say so and told editors not to
% "re-hedge in prose what the ledger already scopes".  That trade is off -- readability is not
% traded for length -- and the ledger has left this document for \S\ref{sec:si-ledger}.  Every
% scope it was carrying is back in the sentence that makes the claim; the inventory is in the
% comment above \S\ref{sec:paradox}.  A sentence here may now repeat a scope the SI table also
% prints, and that is correct, not duplication.
%
% ONE NAME PER THING, fixed 2026-08-10 across BOTH documents.  Three objects had been running under
% synonyms, and a reader had to infer that the synonyms were one object.  The names are now:
%   * THE BINNING BOUND -- the upper bound on \Binsuf\ of Lemma 1(c), the variance of m inside bins
%     of g(z*).  It was also "the model-free law-of-total-variance bound", "the LOTV bound", "the
%     binning bound" and "the coarsening"; LOTV is gone from both documents and from the
%     \abbreviations list.  "law of total variance" survives ONLY where the LAW is invoked (the
%     proof of Lemma 1(c), and the contribution paragraph), never as the name of this estimator.
%     "coarsening" survives only as the VERB and as the sigma-algebra sense ("a coarsening of the
%     conditioning sigma-algebra"), never as a noun standing for the estimator.
%   * THE ESTIMATOR CELL -- one SETTING of that bound's four analyst choices, plus the row-or-pair
%     unit.  It is a configuration and not a second name for the bound; \S\ref{sec:estimator}
%     defines it in that sentence and Table~\ref{tab:notation} carries both rows so the two cannot
%     be read as synonyms.  It was also "the eight-bin coarsening cell".
%   * THE UNBIASED WITHIN-BIN VARIANCE -- the variance convention, glossed "(Bessel-corrected)" ONCE,
%     at \S\ref{sec:estimator}, and nowhere else.  It was also "the within-bin Bessel correction" and
%     "the Bessel-corrected binning"; its alternative is the maximum-likelihood within-bin variance.
% AND THE COMBINATORIAL CONVENTION IS "RESIDUAL-ONLY", NOT "DEPLOYED".  "Deployed" was carrying three
% senses -- ordinary English in the title and the abstract, a kernel vintage, and this named
% convention -- so the convention was renamed, which is the sense that had a spare name available:
% the pair is now \emph{full} against \emph{residual-only}.  Every remaining "deployed" in either
% document is ordinary English ("as deployed", "the deployed pipeline", "the deployed closure").
% Do not reintroduce "deployed convention", and do not name a kernel "the deployed" one.
%
% NUMBER PROVENANCE: tab:runs names the run family, the seed count, the script and the
% artifact behind every number in this document.  It is in the Supporting Information, at the head
% of sec:si-repro, since 2026-08-08 -- it is provenance and not evidence, and no claim here is read
% off it -- and \ref{tab:runs} resolves to its S-number from either document.  Fig. 2 / map numbers
% regenerate from results/e5_sigma_grounding/ and results/b_insuff/.
%
% LAYOUT.  `layout=twocolumn' is the published-article measure and is what the page
% counts refer to.  ACS expects the double-spaced single-column measure at submission:
% drop `,layout=twocolumn' in BOTH files for that.  Re-measure page counts from the
% build, never from a comment.
% =====================================================================
\documentclass[journal=jcisd8,manuscript=article,layout=twocolumn]{achemso}
% The preamble proper -- packages, macros, theorem environments, float tuning, the
% amsthm/achemso patches and the line-breaking parameters -- is shared verbatim with
% the Supporting Information and lives in preamble.tex.
\input{preamble}
% enumitem, for the fourteen-item limitations list of \S\ref{sec:discussion} (2026-08-18).
% It is loaded here rather than in preamble.tex for the reason the Supporting Information
% gives at its own \usepackage line: preamble.tex is shared, and a package that redefines the
% standard list defaults goes only into the documents that use it.
\usepackage{enumitem}
% The other half of the submission. xr-hyper (loaded in preamble.tex) reads
% grounding_paradox_si.aux, so every \ref{} in this file that names a label defined
% in the Supporting Information resolves to its S-number without any change at the
% call site. The first optional argument, the label prefix, is empty because the two
% documents' label names are disjoint; that is checked by
% scripts/analysis/check_split_refs.py, which fails if a name is defined in both.
% THE SECOND OPTIONAL ARGUMENT IS LOAD-BEARING. Without `nocite' xr-hyper also
% imports the other document's \bibcite lines, and since the two bibliographies
% number the same work differently every key cited in both is reported "multiply
% defined". `nocite' stops the import: each document numbers its citations only from
% its own bibliography.
\externaldocument[][nocite]{grounding_paradox_si}
% TITLE, 2026-08-11 (fourth revision).  ONE clause, twelve words.  Keep it identical to the \title
% in grounding_paradox_si.tex.
%
% "SOLVENT" ENTERED 2026-08-11 (referee round 6).  The substitution the paper is named for reaches
% 99.3% of the labelled test rows on the solvent side and 5.3% on the solute side (\S\ref{sec:eval}),
% so what is measured is the substitution of a reference SOLVENT profile; \S\ref{sec:eval} and
% \S\ref{sec:discussion} said so and the title did not.  Twelve words is JCIM's stated preference,
% so the word is affordable and the scope is no longer left to the body.
%
% WHY IT IS ONE CLAUSE NOW.  JCIM's guide for authors asks for "preferably 12 words or fewer"; the
% two-clause form below ran to 22, and all three referees this round stopped on the same structural
% fault rather than on the length: the second clause is a semicolon away from the first, its subject
% is a gerund the reader does not reach until the ninth word of it, and it ends on a negation
% ("Not a Verdict") that tells a reader what the paper did not return.  Twelve words cannot carry
% two findings, so one had to go.
%
% WHICH FINDING THE TITLE KEEPS, AND WHY THAT ONE.  It keeps the substitution.  Three reasons, in
% the order they decided it:
%   (a) It is the finding the article is named for and says so in its own text
%       (\S\ref{sec:paradox}: "that substitution alone is the effect this paper is named for").
%       The map is what the second axis returns; the substitution is what happened.
%   (b) It is the finding that survives compression.  The substitution is three seeds with no
%       per-seed overlap on either arm.  The map's headline is one row set out of fifty-nine, whose
%       margin a chemistry-blind relabelling reaches in a tenth of draws and for which no
%       out-of-sample rows exist (\S\ref{sec:broad}) -- it cannot be stated in a title without the
%       scope clauses a twelve-word limit forbids, and the two forms that tried are struck below.
%   (c) A title clause whose content is what an instrument's OUTPUT is shaped like ("Returns a Map,
%       Not a Verdict") is a methods statement, not a result, and it is the clause the referees
%       reported having to re-read.
% The second finding is not lost to the reader who searches: it is indexed by the keyword
% "infinite-dilution activity coefficients", added in the same pass for exactly that reason, and it
% is the subject of \S\ref{sec:measure}, of the abstract's middle, and of half of Fig. 1.
%
% THE HEDGE IS GONE WITH THE CLAUSE.  "Not a Verdict" was the manuscript's only hedge above the
% abstract.  Nothing replaced it: the surviving clause asserts a measured direction and carries no
% qualifier, and its scope (which substitution, of what, for what) is now IN it rather than trailing
% it -- "for Learned Ones" is the four words the old form left to the reader.
% Do not re-attach a scope, a hedge or a second finding here.  The place a scope belongs is the
% sentence that makes the claim, and every one of them is in the body.
%
% THE THREE CLAIMS THIS TITLE HAS SHED, IN ORDER, AND WHY NONE MAY COME BACK.
%
% (1) "and Separates COSMO-SAC Error from Input Error", the form before 2026-08-05.  Wrong on
%     SCOPE -- the separation holds on one row set of fifty-nine, not on either set whole -- and
%     wrong on GRAMMAR, which here is arithmetic: the subject of "Separates" was the gerund
%     "Substituting reference sigma-profiles", so the sentence said the SUBSTITUTION separates
%     the two error terms.  It does not; the law-of-total-variance decomposition does, on
%     infinite-dilution activity data, a different measurement on a different set.
%
% (2) "and COSMO-SAC Error Exceeds Input Error on One Solvent Class of Activity Data", the form
%     that replaced it.  That fixed the scope error and states a CHEMICAL LOCALISATION: the
%     error is placed on a named class of solvents.  Two of this paper's own results now decline
%     to license it.  The multiplicity null (Sec. S3.1) shows a chemistry-blind relabelling of
%     the same molecules certifies at least one row set in 58% of 2000 draws and returns a
%     certified margin of +2.04 or more in 10.1% -- so the outcome the clause asserts as a fact
%     about chemistry is one a blind search returns more often than not.  And the pre-declared
%     out-of-sample test (Sec. 3.5.1) returned NOT TESTABLE: the only out-of-sample IDAC set that
%     exists carries no glycol-ether solvent with an organic solute at all, and its nearest cell
%     is inadmissible under the paper's own rule.  A title cannot carry "survivor of a 59-fold
%     search", so the localisation leaves the title and lives in Sec. 3.5.1 with its null and the
%     absence of any confirming data attached.  Do not restore it if the abstract prints the 58%:
%     the abstract now prints it, and the clause is still not earned.
%
% (3) "; on Activity Data, Bounding a Deployed COSMO-SAC Against Its Inputs Returns a Map, Not a
%     Verdict", the second clause of the 2026-08-05 form, dropped 2026-08-10.  It was EARNED --
%     that the decomposition's output is a map over chemical strata rather than one verdict is the
%     finding the multiplicity null does not touch, it is the reason an aggregate margin is a
%     composition statistic (Sec. 3.5.1, and the random-partition null in Sec. S3.1: observed MSE
%     range 2.163 against a null median of 0.405, no draw reaching it, while the bound's range is
%     at chance), and the out-of-sample set supports it, its classes running from -2.53 to +1.73 at
%     the same cell.  It goes for length and for structure, not because it stopped being true.
%     Every part of it that a reader needed the clause for is stated where it is measured: "on
%     Activity Data" at the head of Sec. 3.5 and in the abstract's "On infinite-dilution activity
%     data", which is what keeps the cross-regime conflation of limitation (i) out of the reading;
%     and "a Deployed COSMO-SAC" -- B_closure being the error of the fixed kernel together with the
%     one computed conformer it reads, and not of the kernel alone -- in Sec. 2.1.1, Table 1 and
%     Sec. 3.5.3, all three of which say it at more length than a title can.
%     If it is ever restored, it must come back as the WHOLE title and the substitution must leave;
%     two findings do not fit in twelve words and the semicolon form is what the referees rejected.
%
% WHAT THE SURVIVING CLAUSE SAYS, AND WHY EACH WORD IS THERE.  "Substituting Reference
% $\sigma$-Profiles for Learned Ones Degrades COSMO-SAC Solubility Prediction", ten words.
%   "for Learned Ones" is new and is the repair of a real ambiguity: the old form said what was
%       substituted and never what it was substituted FOR, so a reader could take it as substituting
%       reference profiles for a quantum-chemical calculation, which is the pipeline's ordinary
%       operation and not a finding.
%   "COSMO-SAC" names the closure the degradation is measured through, so the title is findable by
%       the mechanistic model and does not read as a claim about solubility prediction at large.
%   The subject is the gerund "Substituting" and the verb "Degrades" takes it, so the grammar bug of
%       form (1) below cannot return.
%   There is no "at Inference".  The two operations differ by when the reference enters, and the
%       title names only the one it is about; adding the timing costs a word and buys a distinction
%       that has no counterpart in the title to be told apart from.  The abstract draws it.
\title{Substituting Reference Solvent $\sigma$-Profiles for Learned Ones Degrades COSMO-SAC
Solubility Prediction}

% NOTE: achemso re-typesets the arguments of \affiliation and \email, so anything outstanding here is
% written as a plain comment rather than through \pending.
% ===================== PRE-SUBMISSION CHECKLIST =====================
% Five items are outstanding. None can be settled from the manuscript; all are author input.
%   1. ORCID for A. V. Rudik. N. L. Polomoshnov is 0009-0001-4342-8539. ORCIDs are entered in the
%      ACS submission form, not typeset here, so this blocks the form and not the PDF.
%   2. Street address and postcode of the Faculty of Bioengineering and Bioinformatics, MSU.
%   3. Street address and postcode of the V. N. Orekhovich Institute of Biomedical Chemistry.
%   4. Whether A. V. Rudik also corresponds; if so, add a second \email under her \author block.
%   5. The AI-use disclosure, removed from the acknowledgement at the author's request and to be
%      rewritten. ACS asks contributors to declare generative-AI use and the manuscript now carries
%      none, so this is the one outstanding item a reviewer would notice.
% CONFIRMED, do not re-flag: the Author contributions block is correct as printed.
% Two more, of a different kind, are in the Data availability statement and are handled by \pending:
% the archive commit hash and the Zenodo DOI.
% ==================================================================================
\author{N.~L. Polomoshnov}
\affiliation[MSU]{Faculty of Bioengineering and Bioinformatics, Lomonosov Moscow State
University, Moscow, Russia}
\alsoaffiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}
\email{nikitapol@fbb.msu.ru}
\author{A.~V. Rudik}
\affiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}

% THE TWO PROFILE DATABASES ARE BOTH LISTED, 2026-08-10 (referee item: they must be
% distinguishable wherever either is named).  UD alone was listed, so the list itself invited the
% reading that there is one database with a name and one without.  Both are now here, each with the
% institution that produced it and the role it plays in this paper, and the same contrast is drawn
% in the sentence that first uses either (Sec. 2.1) and in Table 1.  Do not drop VT-2005 from this
% list to save a line: the abbreviation is used in a table caption, in a figure panel and in five
% sections, and "the successor" only means something beside the thing it succeeds.
\abbreviations{SLE, solid--liquid equilibrium; IDAC, infinite-dilution activity
coefficient; LFER, linear free-energy relationship; NRTL, non-random two-liquid;
VT-2005, Virginia Tech 2005 (the $\sigma$-profile database supervised against and substituted from);
UD, University of Delaware (the successor $\sigma$-profile database, matched to the activity rows)}
% KEYWORDS.  JCIM's submission form takes them and the manuscript has carried five since
% 2026-08-02; nothing was missing.  ONE WAS ADDED on 2026-08-10, in the pass that cut the title to
% one clause: "infinite-dilution activity coefficients".  The title no longer names the second
% axis, so the keyword list is now the only place above the abstract where a search on that data
% type finds this paper -- which is the one thing the shortened title gave up.  Six is inside the
% form's limit.  Do not add a seventh to make up for anything else the title dropped: the rest of
% what it dropped is a scope, and a scope is not a search term.
\keywords{solubility prediction, physics-informed machine learning, COSMO-SAC,
infinite-dilution activity coefficients, model misspecification, representation probing}

\begin{document}

% ACS requires a table-of-contents graphic, and it is the one image the journal places beside
% the article, so it may only carry a claim this paper still makes. It prints the two senses of
% grounding and their opposite signs, which is the title's first clause. Regenerate with
% scripts/analysis/make_toc_figure.py. Do not restore an aggregate here, and do not restore the
% two MAGNITUDES either: the pair -0.20 / +0.41 asserts a two-fold asymmetry that the
% co-adaptation control withdraws, and the pair -0.20 / +0.18 juxtaposes a three-seed number with
% a one-seed one that Sec. S3.1 says cannot be ordered against it. The graphic's own docstring
% carries the argument in full; the signs hold at every seed and are what it may print.
% SIZE. achemso puts the graphic in a \vbox to 1.75in inside a 3.25in minipage. Bounding
% the height as well as the width lets the aspect ratio pick whichever limit binds.
\begin{tocentry}
\includegraphics[width=\linewidth,height=1.7in,keepaspectratio]{figs/fig_toc.pdf}
\end{tocentry}

\maketitle
% ABSTRACT.  THREE RULES, and they are the whole of what governs this block.
% (1) THE ABSTRACT CARRIES THE MAGNITUDES ITS FINDINGS TURN ON.  Reversed 2026-08-10 by the
%     author, and the reversal is recorded here rather than left as a silent edit because the rule
%     it replaces stood for eight days and is quoted in two other files.  What it replaced read
%     "NO NUMERALS.  Not one figure, interval, seed count, sample size or percentage", on the
%     ground that a number here is one a reader carries away without the clause that scopes it.
%     What decided it: the supervising professor's letter objects to an abstract "overloaded with
%     numbers and qualifications" and asks for "only the key qualitative conclusions" -- and
%     OVERLOADED IS NOT CONTAINS.  Against that, three referees independently asked for magnitudes,
%     one of them having measured the venue: five of six recent JCIM Articles print numbers in the
%     abstract, including both whose headline finding is a deflation, which is this paper's genre.
%     The letter's target is the PILE OF QUALIFICATIONS, and that is what this block spends (below).
%     WHAT PRINTS, and nothing else may be added without taking words from connective tissue:
%       the three arm means of \S\ref{sec:paradox}, $2.04$ -> $1.93$ -> $2.34$ in \lnx\ MAE, so
%         that both operations are stated in one unit and the reader can take either difference.  CORRECTED 2026-08-19: this rule named the THREE-seed
%         family's means ($2.043$/$1.846$/$2.252$) while the block below carried the
%         five-seed re-run's, so the rule and the text it governs had disagreed since
%         the discharge.  Two decimals now, because the between-seed sd is $0.06$-$0.09$
%         and the third digit is noise; the body keeps three where a spread earns it;
%       THE RANKING RESULT, added 2026-08-19.  A Spearman that falls at every seed is what a
%         practitioner choosing a solvent loses, and it was absent from this block while the
%         MAE it qualifies was in it twice.  DO NOT ADD "and unlike the MAE it survives
%         recalibration": that was the reason first written here and it is FALSE.  The map
%         that removes 91% of the MAE gap is not monotone -- its fitted slope is negative in
%         10-21% of groups by arm and seed -- so the rank-invariance argument does not cover
%         it, and applying it removes about a third of the Spearman gap.  What survives the
%         restriction to the 485 of 823 groups where both arms' maps increase is rho -0.107,
%         not -0.185, with nDCG@3 and top-1 then spanning zero (the restriction is stated where it is measured).
%         The block prints the unrestricted fall, which is a plain measurement, and makes no
%         invariance claim.  It was
%         paid for with the drift sentence of \S\ref{sec:surrogate}, whose magnitudes
%         \S\ref{sec:discussion} itself calls unstable across analysis configurations;
%       the standing margin with the scope a one-sided bound needs -- $2.04$, $n{=}182$ WITH ITS
%         CLUSTER COUNT $43$ pairs, the $90\%$ interval $[+1.25,+2.87]$ -- and the $59$-fold
%         search it is the survivor of.  THE PAIR COUNT IS NOT OPTIONAL BESIDE THE ROW COUNT.
%         This project's standing rule is that the effective n is the number of clusters, not the
%         number of rows, and the two differ here by a factor of four; the SI records that $141$
%         of the $182$ rows are one oligomer family and that the worst single-publication deletion
%         leaves $+1.60$.  A bare $n{=}182$ therefore reads in the paper's favour, which is the
%         direction that has to be guarded.  Print both counts or neither -- and neither is not
%         available, because dropping the row count drops an n;
%       the null frequencies in words ("in most draws", "in a tenth") WITH THE ESTIMATOR NAMED,
%         "under the binning bound".  The estimator is not decoration: the pre-declared cross-fitted
%         bound puts the same frequency at $0.137$ on its $473$ rows against the binning bound's
%         $0.094$ there, so an unattributed "a tenth" names no quantity.  \S\ref{sec:broad} carries
%         the comparison; the abstract carries the attribution.
%     DO NOT print the pair $-0.20$ / $+0.41$ here.  The endpoints give a reader either difference
%     and the sentence after them refuses the ordering in the same breath; the pair asserts the
%     two-fold asymmetry directly, which is what the graphic for the table of contents is forbidden
%     to print and for the same reason.  Do not print $5608$, $8103$, $477$ or $58\%$: the row sets
%     and the certifying frequency are carried in words, and every one of them is in the body.
% (2) NO SECTION REFERENCES.  An abstract that points into the document is not self-contained.
% (3) THE CEILING IS 250 WORDS and it still binds.  Re-count after ANY edit with
%     scripts/analysis/wc_abstract.py and take the number the script prints, not the number a
%     previous comment printed -- carrying a count forward unchecked is how the old note here
%     came to be wrong three times.  The operative rule is the script's third ("as rendered"),
%     because the production editor's tool reads the page and not the source.
% WHAT MUST NOT BE DROPPED TO SAVE WORDS, in order: that supervision's gain is not certified
% leak-free; that the two operations' magnitudes do not resolve against each other, so only the
% signs carry; that the input bound's flatness is an estimator property and not chemistry; that
% the row set is one of many searched and a chemistry-blind relabelling reaches its margin often;
% that the out-of-sample verdict is NOT TESTABLE and not a failed replication; and the four
% directions the separation must not be over-read in -- no other stratum, neither data set whole,
% nothing on the solubility axis, and the deployed model rather than the kernel alone.  If words
% are ever needed, they come out of connective tissue, never out of one of those.
% THAT LIST SURVIVED THE NUMBERS PASS INTACT, clause for clause, and it was the constraint that
% decided what the magnitudes were paid for with.  Every item above is still in the block; two are
% in new words and neither changed what it scopes.  Where a protected clause and a magnitude
% competed for the same word, the clause won -- which is why "interpretable" is gone from the first
% sentence and the $90\%$ interval is not.
%
% THE NUMBERS PASS, 2026-08-10.  WHAT WAS BOUGHT: $2.043$, $1.846$, $2.252$, \lnx\ as the unit
% they are in, $2.04$, $n{=}182$, $90\%$, $[+1.25,+2.87]$, $59$-fold, and "under the binning bound".
% WHAT PAID FOR IT, in order of size, and every one of them is connective tissue or a term this
% document defines only later -- which is where the referees said the words were:
%   -8  "The output is a map, most strata unboundable." and "Grounding names two operations."
%       Both are frames rather than findings.  "map", "strata" and "unboundable" are three of the
%       four terms a referee named as defined only later in the paper, and the frame they set up is
%       now set by the sentences themselves: the two operations are named as they are measured, and
%       "the survivor of a $59$-fold search" says in five words that a search ran and one row set
%       came out of it.  The word "grounding" survives, in the sentence that uses it.
%   -4  "Trained through this closure," on the departure sentence.  Every sentence in the block is
%       about this closure; the participle restated it.
%   -3  "only the signs carry" -> "only their directions hold", and "Neither resolves against the
%       gain" -> "Grounding's two operations cannot be ordered by size".  This is the fourth term
%       the referee named, and the new form is \S\ref{sec:paradox}'s own sentence ("Which of the two
%       operations is larger is therefore unresolved, and only their signs are carried forward")
%       in the abstract's register.  IT IS LOAD-BEARING NOW IN A WAY IT WAS NOT: with the endpoints
%       printed a reader can compute $0.197$ against $0.406$ and order them, so the sentence that
%       refuses the ordering has to sit against numbers rather than against nothing.  It may not be
%       shortened again and it may not move away from the pair.
%   -2  "stratum"/"row set" -> "subset", the last of the four; "certifies some row set" ->
%       "certifies something".
%   -1  "interpretable" in the first sentence.
%   -1  "learned" in "The learned profile departs from its reference".  The contrast is carried by
%       "its reference", and the full form is still where (C) needs it, in the substitution
%       sentence.  This one was spent on a defect in the GATE, not on a word: wc_abstract.py counted
%       a math atom as one word, and $\ln x_2$ reaches the page as TWO tokens ("ln x2"), which was
%       the one thing its third rule did not model.  A plain pdftotext count of the built page read
%       251 while the script read 250.
%       THE GATE NOW MODELS THE ATOM (2026-08-10) and the three notes that described it now say one
%       thing.  `rendered_atom' in wc_abstract.py splits an atom on commas, on TeX's operator names
%       and on the explicit spacing commands, all three of which put a space on the page, so the
%       script's third rule and pdftotext agree at 250.  Two consequences, and both are the
%       opposite of reassuring: the block was AT its ceiling and had been recorded as under it,
%       and the script now prints "AT (no words left)" rather than "under" when the count equals
%       the ceiling, because a block with nothing left behaves like one over budget on the next
%       edit.  The note that read "both under the ceiling" was wrong in the direction that lets a
%       word in; SI \S\ref{sec:si-repro} and the discharge sheet each attributed the 250 to the
%       script, where it was the page's.  Still count the page after any edit that adds a math
%       atom -- pdftotext -f 1 -l 1 on the built article -- because the gate models the spacing it
%       has been taught and no more.
% WHAT WAS NOT SPENT, and the next pass should not go looking there: no result, no control, no
% null, no interval, no limitation and no disclosure left the block, and the do-not-drop list above
% is complete in it.
%
% THE ADVERSARIAL PASS, 2026-08-10, WHICH COST THE NUMBERS THREE WORDS AND BOUGHT BACK THREE.  Two
% reads of the block with the magnitudes in it found three things they had done to the prose.  All
% three repairs are below; the block is 250 as rendered before and after, so nothing was financed
% by a scope.
%   (i)  THE ZEUGMA.  "raises the error to $2.252$, and by less during training at one seed"
%        coordinated a LEVEL and a DELTA under one elided verb.  Before the magnitudes went in,
%        both conjuncts hung off a bare "raises the error" and the ellipsis was clean; "to $2.252$"
%        broke it, and no single-verb form can carry both, because the second conjunct needs a
%        predication of its own.  It now reads "raises the error to $2.252$ at inference; the rise
%        during training is smaller, at one seed, where it co-adapts" (+1 word).  WHAT THE REPAIR
%        HAD TO KEEP, and does: (C)'s shared grammatical subject, so "at inference" left the
%        subject and became an adjunct on the first clause; the two arms told apart by WHEN, "at
%        inference" against "during training", both verbatim; the level on the arm that has one and
%        the comparison on the arm that does not; "at one seed" scoping the training arm ALONE; and
%        "where it co-adapts" as the restrictive relative it has to stay.  Do not restore a single
%        verb here, and do not give the training arm a level -- there is no fourth arm value to
%        print and the pre-commitment forbids adding one.
%   (ii) $n{=}182$ WITHOUT ITS CLUSTER COUNT.  Handled under WHAT PRINTS above; "$43$ pairs" costs
%        two words and is the standing rule's unit.
%   (iii) THE THREE PAGE-1 NUMERALS NO CHECKER COULD REACH.  $2.043$, $1.846$ and $2.252$ were the
%        only printed arm means in either document that nothing verified, because
%        check_hand_transcribed_displays.py locates a display by \label and the achemso abstract
%        environment has none.  The block is now delimited by the %<abstract-numerals> markers
%        below and the three are bound to the same artifact the body's copies use.  KEEP THE
%        MARKERS: deleting either one is a fatal finding, not a silent loss of coverage.
% WHERE THE THREE WORDS CAME FROM, none of them a scope and none on the do-not-drop list:
%   -2  "during training" on the SUPERVISION sentence.  Supervision has no other time to happen in,
%       and the when-axis is set one sentence later by the contrast that needs it.  It was also the
%       one place the same phrase named two different training-time operations -- supervising the
%       profile and substituting for it -- which invited them to be read as one.
%   -1  "expecting accurate references to help" -> "; accurate references should help".  The
%       expectation is the premise under test either way, "We measure that" says so in the next
%       sentence, and the copula is the register the assertion pass asks for.
% THE MAGNITUDES ARE ENROLLED IN THE PRE-COMMITMENT, in the same pass that printed them and before
% the leak-free re-run's numbers exist: the display inventory of \S\ref{sec:si-repro} carried "the
% abstract's seed count and its leak-free clause, WHICH CARRY NO VALUE OF THEIR OWN", which does not
% reach a printed $2.043$.  Its first item now names all three arm means and says which branch
% rewrites each.  If a later edit adds a fourth arm value here, it goes into that item in the same
% pass -- and after the seeds land it cannot go in at all, so it cannot be printed here either.
%
% THREE DISTINCTIONS THE NUMBERS USED TO CARRY, restored in words on 2026-08-07 after all three
% referees found them missing from the numberless draft.  Each is a WRONGNESS and not a looseness,
% so none may be traded back for words.
% (A) "THE SAME REFERENCE" NAMED ONE TABLE WHERE THERE ARE TWO.  The evaluation-time substitution
%     of \S\ref{sec:paradox} is the VT-2005 database; the standing glycol-ether margin is computed
%     on the UD profiles the broad IDAC set is matched to (\S\ref{sec:broad}, and the
%     database-to-database bracket of \S\ref{sec:badclosure}, which exists precisely because the
%     two sides name different databases).  What the two axes share is that a tabulated reference
%     for the intermediate exists on both -- which is what "another such database" now says, at no
%     word cost, "such" reaching back to "a quantum-chemical database tabulates them".  Do not
%     write "the same reference", "the same database" or "that reference" here.  Fig. 1 carried the
%     same conflation in its panel labels and was corrected in the same pass
%     (scripts/analysis/make_overview_figure.py); the two must stay in step.
% (B) "IT SURVIVED A SEARCH" CLAIMED THE OPPOSITE OF \S\ref{sec:broad} AND LIMITATION (vi).  A
%     chemistry-blind relabelling certifies some row set in most draws and reaches the observed
%     margin in a tenth of them, so the row set is the SURVIVOR OF the search, not something that
%     survived a null.  It now reads "the survivor of a $59$-fold search, not an established
%     effect" -- "not ESTABLISHED" and not "not a chemical effect", because
%     the null bounds what was shown and not what is true.  Do not restore any verb that puts the
%     finding on the passing side of a test.  The count came in on 2026-08-10 in place of "across
%     every stratum", which said the same thing in two more words and in one of them; $59$ is
%     \S\ref{sec:broad}'s own figure for the search and is what makes "the survivor of" a size.
% (C) ONE GRAMMATICAL SUBJECT CARRIED TWO ARMS, and mis-described the second.  The co-adaptation
%     control does not remove the test-time swap by declining to substitute: it substitutes the
%     reference DURING TRAINING, so the model co-adapts to it (\S\ref{sec:paradox}).  The subject
%     is therefore now the operation both arms share -- substituting the reference for the learned
%     profile -- and the arms are told apart by WHEN, "at inference" against "during training".
%     "by less, at one seed" is the seed-42 pair (+0.18 co-adapted against +0.43 evaluation-only,
%     read off one comparator); it may not be widened to the three-seed headline.  It reads
%     "the rise during training is smaller, at one seed" since the zeugma repair of the adversarial
%     pass above, which changed the register of the comparison and not one thing about its scope.
% WHERE THE WORDS CAME FROM.  The repairs cost four net; two were paid back out of connective
% tissue, neither of it a scope clause -- "solubility mean absolute error" lost "solubility"
% (\S2's "We measure that for solid--liquid solubility" already fixes the axis, and the switch is
% marked at "On infinite-dilution activity data"), and one "so" became a colon.  248 as rendered.
%
% TWO MORE REPAIRS, 2026-08-07, and the SECOND ONE IS PAID FOR -- read the ledger before touching
% any of the eleven words it spends.  249 as rendered; re-run wc_abstract.py, do not trust this.
% (D) "FIXED COSMO-SAC TURNS TWO INTO A SOLUBILITY" WAS FALSE, and false in the direction that
%     flatters the paper: COSMO-SAC returns an activity coefficient, and the solubility needs the
%     crystal term as well (Eq. 1, \lnx = -\Phicry - \lng).  Attributing the whole prediction to
%     the closure inflates exactly the object the paper is measuring the error OF.  It now reads
%     "fixed COSMO-SAC and a crystal term turn two into a solubility", and the sentence was
%     re-ordered so the database attaches to the profiles it tabulates ("charge-density profiles
%     a quantum-chemical database tabulates"), which pays one of the four words back.  Do not
%     write a sketch in which the closure alone reaches a solubility.
% (E) THE SECOND CLOSURE WAS MISSING while the Introduction's last paragraph promises exactly its
%     generality ("Neither consequence is peculiar to charge distributions").  An abstract that
%     omits it leaves the paper's own generality claim unsupported at the only place most readers
%     reach.  Added as one sentence, eleven words: "A Hammett $\mathrm pK_a$ closure reverses the
%     substitution's sign where its constants suffice."  It carries the substitution consequence
%     and not the drift one, because the drift is measured on solubility only; and "where its
%     constants suffice" is the clean meta/para pole (\S\ref{sec:pka-main}), so the sentence also
%     says, by implication, that the sign does NOT reverse where they do not -- which is the
%     agreement with the solubility result.  Do not widen it to "both consequences".
% WHAT (D) AND (E) COST, AND WHY EACH TRADE IS THE RIGHT ONE.  Thirteen words, none of them a
% scope clause and none of them on the do-not-drop list above:
%   -3  "not one verdict" -- the TITLE ends "Returns a Map, Not a Verdict", and "the output is a
%       map, most strata unboundable" states it positively two words earlier.
%   -2  "across strata" in "The input bound barely moves" -- "the output is a map, most strata
%       unboundable" in the same sentence supplies the unit it barely moves across.
%   -2  "nothing on the solubility axis" -> "nothing on solubility".  The direction survives
%       whole; only the word "axis" goes, and the abstract names no other axis.
%   -1  "opposite-signed" in "two opposite-signed operations" -- "lowers" against "raises" and
%       "only the signs carry" say it twice more in the same breath.
%   -1  "improve accuracy" -> "help", the paper's own verb for this quantity.
%   -1  "the model co-adapts" -> "it co-adapts"; the subject has not changed since "raises it".
%   -1  "so only the signs carry" -> "; only the signs carry".
%   -1  "the glycol-ether solvents" -> "glycol-ether solvents" (apposition, no article).
%   -1  "chemical" in "not an established chemical effect".  What (B) protects is "not
%       ESTABLISHED"; the noun it qualifies is a row set of one chemistry either way.
%   +0  "the observed margin" -> "that margin", whose antecedent is the margin two sentences up.
% What was NOT touched, and may not be: "not certified leak-free", "over three seeds", "at
% inference"/"during training", "only a lower bound", the four over-reading directions, "in most
% draws", \emph{not testable}, "on separate three-seed runs", and "a deployed thermodynamic
% model" -- dropping "thermodynamic" would let "the deployed model" be read as the network.
%
% THE ASSERTION PASS, 2026-08-10.  The supervisor's instruction is that a qualification blurs a
% statement into saying neither yes nor no, and that the main text should be unambiguous.  It
% collides with the do-not-drop list above, which several referees demanded and which is a list of
% SCOPES, not of hedges.  The resolution is register, not deletion: a scope stated as a trailing
% fragment reads as a hedge ("X, though only a lower bound"), and the same scope given a subject and
% a verb reads as a fact ("X is a lower bound").  Eleven clauses were rewritten that way; NOTHING on
% the do-not-drop list left the block, and no clause changed what it scopes.  What moved:
%   "lowers MAE, over three seeds, not certified leak-free"
%       -> "lowers MAE over three seeds; that gain is not certified leak-free"   (own predication)
%   "raises it, at inference and---by less, at one seed---during training"
%       -> "raises the error at inference, and by less during training at one seed"
%       (the em-dash parenthesis is gone; both scopes are now on the main verb, and "the error"
%        replaces "it" because the preceding sentence now ends on "that gain")
%       -> "raises the error to $2.252$ at inference; the rise during training is smaller, at one
%        seed" once the level went in.  Putting both scopes on the main verb is exactly what stopped
%        working when one of them acquired a magnitude, and the second clause is a predication in
%        its own right, which is the direction this pass was pushing anyway.
%   "moves---an estimator property, not chemistry---while the error does"
%       -> "moves. That is an estimator property, not chemistry; the error moves."  ("That" has
%        exactly one antecedent, the sentence before it, which is the flatness and not the contrast)
%   "by a margin itself only a lower bound"   -> "That margin is a lower bound."
%   "---and no other stratum, ..."            -> "no other stratum stands, ..."   (finite verb)
%   "It is the survivor"                      -> "That set is the survivor"  ("one row set" left the
%        sentence before it, so the pronoun would have taken a plural antecedent)
% THREE CLAUSES COULD NOT BECOME ASSERTIONS WITHOUT CHANGING MEANING AND WERE KEPT AS THEY WERE:
%   "where it co-adapts"     -- a restrictive relative naming the mechanism of the training-time
%        arm; predicating it separately ("it co-adapts there") asserts co-adaptation as a measured
%        finding, which is the reading \S\ref{sec:paradox} declines to make.
%   "on separate three-seed runs"  -- a prepositional adjunct on an assertion, already not a hedge;
%        "separate three-seed runs show it" makes the runs the subject and the drift the object,
%        which reverses what was measured on what.
%   "where its constants suffice"  -- a restrictive relative that IS the finding's boundary; any
%        separate predication either widens the sign reversal past the clean pole or states the
%        degraded pole as a second result, and \S\ref{sec:pka-main} reports one.
% THE BUDGET PAID FOR IT out of the two places a scope was being said twice, not out of a scope:
% "one row set standing, glycol-ether solvents" -> "glycol-ether solvents standing" (the count is
% carried by "no other stratum stands" in the next sentence), and "by a margin itself only a lower
% bound" lost "itself only" when the margin got its own copula.  Re-run wc_abstract.py.
% THE NUMBERS PASS LATER THE SAME DAY LEFT EVERY ONE OF THOSE PREDICATIONS STANDING and touched the
% wording of two, neither in the direction of a hedge: "no other stratum stands" is "no other subset
% stands" (the same finite verb, the noun plainer), and "Neither resolves against the gain; only the
% signs carry" is "Grounding's two operations cannot be ordered by size; only their directions hold"
% -- still one assertion and one refusal, both with subjects and finite verbs.  The three clauses
% that could not become assertions are all still as they were.
%<abstract-numerals>  DO NOT DELETE -- see find_abstract() in
% scripts/analysis/check_hand_transcribed_displays.py.  These two comment lines are the only
% handle any checker has on this block: achemso's abstract environment takes no \label, so
% without them the three arm means on page 1 are unverified.  Deleting either one is a fatal
% finding, not a silent loss of coverage.
\begin{abstract}
Physics-informed models route a prediction through a quantity that can be looked up, so supplying
the tabulated value ought to improve them. We test that assumption for solid--liquid solubility,
where a graph neural network predicts the molecular charge-density profiles a fixed COSMO-SAC layer
consumes and quantum chemistry tabulates independently. Grounding names two opposite operations,
and they do not behave alike. Substituting the tabulated profile for the learned one at prediction
time makes solubility prediction worse at every seed, and degrades the ranking of solvents for a
solute, which is the decision the model is used for. Supervising the same network against the same
database during training carries no established sign. The operation that fails is the one available
in practice, because it needs no retraining. Infinite-dilution activity data separate a deployed
thermodynamic model's own error from what its inputs leave unresolved, and the model's error
dominates on one solvent chemistry. A search over strata identifies that cell, and an independent
set reproduces it at half its size without meeting this paper's own stability rule, so we report it
as the instrument's output and not as an established effect. Where the fixed model is the binding constraint, a more accurate input exposes its
misspecification instead of repairing it.
\end{abstract}
%</abstract-numerals>

% INTRODUCTION.  Field, related work, the assumption under test, what was done and what was
% found, roadmap.  JCIM carries no standalone related-work section, so paragraphs 2-4 are it;
% criterion (5) still holds -- they describe the present work only in the paragraph beginning
% "What this literature lacks".
%
% THE ROADMAP PARAGRAPH IS THE LAST ONE, restored 2026-08-10.  This comment used to read "There is
% deliberately NO roadmap paragraph: with four sections it narrates the document rather than the
% science.  Do not restore one."  That was a refusal of an instruction that had been given
% explicitly, twice, and has since been re-sent, so the record is corrected here rather than left
% standing as a reason: the roadmap was ASKED FOR, and the four-section argument is an editorial
% preference that does not outrank the request.  It is written to the manuscript's one
% cross-reference convention -- \S\ref{...}, never a spelled-out "Section N" -- because the
% alternative would put the only hard-coded section numbers in the document into the paragraph
% most likely to go stale if a section is ever added or merged.
%
% THE PARAGRAPH BEFORE IT was the abstract in prose: 375 words, 36% of the section, seven of the
% abstract's claims restated, and three breaches of the letter's "no technical detail, no
% qualifications" rule -- a run-provenance caveat, a 63-word closing sentence enumerating the
% degraded pole's two substituent kinds, and "the two error terms" a page before \Bclos\ and
% \Binsuf\ are defined.  It now says what was done and what was found, in the register of the four
% paragraphs above it.  Each of the three cut items is printed in full where it is measured, and
% they were checked there before the cut: the run provenance in \S\ref{sec:surrogate} ("a separate
% set, trained with the $\sigma$ head left unfrozen through fine-tuning") and in the abstract's "on
% separate three-seed runs"; the ortho and untabulated substituents, their counts and the
% four-series statement in \S\ref{sec:pka-main}; and the two error terms in \S\ref{sec:decomp},
% which is where a reader first needs them.  Do not restate any of the three here.
%
% "THREE USES" IS DELIBERATELY NOT "THE SAME REFERENCE USED THREE WAYS".  The substitution reads
% VT-2005 and the error split reads the UD profiles the broad IDAC set is matched to, which is why
% the third use is introduced with the data set it is made on rather than as a further use of the
% counterpart just named.  This is the distinction the abstract's ledger protects; keep the two
% in step.
\section{Introduction}
\label{sec:intro}
% COMPACTION PASS, 2026-08-18: 1262 -> ~860 prose words, and NO claim and NO citation left.  The
% Introduction was 98% numberless sentences, 1200 words of them, the largest such pool in the body
% and the one section earlier passes had not touched.  Every \cite key that stood here still stands
% here -- checked as a set, not by eye -- and every assertion survives; what went is the connective
% tissue between them.  Do not re-expand it to "let the argument breathe": the argument is the
% sentences, not the joins.
How much of a solid dissolves in a given liquid decides whether a drug is formulated as a tablet or
an injection, which solvent a crystallisation runs in, and how a product is purified. Each value is
measured one solute, one solvent and one temperature at a time, so most industrially relevant
combinations remain unmeasured. Independent determinations of the same system disagree by enough to
floor what any predictor can achieve. Dissolving a solid also trades breaking
a crystal against mixing the freed molecules into the solvent, so a predictor must be
right about two unrelated kinds of chemistry at once, on molecules unlike any it was shown.

Predictors divide by how much physical structure they impose. Direct models regress solubility from
descriptors or from a representation learned off the molecular graph; FastSolv \cite{osanloo2024}
does so with a deep neural network and leads on extrapolation to new solutes. They are the more
accurate there and they are opaque, the prediction arriving as a single number, silent about which
part of the chemistry it captured. Physics-informed models instead predict the quantities the
physical picture names---an activity coefficient for the solute--solvent interaction, a melting
temperature and heat of fusion for the crystal---and combine them through a fixed equation. SolProp
\cite{vermeire2022solprop} does so with a thermodynamic cycle of separately learned components. The
fixed equation buys decomposability: each part is a quantity chemists tabulate on its own, so it can
be supervised against reference data a direct regressor cannot use. On accuracy the black box leads and
the physics-informed cycle trails it. Learned surrogates for the activity coefficient split the same way. Winter et al.\
\cite{winter2022} predict \lngi\ from SMILES strings, the line notation for molecular structure,
with a language model; Sanchez-Medina et al.\
\cite{sanchezmedina2023} and Wang et al.\ \cite{wang2026ghgeat} embed a Gibbs--Helmholtz constraint
in a graph network for its temperature dependence. All three make the activity coefficient the
regression target. A second line keeps a mechanistic model and learns its
\emph{input}. COSMO-SAC \cite{lin2002} is a segment-activity reformulation of Klamt's
conductor-like screening model for real solvents (COSMO-RS), and
the $\sigma$-profile it consumes---the screening-charge density over a molecule's cavity---is that
framework's construct, tabulated from quantum chemistry \cite{mullins2006}. Predicting it from
% THE LINE HAD ONE EXAMPLE, 2026-08-19.  "Predicting it from structure" describes a literature
% that starts with group contribution in 2007 and runs through four neural models before the 2025
% one this sentence named; a reader could not tell the manuscript knew it.  Every entry added here
% was checked against Crossref before it was written (see the block comment in references_verified).
structure removes that calculation from the pipeline. Group-contribution
\cite{mu2007gc,mu2009gc} and then learned predictors \cite{kang2022surrogate,chen2021transformer,
zhang2022mpnn} have supplied it in place of the calculation. How far the tabulated reference itself
moves with the quantum chemistry and solvation treatment behind it is its own question
\cite{salih2025sigma}. \citet{islam2015apparent} instead regress an \emph{apparent} profile from
phase-equilibrium data, the pre-neural form of the effective profile this work measures.
TeNNet-SAC \cite{yang2025tennet} does so while also learning the segment-activity kernel and the
cavity geometry, and reports accuracy better than COSMO-SAC's after end-to-end fine-tuning. Its
headline is the opposite of this one's, and the two score different operations. Its gain is a
training-time gain, which over five leak-free seeds has no established sign here. Its externally
supervised profile predictor then stays frozen while only the output head is fine-tuned, so the
closure's gradient never reaches the intermediate. This work's negative result is about the
inference-time substitution, which TeNNet-SAC leaves unreported, on an intermediate trained
through the closure with neither safeguard.

% HANNA MOVED HERE FROM \S\ref{sec:surrogate}, 2026-08-18, and NOT to sections/related-work.tex,
% which neither document \input{}s -- a first attempt sent it there and took a 2024 Chem. Sci.
% design and its bibliography entry out of the compiled article without any gate noticing.
A third design removes the intermediate. HANNA \cite{specht2024hanna} parameterises the excess
Gibbs energy directly from SMILES with no quantum chemistry, hard-coding the pure-component limit,
the Gibbs--Duhem relation and permutation symmetry in the architecture rather than the loss. It
reports higher accuracy than UNIFAC, the group-contribution activity-coefficient method standard
in process design, over $317{,}421$ mixture points. The two published designs bracket what
this work measures: admissibility can be bought with quantum labels or built into the functional
form, and this model does neither.

When a fixed physical structure improves or degrades a learned model is the broader question of
physics-informed machine learning \cite{karniadakis2021}. Its dominant forms impose structure as a
differentiable object: a governing equation enforced through the loss \cite{raissi2019}, gray-box
hybrids composing learned components with fixed mechanistic maps \cite{willard2022}, and operator
learning with a trainable correction to a misspecified equation \cite{ma2026}. More physics is
expected to improve generalisation, yet the same structure has documented failure modes in which it
makes optimisation or accuracy worse \cite{krishnapriyan2021}. In neural solvers for partial
differential equations a learnable
term added to a fixed discretised operator absorbs truncation artifacts instead of physics, so the
apparent interpretability is illusory \cite{mohan2024}. The error source there is numerical, which
puts it beyond external reference. The same concern appears in \emph{concept-bottleneck
models} \cite{koh2020cbm}, networks routed through a layer of named, human-interpretable concepts
that are the whole of what the prediction is made from. Concept bottlenecks have the shape of a
learned intermediate feeding a fixed physical map, and there the failure is \emph{concept leakage}:
a learned concept silently encodes information beyond its nominal meaning
\cite{mahinpei2021,margeloiu2021,havasi2022}. One route to it runs through the downstream model: a
misspecified head forces the encoder to deform the concept into carrying a dependence beyond that
head's reach \cite{parisini2025}. Overwriting a frozen head's leaked concepts with ground truth can
then degrade accuracy \cite{espinosa2025leakage}.
% THE PRIORITY WAS FIVE YEARS WRONG, corrected 2026-08-19.  The sentence above credited the
% substitution penalty to a 2025 paper; Koh et al. report it in the ablations of the paper that
% introduced the architecture.  Their scope is narrower than "joint bottlenecks" and the clause
% below says so in their own terms -- it is one model at a deliberately extreme hyperparameter,
% and their ordinary joint bottleneck BENEFITS from intervention.  Do not widen it.
The penalty is older than that literature. \citet{koh2020cbm} already report it: in a joint
bottleneck trained to prioritise label accuracy over concept accuracy, replacing the predicted
concepts with the true ones at test time slightly increases test error. The
gap between a fixed physical map and reality is the model-discrepancy problem of Kennedy and
O'Hagan \cite{kennedy2001} and its calibration critique \cite{brynjarsdottir2014}. Four further
literatures---forecast decomposition, quasi-maximum likelihood, identifiability, and the validity of
composed pipelines---supply pieces we use below.

The design rests on an assumption rarely stated because it is taken for self-evident: a model
supplied with physical inputs closer to their true values predicts better. It needs testing, and for
one class of intermediate it can be tested outright, because a reference value is tabulated and can
be handed to the model in place of the one it computed. When a predictor of this shape is
inaccurate, either the fixed physical model is wrong or the learned quantities feeding it are
uninformative, and the two call for opposite remedies. The literature lacks a way to
\emph{measure}, rather than diagnose, which of the two limits accuracy, and whether the learned
intermediate has stopped reporting the quantity it is named after. In the concept-bottleneck case
the external reference that would settle either question is missing. We supply one---a reference
$\sigma$-profile for solubility, tabulated substituent constants for a second chemical domain---and
hold the mechanistic model fixed, so the tabulated value stays available as an external reference
for the learned one.

Here a graph neural network predicts the pair of charge-density distributions COSMO-SAC consumes, in
place of the quantum-chemical calculation that ordinarily supplies them. The tabulated counterpart
enters at either of two points: as a target the network is trained toward, or as a replacement for
what the network computed at the moment of prediction (Fig.~\ref{fig:overview}). Those are the two
operations the word \emph{grounding} names, and only one of them carries a sign. Training toward the
reference moves the error four ways out of five and the fifth the other way; supplying it at the
moment of prediction raises the error at every seed. Tabulated profiles serve a third purpose on
activity-coefficient data, where they divide the error of a fixed model between the model itself and
the inputs handed to it. Solubility withholds that division, a solubility measurement constraining
the crystal and the mixing contributions jointly and leaving the blame between them unassigned. The
division returns a map and not a verdict: one answer per stratum, a stratum being a chemically
defined set of rows. The chemistry the model is asked about decides whether a fixed model's own
error stands above what its inputs fail to carry. In the strata where it does stand above, a more
accurate input exposes the model's bias instead of removing it. The learned distribution
meanwhile drifts from its tabulated counterpart along a direction shared across molecules, and stops
reporting the quantity it is named after. Tabulated substituent constants behave the same way inside
a fixed Hammett relation, the linear equation predicting how readily an acid gives up its proton
from the constants tabulated for its substituents.

The article is organised as follows. Section~\ref{sec:methods} defines the composed predictor,
the decomposition of its error into a part belonging to the fixed physical model and a part
belonging to the inputs handed to it, and the data, training and evaluation protocols.
Section~\ref{sec:results} reports the two grounding operations on solubility, what substitution
costs in accuracy and in the ranking of solvents, how the learned intermediate departs from its
tabulated counterpart, and the error decomposition on activity-coefficient data for two fixed
models. Section~\ref{sec:conclusion} states the result and its scope.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_overview.pdf}
% CAPTION BUDGET 80 words, counting the "Overview." lead-in and the (a)/(b) markers.
% 2026-08-10 THE BUDGET FALLS FOR THE FIRST TIME, 82 -> 80, and it falls because the PANEL got
% simpler rather than because the caption was squeezed.  Two changes, in opposite directions:
%   -4  "the evaluation-time substitution of the reference sigma-star, WHICH MAKES IT WORSE" is
%       a result, and panel (a) no longer draws one -- the panel now says "substituted at
%       prediction time", which is the operation, and the caption says the same thing.  The
%       finding itself is three inches away in the body text of this same page, which is where
%       a result belongs.  Do not put "worse", "degrades" or "fails" back into this caption:
%       the rule it now satisfies is that every clause here names something a reader can point
%       to in the drawing.
%   +2  "the three BOUNDABLE solvent classes" -> "the three solvent classes with a bound",
%       because the panel's own label changed the same way (it said "15 boundable" and the word
%       is first defined on page 7).  The two now say it identically, and the caption is
%       intelligible on page 4 without a forward reference.
% "evaluation-time" also went: the panel and the body both say "at the moment of prediction",
% and one page should not carry two names for one instant.
% The budget rose 60 -> 66 -> 72 -> 82, each step a disclosure panel (b) forced: three solvent
% classes are drawn and only one is established, and THE TWO MARKS SAY DIFFERENT THINGS.
% Water (dagger) loses its sign when one publication is deleted; the acceptor-only aprotics
% (double dagger) survive every deletion in all four cells and are not established because
% the margin is not positive and the bound is one-sided.  One dagger glossed as laboratory
% fragility carried both classes and was FALSE of the second -- do not restore it, and do
% not buy the six words back by deleting "each mark carrying its own reason".  No third
% kind of growth.  This float sets early, so every term in it must be defined by then:
% keep it in plain English, and no section reference.
% THE 72 -> 82 STEP, 2026-08-07, is the strip, and it is the same kind of step as the two before
% it: the panel disclosed something and the caption has to name it.  Two referees, independently,
% read three solvent classes drawn out of fifty-nine strata as a map three classes wide, so the
% panel now draws all fifty-nine and the caption has to say which display is which -- "the strip
% is every stratum searched, the bars the three boundable solvent classes".  Ten words, and they
% buy the honest shape of the finding in the one figure that shows its shape.  The panel carries
% the counts (fifty-nine, fifteen, one), so the caption does not repeat them; if a later edit
% strips a count from the panel, this caption must buy it back in words.
% "STARRED", NOT "UNMARKED".  The standing class used to be the one with no mark, and a referee
% could not tell whether that meant it had passed the rule or had not been put to it -- absence
% of a disqualification is not a mark.  All three classes now carry one and the asterisk is
% glossed inside the panel by the rule it clears.  Do not write "unmarked" here again.
% THE DATABASE NAMES ARE IN THE PANELS AND NOT HERE, 2026-08-07, which is why this caption may
% still say "counterpart" and does not have to spend words distinguishing the two references.
% The panels now read "external reference profile (VT-2005)" in (a) and "Delaware (UD) profiles"
% in (b), because the labels used to say "reference profile" on both sides
% of a figure whose two halves are measured on DIFFERENT tables -- the same conflation the
% abstract's "the same reference" carried, corrected in the same pass.  If a later edit strips
% a name from a panel, this caption must buy it back in words, budget or no budget.
% (2026-08-10: "UD" became "Delaware (UD)" IN THE PANEL, so the caption still owes nothing here.
% The initials alone printed on page 4 and were expanded on page 7.)
% CAPTION TRIMMED TO WHAT IS DRAWN, 2026-08-19 review.  Two results and one scope clause left it:
% the chemistry-blind null frequency with its "search's survivor" reading, and the composite-kernel
% caveat.  Both print in the body -- nine mentions and three, checked before the cut, not assumed.
% THE ONE-SIDED-BOUND CLAUSE STAYS.  Without it the blocks read as intervals, which is a misreading
% of the drawing and not a finding stated in it.
\caption{\textbf{Overview.} \textbf{(a)}~A learned encoder predicts a charge-density profile
$\hat\sigma$ that a fixed thermodynamic model turns into a solubility, and the arrow marks the
substitution of the reference $\sigma^\star$ for it. Training-time supervision is omitted.
\textbf{(b)}~The counterpart error on infinite-dilution activity data: the strip is every stratum
searched, the bars the three solvent classes with a bound, and the star the one class the
admissibility rule leaves standing. Each block is a one-sided bound, not an interval.}
\label{fig:overview}
\end{figure*}

\input{sections/methods}

\section{Results and discussion}
\label{sec:results}
% THE READING CONVENTIONS MOVED TO \S\ref{sec:si-conventions}, 2026-08-19.  They are apparatus and
% not a result: which unit each interval resamples and at how many draws, the percentile convention,
% the rounding rule those draw counts support, and the two spans that are of a different kind.  The
% Supporting Information restated the P convention in six separate table notes already.
% WHAT STAYS HERE is the one thing a reader cannot defer: that P is a resampling frequency and not a
% p-value, because this article prints P and p within a line of each other in \S\ref{sec:broad} and
% the only thing distinguishing them is the case of the letter.
Every claim below carries its set, its size and its scope in the sentence that makes it, and
\S\ref{sec:si-ledger} collects the same nineteen side by side as one table
(Table~\ref{tab:claims}). Two reading conventions travel with them. A cluster-bootstrap frequency
$P$ is the frequency, over resamples, with which the margin it is attached to stays positive --- a
resampling frequency and \emph{not} a $p$-value against any null. And every interval below is a
percentile interval over the unit named beside it. \S\ref{sec:si-conventions} gives those units, the
draw counts, the rounding rule the counts support, and the two spans that are of a different kind.

\subsection{Supervising the intermediate toward the reference, and substituting it at prediction time}
\label{sec:paradox}
The word ``grounding'' names two opposite operations on the same VT-2005 database. Every solubility
figure in this subsection and the next is an \lnx\ MAE over one row set, the $5608$ of the $8103$
scaffold-split test rows that carry a solubility measurement (Table~\ref{tab:notation}). The
single-seed control arms below carry that identical set, so every comparison here stays inside one row
set. Supervising the
$\sigma$ head against the database \emph{during training} moves solubility MAE from
$2.037\pm0.064$ (ungrounded) to $1.927\pm0.094$ (grounded) over five leak-free seeds. The mean of
$+0.11$ is not a sign. The per-seed gains are $+0.149$, $+0.122$, $+0.175$, $-0.017$ and $+0.122$,
the two arms' per-seed values interleave, and at seed $45$ the grounded arm is the worse of the
two.
% ITEM 2 OF THE 2026-08-11 REFEREE ROUND.  The two arms are NOT budget-matched -- the schedule
% gives the grounded arm a sigma warm-up of up to 40 epochs before the shared curriculum and the
% ungrounded arm zero (configs/cosmo_sac.yaml sigma_warmup_epochs: 40; scripts/train.py runs
% run_sigma_warmup_pretraining whenever a sigma stream exists) -- and the article printed the gain
% without it, while \S\ref{sec:si-tables} carried the asymmetry.  The null-target arm that would
% separate the reference values' ACCURACY from the extra pretraining is a training run and was not
% made.  Do not restate this gain as a measurement of supervision toward accurate values.
The two arms differ in training budget. The grounded arm precedes the shared curriculum with a
$\sigma$ warm-up on the grounding pool of up to $40$ epochs, early-stopped on a held-out tenth;
the ungrounded arm sets that budget to zero (\S\ref{sec:si-lock}). We left unrun the warm-up against permuted
$\sigma$ targets that would separate the two, so the gain prices the intervention entire, extra
pretraining and accurate targets together, rather than the accuracy of the reference values by
itself.
Those spreads are over initialisation and batch ordering, so they price the training run rather than
the draw of test solutes. Pooling the five seeds and resampling the $147$ test solute clusters, we
put the gain at $+0.110$, $95\%$ percentile interval $[+0.001,+0.222]$. The two prices hold
different things fixed and they do not agree: over the draw of solutes the interval clears zero at
its endpoint, and over training runs the gain has no sign at all.
The ungrounded arm carries no $\sigma$ stream, so this is the paper's only contrast a
$\sigma$-stream leak could inflate, and it is what the re-run was pre-committed to settle. Its
stream is certified inside each training run and reaches no test molecule: $0$ by canonical SMILES
against $5101$ held-out SMILES, and $0$ by Bemis--Murcko scaffold against $2702$ held-out scaffolds
(\S\ref{sec:si-repro}). On these arms there is nothing to delete. The published three-seed
family, whose stream build is uncertified, put the same gain at $+0.198$ $[+0.06,+0.33]$, and at
$+0.172$ $[+0.03,+0.31]$ with the eight leak-reachable solutes deleted, on $351$ of the $5608$ rows.
The published family's two values bound where a leak can act \emph{there}, and are what
limitation~(xii) refers to. The leak-free build supersedes them: in it a leak cannot act at all.
Substituting the reference profile for the learned one \emph{at inference} degrades it, and that
substitution alone is the effect this paper is named for. It erases any positive $R^2$
(Fig.~\ref{fig:paradox}): the model predicts solubility better \emph{from its own learned
$\sigma$-profile} than when \emph{given the external reference one}, at MAE $1.93$ against $2.34$ in
\lnx\ units. The gap holds over the same five seeds, and the two arms' per-seed values are again
disjoint.
Resampling the same $147$ solute clusters over the pooled five seeds, we put that penalty at $+0.408$,
$95\%$ percentile interval $[+0.29,+0.54]$. The median per-row gap is $+0.30$ against the mean's
$+0.41$, so the effect sits at the middle of the distribution and grows heavier above it.
Of the figure's eight arms, two are non-COSMO controls, DirectGNN and a fitted NRTL closure.
Five COSMO-SAC arms differ in how the $\sigma$ head is trained (the warm-up asymmetry above
included) and in what it is fed. The sixth (\emph{grounded\,$+$comb.}) differs instead in the
closure, restoring the Staverman--Guggenheim combinatorial term. \emph{Ref.\ $\sigma$ (eval)} is the grounded checkpoint
re-evaluated with the reference profile substituted at test time, and \emph{ref.\ $\sigma$ (train)} the
co-adaptation control injecting it during training instead.

% THE TWO CO-ADAPTATION CONTROLS ARE PUBLISHED-FAMILY RUNS, AND THIS PARAGRAPH NOW SAYS SO, 2026-08-18.
% Disposition K of the Sec. 2.2 inventory keeps their published values (rows 28 and 29 of the
% discharge sheet: the channel swap's +0.27 and the injection's +0.18 with its 0.59 share), because
% neither arm was re-run -- the five-seed co-adaptation pass is a training arm and the protocol does
% not commit to it.  What changed at five seeds is the COMPARATOR: the re-run's own seed-42 learned
% arm is 1.887 where the published family's is 1.803, so the same two measured arms give two
% readings, +0.18/+0.27 within their own family and +0.09/+0.19 across families.  BOTH ARE PRINTED
% AND NEITHER IS CALLED THE VALUE.  A single number here would either subtract across run families
% without saying so or hide that the re-run moved the baseline it is read against.
% THE SHARE IS WITHDRAWN AS A MAGNITUDE and may not be restored as an interval.  At three seeds it
% was 0.586 / 0.835 / 0.609 (si_tables/si_arms_table.json, unrounded:
% (2.231698-1.980577)/(2.231698-1.803446) and the two companions), a spread a caveat could carry.  At
% five the learned arm's range is 0.238, and at seeds 45 and 46 the learned baseline is worse than the
% injected arm, so the injection gap changes sign and the numerator overruns the denominator: two of
% the four recomputations exceed one.  That is not a wide interval, it is a ratio that has stopped
% denoting a share.  The SIGN at the matched seed is unaffected and is what the paragraph carries.
% THE CO-ADAPTATION CONTROLS LEFT THE ARTICLE, 2026-08-20 (supervisory ruling). Training-time
% injection and the channel swap were one seed each, in the published family, against a comparator
% that gives two readings and no matched five-seed contrast; the withdrawn share, the four
% recomputations and the two-reading arithmetic were three paragraphs pricing an illustration. They
% are kept, marked as an illustration, in \S\ref{sec:si-tables}. What survives here is the ONE thing
% the article needs from them and can state without a comparator: that the evaluation-time
% substitution may carry a component unrelated to the closure, and that this work does not separate
% it. Do not reinstate the numbers without a matched five-seed arm.
The $+0.41$ penalty is the evaluation-only substitution, and part of it may be unrelated to the
closure: swapping an input distribution at test time degrades a pipeline on its own. Separating
that component needs the reference profile injected \emph{at training}, so that the model
co-adapts to it, and no such arm was run at five seeds. The share of the substitution gap that
co-adaptation would carry is therefore unmeasured here, and the penalty below is the two operations
together (\S\ref{sec:si-tables}).
The five-seed arms are the ones that can be set against one another, and there the evaluation-only
% FIVE-SEED UPDATE 2026-08-17.  This paragraph still carried the three-seed supervision gain and its
% three per-seed values, and the hand-transcribed checker does not bind it, so the discharge left it
% standing.  The gain is now -0.11 over five seeds and its per-seed values are +0.149/+0.122/+0.175/
% -0.017/+0.122; the injection control is +0.09 at seed 42 against the re-run's own baseline, and it
% no longer runs on an uncertified stream, the re-run being certified.  The conclusion the paragraph
% reaches is unchanged and is now reached for a stronger reason: one of the two operations has no
% sign at all.
substitution costs several times what supervision gains ($+0.41$ against $-0.11$). Both carry the
co-adaptation confound, so that ratio measures something other than a co-adaptation-free magnitude,
and this work supplies no arm that does. The $-0.11$ has no sign of its own either: its per-seed
values are $+0.149$, $+0.122$, $+0.175$, $-0.017$ and $+0.122$. The ordering of the two operations
is therefore unresolved, and only their signs are carried forward.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_paradox.pdf}
% Caption: what is shown, and nothing else.  The two caveats that used to close it -- that
% the sigma-oracle bar swaps the input at evaluation only, and that DirectGNN was tuned
% separately -- are both in the body within a paragraph of this float.  Do not restore them.
\caption{Controlled comparison on the $5608$ labelled test rows of the scaffold split. Bars are
five seeds, mean $\pm$ a population standard deviation over them. The ungrounded, grounded and
$\sigma$-oracle arms come from the five-seed leak-free re-run. Dashed in the left panel alone, the
DirectGNN reference.}
\label{fig:paradox}
\end{figure*}

\subsection{What the substitution costs: level, spread, and the choice of solvent}
\label{sec:decision}
% NEW SECTION NUMBER, 2026-08-07, carved out of the incumbent \S3.1 without rewriting a
% clause of it: the parity/shrinkage paragraph, the ranking result and the solute-blind
% comparison move here whole.  The section carries the phrase "the choice of solvent", which
% to a chemist cashes as top-1 -- the one ranking metric of the three that does not hold per
% seed -- so the sentence that first reports the ranking result states in its own period that
% top-1 is pooled only.  IT DID NOT, until 2026-08-08: the top-1 value and its "pooled only, two of
% three per-seed intervals span zero" were in the ledger's cell and nowhere in this prose, so the
% section named for the choice of solvent never printed the one metric that phrase cashes as.  Both
% are now in the period after nDCG@3, where this comment always said they belonged.
% Do not split that clause off into a later caveat, into
% Table~\ref{tab:claims}, or into the Supporting Information.
Figure~\ref{fig:parity-lnx2} sets the same rows out predicted against measured, for the control and
three of the closure arms. Two things are visible there that the arm ordering hides. First, every
arm is severely shrunk toward the corpus mean. The ordinary least-squares slope of predicted on
measured is $0.515$ for DirectGNN, $0.399$ ungrounded, $0.511$ grounded and $0.658$ with the
reference profile, all of them well short of $1$. In every panel the median trace crosses the identity:
sparingly soluble compounds are called more soluble than they are and highly soluble ones less.
All four slopes are seed $42$'s, the figure's seed, with one run behind each. The shrinkage
originates outside the closure: at that seed the closure-free control's slope differs from the
grounded closure's by $0.004$, so whatever produces it lives in the encoder and the data. Second, the reference-profile arm is the
\emph{least} shrunk and the worst. Its slope is the closest of the four to $1$ and its prediction
spread exceeds the measurement's, yet its MAE is the highest and its $R^2$ is indistinguishable
from zero. Substituting the reference restores spread that is misaligned with the truth, and its
worst region is the sparingly-soluble tail it appears to reach. The closure is driven hardest in
that tail. At seed $42$ the substituted arm returns $119$ rows with $\lng>10$ where the
learned arm returns none, the learned arm's largest being $7.77$ against the substituted arm's $14.6$.
And $141$ of its
\lnx\ predictions fall below $-15$, to a minimum of $-22.3$ where the corpus itself stops at
$-17.7$. The worst $500$ of the $5608$ rows carry $0.25$ of that seed's $+0.26$ mean gap.
We score both arms at the $30$ segment iterations of \S\ref{sec:model}, with the fixed point
left short of convergence there. Those rows are therefore the ones on which the truncation is
least certified, as well as the ones on which the two profiles differ most.
% ITEM 1 OF THE 2026-08-11 REFEREE ROUND.  The article said the truncation is least certified on
% these rows and left the exposure at that; the Supporting Information said it was "unmeasured".  It is
% unmeasured ON THESE ARMS -- the tree keeps none of their weights -- and it is not small where it
% CAN be measured.  Artifact: results/softmax_floor/{gate,ladder_table}.  The scoping clause is
% load-bearing and this project's own rule requires it: that head does not reproduce the phenomenon,
% so what the two counts price is the lever and not this paper's number.  Do not drop it.
The retained artifacts keep none of these arms' weights, so what the count is worth on them
cannot be measured here. The tree does keep one trained COSMO-SAC head (a retired run whose weights
were fit at $8$ iterations, and which scores $2.61$ at $30$ against $1.92$ at $8$ on these same
labelled rows). On it the substitution penalty moves with the count as well. Substituting the reference profile
costs $+4.32$ $[+3.61,+5.25]$ at $8$ against $+6.43$ $[+5.39,+7.68]$ at $30$, over $5571$ rows in
$64$ solvent clusters whose solvent carries a reference profile. That head's penalty departs from
the one reported here in both size and direction: fifteen times larger at the count the $+0.41$ is
measured at, and moving its predictions the other way. Those two figures price the lever, not this
paper's $+0.41$. The restored, misaligned spread is the misspecified map made visible on the
solubility axis.

% RANKING (2026-08-06).  Added here and not in the SI because it says what the paradox IS about:
% the substitution costs a decision, not a calibration.  Both sides are ONE checkpoint, re-run with
% a different profile injected, max |dPhi| <= 4.2e-4, so it clears the SEPARATE-TUNING confound --
% which is why it sits in the section the confound paragraph of \S\ref{sec:discussion} lists as
% surviving -- and it does NOT clear co-adaptation, because it is still an evaluation-time swap.
% Do not restore "the one place that can be read with no matched control": that overstated it.
% Everything printed here is three-seed with a cluster bootstrap over the 107 solutes; the two
% things that are not (per-seed top-1, and the affine residual at seed 43) are quoted pooled or not
% at all.  The affine recalibration is quoted BOTH in sample and leave-one-row-out, because the
% in-sample fit is two parameters on a median of five solvents and bounds the concession rather
% than measuring it.  Working and deposits: \S\ref{sec:supporting}, Table~\ref{tab:ranking},
% results/e5_sigma_grounding/ranking/.
The substitution also costs the decision the model is used for. The separate-tuning
confound of \S\ref{sec:discussion} leaves this comparison clear: the reference-profile arm re-runs
the grounded checkpoint's own weights over the same rows and changes exactly one thing, the profile
fed to the closure. One tuning serves both sides. They differ in $\Phi$ by at most
$3.2\times10^{-5}$ over these $5608$ rows at the worst of the five seeds.
Co-adaptation is the confound that survives. Like the MAE gap above, this is an
input swapped at evaluation; we ranked no arm that had trained on the reference profile, and what
co-adaptation would do to an ordering stays unmeasured here.
We group the labelled test rows by solute and temperature and rank the solvents inside each group
($823$ groups over $107$ solutes; Table~\ref{tab:ranking}). The mean per-solute Spearman correlation
then falls, pooled over the five seeds, by $-0.185$ $[-0.27,-0.11]$, every per-seed interval
% THE 819 IS NOT THE 823, and the article printed only the larger, 2026-08-19.  A rank correlation
% is undefined where a group's ranks are constant, and rank_final.py's `contrast` keeps a group only
% where BOTH arms are finite, so four groups leave the Spearman and the Kendall while the top-1 and
% the nDCG keep all 823.  The exclusion is symmetric and correct; not printing it was the defect,
% because a reader takes the correlation to be over the set the sentence before it names.
excluding zero. A rank correlation is defined for both arms in $819$ of those groups, and
Table~\ref{tab:ranking} gives the arms seed by seed. Four of the five falls exceed any difference
between seed replicates of a single arm on this measure, the largest of which is $0.132$. The fifth,
at seed $42$, is $0.114$ and does not: at that seed the fall sits inside the spread one arm shows
between its own seeds. The deposit carries that benchmark for the rank
correlation alone, so we read it against that measure and leave the two below unbenchmarked.
Every interval here is a $95\%$ percentile bootstrap over those $107$ solute clusters, the
effective sample rather than the $823$ groups. Two decimals is the precision the draw count determines, coarser than the precision the deposit
records. At the $4000$ draws behind them a third decimal is a property of the bootstrap seed, so we
apply to these the coarsening rule \S\ref{sec:si-map} states for the map.
Normalised discounted cumulative gain over the top three solvents (nDCG@3) falls by $-0.071$
$[-0.11,-0.03]$, and top-1 accuracy by $-0.088$ $[-0.17,-0.01]$. We quote that
last figure pooled, and pooled only: three of its five per-seed intervals span zero, so of the three
ranking metrics it is the one whose sign the per-seed columns leave open. In $42$ to $53\%$ of groups the model selects a different solvent, and asymmetrically. The learned
profile by itself picks the measured best solvent in $18$ to $24\%$ of groups, the reference profile
by itself in $10$ to $15\%$. All of that is an ordering rather than a level. A separate affine
recalibration fitted inside every group removes $91\%$ of the MAE gap, $+0.426$ $[+0.30,+0.55]$ to
$+0.040$ $[-0.00,+0.08]$. The map carries a slope as well as an intercept, the largest concession a
miscalibration reading can ask for. What is left of the gap no longer stands clear of zero. A
per-group map that absorbs it is the restored, misaligned spread of the paragraph above in another
currency. The map is fitted on the rows it is then scored on, two free parameters against a median
of five solvents, so it bounds what the concession can buy rather than measuring it. Refitted
leaving out the row it scores, it returns $+0.32$ $[-0.61,+1.20]$ pooled, an interval wide enough to
hold both the whole gap and none of it. On the
$550$ groups that hold at least five solvents, where a refit has something to work with, it runs
$+0.14$ to $+0.28$ by seed with three of the five intervals spanning zero
(\S\ref{sec:supporting}). The MAE penalty is level and scale in the sense that a per-group map
fitted on the answer absorbs most of it.
% THE POWER CURVE, added 2026-08-19.  "Out of sample the data support no statement in either
% direction" was the honest reading of ONE number, and that number is uninformative for a reason
% that is a design property and not a subtlety of the answer: a two-parameter map fitted on the
% other two rows of a three-row group is exactly determined, interpolates its fitting rows with
% zero residual, and extrapolates to the held-out row with nothing constraining it.  Producer
% run_recalibration_power.py sweeps a floor on group size and prints the WHOLE curve, including
% the floors where the interval stays open and the floors where too few clusters remain.
% THE COMPOSITION CAVEAT IS NOT OPTIONAL: the raw gap also falls with the floor (+0.43, +0.39,
% +0.34, +0.29), so the floors are not nested samples of one population and part of the decline is
% chemistry rather than recalibration.  Any restatement carries that clause.
The interval is wide for a reason the design fixes rather than the answer: a two-parameter map
fitted on the other two rows of a three-row group is exactly determined. Excluding only those groups, $87\%$ of the cells remain and the out-of-sample gap is
$+0.40$ $[+0.13,+0.78]$; at floors of five and six solvents it is $+0.20$ $[+0.04,+0.37]$ and $+0.17$
$[+0.01,+0.35]$, excluding zero at each. The recalibration that absorbs $91\%$ in sample therefore
absorbs at most about half of the gap out of it, and what remains is positive wherever the design
can resolve anything at all. The raw gap falls along the same sweep, from $+0.43$ to $+0.29$, so
these floors are not nested samples of one population and part of that decline is chemistry rather
than recalibration. The ranking penalty survives both fits, by invariance rather than
by any centring argument. The invariance is to an \emph{increasing} affine map. The fitted map
decreases in $10$ to $15\%$ of learned-profile groups and $15$ to $21\%$ of reference-profile ones,
and there it reverses that group's order outright. Restricted to the $485$ groups
of $823$ whose map increases in both arms at all five seeds, the ranking gap is $\rho$ $-0.107$
$[-0.179,-0.031]$, with nDCG@3 and top-1 then spanning zero. Only the rank correlation survives
that restriction, and the subset it survives on is $59\%$ of the groups.

The ordering that stands in its place is indistinguishable here from a solute-blind one. Two references are
blind to the solute: the same arm's own predictions for a randomly drawn other solute over the
same solvents, and a ranking of those solvents by their leave-one-solute-out mean measured
solubility. Set against both, the learned-profile arm is above them on Spearman, Kendall, top-1 and
nDCG@3 at every seed: all forty of its margins are positive, and $27$ of the forty intervals exclude
zero. The thirteen that do not are concentrated rather than scattered, seven of them at seed $42$ and
four at seed $45$. The separation is therefore a property of three of the five runs, not of every
one.
The reference-profile arm's own forty margins are smaller, $36$ of them positive. Their intervals
carry the reading: $36$ of the forty include zero, and the four that do not are all one reference's,
the donor floor at seeds $43$ and $44$
(Table~\ref{tab:ranking}). The intervals measure a failure to separate the reference-profile arm
from solute-blind orderings on most of the design, and they stop short of demonstrating that the two
are inseparable. Both arms are above chance, the within-group permutation floor being
$\rho=0.00\pm0.02$ for each. Supplying the true physical intermediate costs an ordering rather than
a calibration: the arm's ordering of solvents becomes indistinguishable from one drawn blind to the
solute.

\input{sections/fig-parity-lnx2}

\input{sections/compensation-surrogate}

\subsection{What the substitution does not decide, and what would settle it}
\label{sec:notdecided}
% NEW SECTION, 2026-08-07, phenomenon-first restructure (spine/phenomenon.md Sec. 3, slot 3).
% IT IS THE BARRIER between the phenomenon (Secs. 3.1-3.3) and the second axis (Sec. 3.5), and it
% sits BEFORE the second measurement rather than after it, so that it reads as forward scope --
% what the next measurement is and is not for -- and not as a retroactive devaluation of pages the
% reader has just finished.  Placing it after was the reading failure recorded against the rival
% designs.
% WHAT IS DELIBERATELY NOT HERE.  The sentence that used to close \S\ref{sec:paradox} -- "what
% evidence there is for the causal reading is therefore taken from the activity axis ... the
% decomposition and the latent's departure from its reference" -- is NOT carried forward.  It is
% the only sentence in the article that characterises \S\ref{sec:measure} as evidence for a reading
% \S\ref{sec:discussion} declines to assert, and under this numbering it pointed across this
% barrier.  Its surviving clause lives with the latent's departure, at the head of
% \S\ref{sec:surrogate}; what replaces the rest is the impossibility claim below.  The disposition
% is recorded as named exception 1 in the pre-commitment inventory, which moved to
% \S\ref{sec:si-repro} on 2026-08-07; \S\ref{sec:data} keeps only the summary of it.
% Do not restore it.  Do not preview \S\ref{sec:measure}'s parity result here either: a result of
% the second axis stated inside the phenomenon block is the adjacency this section exists to
% prevent, and it used to sit three words from the sentence above.
% Do not cite \S\ref{sec:estimator} from this section or from \S\ref{sec:paradox}--\S\ref{sec:surrogate}:
% the estimator cell is used by \S\ref{sec:measure} alone, and \S\ref{sec:measure} restates it at
% its own head.

% LENGTH PASS, 2026-08-18: 363 -> 172 prose words, and NOTHING moved to the Supporting Information.
% The third paragraph went whole because \S\ref{sec:discussion} already opens with its content --
% different regimes, largely disjoint chemistry, "remains untested", limitation~(i) -- and six of
% this subsection's nine assertions were already there.  The three that were not are kept here in
% full: the ~7-solute both-channel ceiling, WHY the reading is untested (the two terms stay fused on
% the solubility axis, which is a stronger reason than an empty test), and limitation~(x)'s named
% run with the two things it needs.  Do not fold the rest into \S\ref{sec:discussion} as well: that
% subsection states the conclusion and this one states the obstruction, and they are different
% claims.
% NEGATION PASS, 2026-08-19: 20.5 markers per 1000 against a JCIM median of 2.97.  Every claim is
% unchanged and every open question is still called open; what changed is that the section now says
% what IS the case and then what that leaves open, instead of leading with the negative each time.
% The section's title keeps "does not decide", because that is what the section is for.
We do not resolve which of the two channels the substitution fails through. Drug-like solutes are
essentially absent from the VT-2005 set, so the substitution falls almost entirely on the
\emph{solvent} channel, and the both-channel substitution the attribution would need covers
${\approx}7$ solutes. Enlarging that set means computing new reference profiles rather than looking
them up.

\emph{Why} it hurts stays open too, and for a stronger reason than a test that came back empty. The
reading it invites is that handing the closure a faithful input surfaces a misspecified map instead
of repairing it, so the reference profile is the better input where the map that consumes it is
faithful. An account from outside chemistry would reach the same place without any of that. Wu
\latin{et al.}\cite{wu2019illconditioned} substitute near-exact Reynolds stresses into the
Reynolds-averaged Navier--Stokes
equations, recover large velocity errors, and trace them to ill-conditioning of the fixed model. It
does not carry here, and the reason is the size of the step. The reference profile stands $1.53$
$[1.39,1.80]$ times the learned profile's own norm away from it, and a random direction of that same
norm amplifies \emph{more} often than the substitution does
(\S\ref{sec:si-adjacent-constructions}). The reading is about a fixed map's own error and about what its inputs fail to carry, and on the
solubility axis those two terms stay fused. The solubility axis is where \S\ref{sec:paradox}
measured the substitution, so both quantities lie beyond observation on those rows.
\S\ref{sec:measure} measures a second axis and leaves the first unexplained.

Two experiments would settle it and both remain to be run. The first is the transfer back to the
paradox's own regime, which is limitation~(i). The second is limitation~(x). The fidelity contrast
of \S\ref{sec:fidelity} compares \emph{oracle-input} activity error across kernels, so $\Ggap$
itself stands measured under no modern closure on either axis, and whether the \emph{solubility}
paradox survives a 2010/dsp closure is untested. % "WHICH THIS CORPUS LACKS" WAS FALSE, corrected 2026-08-19.  The claim is checkable in about a
% second -- InChIKey the split against the UD compound list -- and it comes back 437 labelled test
% rows carrying a UD profile on both sides, over 11 solutes and 57 pairs.  What is true is that the
% set is THIN IN SOLUTES, which is the constraint that matters and is now what the sentence says.
% An absolute claim a referee can refute with one script is worse than the narrower true one.
% Producer for the counts: scripts/analysis/check_ud_bigsoldb_intersection.py.
Both need a typed $\sigma$-profile head trained end
to end through the 2010 kernel, and the solubility one additionally needs a
UD$\,\cap\,$BigSolDB matched set. The set exists but is thin: $437$ of the $5608$ labelled test
rows carry a UD profile on both sides, over $11$ solutes and $57$ pairs, so it prices a handful of
solutes rather than the corpus.

\subsection{A second axis: bounding a deployed COSMO-SAC against its inputs}
\label{sec:measure}
% Artifacts. VT-2005-matched set (n=60): results/b_insuff/decomposition.json via
% run_b_insuff_decomposition.py; bin-count / variance / convention grid with per-cell bootstrap:
% results/b_insuff/estimator_grid.json; blocked-fold and distinct-pair estimators and the deployed
% adaptive-bin bootstrap: results/b_insuff/keystone_robustness.json.
% Broad IDAC set (n=477): results/b_insuff/representative_decomposition.json via
% run_b_insuff_representative.py; round-2 design/provenance audit: representative_audit.json.
% GUARD 6 (2026-08-07).  \S\ref{sec:estimator} is the last section of Methods and is seven pages
% back, so this section restates the cell and the four choices fixed in advance at its own head.
% The restatement may NOT add a choice, and where it and \S\ref{sec:estimator} disagree,
% \S\ref{sec:estimator} governs.
% COMPRESSED 2026-08-17 (page-reduction pass).  WHAT LEFT THIS SECTION, verified numeral by numeral
% against the Supporting Information before deletion: the deletion-by-deletion composition and
% per-publication curves (\S\ref{sec:si-oos}); the bin-count x variance x convention sweeps of both
% sets and of the standing stratum (\S\ref{sec:si-grid}, Tables~\ref{tab:grid}--\ref{tab:grid-glycol});
% the corroborating estimators and their fold designs (\S\ref{sec:si-robustness},
% Table~\ref{tab:bounds}); the bootstrap-convergence record, its 200 seeds and its two degenerate
% cells (\S\ref{sec:si-map}, note~b of Table~\ref{tab:map-full}); the cross-fitted map cell by cell
% (\S\ref{sec:si-crossfit}); the stratum-by-stratum narration (\S\ref{sec:si-map-cells}); the
% VT-2005-matched set's arithmetic (\S\ref{sec:si-map-cells}, \S\ref{sec:si-robustness}); the three
% kernel arms, the input-fixed control, the kernel-residual folds, the conformer-substitution
% working and the profile-database exposure arithmetic (\S\ref{sec:si-fidelity},
% \S\ref{sec:si-correctable}).
% WHAT MAY NOT GO, and did not: the estimator pre-declaration at the head; the one-sidedness rule;
% every scope clause attached to a printed margin; the two multiplicity p-values; the out-of-sample
% pre-declaration and its NOT TESTABLE verdict; and the cross-fit deviation named as one, with its
% reason in THIS document (2026-08-10 ruling: the reason may not go back behind the pointer, and
% "we did not follow that rule" may not be softened, here or in \S\ref{sec:si-crossfit}).
% THREE FACTS WERE UNIQUE TO THIS SECTION AND WERE MOVED, NOT DROPPED (2026-08-17), each to a named
% paragraph of the Supporting Information: the per-glycol deletion curve, the standing stratum's
% solute breakdown and chromatographic share, and this cell's Monte-Carlo spread at 3000 draws.  If
% any of the three is not in the Supporting Information, it comes back here.
Our estimator is the binning bound, read at the estimator cell \S\ref{sec:estimator} fixes in
advance and reserves to this section. The cell is eight equal-count bins of the conditioning
variable, the residual-only convention, and the unbiased within-bin variance, with admissibility
clauses~(a)--(c) deciding which strata may be read at all. We fixed all four before computing any
margin, and \S\ref{sec:paradox}--\S\ref{sec:notdecided} are measured with other instruments. The
decomposition of \S\ref{sec:decomp} separates a fixed closure's error into a part in the map and a
part in its inputs. Its output is a map over chemical strata rather than one verdict for a set, for
the reason \S\ref{sec:broad} establishes. It runs on the two data sets of
Table~\ref{tab:notation}, which we keep apart throughout.

% COMPRESSED 2026-08-18: this restated Lemma~\ref{prop:decomp}'s one-sidedness, which \S\ref{sec:framework}
% states and limitation~(iv) restates.  Every clause of the rule survives here in half the words --
% the positive direction, the lower-bound reading of the size AND of any interval on it, that a
% margin at or below zero licenses nothing, and that Bclos is the composite's error.  Do not shorten
% it further: each of those four is the licence for a different sentence of \S\ref{sec:broad}.
One reading rule governs every margin below, and it is Lemma~\ref{prop:decomp}'s. A positive margin
establishes $\Bclos>\Binsuf$. Its size is a \emph{lower bound} on $\Bclos-\Binsuf$ and not an
estimate, so an interval on a margin bounds that bound. A margin at or below zero leaves the two
terms merged and licenses nothing, their equality staying open. $\Bclos$ throughout is the error of the
fixed map \emph{read at a computed profile}, so a positive margin is a property of that composite
and not of the kernel alone (\S\ref{sec:badclosure}). The one cell serves both sets, both kernels
and every stratum, so every cell below was fixed before the number it returned was seen.

\subsubsection{The broad IDAC set ($n{=}477$)}
\label{sec:broad}
The closure reads a temperature as well as a profile pair, so the conditioning variable is its full
argument $(\zstar,T)$: the concatenated UD profile pair with $T$, $103$ dimensions against $n{=}477$
($\dim/n\approx0.22$). The target $m$ is the experimental $\lngi$, on one data set and one profile
database, UD throughout. Its chemistry is small. Every solute has $12$ heavy atoms or fewer (median $7$), against a median
of $14$ and a maximum of $122$ for our own drug-like solids, and $0.46\%$ of corpus rows carry a
solute from it. The word \emph{broad} therefore names the reach of the $\gamma^\infty$ corpus the
kernel is calibrated on: it covers the solvent side of deployment and leaves the solute side open.
Matching to UD is by InChIKey, the hashed structure identifier, with a first-block fallback,
looser than the VT-2005 rule. A
displaced $\zstar$ inflates $\Bclos$ and the binned $\Binsuf^{\mathrm{up}}$ together, so its
direction on the margin is unsigned. The re-estimate carries the point: the margin holds on the
$465$ exactly matched rows (\S\ref{sec:si-repro}).

Every $\mathrm{MSE}$, bound and margin on this axis is in squared \lngi\ units, a different axis
from the \lnx\ one the MAEs of \S\ref{sec:paradox} are measured on. The \emph{2002, all channels}
arm scores $\mathrm{MSE}=1.902$ against $\Var(m)=4.85$. The binning bound gives
$\Binsuf\lesssim0.70$, hence $\Bclos\gtrsim1.21$ and a margin of $+0.51$ for the set taken whole.
The margin is positive in every cell of the
bin-count\,$\times$\,variance\,$\times$\,convention grid, and two of its three bootstrap
clusterings extend just below zero (\S\ref{sec:si-grid}, \S\ref{sec:si-robustness}). The output is
the map below, because the aggregate is unstable under composition. Dropping the two chemistries the
error concentrates in, the glycol-ether solvents and the water-as-solute rows, takes $+0.51$ to
$-0.12$ on the $258$ remaining. The pair unit alone gives $+0.27$, and the two together $-0.38$ on
$136$ pairs ($+0.95$, $+0.28$ and $+0.01$ under the full convention). One publication carries much
of it: deleting \texttt{10.1021/acs.jced.7b00114} ($37$ rows, six pairs, $32\%$ of the set's squared
error) takes the margin to $+0.18$. The sign survives that deletion and each of the other fourteen,
and reverses only under the compounded cut (\S\ref{sec:si-oos}). Every margin we report rests on
the binning bound (Table~\ref{tab:bounds}). The corroborating estimators say that bound is slack,
not that $\Binsuf$ is negligible (\S\ref{sec:si-tables}).

% THE BOLD SENTENCE, 2026-08-05 (round 3, referee 1, must-fix).  It used to read "That row set is
% the one established finding", a status this paper's own controls decline to give: the row set is
% the survivor of a 59-fold search, its magnitude is at the ninetieth percentile of the
% chemistry-blind null below, and the pre-declared out-of-sample cell is empty.  The null number is
% INSIDE the sentence rather than one paragraph later, so a reader cannot take the claim and leave
% the caveat.  Do not restore "established finding" here, the bold in the map tables (tab:map-*,
% generated by scripts/analysis/make_map_table_tex.py), or "the established stratum" as a name.
\emph{The map, and how much of it survives the rule.} We run the instrument over $59$ strata of the
broad IDAC set ($477$ rows, $185$ pairs, $78$ solutes, $36$ solvents, $15$ source publications).
Fifteen have $n\ge40$ and are boundable, which is the condition for the rule to act on a margin.
Tables~\ref{tab:map-article} and~\ref{tab:map-article-solute} print all fifteen, and
\S\ref{sec:si-map} the other $44$, the $24$ carrying no estimate at all included, each with the test
it fails. Six of the fifteen are \emph{admissible}, and clause~(c) makes those six three distinct
row sets, of which one is both positive and left in place by its residual test. \textbf{The
glycol-ether solvents are the one row set the rule leaves standing, and we report that as the
instrument's output rather than as a finding of this work.} A chemistry-blind relabelling of the
same molecules reaches its margin in $10.1\%$ of draws (below), which is the frequency at which the
search would produce it on chemistry that carries no signal. Its clusters are few and its chemistry
narrow, and both belong in the sentence that states it. It is $182$ rows over $43$ pairs,
carrying $45\%$ of the set's squared error. The solvents are $4$ glycols of one homologous series,
the solutes $19$, every one a hydrocarbon or a halobenzene, and two of three publications carry
$177$ of the rows. % BOTH BOUNDS, WHEREVER THE HEADLINE MARGIN PRINTS, 2026-08-19.  The abstract carried the
% pre-declared estimator's +1.76 beside the binning bound's +2.04 and the body did not, so a reader
% of Sec. 3.2.1 alone met one number where two are valid.  The answer to the deviation below IS
% that the sign holds under both where both are valid; printing only one forfeits it.
On them the closure's own error exceeds what input insufficiency can account for by at least
$+2.04$, $90\%$ interval $[+1.25,+2.87]$ at the residual-only convention and $+2.94$ at the pair
unit under the full one. The pre-declared cross-fitted bound, where that estimator is valid, puts
the same excess at $+1.76$ $[+1.02,+2.49]$. The binning margin is positive in all four
unit\,$\times$\,convention cells at $P>0.95$ and under every single-publication deletion, the worst
leaving $+1.60$.
% ONE SENTENCE, TWO CURVES, corrected 2026-08-19.  It read "Deleting each glycol in turn ... the
% magnitude runs over a factor of two, set by the shortest oligomer", and those are two different
% operations: the DELETION curve is +1.72/+1.94/+2.05/+2.29, a factor of 1.33, and the factor of
% two with the shortest oligomer on top belongs to the SINGLETON curve, +3.17/+2.44/+1.71/+1.21
% (\S\ref{sec:si-grid} prints both, in one sentence, which is how they were crossed).  This is
% same defect the 11-of-60 note above records -- an operation from one cell and a magnitude from
% another -- and it is the second instance, so BOTH curves now print here with their own numbers.
Deleting each glycol in turn is the sharper test, three publications mapping onto four molecules,
and the sign survives all four deletions, at $+1.72$ to $+2.29$. Taken one glycol at a time instead
the margin spans a factor of two and a half and is set by the shortest oligomer
(\S\ref{sec:si-grid}). Either spread, and not the interval, is the honest width on the solvent
axis, the bootstrap resampling the same four molecules the deletions delete. The margin survives
the loss of the alkane solutes ($74$ rows, $+1.40$) and every cell of the bin-count sweep, the
analyst choice with the largest leverage here (\S\ref{sec:si-map-cells}, Table~\ref{tab:grid-glycol}).
% THE OUT-OF-SAMPLE READING, added 2026-08-19.  Pre-declared and hashed before the number existed
% (results/b_insuff/glycol_oos_thermoml/PRE_DECLARATION.md, sha256 8744f9e1..., with amendment 1
% correcting the prediction side from VT-2005 to UD, 5e9ac5ee...); producer
% run_glycol_oos_margin.py, which reproduces the published in-sample +2.04 before it reads the new
% rows and exits nonzero if it stops doing so.  THE HALF-SIZE IS NOT OPTIONAL BESIDE THE SIGN: a
% sign that reproduces while the magnitude halves is what a search selecting on magnitude leaves
% behind, and the sentence must not be shortened to "it reproduces".
% IT IS SINGLE-SOURCE and therefore NOT admissible under this section's own rule.  Say so wherever
% the number is restated.
On rows the search never saw the separation reproduces at about half its size. An
independent ThermoML publication supplies $95$ rows over $32$ pairs the broad set does not carry,
$21$ solutes in diethylene and triethylene glycol at three temperatures. There the margin is
$+1.06$ $[+0.74,+1.29]$, against the in-sample $+2.04$ $[+1.25,+2.87]$, and the two intervals
overlap only in a narrow band. The ThermoML set carries one source publication, so the
leave-one-source-out clause this section's rule turns on cannot be applied to it, and the reading is
descriptive rather than admissible. It settles the sign. It suggests the in-sample magnitude is the
upper end of what this chemistry supports rather than its size.

The sweep varies the binning bound's own settings and not the family of estimator.

% RESTRUCTURED 2026-08-19.  The block opened on the concession and reached its technical ground
% mid-sentence, which handed a referee the weapon handle-first.  The ground is strong and now leads:
% Lemma 1(a) puts B_insuff in [0, MSE], and on the declaration's own conservative folds the
% cross-fitted estimator returns 1.6 to 2.9 times MSE with a forest and 10^27 times with ridge.
% That is a range violation, not evidence about chemistry, and the criterion has no free parameter.
% WHAT MAY NOT BE SOFTENED BY THE REORDERING, and is now IN THE ARTICLE rather than only the SI:
% where the cross-fit PASSES the declared gate it puts this finding AT CHANCE -- 54% of certifying
% draws against 16%.  The deviation is load-bearing for the finding and the paragraph says so.
% A subheading, because a referee should find this where they look for it.
\paragraph{Deviation from the declaration.}
A pre-declared cross-fitted regressor was to replace the binning bound if it passed a validity gate,
and on the declaration's own conservative source-grouped folds it does not return a bound at all.
% THE R^2 WAS MISATTRIBUTED, corrected 2026-08-19 while restructuring.  It read "-0.60 out of fold
% on the matched set", and -0.602111 belongs to the cells with n=182, 19 solutes and 4 solvents --
% the GLYCOL-ETHER STRATUM itself.  Which is the stronger fact and the one the paragraph needs: the
% range violation is not on some other row set, it is on the row set the finding rests on.  Same
% defect class as the 11-of-60 and the two crossed glycol curves: a number from one cell restated
% against another.  Verified in results/b_insuff/crossfit_map.json.
Lemma~\ref{prop:decomp}(a) puts $\Binsuf$ in $[0,\mathrm{MSE}]$, and on the glycol-ether stratum
itself the estimator returns $\Binsuf=4.92$ against that stratum's own $\mathrm{MSE}$ of $2.25$,
twice what the decomposition allows, at $R^2=-0.60$ out of fold. Across the source-folded cells the
same violation runs from $1.6$ to $2.9$ times $\mathrm{MSE}$ with a random forest and some
twenty-seven orders of magnitude more with ridge. An upper bound outside the range its estimand can
occupy is not a bound on it, so the $-7.58$ it prints here, and the loss of every admissible
positive row set, are uninformative rather than reversals. The criterion has no free parameter and no direction: it
fires whenever the output leaves the interval the decomposition allows. \textbf{The declared gate did not test it.} The gate read an out-of-fold $R^2$ and a
pair-conditional variance threshold, and the permissive pair-grouped configuration passed both.
\textbf{The declaration required unconditional adoption on that pass, and every margin printed in
this paper is nevertheless the binning bound's.}

The cross-fit, where it does pass, is uniformly \emph{looser}, and it does not favour us. On the
glycol ethers it returns $+1.76$ $[+1.02,+2.49]$, so the sign holds. But the chemistry-blind null
moves from $0.094$ to $0.137$. Conditioned on drawing a map that certifies anything at all, a
relabelling reaches the observed maximum in $54\%$ of draws against $16\%$ under the binning bound
(\S\ref{sec:si-crossfit}; note~\emph{e} of Table~\ref{tab:map-article}). Under the estimator the declaration named, this cell sits at chance. That does not subtract from
what is claimed, because nothing is claimed for it: it is reported as the survivor of a search and
not as an established effect, and a survivor's frequency under a null is what it is under either
estimator. The deviation therefore runs in this paper's favour
and was chosen after the two bounds were compared, which is what a declaration exists to prevent.
The rule was written against one hazard: taking the smaller bound cell by cell once the cells are
seen. It did not anticipate that one valid bound would be uniformly looser than the other, where
obeying it prints a bound that is valid and strictly less informative. Two things limit the
deviation. It followed one global comparison of two bounds on one row set, not a per-cell selection.
And the poorer cross-fitted map prints cell by cell in \S\ref{par:crossfit-deviation}, where the
declared rule can be held against it. The sweep therefore establishes robustness within the binning
bound, not across estimators.

% ===========================================================================================
% THE MAP, PROMOTED INTO THE ARTICLE 2026-08-11 (referee round 6, all three referees).  The
% instrument's output was legible in the article only as the three-bar strip of Fig. 1b, and a
% referee could not check "no other stratum separates" without the Supporting Information.  These
% are the FIFTEEN strata the fixed estimator can bound -- the only rows the admissibility rule can
% act on -- on both axes and at every granularity.  The two bodies are GENERATED by
% scripts/analysis/make_map_table_tex.py from results/b_insuff/admissibility_table.csv, the same
% generator and the same cell as the SI's five map tables; the 44 unboundable strata stay there.
% Do not edit the bodies here.  If a granularity is added to the map, it lands in both places in
% the same pass.
% ===========================================================================================
% TIER 2, 2026-08-19: the two article map floats (tab:map-article, tab:map-article-solute -- the
% 15 boundable strata with their margins, sources, cells and per-row verdicts, plus the shared
% note block) moved to \S\ref{sec:si-map-cells}, which prints all 59 strata from the same
% generator and therefore already contained every row of both.  NO CHECKER BINDING MOVED: the
% CaptionSpecs named "tab:solvent-map" and "tab:solute-map" in
% check_hand_transcribed_displays.py are the per-class ACCURACY maps of
% sections/chemistry-map.tex, a different pair of tables; verified before the move.
% WHAT STAYS IN THE ARTICLE is the sentence that reads the map, with the one row set the rule
% leaves standing and its narrowness in the same sentence.


% THE TWO BIASES STAY IN THIS DOCUMENT, 2026-08-10.  How the p and the 10.1% may be read is decided
% by their DIRECTIONS, so those are printed here and not left behind a pointer: bias (1) has two
% halves running in opposite directions and left un-netted; bias (2) runs one way and inflates the
% 58% rather than the 10.1%.  Do not describe the pair as "running against each other", and do not
% reduce either direction to a pointer.  The four figures behind bias (1) are in \S\ref{sec:si-null}.
The row set clears no multiplicity null, and that belongs beside the finding and not in a
limitation: it is by construction the survivor of a $59$-fold search. Our null relabels the
chemistry blindly---each solvent and solute keeps its rows and takes another's class labels, and the
stratum family is rebuilt and put through the same rule. Over $2000$ such relabellings, on all
$477$ rows and at the cell the map deploys, its six admissible strata are above chance ($p=0.01$)
while its two admissible positive row sets sit at chance ($p=0.286$). One row set or more is
certified in $58\%$ of draws. The size is the sharper statistic, and it leaves the finding where the
counts do: a certified margin of $+2.04$ or more arises in $10.1\%$ of all draws, $17\%$ of those
that certify anything. The instrument licenses the separation on the rows it was computed on. As a
chemistry-specific effect it is unestablished against the search that found it. Each $p$ is a
frequency, not a bound in either direction, and the null measures two biases. Provenance survives
the permutation in two halves that run opposite and stand un-netted, so the $10.1\%$ cannot be read
as conservative. The set taken whole also has a positive margin at this cell, so a randomly
assembled stratum inherits one on average, inflating the $58\%$ and not the $10.1\%$. The exactly matched null would permute chemistry and provenance jointly, which these data
place out of reach, the two being confounded in them (\S\ref{sec:si-null}).

% OUT-OF-SAMPLE TEST, 2026-08-05.  A selection objection is settled by rows the search never saw, so
% one test was declared in full -- row set, cell, rule, and what would count as confirmation in each
% direction -- and run once.  It returned NOT TESTABLE, the third of the three verdicts the
% pre-declaration lists, which it counts as non-confirmation.  THE VERB (round 3, three referees,
% must-fix): the cell the finding is about has ZERO rows out of sample, so no test of it was possible
% on any public data, and "did not confirm" credits the design with a replication attempt it never
% got to make.  Do not restore "did not confirm" here or anywhere (abstract, Table 4, limitation
% (vi), Conclusions, Sec. S3.1).  Do not promote the +0.35: the pre-declaration fixed in advance
% that an inadmissible row set licenses nothing.  Artifact:
% results/b_insuff/glycol_ether_out_of_sample.json, from run_glycol_ether_out_of_sample.py, whose
% docstring is the pre-declaration.
A selection objection is settled by the same measurement on rows the search never saw, so we ran
one. The PGL 6th-edition IDAC database is another group's compilation, sixteen times the broad set's
rows on a different profile database, scored by the same deployed closure
(Table~\ref{tab:closure-anchor}). The row set, the estimator cell, the admissibility rule and what
would count as confirmation in each direction were fixed before any margin was computed
(\S\ref{sec:si-oos}). \textbf{The chemistry stayed untestable there}, for a reason about the data
rather than the number: the cell the finding is about, a glycol-ether solvent with an
\emph{organic} solute, is empty there. \emph{Not testable} is the third verdict the pre-declaration
lists and counts as non-confirmation, and it follows from the row inventory alone. The
pre-registration delivers the absence of confirming data here, not a failed replication. The nearest
cell that database carries is water in glycol-ether and polyol solvents. The rule refuses it out of
sample for the reason it refuses water-as-solute in sample, so its margin licenses nothing and the
sign stays out of risk. Over that database's ten solvent classes, stated as
description and not as a test (Table~\ref{tab:oos-map}), every class either fails the rule or leaves
the two terms merged. Every margin above the one admitted positive class sits on a class the rule
refuses: out of sample the glycol ethers sit away from where this closure errs hardest.

The glycol-ether row set is the whole of what the instrument returned, and this work does not
advance it as an established chemical effect. Reading it as ``COSMO-SAC error exceeds input error''
would carry it past five boundaries, each measured here. They are the map's other strata; both data
sets taken whole; the solubility axis of \S\ref{sec:paradox}, where the two terms stay merged; the
kernel as against the composite it is deployed in; and the chemistry-blind null. The margin sits at
the ninetieth percentile of that null, on the one chemistry for which out-of-sample rows are
missing. What the second axis establishes is therefore the instrument and its map, and not this
cell: a procedure that refuses fifty-eight strata of fifty-nine, and returns for the fifty-ninth a
number it cannot separate from its own search.


\emph{What the other fifty-eight strata are.} They are the rule refusing to answer, and the
refusal is calibrated rather than severe. The chemistry-blind null above still certifies some row
set in $58\%$ of draws, so the rule accepts about as often on relabelled chemistry as on real.
What refuses fifty-eight strata here is therefore the resolution of the data, not a threshold
tuned until one cell survived. An instrument that returned a verdict on every stratum of a
$477$-row set would be reporting resolution it does not have, and the admissibility clauses exist
to withhold exactly that. The $10.1\%$ beside it is a different statistic about a different object.
It is the frequency with which a relabelling reaches this stratum's own margin, and it is why the
finding is stated as a search's survivor rather than as an established chemical effect. Of the six
admissible cells, three are the glycol ethers at three granularities. Of the other two,
the alkane solutes are positive and demoted to the glycol ethers by clause~(c); the acceptor-only
aprotics pass the rule and leave the two terms merged, the instrument being one-sided. The uniqueness is therefore narrower than being the sole admissible stratum, or the sole
stratum sign-stable under every deletion: it is one row set admissible, positive and left standing
by clause~(c). \S\ref{sec:si-map} records which cell each remaining stratum fails on, none of them
used to explain a reversal.

% WHAT MUST STAY IN THIS PARAGRAPH: the ratio statistics (they are a row of Table~\ref{tab:claims}),
% the null they are read against, the estimator-geometry REASON -- without it "why a map" is
% asserted and not shown -- and the closing clause that the bound stays valid in each stratum, which
% is what stops the paragraph from reading as a retraction of the instrument.
\emph{Why the output is a map.} $\Binsuf^{\mathrm{up}}$ is nearly the same in every stratum while
$\mathrm{MSE}$ moves: a factor of $2.0$ against $25$ over the three classes the design can bound,
and $6.8$ against $51$ once the three that carry an estimate are added. The $6.8$ rests on a
mono-alcohol figure the artifact flags as an invalid bound on a stratum already excluded. Against
$2000$ random partitions of the $477$ rows into blocks of the nine observed class sizes, the
chemical partition's range in $\mathrm{MSE}$ exceeds every draw while its range in the bound is
unremarkable ($p=0.26$). Their ratio gives $p=0.02$ (\S\ref{sec:si-map}). The estimator is showing
through. Each stratum is summarised by eight quantile bins of one scalar, so the bound measures the
residual spread of $m$ inside such a bin. The spread is a property of the binning and not of the
chemistry, and the error moves independently of it. An aggregate margin is therefore a
composition-weighted average of a term free to move $25$-fold against one that barely moves. It
reports a binding closure for any set whose composition includes a chemistry the closure fails on.
The statement is about the aggregate, not about the bound, which stays valid in each stratum where
it is computed.

\subsubsection{The VT-2005-matched set ($n{=}60$)}
\label{sec:vt2005}
% WHAT MUST STAY: that this set's aggregate margin DOES NOT STAND, that it supports no stratified
% bound and no stratum of it is stated as a finding, and that its 57 shared pairs make it not an
% independent sample from the broad set -- which is what makes the two sets' agreement a consistency
% check rather than corroboration.  The arithmetic is in \S\ref{sec:si-map-cells} and
% \S\ref{sec:si-robustness}.

This set carries the paper's other reference-matched analyses and is far weaker. All $60$ rows sit
at $298$\,K, so that $\dim(\zstar)/n\approx102/60$, and methanol occupies $25$ of them. Its $17$
solvents fall in six of the nine classes, each short of the $n{=}40$ a bound needs. The set supports
% THE DELETION COUNT WAS FROM A DIFFERENT CELL THAN THE MARGIN IT QUALIFIES, fixed 2026-08-19.
% This sentence read "under 11 of the 60 single-pair deletions" beside an aggregate margin of +0.05.
% Both numbers are real and they are not the same estimator cell: recomputed from the deposited
% row-level table (scripts/analysis/check_vt2005_leverage_counts.py, which prints the whole grid),
%   8 bins, unbiased variance -- THE HEADLINE CELL of Sec. 2.5 --  margin +0.0455, 4 of 60 fail, min -0.033
%   5 bins, unbiased variance                                     margin +0.0970, 11 of 60 fail
%   8 bins, maximum-likelihood variance (the deposited artifact)   margin +0.2362, 0 of 60 fail, min +0.151
% So the sentence took its margin from the headline cell and its count from the five-bin one, which
% is the hazard Sec. 2.5 fixes four analyst choices in advance to prevent.  The count is now the
% headline cell's, and it agrees with the Supporting Information's "56/60 (minimum -0.03)", which
% was right all along and reproduces exactly.  The artifact's 60/60 is right about its own cell.
% ANY NUMBER ADDED TO THIS SENTENCE MUST COME FROM THE HEADLINE CELL.  Run the script.
no stratified bound, and we state no stratum of it as a finding. Its aggregate margin of $+0.05$
collapses four ways: under deletion of the two leverage pairs, under dropping methanol, under
blocked out-of-fold estimators, and under $4$ of the $60$ single-pair deletions. The worst of those
deletions leaves $-0.03$, where on the broad set every single-pair deletion leaves the sign intact.
A margin here would in any case bound a label systematic tracking $\zstar$, which the unbiased
within-bin variance leaves \emph{open} (\S\ref{sec:si-robustness}). The reported cell is not a
favourable one: drawn over the whole grid of bin counts and conventions the aggregate changes sign,
and of the cells returning a positive margin the reported one is near the bottom
(Fig.~\ref{fig:cell}, \S\ref{sec:si-grid}). Of the $60$ pairs, $57$
also appear in the broad IDAC set at $298$\,K under UD profiles, so the two are overlapping samples
rather than independent ones. VT-2005, and therefore the paradox's own oracle, is defined on this
sub-design, so the agreement between the two sets' error orderings is a consistency check rather
than corroboration.

\subsubsection{Whether a better closure moves the ceiling, and where this one fails}
\label{sec:fidelity}
\label{sec:badclosure}
% BOTH LABELS ARE KEPT HERE on purpose -- \S\ref{sec:fidelity} and \S\ref{sec:badclosure} are cited
% from Table~\ref{tab:claims}, Table~\ref{tab:notation}, the head of \S\ref{sec:results} and six
% places in the Supporting Information, and all of them still mean this block.
% WHAT MUST STAY: that the ceiling is in the map AS DEPLOYED and not the kernel; that this section
% does not say which half of the composite carries the error; that dominance does not say the
% composite is fixably wrong; that the kernel-residual result is a NULL and not a bound (a negative
% quoted without that clause reads as a measured zero); and that the low fidelity is the
% contribution and not a qualification.  The profile-database exposure arithmetic went to
% \S\ref{sec:si-fidelity} WITH its own scope clause -- it sizes the exposure and does not bound the
% share -- and the two travel together or neither is stated.
% NEGATION PASS, 2026-08-19.  This subsection ran at 28.7 negation markers per 1000 words against a
% JCIM median of 2.97 and a corpus maximum of 7.31 -- the most negated passage in the manuscript, and
% it got worse as the length passes removed prose and left the caveats standing.
% EVERY CLAIM IS UNCHANGED.  What changed is the polarity of the sentence carrying it: "is not
% positive, so the terms are not separated" says the same thing as "sits at or below zero, so the
% terms stay merged", and the second is readable.  Do NOT read this as softening -- a claim that was
% one-sided is still one-sided, a null is still called a null, and "untested" still prints where the
% thing is untested.  What may not come back is a double negative or a claim stated only by what it
% fails to be.
If the ceiling is the closure, the direct remedy is a better closure. The standard $2002\to2010$
step delivers one on the VT-2005-matched set and withholds it on the broad IDAC set. Neither half
of that pair settles anything. The improvement is on a $57$-pair overlap with the larger set rather
than an independent sample, and the withholding is $-0.99$ at $P\approx0.2$, which is not a measured
equality either. Three further readings are of the same kind: the 2010 kernel on its own typed
latent, a fitted kernel residual asked to transfer across solvents, and the block split that places
the reversal in the chemistry rather than the temperature range. Each prints with its interval in
Table~\ref{tab:kernels} and \S\ref{sec:si-fidelity}. We claim only that
\textbf{the fidelity lever we can demonstrate is confined to the smaller set's chemistry}, and that
broad-set null is itself a small-molecule and glycol-heavy one, so on drug-like solids the lever
stands untested.

On the one row set the rule leaves standing, where $\Bclos$ dominates, the ceiling is in the map
\emph{as it is deployed}: the fixed kernel together with the one computed conformer it reads. The
composite is what $\Bclos$ is the error of, and the object the pipeline runs on. The split between
the two stays open here, and so does whether the composite is \emph{fixably} wrong. A fitted kernel
residual on the $60$ matched pairs transfers within a solvent and fails to transfer across solvents,
under the solvent-grouped folds that are the one fold design free of leakage. The result is a
null. So $\Bclos$ is an error a handful of kernel parameters can absorb
\emph{within} a solvent, and its correctability across solvents we leave unshown.

The failure is chemically placed. On the VT-2005-matched set $90\%$ of the squared error falls on
strong hydrogen-bond-donor solvents, and the closure is near-exact on acceptor-only ones. Those
are the chemistry the documented single-parameter H-bond term governs, and the term the
donor/acceptor-typed 2010 closure was built to replace \cite{hsieh2010,fingerhut2017}. That failure
is established already and the localisation restates it. The new part is that on one such chemistry
the failure is bounded against what the closure's inputs leave unresolved. The bound also survives
the obvious alternative: replacing the four glycol-ether references with Boltzmann ensembles moves
the stratum's margin from $+2.04$ to $+2.27$, so the error sits in a \emph{chemistry} rather than in
the conformer half of the composite (\S\ref{sec:si-correctable}).

COSMO-SAC-2002 being a low-fidelity closure is the contribution and not a qualification of the
result. It is what a practitioner adopts by default, and a closure's fidelity is \emph{unknowable in
advance} on a new system. The field routinely composes an expressive encoder with a fixed
mechanistic map and assumes the map is good enough. Once that assumption fails, the learned latent
silently departs from the quantity it is named after (\S\ref{sec:surrogate}), and an external oracle
is what makes the departure visible. Outside that one row set the map reports cells it can only
leave unresolved, which is a weaker statement than cells in which the closure is sound. The exposure
the choice of profile database creates, and why that arithmetic sizes it while leaving its share
unbounded, is in \S\ref{sec:si-fidelity}.

\subsection{The same split on a second closure: pKa through a fixed Hammett relation}
\label{sec:pka-main}
% LENGTH PASS, 2026-08-18.  This subsection ran 1848 prose words and is cut to ~1010.  The
% construction, the estimator validation and the competence sweep are in \S\ref{sec:pka}, which
% carries every number this cut drops: the offsets 0.429/+0.303/+0.034/-0.198 and the 0.371, the
% filter's 5-of-163 arithmetic with the +0.55 it restores, the 8.3-against-5.1 pooled estimator, the
% 12% interpolation floor, the 2.6+-2.0 extrapolation break, and the p=1/16 sign test.
% TWO CLAUSES ARE VERBATIM-MANDATED and may not be paraphrased or moved into a caveat list:
% "The flip is an in-distribution result and the scaffold protocol does not certify it" and
% "We measure every solubility result in this paper on a scaffold split".  Both are bolded in the
% source and both print here.  The one MSE contrast the SI does not carry -- 3.33 to 0.30, the
% factor of eleven the filter makes on the clean pole -- is kept for that reason.
% TIER 1 OF THE LENGTH DECISION, 2026-08-19.  The second domain's construction, its estimator
% validation, its bounds on both poles, the competence sweep and the scaffold re-certification moved
% to \S\ref{sec:si-pka-main}; this subsection goes 1341 -> ~200 prose words and keeps \label so that
% all 26 cross-references into it -- 12 from this document, 14 from the Supporting Information --
% still resolve.
% WHAT THIS IS AND IS NOT.  It is a RELOCATION of the evidence, not a withdrawal of the claim: the
% article still asserts that the instrument generalises and that the sign reverses inside one
% closure, and the abstract's closing clause still stands.  Dropping the claim outright would mean
% striking that clause, the Introduction's second-domain sentence and \S\ref{sec:framework}'s second
% instance as well, which is a larger edit and a different decision.
% THE TWO MANDATED CLAUSES PRINT HERE VERBATIM and may not be paraphrased, moved into a caveat list,
% or left to the Supporting Information: "The flip is an in-distribution result and the scaffold
% protocol does not certify it" and "We measure every solubility result in this paper on a scaffold
% split".  A reader who meets the reversal must meet its protocol limit on the same page.
The instrument generalises beyond COSMO-SAC, and a second domain runs it where the sign of grounding
can be predicted before the measurement. The closure is a Hammett linear free-energy relationship
$g(\zstar)=\mathrm pK_{a,0}-\rho\,\hammett^\star$ in tabulated Hansch--Leo--Taft constants
\cite{hansch1991survey}, unfitted on these data, so $g$ is fixed in the sense
Lemma~\ref{prop:decomp} requires. The learned quantity is the substituent constant predicted from
structure, and the data are the OPERA compilation \cite{mansouri2019opera}. A rule reading structure
alone splits the records into two poles. The \emph{clean} pole is the $158$ whose every substituted
position is meta or para with a tabulated substituent. The \emph{degraded} pole is the $846$ where
$\hammett^\star$ is a partial sum over the positions the table can see.

The sign reverses between them, in the direction that construction fixes in advance. Across $K{=}20$
within-scaffold splits the reference \emph{improves} accuracy on the clean pole (MAE $0.48$ learned
against $0.315$, a gap of $-0.165$ at a $90\%$ confidence interval of $[-0.28,-0.04]$) and \emph{degrades} it on the
degraded pole ($0.83$ against $1.62$, $+0.79$ $[{+}0.57,{+}0.95]$). The freely learned constant
absorbs what the zeroed positions carry and the tabulated one lacks. \textbf{The flip is an
in-distribution result and the scaffold protocol does not certify it.} Re-run on a scaffold split
the reference helps on both poles, so the degraded pole's ``grounding hurts'' half fails to certify
and so does the reversal. The scaffold run confirms the mechanism, not the flip. We
measure every solubility result in this paper on a scaffold split, and this is the one place the
protocol changes. \S\ref{sec:si-pka-main} carries the assignment rule, the bounds on both poles, what the quality
filter decides, the estimator's two conditions of use, the competence sweep and the $G{=}4$ sign
test.

\subsection{Scope, accuracy, and limitations}
\label{sec:discussion}
% COMPRESSED 2026-08-18, 2531 -> ~1050 prose words, against the measured JCIM profile (147 full
% texts, 2024-2026): a limitations block runs 400-750 words there and this one ran 2531 with its
% caveats welded into running prose.  WHERE EACH DELETED BLOCK WENT, checked location by location
% BEFORE it was cut, because a caveat travels with the sentence it qualifies:
%   * \paragraph{The contribution.} -- deleted whole, and every clause survives INSIDE THIS ARTICLE,
%     which is stronger than survival in the Supporting Information: the from-below/from-above
%     bounding in the abstract; Kennedy-O'Hagan, the concept-bottleneck contrast and all four of its
%     citations in the Introduction; the division itself as Lemma~\ref{prop:decomp} in
%     \S\ref{sec:framework}; the two mandated pKa scope clauses ("The flip is an in-distribution
%     result and the scaffold protocol does not certify it", "We measure every solubility result in
%     this paper on a scaffold split") in \S\ref{sec:pka-main}; the one-sided design rule in the
%     Conclusions.  The one phrase that leaves the article is "the law of total variance applied to
%     a composition"; the law is named in the proof of Lemma~\ref{prop:decomp} in the Supporting
%     Information, which is the only other place the 2026-08-10 one-name-per-thing rule allows it.
%   * the crystal pool's two halves (97.76%, 345 net-new, -12.233 K, 45.0->46.6 K, +1.59 K) and the
%     downstream +0.065/+0.088 -- sections/crystal-null.tex, BOTH HALVES TOGETHER, which is what the
%     2026-08-10 guard on that estimand requires.  The verdict stays in the article: the arm is
%     "underpowered rather than null", never "tested nothing".
%   * the stratification's overlap arithmetic (362 of 1711 stratum pairs, the 37 water-as-solute
%     rows, 108 of the 129 alkane rows) -- sections/SI.tex; item (vi) keeps the consequence, that
%     cells from different axes are dependent evidence.
%   * the enlarged ThermoML pull's values -- sections/SI.tex; item (i) keeps the direction.
%   * the per-model, per-corpus breakdown of the six published figures, the clip sweep and the
%     random-pair rows -- \S\ref{sec:si-baselines}.  BOTH RANGES, BOTH CORPORA, BOTH CITATIONS AND
%     THE UNIT STAY HERE: a comparison a reader cannot source is not a comparison, and the article
%     must be sourceable on its own.
%   * the Jensen bound's reference-state condition -- the Supporting Information's proofs and
%     sections/methods.tex.
%   * the nine-limitation index paragraph -- converted, not deleted: all fourteen limitations now
%     print here, one to three lines each, and \S\ref{sec:si-limitations} carries the detail that
%     qualifies the nine it holds.  Keep that section's own preamble in step with this list.
% WHAT WAS NOT COMPRESSED AND MAY NOT BE: the admissibility rule as a stability filter rather than a
% correction for the search; both tests of the search; "not as a multiplicity-controlled chemical
% effect"; the one-sided clause on \Binsuf; the separate-tuning confound together with the three
% contrasts that fall outside its immunity; and every item's assertion of its own weakness.  A
% shorter limitation that gestures instead of asserting is not a shorter limitation.
% AND THE OPENING NO LONGER SAYS SUPERVISION "HELPS".  It did until this pass, four days after the
% five-seed leak-free re-run took that sign away (abstract: "a mean without a sign: four gain, one
% loses").  The checker binds floats and the abstract, not running prose, so the discharge passed
% over it.  Do not restore a sign for the training-time operation without a run that gives it one.
Two measurements, in different places. On solubility, supervising a learned physical latent on
reference values during training moves the error without an established sign, while
\emph{supplying} those values at inference hurts at every seed. On infinite-dilution activity data,
and there on one class of solvent, the fixed map's own error exceeds the bound on what its inputs
leave undetermined. The two sit in different regimes on largely disjoint chemistry, so ``the
substitution hurts \emph{because} the map is misspecified'' remains untested (limitation~(i)). The
finding is one-sided: one row set of the map on which grounding effort is \emph{not} the remedy.

% TIER 2 OF THE LENGTH DECISION, 2026-08-19: the black-box probe block (414 words) left the article
% for the Supporting Information, which already carried all of it -- \S\ref{sec:si-blackbox} has the
% three studies with their full tables, and limitation~(ix) of \S\ref{sec:si-limitations} carries
% every caveat the block made, verified line by line before the cut: the 13 held-out solutes and
% that they cannot be enlarged, the bound-not-a-demonstration reading, that no power calculation was
% run, the three R^2 against the null's 95th percentiles and maxima, "nothing below that ceiling is
% excluded", the refusal to read the between-seed spread against the null's support, the corpus cap
% at r=+0.24, and the weak-evidence reading of the scaffold partitions.
% WHAT STAYS HERE IS THE CLAIM, not the apparatus: the control does not carry the thermodynamics
% implicitly, and the negative is a bound.  Do not delete the second sentence to save a line -- a
% negative printed without "falls short of an exclusion" is the failure this project treats as worse
% than a wrong positive.
\paragraph{The unconstrained control's representation.} If the physics arm's intermediate is a
surrogate, the thermodynamics might still be present implicitly in the control, which is free to
represent it. It is absent: a ridge probe of the control's pair representation recovers the model's
own prediction at $R^{2}=0.98$ and leaves the measured ideal-solubility term unrecovered. The
negative falls short of an exclusion. It rests on $13$ held-out solute clusters whose reference
scarcity caps them, each measured $R^{2}$ sits at or below its own permutation null's ceiling, and
no power calculation says how far above that ceiling the design reaches. It is limitation~(ix), and
\S\ref{sec:si-blackbox} carries the three studies, the controls and the temperature probe.

% THE SEED COUNT WAS DROPPED FROM THIS SENTENCE ON PURPOSE, 2026-08-18.  A draft of this compression
% wrote "in the seed mean over three seeds", which is wrong in the one direction that matters: the
% control and the NRTL arm carry three seeds and the COSMO-SAC arms five, the caption below says
% "the five-seed leak-free re-run", and the +0.23 is a five-seed mean set against a three-seed mean.
% The counts are in Table~\ref{tab:si-arms}, where they can be read per arm; a single count here can
% only be wrong for one of the two families.
\paragraph{Where the accuracy stands, and the confound in it.} Table~\ref{tab:baselines} places every
arm of this work beside the external models, in two blocks that may not be read across. The physics
arms trail their own control in the seed mean, the COSMO-SAC arms at each of the three seeds the two
families share and the NRTL arm at two (Table~\ref{tab:si-arms}). The two sides are separately tuned
with no hyperparameter-matched control, so no difference between them, and none of the chemical
strata that resolve the same difference (\S\ref{sec:map}, \S\ref{sec:data-efficiency}), is
attributable to the thermodynamic bottleneck. We claim no accuracy result in either direction, and
the matched control that would repair this is unrun. Untouched by that confound is every \emph{within-checkpoint} contrast, where the two
sides share one trained model and one tuning (\S\ref{sec:paradox}, \S\ref{sec:decision},
\S\ref{sec:surrogate}, \S\ref{sec:measure}). Two fall outside it, comparing across trainings or across the separately tuned control: the
supervision gain and \S\ref{sec:decision}'s shrinkage slopes. Grounding the crystal factor on external labels is underpowered rather
than null. $97.76\%$ of that pool was melting points the model had already been trained on, and
where it carried something new the crystal head moved. On the held-out solutes it is the worse of
the two, and the downstream solubility gap crosses zero once clustered by solute
(\S\ref{sec:e2}). Temperature extrapolation is the one place the physical \emph{structure} helps,
and it helps as a property of the hypothesis class and not of any arm trained here. Fitting
below $T\le309.75$~K and scoring at $T\ge330$~K on the same $1751$ solute--solvent pairs withholds
temperature and keeps structure. On that split a structured van't Hoff class reaches an \lnx\ MAE
of $0.37$ against $1.29$ for a random forest over Morgan fingerprints and temperature, each one fit
on one temperature cut with no replicate behind it (\S\ref{sec:supporting}).

% THE ATTRIBUTION IS SPLIT BETWEEN TWO WORKS AND BOTH CORPORA ARE NAMED, 2026-08-18.  A draft of
% this compression bundled \cite{osanloo2024,vermeire2022solprop} under "as published by Attia et
% al." -- the evaluation and the model it scores are different works -- and dropped
% \cite{boobier2020} from the article entirely, which leaves the two corpora unidentifiable from
% here.  osanloo2024 is the historical key for the Attia et al. evaluation, which is where all six
% figures are published; vermeire2022solprop is the SolProp cycle it scores.  "None of the six
% carries a published uncertainty" is what keeps a range from being read as a spread, and the unit
% is stated because the published column is not the one this work's arms are argued in elsewhere.
The external block is on an easier by-solute split, and the harness for a like-for-like comparison
is deposited and unrun. The difference that remains is a scale rather than a placement. This
work's control reaches
% STALE BY ONE RUN FAMILY, corrected 2026-08-19.  This range was the pre-leak-free three-seed
% block's (1.387 and 1.598 in results/external_baseline_comparison/summary.json) and stood beside a
% Table 2 that has printed the five-seed re-run's since the discharge.  Seventh instance of the same
% pattern: the display gate binds floats and the abstract, and this is running prose.
$\log_{10}S$ RMSE $1.00$ on held-out scaffolds and its physics arms $1.44$ to $1.64$, against $0.83$
to $0.99$ for the two leading direct models and $1.43$ to $2.16$ for the physics-informed SolProp
cycle \cite{vermeire2022solprop}. All six are as published by Attia et al.\ \cite{osanloo2024}, in
$\log_{10}S$ RMSE in mol\,L$^{-1}$, on two other corpora: the SolProp test set, whose solutes are
disjoint from its training corpus, and the near-ambient Leeds set of Boobier et al.\
\cite{boobier2020}. Sharing no row with these, they set a scale and withhold an ordering; each range
is two point values on two corpora rather than a spread, and none of the six carries a published
uncertainty. The clip sweep, the random-pair rows and the conversion behind the $\log_{10}S$ column
are in \S\ref{sec:si-baselines}.

% ---------------------------------------------------------------------------
% EXTERNAL BASELINES, IN THE ARTICLE (2026-08-02, editorial item A3).  The only comparison against
% published solubility models used to be Supporting Information, which is what three referees
% objected to.  EACH BLOCK IS LABELLED WITH WHAT IT MAY BE COMPARED TO, and the table refuses the
% flattering comparison in both directions: the physics arms trail their own control in block one,
% and block two is on an easier split than block one so it is not a ranking.
%
% CUT TO 10 PT, 2026-08-08.  This float used to be the whole transcription -- seventeen rows in
% three blocks, six columns, and a 489-word note in twenty lines -- set at \scriptsize, which is
% 8 pt against a 12 pt body, and filling 82% of a float page that carried no body text.  All three
% referees named the type size.  Nothing about the measure fixes it: this is already a full-width
% `table*', so at 10 pt the only lever left is rows, and these went, in this order:
%   (1) the note block, whole;
%   (2) the third block, six rows quoted as published on other corpora in mol/L, three of whose
%       four data columns are a dash;
%   (3) the two random-pair rows, a split control the article makes no claim from.
% Every one of them is printed in Table \ref{tab:si-baselines-full}, from the same source file,
% and the caption says so.  What had to STAY is the comparison \S\ref{sec:discussion} makes
% explicitly -- this work's arms against external models re-run on this corpus -- so blocks one
% and two and the scale reference are here in full.  The published leaders' range is not a table
% row any more, so the sentence that compares against it now prints it (2026-08-08); do not take
% it back out on the grounds that a table has it.
% Rows: si_tables/external_baselines_rows_article.tex, cut from the full transcription in
% si_tables/external_baselines_rows.tex; see that file's header for the four typesetting edits
% and the regeneration command.
% The n column carries TWO sizes on purpose: the log10 S column is not computed on the rows the MAE
% column is, and the rows it loses used to be ARM-DEPENDENT (2026-08-04 repair; see the generator's
% `THE COMPARISON THIS TABLE MUST NOT FLATTER' block).
% THE `Test set' COLUMN IS GONE, 2026-08-08.  The column printed the same two strings nine times
% and both are in the block heads, so it said nothing and cost 116 pt -- which is why every model
% name wrapped onto two or three lines and the block contrast, the one thing this table is for,
% was the hardest thing in it to see.
% THERE IS NO \tabcolsep HERE ANY MORE, 2026-08-09.  Every one of the five columns' boundaries is
% an explicit @{\hspace{}} in the rows file, because the spacing this table needs is NOT uniform:
% the model column is ragged-right prose and the four numeric columns are right-aligned numbers
% that abut.  Setting \tabcolsep here would change nothing and mislead the next editor.  The
% arithmetic, the printed gutters and how to re-measure them are in the rows file's header; the
% five-column tabular measures 498.75 pt against a 504.47 pt \textwidth, and the LINE it is
% centred in measures 502.08 pt because the newline after \end{tabular} leaves an interword space
% behind it, so a widened cell has 2.4 pt and not 5.7 pt before the line overflows -- and an
% over-wide tabular in a centred float does NOT reliably raise an Overfull \hbox.
% ---------------------------------------------------------------------------
\begin{table*}[tp]
\centering
% THE POINTER AT A SUPPORTING-INFORMATION TABLE LEFT THIS CAPTION, 2026-08-10 -- "The published
% figures, the random-pair rows, and the conversion and saturation clip behind the log10 S column
% are in Table \ref{tab:si-baselines-full}."  A caption states what is shown.  All three
% destinations are in the body of \S\ref{sec:discussion} on the same page: the published figures
% now print there in full with their models, corpora and citations, and the sentence that closes
% that paragraph names the sweep, the random-pair rows and the conversion.  Do not restore a
% cross-reference here; if a destination is ever needed again it goes in the body sentence.
% NO NUMERAL MAY ENTER THIS CAPTION.  It is one of the nine displays
% scripts/analysis/check_hand_transcribed_displays.py binds to artifacts, and a numeral that is
% neither bound nor declared there is a fatal UNREGISTERED finding.
\caption{Solubility accuracy in two blocks that may not be read across: this work on the
solute-scaffold split, external models re-run here on an easier by-solute split. Two sample sizes
appear where a row's two metric columns are scored on different rows, one where they coincide. The
grounded, ungrounded and $\sigma$-oracle arms of the first block come from the five-seed leak-free
re-run.}
\label{tab:baselines}
{\footnotesize
\renewcommand{\arraystretch}{0.95}
\input{si_tables/external_baselines_rows_article}}
\end{table*}

% THE TWO-AXES CLAUSE MOVED INTO ITEM (xi), 2026-08-18, and did not go.  "Whether grounding helps is
% shaped by two axes, closure fidelity and how the latent is trained, and this work leaves that
% plane uncharacterised on real systems" has no home anywhere else: \S\ref{sec:notdecided} covers
% the fidelity axis alone and neither \S\ref{sec:si-limitations} nor any other section states the
% plane.  It now sits on the limitation it qualifies.
These negatives fall where Remark~\ref{prop:tax} predicts a prior will hurt (\S\ref{sec:tax}).
The synthetic sweep of Fig.~\ref{fig:dial} is true by construction there, so it verifies the
instrument and not the hypothesis; the pKa closure exercises the sufficiency channel on real
data.

% Limitation (i) enlarged-set numbers: run_b_insuff_decomposition.{match_pairs,evaluate_closure,
% lotv_binning} (res convention, 8 equal-count bins) on notebooks/data/raw/idac_expanded{,_raw}.csv
% against results/sigma_profile_artifact/sigma_profiles.csv; the same call reproduces the deployed
% n=60 set (MSE 1.4723, B_insuff 0.6180, margin +0.2362) exactly. Expanded: n=115, MSE 1.2553,
% B_insuff 0.4545, margin +0.3463; gas-chromatography-only subset n=100, MSE 0.9250, B_insuff 0.5274,
% margin -0.1298. Provenance of the n=60 pairs: notebooks/data/raw/idac.csv DOI counts 33/20/7.
% Limitation (vi) scaffold geometry: Murcko scaffolds over the 147 solutes of the has_solubility
% test rows -> 124 scaffolds, 11 multi-member holding 34 solutes and 492 of 5608 rows (8.8%).
%
% ALL FOURTEEN PRINT HERE AS OF 2026-08-18.  Until this pass the article carried (i), (iv), (vi),
% (xii) and (xiv) as prose paragraphs and sent the reader to \S\ref{sec:si-limitations} for the
% other nine through a 194-word index that gave each one's location and kind.  The index is now the
% list: each of the nine keeps its numeral and states its own weakness in one line, and the
% Supporting Information keeps the detail that qualifies it (which two cells, which magnitudes,
% which of the probe's tests).  IF A LIMITATION MOVES BETWEEN THE TWO DOCUMENTS, its line moves with
% it and \S\ref{sec:si-limitations}'s preamble is edited in the same pass -- that preamble names
% which numerals sit where, and a list that disagrees with it sends the reader astray.
\paragraph{Limitations.} All fourteen, keeping the numerals used throughout;
\S\ref{sec:si-limitations} carries the qualifying detail for the nine it holds.
\begin{enumerate}[label=(\roman*),leftmargin=*,itemsep=1.5pt,topsep=3pt,parsep=0pt]
\item The phenomenon and its causal attribution lie in \emph{different regimes}. The paradox is
where the model operates (finite composition, all $\gamma$, $n{=}5608$). The attribution and the
latent-departure result rest on infinite-dilution activity data alone ($n{=}477$ across
temperatures, and the $298$\,K, low-$\gamma$, VT-2005-matched $n{=}60$ and $n{=}44$ sets). Transfer
back to the paradox's own regime is a conjecture and the principal open question here. The $n{=}60$
set is all gas chromatography and all from three publications, $33$ pairs from one, so a
method-stratified or source-clustered re-estimate lies beyond it. \emph{This set's} aggregate margin
inverts under a broader ThermoML pull where the broad set holds steady. It is a different object
from the glycol-ether stratum of limitation~(vi), which lives on the broad set and whose own
ThermoML test reproduces. Both sets sit outside the deployment regime
(\S\ref{sec:si-repro}). No set carries both quantities, and none is waiting to be assembled:
across the labelled corpus's $12{,}129$ solute--solvent pairs and the $3{,}145$ of the largest
infinite-dilution pull we hold, not one pair carries a solubility and a $\gamma^\infty$ alike, and
the $22$ solvents and $9$ solutes the two sides share still meet in no common pair.
\item What the convention rule costs the aggregate (\S\ref{sec:estimator}).
\item That other cells of the map keep their sign as well (\S\ref{sec:broad}).
\item $\Binsuf$ is upper-bounded rather than point-identified, the design holding no replicates at
fixed $\zstar$. $\Bclos$ is the error of a composite, the fixed map together with the calculation
that produced $\zstar$, and the measurements here leave it undivided between the two and its share
unbounded (\S\ref{sec:badclosure}).
\item The constant-offset bound's convention-specificity (\S\ref{sec:estimator}).
\item The stratification reads structure alone, so every cell was fixed before its outcome was seen.
Cells from different axes share rows and are therefore dependent evidence, counted once; granularity
is an analyst choice, which is why we report three solvent levels and two solute levels. The
per-cell bootstrap frequencies carry no multiplicity control, so one cell's $P$ describes that cell
alone, and the admissibility rule is a stability filter rather than a correction for the search.
Neither test of the search establishes a chemical effect: under the chemistry-blind relabelling null
the shape of the finding actually claimed sits at chance (\S\ref{sec:broad}), and the pre-declared
out-of-sample test on the PGL set returned \emph{not testable}. A second pre-declared test, on $95$
ThermoML rows the search never saw, does return a positive margin at about half the in-sample size
(\S\ref{sec:broad}). It rests on one source publication, so it settles the sign without meeting
this rule's own stability clause. We therefore do not advance that cell as a chemical result at all. It is reported as what the
instrument returned on its own rows, and
the same holds a fortiori for every cell the map reports and leaves unstated. With $\Binsuf$ bounded
from above alone, every cell is also silent on whether better inputs would help there, so the map's
one action is to withhold a chemistry from an allocation of grounding effort.
\item The accuracy ceiling standing a factor of five above the label-noise floor (\S\ref{sec:data}).
\item What the three-seed latent-departure result establishes and where it stops
(\S\ref{sec:surrogate}).
\item What bounds the black-box probe (the probe paragraph above).
\item What the closure-fidelity contrast leaves unmeasured, and what would settle it
(\S\ref{sec:notdecided}).
\item No accuracy difference between the arms is attributable to the bottleneck (above). Two axes
shape whether grounding helps, closure fidelity and how the latent is trained, and this work leaves
that plane uncharacterised on real systems. The chemical strata form a pattern in
the direction the sign rule would give, but separately tuned pipelines can produce one and a
multiplicity control is missing. \S\ref{sec:map} therefore records it as the experiment a matched
control should run, and the diagnostic is \emph{proposed} and not validated.
% REORDERED 2026-08-19.  The item opened on the THREE-SEED $0.20$ and reached the certified
% five-seed result in its last clause, so a reader skimming the limitations met the superseded
% number first and the measurement of record last.  Nothing is deleted: the three-seed pair is what
% bounds where a leak could have acted and the item cannot make its point without it.  Order only.
\item The scaffold-leak guard is a property of the released stream builder rather than a certified
property of the runs \S\ref{sec:paradox} reports. The re-run that \emph{is} certified leak-free
returns the supervision gain without a sign, so no reported number now rests on the uncertified
build. The superseded three-seed gain of $0.20$ was the one that did, and deleting the eight
reachable solutes left it at $+0.17$ $[+0.03,+0.31]$. The deletion bounds where a leak could have
acted, as do the arguments that bound the other contrasts (\S\ref{sec:data}).
\item What the deposit leaves uncheckable (the data-availability statement).
\item Two terms the thermodynamics asks for are set to zero in the deployed model, and both are
absorbed at training time by the same learned $\sigma$ head. The first is $\Delta C_p$ in the ideal
term, one-signed and worth a median $0.32$ in \lnx\ where a melting point is measured
(\S\ref{sec:framework}). The second is the Staverman--Guggenheim combinatorial term, off in every
arm the paradox is reported on. Both cancel in the evaluation-time substitution and both survive the
training that produced the drift of \S\ref{sec:surrogate}, so the mechanism there reaches beyond the
activity kernel.
\end{enumerate}

% CONCLUSIONS.  Two paragraphs, no numbers, no section references, and NOT a restatement of the
% abstract: the consequence and the design rule, touching a finding only to carry the "therefore".
%
% CUT FROM 451 WORDS TO 208, 2026-08-10, and the cut is of RESTATED FINDINGS, not of scope.  Five of
% the abstract's seven findings were being told again here, two of them in nearly the abstract's own
% words -- the supervision gain, the inference-time loss, the error split and its glycol-ether cell,
% the multiplicity search, the latent's departure -- which is what a conclusion is asked not to do.
% Four went; the fifth, the latent's departure, is the "therefore" this section exists to carry and
% stays.  The first paragraph's opening clause is the one other touch, and it is the premise the
% rest of the section is a consequence of.
% WHERE EACH CUT FINDING NOW LIVES, checked before it was cut, because a caveat travels with the
% sentence it qualifies and both have to survive somewhere: the supervision gain and its leak
% certification in \S\ref{sec:paradox}, limitation (xii) and the abstract; the inference-time loss
% at every seed in \S\ref{sec:paradox}, the Introduction's fifth paragraph and the abstract; the
% error split, its one class of solvent, the composite scope ("the model as deployed ... not its
% kernel alone") and the reading-not-a-test clause in \S\ref{sec:decomp}, \S\ref{sec:broad},
% \S\ref{sec:discussion} and the abstract; the search, the null it does not clear and the empty
% out-of-sample cell in \S\ref{sec:broad}, limitation (vi) and the abstract; and "a single figure
% for a whole data set is an average over that set's composition" in \S\ref{sec:broad}, which is
% where it is measured.  NOTHING WAS DROPPED THAT IS NOT PRINTED IN AT LEAST TWO OTHER PLACES.
% THREE SCOPE CLAUSES REMAIN AND MAY NOT GO.  They are not restatements and they are not decoration;
% each is the boundary of a sentence that is still here, and deleting one makes that sentence claim
% more than the body supports.  (1) "measured on runs of its own" -- without it the departure reads
% as measured on the checkpoints whose paradox it is offered to explain, which is the one thing
% \S\ref{sec:surrogate} says it is not.  (2) "wherever a reference existed to hold it against" --
% the departure is only observable where a tabulated value exists, and without the clause it becomes
% a claim about the latent everywhere.  (3) "it comes one-sided, so what the measurement returns is
% the chemistry on which better inputs are not the remedy" -- with the insufficiency side bounded
% from above only, no cell can ever return "better inputs would help here", and the design rule
% overclaims in exactly that direction without it.
\section{Conclusions}
\label{sec:conclusion}
% OPENING SENTENCE REWRITTEN 2026-08-12 by the second branch of the Sec. 2.2 pre-commitment: the
% five-seed re-run leaves the supervision gains straddling zero, so "they carried opposite signs"
% asserts a sign the re-run declines to give one of the two operations. The branch names this
% sentence, the heading of Sec. 3.1, the abstract, the supervision row of Table S2 and the
% table-of-contents graphic as the displays it rewrites.
% REWRITTEN 2026-08-19.  What this replaces named no claim and no number.  It opened on a metaphor
% ("The intermediate paid for the arrangement"), and a reader who stopped here could not say what
% the paper had established -- which is what a referee reported after reading the whole of it twice.
% The finding is stated FIRST and scoped SECOND, in the paragraph below, every item of which points
% at the section that owns it rather than re-arguing it here.
% DO NOT print the +0.41 / +0.11 pair.  Sec. 2.2 withholds the ordering of the two operations'
% magnitudes and the graphic for the table of contents is forbidden the same pair; a conclusion is
% not where the withholding lapses.  The signs are what carry, and they print.
Grounding a learned physical intermediate in reference values is two operations rather than one, and
here only one of them carried a sign. Substituting the reference profile for the learned one at
prediction time raised solubility error at every one of five leak-free seeds, and moved the ranking
of solvents for a solute against the practitioner at every one of them. Supervising the same head
against the same database during training moved the error the other way at four seeds of five. The
operation that failed is the one a practitioner reaches for, because it is the one that runs without
retraining. That asymmetry is what this work establishes.

% TWO PARAGRAPHS, per the 2026-08-19 supervisory review: the result, and what it generalises to.
% WHAT LEFT AND WHERE IT IS.  The caveat paragraph went in full -- every clause of it is stated in
% Sec. 3.7 beside its own measurement, which is where the review puts limitations, and "not
% testable", the separate tuning and the pKa protocol each print there.  The regime-bridge counts
% (12,129 and 3,145) appeared ONCE in the whole article, here, so they moved into limitation (i)
% rather than out of the manuscript.  The kernel-mobility concession stays: it is the article's own
% statement of what its negative result is not about.
A prediction generalises from this, rather than a design rule. Wherever a predictor composes a
fixed physical model over a learned intermediate for which an external reference exists, that
reference measures whether the fixed model itself limits accuracy. Where it answers, the answer
decides the remedy: a more faithful physical model, the calculation that supplies its reference
values included. The answer comes chemistry by chemistry rather than once for a field, and it comes
one-sided, returning the chemistry on which better inputs are not the remedy. Our fixed model is
COSMO-SAC at its $2002$ parameterisation, whose kernel cannot move, so a sign that turned on kernel
mobility would lie outside what these measurements reach \cite{yang2025tennet}. The experiment this
work leaves is to \emph{measure} $\gamma^\infty$ on pairs that already carry a solubility, because
the two literatures share no pair and no wider pull will supply one.


\section*{Author contributions}
\noindent
%% PROVISIONAL, for both authors to confirm before submission: the roles below are inferred from the
%% work as recorded and are not an agreed statement.
N.~L. Polomoshnov: Conceptualization, Methodology, Software, Formal analysis, Investigation,
Data curation, Visualization, Writing -- original draft.
A.~V. Rudik: Writing -- review \& editing.
%% CRediT vocabulary (use only these 14 names): Conceptualization; Data curation; Formal analysis;
%% Funding acquisition; Investigation; Methodology; Project administration; Resources; Software;
%% Supervision; Validation; Visualization; Writing -- original draft; Writing -- review \& editing.

\section*{Conflicts of interest}
\noindent There are no conflicts to declare.

\section*{Data and Software Availability}
The analysis code, and those intermediate artifacts small enough to version, are available at
\url{https://github.com/doctawho42/tgnn-solv} (commit
\pending[hash to be supplied at submission]) under the MIT licence, that commit being the one the
reported numbers were produced from. The model checkpoints, the processed split, the full per-arm
prediction files and the larger analysis artifacts will be deposited in a Zenodo archive,
\pending[whose DOI is registered at submission]. The data sources are public. Solubility is
BigSolDB~2.0 \cite{krasnov2025bigsoldb}; the reference $\sigma$-profiles are the VT-2005 database,
and the UD profiles Ref.~\cite{fingerhut2017} as redistributed with
Ref.~\cite{bell2020cosmosac}; the infinite-dilution activity coefficients are ThermoML, with the
PGL 6th-edition IDAC database behind Table~\ref{tab:closure-anchor}; the pKa data are the OPERA
compilation \cite{mansouri2019opera}. Four disclosed defects sit on the deposited
record, in \S\ref{sec:si-repro} and \S\ref{sec:si-tables}. The three-seed surrogate run's per-run
split provenance was not logged; one $\sigma$-stream build on disk was written against the wrong
split files; the two VT-2005-matched audit artifacts disagree on one out-of-fold estimate.
% THE FOURTH, disclosed 2026-08-19, and found by a digest assertion while packing a GPU run rather
% than by an audit.  The certified re-run records its stream by sha256 in every checkpoint
% manifest, and neither file is retained: the directory those paths name holds a DIFFERENT build
% (no validation split at all) and sigma_val.csv is absent.  Rebuilding with the recorded
% parameters reproduces the pool exactly -- the same 1319 molecules, profiles bit-equal -- and the
% split sizes exactly, but not the train/val ASSIGNMENT, which no retained file can settle.
% The evidence is results/stream_equivalence.json via scripts/kaggle/check_stream_equivalence.py.
% WHAT IT COSTS: sigma_val early-stops the warm-up, so a new arm trained on the rebuild is not on
% the identical schedule.  It does NOT touch any number already reported -- those arms' weights,
% per-row predictions and manifests are all deposited -- it bounds what can be ADDED to them.
The fourth is that the $\sigma$-stream those runs consumed is recorded by digest in every
checkpoint manifest and was not itself retained. Rebuilding it from the recorded parameters reproduces the pool exactly, the same $1319$ molecules
with bit-equal profiles, and the train/validation split sizes exactly, but no retained file settles
the assignment between them. That comparison is itself deposited, digest by digest, and
regenerates from the released code. Every number reported here was measured
on arms whose weights, predictions and manifests are deposited; what the loss bounds is what can be
added to them, since the validation half early-stops the $\sigma$ warm-up. Two further defects are
repaired rather than disclosed, each described where its numbers are printed. We re-scored the two
FastSolv retraining bundles (Tables~\ref{tab:baselines} and~\ref{tab:si-baselines-full}, with
the diagnosis, the withdrawn
values and the control that reproduces them in \S\ref{sec:si-baselines}), and the
withdrawn bundle is kept beside the re-scored one. And the truncated seed-$44$ $\sigma$-oracle
per-row file has been recovered complete and now stands in the deposit
(Table~\ref{tab:runs}, \S\ref{sec:si-repro}).
% LIMITATION (xiii) PRINTS ITS CONTENT HERE, 2026-08-10 (same referee item as (ix) and (x)).  This
% is the sentence that rests on it: a data-availability statement that lists what is deposited and
% not what a reader can therefore check is the half of the disclosure that flatters.  What stays in
% \S\ref{sec:si-limitations} is the numeral and the same words; what a reader needs is here, where
% the deposit is described.  Do not delete this on the ground that the SI carries (xiii).
\emph{The audit in one place.} Nine defects are disclosed in this submission, each described
where its numbers print, and they are of three kinds. Four are properties of the deposit and are
disclosed rather than repaired: an unlogged split provenance, a stream build written against the
wrong split files, two audit artifacts disagreeing on one out-of-fold estimate, and the
$\sigma$-stream that was not retained. Two were errors in this work's own numbers and are repaired,
with the withdrawn values kept beside the corrected ones: the twice-scaled temperature in the
FastSolv retraining bundles, and the truncated seed-$44$ per-row file, now recovered complete.
Three are cases where a check this work built refuted or weakened something this work had claimed:
the departure from the estimator declaration (\S\ref{sec:broad}), the linear probe whose verdict an
untrained encoder of the same architecture also earns, and the donor-window occupancy that an
untrained network reaches as fully as the trained one (\S\ref{sec:surrogate}). None of the nine is
reported by anyone but us, and each is stated where it bites rather than collected into a caveat.

What that deposit does and does not make checkable is limitation~(xiii). Several of the numbers
the verdict rests on are checkable by re-running the released code and by that route alone, and its
input artifacts are larger than the repository carries. We therefore deposit the two decomposition
sets as row-level
tables (Data Sets~S1 and~S2), so those sets and their margins can be recomputed with no code at all.
That leaves the provenance of the closure evaluations inside them, and of the trained arms, undelivered.

\begin{acknowledgement}
This research received no external funding.
Computations were carried out on commercial cloud services (Google Colab Pro+, Modal, and Google Cloud Platform) using NVIDIA T4 and L4 GPUs.
% CONDENSED 2026-08-19, and the commit-count sentence went for a reason beyond length. It read
% "the public history records it as a co-author on 377 of the repository's 449 commits" -- a claim
% about repository metadata, resting on a trailer convention that ACS's own policy contradicts,
% since a tool cannot be an author. What a disclosure owes a reader is the tool, the stages, and
% whether any reported quantity came from it; that is what is left, and none of it depends on how
% the version-control history is written.
% THE AUTHORS MUST STILL CONFIRM THIS AND ADD ANY TOOL USED OUTSIDE THIS WORK before submission.
% It no longer prints a red \pending because two already stand a paragraph above -- the commit
% hash and the Zenodo DOI -- and those force the same final pass.
Generative-AI use: the authors used Anthropic's Claude for analysis and verification code, for
the figures that code generates, and for drafting and editing. It generated and altered no
measurement: every number in both documents is produced by a deposited script from a deposited
artifact, and the checks of \S\ref{sec:si-repro} bind them there. The authors are responsible for
all of the content.
% The disclosure ACS requires names the tools and the stages they were used at, and that is author
% knowledge, not manuscript content -- it cannot be written here without being invented.  It is now
% a \pending so that it prints in red on every draft instead of sitting in a comment: item 5 of the
% pre-submission checklist above, and the last of the five to be settled.
\end{acknowledgement}

% The Supporting Information is a separate document, grounding_paradox_si.tex. This paragraph is what
% ACS prints as the "Supporting Information Available" note, and it is also where this article tells
% its reader how to read an S-number, since every pointer from here into that document resolves to one.
\begin{suppinfo}
The Supporting Information is a separate document. Its sections, tables, figures and equations are
numbered S1, S2, \ldots, and every reference above that carries an S is to it. It contains
supplementary methods; the proofs of Lemma~\ref{prop:decomp} and Remark~\ref{prop:tax} and
three further results; per-arm tables and decomposition robustness; the map's
$59$ strata in full, including the $44$ the estimator cannot bound. It also contains further external ablations;
supplementary mechanism results; supplementary diagnostics and broad negatives; the second-domain
pKa construction; data provenance and reproducibility; the black-box probe tables; the synthetic
closure-fidelity family; and the positioning against adjacent literatures. We deposit two machine-readable
tables with it: the $477$-row broad IDAC set (Data Set~S1) and the $60$-row
VT-2005-matched set (Data Set~S2), and their columns are listed in \S\ref{sec:si-repro}.
\end{suppinfo}

% DO NOT \input sections/SI here. It is the SI document's body now, it declares S-numbered
% floats, and pulling it back in would restore the reading-order defect (the reference list
% printing forty pages after the text that cites it) and give this document a second,
% conflicting set of table and figure numbers.

% achemso selects achemso.bst automatically; no \bibliographystyle is given.
\bibliography{references_verified}

\end{document}
