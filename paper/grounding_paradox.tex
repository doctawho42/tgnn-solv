% =====================================================================
% THE ARTICLE.  The Supporting Information is a separate document,
% grounding_paradox_si.tex; both \input preamble.tex and neither \inputs the other.
%
% SECTION SHAPE (2026-08-02, referee request): four sections only --
%   1 Introduction            field, the assumption under test, what was done and found
%   2 Computational methods   the composed predictor, the error split and its estimator,
%                             the data, the model, the evaluation protocol
%   3 Results and discussion  the two senses of grounding; the split on the activity data;
%                             the latent's departure; the second closure; what it licenses;
%                             limitations
%   4 Conclusions
% There is no separate theory section and no separate related-work section: JCIM carries
% neither, and the material is inside 2 and 1 respectively.
%
% CROSS-REFERENCES: one form.  Every section reference is \S\ref{...}.  An S in the
% resolved number means the Supporting Information (achemso's suppinfo mode numbers it
% S1, S2, ...), so no prefix, sigil or "App." is needed at any call site.  Do not
% reintroduce "Sec.", "App.", "Appendix", a spelled-out "Section N", or \pageref.
%
% GRADES: there is no grading vocabulary in this manuscript.  Every claim carries its
% uncertainty in the sentence that makes it -- a value, its interval or its per-seed
% spread, its n, and the set it holds on -- and a claim the data do not resolve says so
% in words.  Table 2 is the claim ledger; its value cells carry what the retired Grade
% column carried.  Do not reintroduce "certified", "likely", "open", "no claim" or
% "retired" as labels.
%
% NUMBER PROVENANCE: Fig. 2 / chemistry-map numbers regenerate from
% results/e5_sigma_grounding/ via scripts/analysis/run_e5_comparison.py
% (3-seed aggregate: results/e5_sigma_grounding/THREE_SEED_SUMMARY.md).
% Decomposition numbers regenerate from scripts/analysis/run_b_insuff_decomposition.py
% and run_b_insuff_representative.py.
%
% LAYOUT.  `layout=twocolumn' is the published-article measure and is what the page
% counts refer to.  ACS expects the double-spaced single-column measure at submission:
% drop `,layout=twocolumn' in BOTH files for that.  Re-measure page counts from the
% build, never from a comment.
% =====================================================================
\documentclass[journal=jcisd8,manuscript=article,layout=twocolumn]{achemso}
% The preamble proper -- packages, macros, theorem environments, float tuning, the
% amsthm/achemso patches and the line-breaking parameters -- is shared verbatim with
% the Supporting Information and lives in preamble.tex.
\input{preamble}
% The other half of the submission. xr-hyper (loaded in preamble.tex) reads
% grounding_paradox_si.aux, so every \ref{} in this file that names a label defined
% in the Supporting Information resolves to its S-number without any change at the
% call site. The first optional argument, the label prefix, is empty because the two
% documents' label names are disjoint; that is checked by
% scripts/analysis/check_split_refs.py, which fails if a name is defined in both.
% THE SECOND OPTIONAL ARGUMENT IS LOAD-BEARING. Without `nocite' xr-hyper also
% imports the other document's \bibcite lines, and since the two bibliographies
% number the same work differently every key cited in both is reported "multiply
% defined". `nocite' stops the import: each document numbers its citations only from
% its own bibliography.
\externaldocument[][nocite]{grounding_paradox_si}
% TITLE, 2026-08-02.  Previous title: "To Thine Own $\sigma$ Be False: Bounding Closure
% Misspecification against Input Insufficiency on the Activity Axis When Reference
% $\sigma$-Profiles Hurt COSMO-SAC Solubility Prediction" (24 words, and its head was a
% pun rather than a statement).  The title below is declarative, is 13 words, and names
% the two things measured -- the substitution and the separation -- rather than the
% phenomenon's nickname.  Keep it identical to the \title in grounding_paradox_si.tex.
\title{Substituting Reference $\sigma$-Profiles Degrades Solubility Prediction and
Separates COSMO-SAC Error from Input Error}

% NOTE: achemso re-typesets the arguments of \affiliation and \email, so anything outstanding here is
% written as a plain comment rather than through \pending.
% ===================== PRE-SUBMISSION CHECKLIST =====================
% Five items are outstanding. None can be settled from the manuscript; all are author input.
%   1. ORCID for A. V. Rudik. N. L. Polomoshnov is 0009-0001-4342-8539. ORCIDs are entered in the
%      ACS submission form, not typeset here, so this blocks the form and not the PDF.
%   2. Street address and postcode of the Faculty of Bioengineering and Bioinformatics, MSU.
%   3. Street address and postcode of the V. N. Orekhovich Institute of Biomedical Chemistry.
%   4. Whether A. V. Rudik also corresponds; if so, add a second \email under her \author block.
%   5. The AI-use disclosure, removed from the acknowledgement at the author's request and to be
%      rewritten. ACS asks contributors to declare generative-AI use and the manuscript now carries
%      none, so this is the one outstanding item a reviewer would notice.
% CONFIRMED, do not re-flag: the Author contributions block is correct as printed.
% Two more, of a different kind, are in the Data availability statement and are handled by \pending:
% the archive commit hash and the Zenodo DOI.
% ==================================================================================
\author{N.~L. Polomoshnov}
\affiliation[MSU]{Faculty of Bioengineering and Bioinformatics, Lomonosov Moscow State
University, Moscow, Russia}
\alsoaffiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}
\email{nikitapol@fbb.msu.ru}
\author{A.~V. Rudik}
\affiliation[IBMC]{V.~N. Orekhovich Institute of Biomedical Chemistry, Moscow, Russia}

\abbreviations{SLE, solid--liquid equilibrium; IDAC, infinite-dilution activity
coefficient; LFER, linear free-energy relationship; LOTV, law of total variance;
UD, University of Delaware (the successor $\sigma$-profile database)}
\keywords{solubility prediction, physics-informed machine learning, COSMO-SAC,
model misspecification, representation probing}

\begin{document}

% ACS requires a table-of-contents graphic, and it is the one image the journal places beside
% the article, so it may only carry a claim this paper still makes. It used to print the
% aggregate verdict ("On 477 activity measurements ... margin +0.51"), which Sec. 3.2.1
% retires; it now prints the two senses of grounding and their opposite signs, which is the
% title's own statement. Regenerate with scripts/analysis/make_toc_figure.py, whose docstring
% records what was removed and why. Do not restore an aggregate here.
% SIZE. achemso puts the graphic in a \vbox to 1.75in inside a 3.25in minipage. Bounding
% the height as well as the width lets the aspect ratio pick whichever limit binds.
% Do not go back to a width-only \includegraphics here.
\begin{tocentry}
\includegraphics[width=\linewidth,height=1.7in,keepaspectratio]{figs/fig_toc.pdf}
\end{tocentry}

\maketitle
% ABSTRACT: 250 words is the ceiling and this one is at it.  Every assertion is scoped by
% the set it was measured on rather than hedged with a modality.  No grade words, and
% none may come back: if a sentence here needs one, the sentence is claiming more than
% the body supports and the sentence is what changes.
\begin{abstract}
Physics-informed models route predictions through interpretable physical quantities,
expecting that accurate reference values for them improve accuracy. We measure
that for solid--liquid solubility. A graph neural network predicts a molecule's
charge-density profile, the fixed thermodynamic model COSMO-SAC turns a pair of profiles
into an activity coefficient and hence a solubility, and a quantum-chemical database
tabulates the same profiles. Grounding names two opposite operations on it, with opposite
signs: supervising the predicted profile against it during training improves
solubility accuracy over three seeds by $0.20$ in mean absolute error; substituting the
reference for the learned profile at inference degrades it by $0.41$. It is also an instrument: on
infinite-dilution activity data it bounds the model's own error from below and its inputs'
from above. The bound on the inputs barely moves between
chemical strata while the total error differs across them by more than an order of
magnitude, so the comparison is a map over $59$ strata, not one verdict. Under one
admissibility rule applied alike to every stratum, fifteen are large enough to bound and six
keep their margin's sign under every publication deletion; those six are three
row sets, and on one---the glycol-ether solvents, $182$ measurements over $43$ molecule
pairs---the model's error is the larger term, by $+2.04$ ($90\%$ interval
$[+1.27,+2.83]$). The map reports the rest without establishing it. Trained through the
misspecified model, the learned profile departs from its reference along a shared direction.
Grounded latents are not reliably interpretable when the model consuming them is
misspecified.
\end{abstract}

% INTRODUCTION.  Field, then the assumption under test, then what was done and found.
% The field review is folded in here (JCIM carries no standalone related-work section) and
% describes the present work only in the paragraph beginning "What this literature lacks".
% There is deliberately NO roadmap paragraph: with four sections it narrates the document
% rather than the science.  Do not restore one.
\section{Introduction}
\label{sec:intro}
How much of a solid dissolves in a given liquid decides whether a drug can be formulated as
a tablet or an injection, which solvent a crystallisation is run in, and how a product is
purified. It has to be measured one solute, one solvent and one temperature at a time, so
most of the combinations that matter industrially have never been determined, and
independent determinations of the same system disagree by enough to put a floor under what
any predictor can achieve. Dissolving a solid also trades the cost of breaking up its
crystal against the cost of mixing the freed molecules into the solvent, so a predictor has
to be right about two unrelated kinds of chemistry at once, on molecules unlike any it was
shown.

Predictors divide by how much physical structure they impose. Direct models regress
solubility from descriptors or from a representation learned off the molecular graph;
FastSolv \cite{osanloo2024} does so with a deep neural network and leads on extrapolation
to new solutes. They are the more accurate there and they are opaque: the prediction
arrives as a single number, with no account of which part of the chemistry it captured.
Physics-informed models instead predict the thermodynamic quantities the physical picture
names---an activity coefficient for the solute--solvent interaction, a melting temperature
and heat of fusion for the crystal---and combine them through a fixed equation, as SolProp
\cite{vermeire2022solprop} does with a thermodynamic cycle of separately learned
components. What that buys is decomposability: each part is a quantity chemists tabulate on
its own, so it can be supervised against reference data a direct regressor has no way to
use. On accuracy the black box leads and the physics-informed cycle trails it.

The design rests on an assumption rarely stated because it is taken for self-evident: a
model supplied with physical inputs closer to their true values predicts better. For one
class of intermediate that can be tested outright, because a reference value is tabulated
and can be handed to the model in place of the one it computed. It needs testing, because
when a predictor of this shape is inaccurate either the fixed physical model is wrong or
the learned quantities feeding it are uninformative, and the two call for opposite
remedies: a more faithful model in the first case, better representations or more data in
the second.

Learned surrogates for the activity coefficient have advanced quickly. Winter et al.\
\cite{winter2022} predict infinite-dilution activity coefficients \lngi\ directly from
SMILES with a language model, and Sanchez-Medina et al.\ \cite{sanchezmedina2023} embed a
Gibbs--Helmholtz constraint in a graph network for their temperature dependence; both make
the activity coefficient the regression target rather than retaining a mechanistic model
that consumes a predicted molecular property. A second line keeps the mechanistic model and
learns its input. COSMO-SAC \cite{lin2002} is a segment-activity reformulation of Klamt's
COSMO-RS, and the $\sigma$-profile it consumes---screening-charge density over a molecule's
cavity---is that framework's construct, tabulated from quantum chemistry \cite{mullins2006}.
Predicting it from structure removes that calculation from the pipeline; TeNNet-SAC
\cite{yang2025tennet} does so while also learning the segment-activity kernel, and gains
from truer profiles.

When a fixed physical structure improves or degrades a learned model is the broader question
of physics-informed machine learning \cite{karniadakis2021}, whose dominant forms impose
structure as a differentiable object: a governing equation enforced through the loss
\cite{raissi2019}, gray-box hybrids composing learned components with fixed mechanistic maps
\cite{willard2022}, and operator learning with a trainable correction to a misspecified
equation \cite{ma2026}. The prevailing expectation is that more physics improves
generalisation, yet the same structure has documented failure modes in which it makes
optimisation or accuracy worse \cite{krishnapriyan2021}. In neural PDE solvers, Mohan et
al.\ \cite{mohan2024} show that a learnable term added to a fixed discretised operator
absorbs truncation artifacts rather than physics, so the apparent interpretability is
illusory---though their error source is numerical and has no external reference. The same
concern appears in concept-bottleneck models as \emph{concept leakage}, a learned concept
silently encoding information beyond its nominal meaning
\cite{mahinpei2021,margeloiu2021,havasi2022}. Recent work traces one route to leakage to the
downstream model: a misspecified head forces the encoder to deform the concept into carrying
a dependence the head cannot represent \cite{parisini2025}, and overwriting a frozen head's
leaked concepts with ground truth can then degrade accuracy \cite{espinosa2025leakage}. The
gap between a fixed physical map and reality is the model-discrepancy problem of Kennedy and
O'Hagan \cite{kennedy2001} and its calibration critique \cite{brynjarsdottir2014}. Four
further literatures bear on it---forecast decomposition, quasi-maximum likelihood,
identifiability, and the validity of composed pipelines---and are reviewed in
\S\ref{sec:si-positioning}.

What this literature lacks is a way to \emph{measure}, rather than diagnose, whether a fixed
physical model or its learned inputs limits a composed predictor's accuracy, and whether the
learned intermediate has stopped reporting the quantity it is named after; in the
concept-bottleneck case no external reference exists against which either question could be
settled. This work supplies one---a reference $\sigma$-profile for solubility, tabulated
substituent constants for a second chemical domain---and holds the mechanistic kernel fixed,
so the tabulated value stays available as an external reference for the learned one.

Here a graph neural network predicts the pair of charge-density distributions COSMO-SAC
consumes, in place of the quantum-chemical calculation that ordinarily supplies them, which
leaves the network's own intermediate with a tabulated counterpart to be held against. That
counterpart can enter the pipeline at either of two points: as a target the network is
trained toward, or as a replacement for what the network computed at the moment of
prediction (Fig.~\ref{fig:overview}). The thermodynamic model is implemented in
differentiable form so that both routes are open on a single trained network, and the
tabulated values are put to a third use, dividing the fixed model's error between the map
and the inputs handed to it.

Only one of the two routes helps, and the reason is a property of the fixed map. Solubility
data cannot say which component is at fault, because one measurement constrains the pair
jointly; activity coefficients can, because both molecules carry a tabulated distribution
and the two error terms separate. Where they separate, an ordering decides the remedy: a map
whose own error stands above what its inputs fail to carry does not become more accurate
when handed a better input, but more exposed, the accurate input no longer masking the bias
the map introduces. That ordering is a property of the chemistry the map is asked about, so
the instrument returns a map over chemical strata rather than a verdict, and one rule
applied alike to every stratum decides what the map establishes. Two consequences follow. A
learned intermediate trained through a misspecified map cannot be read as the physical
quantity it is named after: the predicted distribution drifts away from the tabulated one,
along a direction shared across molecules. And substituting a reference value for it at
prediction time is not a safe improvement but a change of regime, which is why the
substitution here costs more than the supervision gained. Neither consequence is peculiar to
charge distributions: tabulated substituent constants behave the same way inside a fixed
Hammett relation for acid dissociation, improving the aromatic series that relation was
built for and degrading the ones outside it.

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_overview.pdf}
% CAPTION BUDGET 72 words, counting the "Overview." lead-in and the (a)/(b) markers.
% The budget rose 60 -> 66 -> 72, each step a disclosure panel (b) forced: three solvent
% classes are drawn and only one is established, and THE TWO MARKS SAY DIFFERENT THINGS.
% Water (dagger) loses its sign when one publication is deleted; the acceptor-only aprotics
% (double dagger) survive every deletion in all four cells and are not established because
% the margin is not positive and the bound is one-sided.  One dagger glossed as laboratory
% fragility carried both classes and was FALSE of the second -- do not restore it, and do
% not buy the six words back by deleting "each mark carrying its own reason".  No third
% kind of growth.  This float sets early, so every term in it must be defined by then:
% keep it in plain English, and no section reference.
\caption{\textbf{Overview.} \textbf{(a)}~A learned encoder predicts a charge-density profile
$\hat\sigma$ that a fixed thermodynamic model turns into a solubility; the arrow marks the
evaluation-time substitution of the reference $\sigma^\star$, which makes it
worse. \textbf{(b)}~The counterpart error on infinite-dilution activity data, by solvent class; only
the unmarked class is established, each mark carrying its own reason, and each block is a one-sided
bound rather than an interval. Training-time supervision is not drawn.}
\label{fig:overview}
\end{figure*}

\input{sections/methods}

\section{Results and discussion}
\label{sec:results}
Cluster-bootstrap frequencies $P$ are quoted as bands---rounded to one decimal, with
$>0.95$ and $<0.2$ closing the ends---because with $17$ solvent and $41$ solute clusters on
the VT-2005-matched set they do not carry two-decimal resolution (\S\ref{sec:si-tables});
the two-decimal values are in the deposited artifacts. Table~\ref{tab:claims} collects the
load-bearing claims with the set each was measured on, its size, and the value with its
uncertainty and scope.

% ---------------------------------------------------------------------------
% Claim ledger: one row per load-bearing claim, so that data set, n, estimator and value
% are legible without reconstructing them from the prose.  The Grade column was retired on
% 2026-08-02 and what it carried is now inside the value cell of each row, in words: a
% per-seed spread, an interval, an n, "one seed", "the interval spans zero", "not
% separable".  Do not reintroduce the column or its vocabulary.
% `p' is in the specifier because this table sits within a few lines of the top-float
% height limit: without it, one extra wrapped row makes it unplaceable as a top float and
% it is deferred past the Discussion, taking every later starred float with it.
% ---------------------------------------------------------------------------
\begin{table*}[tp]
\centering
\caption{One row per load-bearing claim: what is claimed, the data set and its size, and
the value measured with its uncertainty and its scope. Notes \emph{a}--\emph{k} qualify a
row or a column.}
\label{tab:claims}
{\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{0.86}
\begin{tabular}{@{}L{0.27\linewidth}L{0.19\linewidth}L{0.46\linewidth}@{}}
\toprule
Claim & Set ($n$)\tnk{a} & Value, with its uncertainty and scope\tnk{b} \\
\midrule
\multicolumn{3}{@{}l@{}}{\emph{The two senses of grounding} (\S\ref{sec:paradox})}\\
Reference profiles hurt solubility at evaluation (MAE) & test split ($5608$), 3 seeds & $1.85\to2.25$; the per-seed values do not overlap \\
\ldots and still hurt when injected at training & cross-arm rows\tnk{c}, seed $42$ & $1.80\to1.98$ (own baseline $1.80\to2.23$); one seed, so a sign and not an effect size \\
\ldots so part of that gap is test-time distribution shift & cross-arm rows\tnk{c}, seed $42$ & share $0.59$ on one seed: a description of that seed, not a magnitude \\
Training-time $\sigma$ supervision \emph{helps} (MAE) & cross-arm rows\tnk{c}, 3 seeds & $2.04\to1.85$; the per-seed values do not overlap \\
\addlinespace[2pt]
\multicolumn{3}{@{}l@{}}{\emph{Closure against inputs on the activity axis} (\S\ref{sec:measure})}\\
$\Bclos>\Binsuf$ on \emph{glycol-ether} solvents\tnk{d,i,k} & broad IDAC, $182$, $3$ sources & $+2.04$ $[+1.27,+2.83]$ to $+2.94$, $P>0.95$ in all four cells; worst deletion $+1.60$ \\
\ldots\ on every other stratum of the map & broad IDAC, $59$ strata & Tables~\ref{tab:map}--\ref{tab:map-b} and \S\ref{sec:si-map}, each with its margin, interval and the test it fails; none is stated as a finding\tnk{k} \\
\ldots\ for either set \emph{as a whole} & broad IDAC ($477$); VT-2005-matched ($60$) & $+0.51$ (full $+0.95$); $-0.38$ compounding two cuts ($+0.01$ full); $+0.18$ less one publication; $+0.05$ on the smaller set: an average weighted by composition\tnk{j} \\
$\Binsuf^{\mathrm{up}}$ near stratum-independent, $\mathrm{MSE}$ not & broad IDAC, $3$ boundable classes; $6$ with an estimate & $\times2.0$ against $\times25$; $\times6.8$ against $\times51$; $p{=}0.02$ against $2000$ null partitions of $9$ classes \\
$\Bclos>\Binsuf$, 2010 on its \emph{own} typed latent\tnk{j} & broad IDAC ($477$); VT-2005-matched ($60$) & $+1.29$, $P>0.95/{\approx}0.9/{\approx}0.9$\tnk{e}; $-0.77$ at $P\approx0.3$ on the smaller set, where the ordering is lost: loss of resolution, not a reversal \\
\addlinespace[2pt]
\multicolumn{3}{@{}l@{}}{\emph{Whether a better closure moves the ceiling} (\S\ref{sec:fidelity})}\\
The $2002\to2010$ step lowers the error & broad IDAC ($477$) & $-0.99$ at $P\approx0.2$\tnk{f}: no improvement, and not a measured equality either \\
\ldots but does on the VT-2005 set's chemistry\tnk{g} & $57$ shared pairs, UD & $1.78\to0.77$, $P>0.95$; those $57$ pairs are also broad-set rows, so not an independent sample \\
A fitted kernel residual transfers across solvents & VT-2005-matched ($60$), grouped folds & $-5\pm16\%$: the interval spans zero, and excludes a cross-solvent reduction above about $30\%$ \\
\addlinespace[2pt]
\multicolumn{3}{@{}l@{}}{\emph{Mechanism} (\S\ref{sec:surrogate})}\\
The latent departs from its reference along a shared direction & matched molecules ($44$), 3 seeds & $51\pm2\%$; the magnitude is not stable across analysis configurations \\
\ldots and that departure's top-two mode share exceeds a phase-randomised null & one grounded checkpoint, same $44$ & $0.40$ against $0.22\pm0.01$\tnk{h}; one checkpoint, and a wrong-molecule null is not cleared \\
\ldots and that direction \emph{transfers} to molecules outside the fit & matched molecules ($44$), 3 seeds & $4.1\pm0.5\times$ a zero-drift null, with no constant-offset control on these seeds; the $\pm$ spans seeds at one partition \\
That departure \emph{cancels} closure error & not measured & the one direct check is under-powered and runs against it \\
\addlinespace[2pt]
\multicolumn{3}{@{}l@{}}{\emph{Accuracy} (\S\ref{sec:discussion})}\\
Physics arm vs.\ DirectGNN accuracy ($\Delta$MAE) & test split ($5608$) & $+0.14$ between separately tuned pipelines, so not attributable to the thermodynamic bottleneck \\
\bottomrule
\end{tabular}
\tabnotes{\scriptsize%
\tnk{a}\,\emph{Broad IDAC}, the $477$ IDAC\,$\cap$\,UD measurements over $185$ distinct
pairs at all temperatures; \emph{VT-2005-matched}, the $n{=}60$ pairs at $298$\,K carrying a
VT-2005 profile; UD, the University of Delaware $\sigma$-profile database. Resolution
$\dim(\zstar,T)/n$ per set: \S\ref{sec:broad}--\S\ref{sec:vt2005}.\quad
\tnk{b}\,\emph{Margin} is $\mathrm{MSE}-2\Binsuf^{\mathrm{up}}$, positive when $\Bclos$ exceeds
$\Binsuf$; $P$ is a two-way (solute\,$\times$\,solvent) cluster-bootstrap frequency, reported as a
band, by the rounding rule at the head of this section.\quad
\tnk{c}\,The cross-arm row set common to every arm of Table~\ref{tab:si-arms}.\quad
\tnk{d}\,LOTV rows use eight equal-count bins, the unbiased within-bin variance, and the
\emph{deployed residual-only} convention on both sets and both kernels
(\S\ref{sec:estimator}).\quad
\tnk{e}\,Bootstrap band over pair\,/\,two-way\,/\,source clusters.\quad
\tnk{f}\,Deployed convention, dispersion off; under the full convention $-0.07$, $P\approx0.6$,
and $+0.06$, $P\approx0.6$ with dispersion on (Table~\ref{tab:kernels}).\quad
\tnk{g}\,At fixed profile database and temperature, full convention.\quad
\tnk{h}\,$\pm$ is the null's spread; the fine-tuning drift has no matched null of its own.\quad
\tnk{i}\,One estimator cell for every stratum, under a rule reading molecular structure only
(\S\ref{sec:estimator}). $\Binsuf$ is bounded above and $\Bclos$ below, so a positive margin
establishes that the closure's own error exceeds what input insufficiency can account for; a
non-positive one is failure to separate and licenses nothing, a reversal needing a \emph{lower}
bound on $\Binsuf$ this instrument cannot give.\quad
\tnk{j}\,An aggregate margin is a composition-weighted average, and the deletions and cuts
recorded here are failures of the headline cell rather than robustness passes
(\S\ref{sec:broad}). These rows record what the aggregate was and how it fails; the rows
above replace it.\quad
\tnk{k}\,\emph{Admissible} (\S\ref{sec:estimator}, applied alike to every stratum): boundable at the
fixed cell \emph{and} keeping the margin's sign when each contributing publication is deleted, in all
four unit\,$\times$\,convention cells. Of fifty-nine strata, fifteen are boundable and six
admissible, of which one row set is also positive and not contained in another---the glycol ethers,
the one row set stated as a finding. Every boundable stratum is in Tables~\ref{tab:map}--\ref{tab:map-b} and the rest
in \S\ref{sec:si-map}, each stating what it shows or the test it fails.}}
\end{table*}

\subsection{The two senses of grounding}
\label{sec:paradox}
% Artifacts: results/e5_sigma_grounding/seed_{42,43,44}/comparison.json (3-seed e5 lock, n=5608); aggregate THREE_SEED_SUMMARY.md via run_e5_comparison.py.
The word ``grounding'' names two opposite operations on the same VT-2005 database.
Supervising the $\sigma$ head against the database \emph{during training} improves
solubility MAE from $2.043\pm0.040$ (ungrounded) to $1.846\pm0.053$ (grounded), a gain of
$0.20$ over three seeds (Table~\ref{tab:si-arms}). Substituting the reference profile for
the learned one \emph{at inference} degrades it, and that substitution alone is the effect
this paper is named for. It erases any positive $R^2$ (Fig.~\ref{fig:paradox}): the model
predicts solubility better \emph{from its own learned $\sigma$-profile} than when
\emph{given the external reference one} (MAE $1.85$ vs $2.25$, in $\lnx$ units). Of the
figure's eight arms two are non-COSMO controls, DirectGNN and a fitted NRTL closure; five
COSMO-SAC arms differ only in how the $\sigma$ head is trained and in what it is fed, and the
sixth (\emph{grounded\,$+$comb.}) differs instead in the closure, restoring the
Staverman--Guggenheim combinatorial term. \emph{Ref.\ $\sigma$ (eval)} is the grounded
checkpoint re-evaluated with the reference profile substituted at test time, \emph{ref.\
$\sigma$ (train)} the co-adaptation control injecting it during training instead; per-arm
settings are in \S\ref{sec:si-tables}. The physics arms trail DirectGNN, tuned separately
from them, so that gap is not attributable to the bottleneck (\S\ref{sec:eval}).

The headline $+0.41$ is the evaluation-only substitution, and part of it may be unrelated
to the closure: swapping an input distribution at test time degrades a pipeline on its own.
The control that removes that component injects the reference profile \emph{at training}, so
the model co-adapts; at seed $42$ it costs $+0.18$ ($1.803\to1.981$) where the
evaluation-only substitution costs $+0.43$ at that same seed ($1.803\to2.232$). One seed
does not establish it, but the direction points to part of the headline gap going with
co-adaptation; the co-adapted arm is by design the confound-free one. A channel-swap control
costs $+0.27$, to $2.08$. Both are worse than the learned-$\sigma$ arm, which rules out an
implementation artifact, and both are single-seed, so they fix a sign and not an effect size,
as does the $0.59$ share they imply. Only the three-seed arms can be set against one another,
and there the substitution costs about twice what supervision gains ($+0.41$ against
$-0.20$); the co-adapted arm's $+0.18$ cannot enter that comparison. The between-seed
arithmetic that excludes it, and the row set the three oracle controls are computed on, are
in \S\ref{sec:si-tables}.

Which of the two causes the substitution fails from is not decided here. Drug-like solutes
are essentially absent from the VT-2005 set, so the substitution falls almost entirely on the
\emph{solvent} channel, and the both-channel substitution the attribution would need covers
only ${\approx}7$ solutes---too few either way, and enlarging that set would mean computing
new reference profiles rather than looking them up. The causal claim is therefore carried on
the activity axis, where the reference input is clean and matched on both channels: the
decomposition (\S\ref{sec:measure}) and the latent's departure from its reference ($n{=}44$,
\S\ref{sec:surrogate}). There the fixed reference-input closure sits well above the bound on
what its inputs can fail to resolve, under-predicting $\lngi$ with a systematic bias
(Fig.~\ref{fig:parity}).

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_paradox.pdf}
% Caption: what is shown, and nothing else.  The two caveats that used to close it -- that
% the sigma-oracle bar swaps the input at evaluation only, and that DirectGNN was tuned
% separately -- are both in the body within a paragraph of this float.  Do not restore them.
\caption{Controlled comparison on the scaffold split ($n{=}5608$). Solid bars: three seeds,
mean $\pm$ a population standard deviation (\texttt{ddof}$=0$). The two hatched bars are the
co-adaptation controls, seed $42$ only, so they carry no error bar and no $R^2$, and keep an empty
slot in the right panel. Dashed in the left panel only, the DirectGNN reference.}
\label{fig:paradox}
\end{figure*}

\subsection{Closure against inputs on the activity axis}
\label{sec:measure}
% Artifacts. VT-2005-matched set (n=60): results/b_insuff/decomposition.json via
% run_b_insuff_decomposition.py; bin-count / Bessel / convention grid with per-cell bootstrap:
% results/b_insuff/estimator_grid.json; blocked-fold and distinct-pair estimators and the deployed
% adaptive-bin bootstrap: results/b_insuff/keystone_robustness.json; design composition:
% results/b_insuff/matched_pairs.csv and leverage_robustness.json.
% Broad IDAC set (n=477): results/b_insuff/representative_decomposition.json via
% run_b_insuff_representative.py; kernel MSEs cross-checked against fidelity_lever_fair2002.json;
% round-2 design/provenance audit: results/b_insuff/representative_audit.json.
The externally-referenced decomposition of \S\ref{sec:decomp} separates a fixed closure's
error into a part in the map and a part in its inputs. Its output is a map over chemical
strata rather than one verdict for a set, for the reason \S\ref{sec:broad} establishes. It
runs on two data sets that must not be conflated---the \emph{broad IDAC set} ($n{=}477$, UD
profiles, all temperatures) and the \emph{VT-2005-matched set} ($n{=}60$ at $298$\,K, where
the paradox's own database supplies both profiles)---and then asks whether a better closure
moves the ceiling.

\subsubsection{The broad IDAC set ($n{=}477$)}
\label{sec:broad}
The closure reads a temperature as well as a profile pair and is evaluated at each row's own
temperature, so the conditioning variable is its full argument $(\zstar,T)$---the
concatenated UD profile pair with $T$, $103$ dimensions against $n{=}477$
($\dim/n\approx0.22$)---as \S\ref{sec:decomp} requires, and the target $m$ is the
experimental $\lngi$, so floor and closure error are measured on one data set and one
profile database. Its chemistry is small: no solute exceeds $12$ heavy atoms (median $7$),
against a median of $14$ and a maximum of $122$ for the drug-like solids of this paper's own
solubility corpus, and only $0.46\%$ of corpus rows carry a solute from this set.
\emph{Broad} therefore names the reach of the $\gamma^\infty$ corpus the kernel is
calibrated and benchmarked on and not coverage of the chemistry a solubility model is
deployed against: it covers the solvent side of deployment and not the solute side. It is
matched to UD by InChIKey with a first-block fallback, a looser rule than the VT-2005 set's;
a wrong $\zstar$ inflates $\Bclos$, so the looser rule runs against the ordering we claim,
and the aggregate margin is unchanged on the $465$ exactly matched rows
(\S\ref{sec:si-repro}).

Under the convention rule of \S\ref{sec:estimator} the fair 2002 arm scores
$\mathrm{MSE}=1.902$ against $\Var(m)=4.85$, the LOTV bound gives $\Binsuf\lesssim0.70$ and
hence $\Bclos\gtrsim1.21$: a separation margin of $+0.51$ for the set taken whole, $+0.95$
under the full convention and $+0.27$ collapsed to one row per distinct pair, positive in
every cell of the bin-count\,$\times$\,variance\,$\times$\,convention grid, with two of the
three bootstrap clusterings extending just below zero. Both sweeps, and the
tighter-of-the-two-conventions reading we do not headline, are in \S\ref{sec:si-tables}.

That aggregate is not the instrument's output. Two operations reported separately---the pair
unit, and dropping the two chemistries the error concentrates in, the glycol-ether solvents
and the rows carrying water as the solute---compound badly. The chemistry cut alone takes
$+0.51$ to $-0.12$ on the $258$ rows that remain, the pair unit alone gives $+0.27$, and the
two together give $-0.38$ on $136$ pairs; under the full convention the same pair of cuts
takes $+0.95$ to $+0.28$ to $+0.01$. Nor is the aggregate carried by the set: deleting one
source publication at a time, \texttt{10.1021/acs.jced.7b00114}---$37$ rows, six molecule
pairs, $32\%$ of the set's squared error---takes the margin to $+0.18$, while each of the
other fourteen deletions leaves it between $+0.42$ and $+0.87$ (under the full convention
that curve is flat, $[+0.82,+1.07]$ against $+0.95$). That single deletion loses two thirds
of the margin and its resolution without reversing its sign; the sign reverses only under
the compounded cut. Both are failures of the headline cell rather than robustness passes,
and the aggregate margin is an average weighted by one set's chemical composition. Two
further caveats qualify the corroborating estimators and not the bound. Under random folds
they leak, and under folds blocked on molecules they are uninformative on the VT-2005-matched
set and split on the broad set, so on neither set do they support the separation; and the
binning-free pair bound, tighter though it is, is not promoted, because its multi-row pairs
each come from a \emph{single} publication and it therefore samples no between-laboratory
component. Read together, the two say the binning bound is slack rather than that $\Binsuf$
is negligible. What is reported rests on the LOTV bound, which fits no model and so has no
fold design to leak through (\S\ref{sec:si-tables}, Table~\ref{tab:bounds}).

% ---------------------------------------------------------------------------
% THE MAP, IN THE ARTICLE.  Until 2026-08-02 both halves of this table and the drawn map were
% Supporting Information floats while the abstract named the map as what replaces the retired
% aggregate verdict -- so the article named an output it did not contain, which is the referees'
% own complaint about load-bearing results in the SI.  The instrument's output belongs here.
% WHAT IS HERE AND WHAT IS IN THE SI, and why that split and no other: a stratum below n=40 has
% no margin the admissibility rule can act on, so the fifteen boundable strata are exactly the
% ones that can separate anything, and they print in the article at every granularity and on both
% axes.  The 44 smaller strata -- 24 of them with no estimate at all -- print in the SI with the
% test each fails.  Nothing is dropped from either place.  The drawn map (Fig. S-decomp) stays in
% the SI because it draws these same rows; the numbers a reader needs are the ones below.
% GENERATED by scripts/analysis/make_map_table_tex.py: do not retype a value.
% ---------------------------------------------------------------------------
\begin{table*}[tp]
\centering
\caption{\textbf{The map, solvent axis: every solvent stratum the estimator can bound.} One cell
applied identically to all of them---eight equal-count bins of $g(\zstar)$, unbiased within-bin
variance, row unit, deployed residual-only convention; \emph{full} is the same cell under the full
combinatorial convention. With Table~\ref{tab:map-b} these are the $15$ of the map's $59$ strata
with $n\ge40$, where eight bins hold five rows each; the $44$ smaller ones, $15$ of them with no
estimate at any binning, are in \S\ref{sec:si-map} with the test each fails and with every
stratum's error, bound and error share. Nothing is dropped from either place, and the
stratification reads molecular structure alone, so no cell was selected on its outcome.}
\label{tab:map}
{\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{0.7}
\input{si_tables/stratified_map_rows_article}
\tabnotes{\scriptsize%
\tnk{a}\,Source publications, then the largest and its share of that stratum's squared error. The
keys, used in both documents: \input{si_tables/map_source_key}\quad
\tnk{b}\,Two-way solute\,$\times$\,solvent cluster bootstrap, $3000$ draws, redrawn at each
granularity, so one row set's margin repeats while its endpoints differ in the last digits.\quad
\tnk{c}\,In how many of the four unit\,$\times$\,convention cells the stratum is boundable
\emph{and} keeps its margin's sign under deletion of each contributing publication; the rule
(\S\ref{sec:estimator}) requires all four.\quad
\tnk{d}\,The condition the row is in, not a label for it. A margin is one-sided: positive
establishes $\Bclos>\Binsuf$ there, non-positive is failure to separate and licenses nothing
(Table~\ref{tab:claims}, note~\emph{i}). Rows at different granularities, and on the two axes, can
be the same measurements, so each row that keeps its sign carries its overlap and no row set is
counted twice.}}
\end{table*}

\begin{table*}[tp]
\centering
\caption{\textbf{The map, solute axis, the solvent\,$\times$\,solute cross-tab, and the set as a
whole}---the second half of Table~\ref{tab:map}, at the same estimator cell and under the same
rule, whose notes apply here unchanged. The whole-set row is the aggregate \S\ref{sec:broad}
declines to state, printed with the rest.}
\label{tab:map-b}
{\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{0.7}
\input{si_tables/stratified_map_rows_article_solute}}
\end{table*}

\emph{The map, and how much of it survives the rule.} The map runs over $59$ strata of the broad
IDAC set---$477$ rows, $185$ pairs, $78$ solutes, $36$ solvents, $15$ source publications---on the
solvent axis at three granularities and the solute axis at two, plus their cross-tab and the set as
a whole. Tables~\ref{tab:map} and~\ref{tab:map-b} are the fifteen the fixed estimator can bound, the only ones whose
margins the rule can act on; the other $44$, including the $24$ that carry no estimate at all, are
in \S\ref{sec:si-map} with the test each fails, drawn there
(Fig.~\ref{fig:decomp}) and with their error, bound and error share
(Tables~\ref{tab:map-full}--\ref{tab:map-detail}). Six of the fifteen are
\emph{admissible}---boundable and sign-stable under deletion of
each contributing publication, in all four unit\,$\times$\,convention cells. Those six are
three distinct row sets, of which one is both positive and not contained in another: the
glycol-ether solvents. \textbf{That row set is the one established finding}: on $182$ rows
over $43$ pairs from three publications carrying $45\%$ of the set's squared error, the
closure's own error exceeds what input insufficiency can account for, by $+2.04$ with a
$90\%$ interval of $[+1.27,+2.83]$ at the deployed convention---$+2.94$ at the pair unit
under the full one---positive in all four cells and under every single-publication deletion,
the worst leaving $+1.60$ when \texttt{10.1016/j.jct.2016.10.013}, which carries $53\%$ of
that stratum's squared error, is removed. It is not an artifact of one solute family: the
$74$ rows left when its alkane solutes are dropped keep the sign at $+1.40$ deployed and
$+1.69$ full, under all three deletions.

Three of the six are that same row set at three granularities. The remaining three are two
row sets, each carrying its reason in Tables~\ref{tab:map}--\ref{tab:map-b}. The alkane solutes ($129$ rows,
$45$ pairs, $+1.38$, $[+0.79,+2.33]$) are $84\%$ the glycol-ether rows and return $-0.22$ on
the $21$ outside them---itself a sub-threshold number, so the demotion rests on the overlap
arithmetic and not on it. The acceptor-only aprotics ($57$ rows, $-0.19$, negative in all
four cells, $P<0.2$ at both units under the deployed convention and $P\approx0.3$ under the
full one, at an $\mathrm{MSE}$ of $0.089$ that is at the scale of the measurements) occupy
two cells, pass the rule and license nothing, the instrument being one-sided; their deletion
test is additionally \emph{anti}-conservative, two of the four deletions leaving $n<40$,
where the Bessel within-bin variance inflates---$0.140$ to $0.258$ on the $n{=}38$
remainder---and drives a negative margin further negative. That is the whole of the
uniqueness claimed for the glycol ethers: the only row set admissible, positive and not
contained in another---\emph{not} the only admissible stratum and not the only one
sign-stable under every deletion.

Every other stratum is reported and not established, each with the test it fails. Water
carried as the dilute solute ($37$ rows, $+6.33$) and the halogenated solvents ($30$ rows,
$+3.39$) are too small to bound, one publication supplies both and its deletion cannot be
run, so those two large margins were never put at risk; the alkane solutes and the
acceptor-only aprotics pass the rule and establish nothing, for the reasons above; the coarse
acceptor-only class is carried by one publication's aryl-ether rows and turns negative
without them; water as a solvent, the coarse hydroxylic class and the organic-solute subset
each lose their sign under a deletion, which is why the finding belongs to the glycol-ether
sub-class and not to the hydroxylic solvents as a class; the mono-alcohols, the N--H protics
and the ester and azine sub-families, which an acceptor-only reading would lean on, are
unboundable with a deletion that cannot be run. None may be used to explain a reversal. The
whole-set row passes at the headline cell and fails once the pair unit is required, so the
rule does not by itself dispose of the aggregate there; the composition argument above does.

\emph{Why the output is a map.} $\Binsuf^{\mathrm{up}}$ is nearly
stratum-independent and $\mathrm{MSE}$ is not. Across the three classes the design can
bound---glycol ether, water and the acceptor-only aprotics, bounds $0.108$, $0.215$ and
$0.140$---the bound spans a factor of $2.0$ against errors ($2.252$, $0.476$, $0.089$)
spanning a factor of $25$; adding the three that carry an estimate without being
boundable---mono-alcohol $0.733$, halogenated $0.592$, N--H protic $0.281$---gives $6.8$
against $51$, that $6.8$ being set by the mono-alcohols' $0.733$, which exceeds that
stratum's own $\Var(m)=0.604$ and is flagged in the artifact as an invalid bound on a
stratum the boundability criterion has already excluded. The two orderings are
unrelated---the glycol ethers carry the lowest bound of any class and the second-highest
error---and against $2000$ random partitions into blocks of the observed class sizes, at the
same cell, the chemical partition's spread in $\mathrm{MSE}$ exceeds every draw while its
spread in the bound is unremarkable ($p=0.26$); their ratio gives $p=0.02$. That is the
estimator showing through: every stratum is summarised by eight quantile bins of one scalar,
so the bound measures the residual spread of $m$ inside such a bin, a property of the
binning rather than of the chemistry, while nothing constrains the error to follow it. An
aggregate margin is therefore a composition-weighted average of a term free to move
$25$-fold offset against one that barely moves, and it will report a binding closure for any
set whose composition happens to include a chemistry the closure fails on---a statement
about the aggregate and not about the bound, which stays valid in each stratum where it is
computed.

\subsubsection{The VT-2005-matched set ($n{=}60$)}
\label{sec:vt2005}
This set carries the paper's other reference-matched analyses, and it is far weaker. All
$60$ rows sit at $298$\,K, so $\zstar$ is the profile pair alone and
$\dim(\zstar)/n\approx102/60$ leaves the split behind one-sided bounds; it is strongly
unbalanced, methanol occupying $25$ of the $60$ rows and the two worst-fit pairs carrying
$34\%$ of the squared error; and its $17$ solvents fall in six of the nine classes, none
reaching the $n{=}40$ a bound needs, so it supports no stratified bound and no stratum of it
is stated as a finding. Its aggregate margin is $+0.05$, and it does not stand: deleting the
two leverage pairs takes it to $-0.002$, dropping methanol loses it, blocked out-of-fold
estimators do not support it, and $11$ of the $60$ single-pair deletions take it
non-positive, where on the broad set no single-pair deletion moves the sign at all.
Zero-mean label noise can understate a separation but not create it (\S\ref{sec:decomp});
what a margin does bound is a label systematic tracking $\zstar$, implausible on the
maximum-likelihood binning and \emph{not excluded} on the Bessel-corrected one. Of the $60$
pairs, $57$ also appear in the broad IDAC set at $298$\,K under UD profiles, so the two are
not independent samples---this is the sub-design VT-2005, and therefore the paradox's own
oracle, is defined on, and the agreement between the two sets' error orderings is a
consistency check rather than corroboration. The composition audit, the stratified error
ordering, the four fold designs, the cell grids, the cluster bootstraps, the label-noise
arithmetic and the convention, leakage and $\gamma$-stratification stress-tests are in
\S\ref{sec:si-tables} and \S\ref{sec:si-map}.

\subsubsection{Whether a better closure moves the ceiling}
\label{sec:fidelity}
% Artifacts: results/b_insuff/fidelity_lever_fair2002.json via run_fidelity_lever_fair2002.py;
% results/b_insuff/closure_flip_ci.json via run_closure_flip_ci.py; block split of the kernel
% contrast (shared 57 / complementary 420) in results/b_insuff/representative_audit.json.
If the ceiling is the closure, the direct remedy is a better closure. The standard
$2002\to2010$ step delivers one on the VT-2005-matched set ($n{=}60$) and not on the broad
IDAC set ($n{=}477$), and a block split identifies the factor that carries that reversal.

The two kernels have no \emph{prescribed} common input, $\sigma$-profile averaging being part
of each model's definition, so evaluating each on its native profile is the specified use. An
input-fixed \emph{control}---profile database, cavity area, volume and averaging held fixed,
kernel alone varied---gives $2.047\to0.765$ on the smaller set ($P>0.95$), under three
limits: it cannot separate the donor/acceptor typing, which the 2002 kernel has no channel to
consume and which is therefore charged to the kernel by construction; its step is larger than
the native-input one, so it is favourable to the conclusion it supports; and its re-averaging
term is unresolved. That arithmetic, the NIST implementation detail fixing the \emph{fair
2002} arm at $1.757$, and the three arms---\emph{fair 2002}, \emph{2010, dispersion off},
\emph{2010, dispersion on}---on both sets under both conventions are in \S\ref{sec:si-tables}
and Table~\ref{tab:kernels}.

The step more than halves the smaller set's error under the full convention ($P>0.95$), the
convention under which the two kernels' published error figures are reported, and cuts it
$42\%$ under the deployed one. On the broad IDAC set it is not an improvement under either
rule, and the $3\%$ the dispersion term recovers there is a null whose interval is an order
of magnitude wider than the effect, not a measured equality. That arm's run on the smaller
set is on $n{=}57$, three halogenated pairs having no tabulated dispersion value---a coverage
limit of the model offered as the lever; no claim rests on the $0.765$-versus-$0.785$
ordering, which \S\ref{sec:si-tables} shows is not an artifact of the differing $n$.

The reversal is the \emph{chemistry} and neither the profile database nor the temperature
range: with UD profiles, one extraction and one convention throughout, the \emph{2010,
dispersion off} arm wins on the $57$ pairs shared with the smaller set, does not win on the
complementary $420$, and still does not win on the $80$ complementary rows that are
\emph{also} at $298$\,K---that last block at $P\approx0.3$ and no interval, the three blocks
not having been tested against one another (Table~\ref{tab:kernels}). The VT-2005-matched set
is a low-$\gamma$, hydrogen-bond-dominated sub-design, the regime the donor/acceptor-typed
2010 kernel was built for. The map does not corroborate the reversal from the other side and
we do not ask it to: the typed kernel is not an improvement on the broad set, of whose
squared error the glycol ethers carry $45\%$, and the classes that would explain a
reversal---the halogenated solvents, the aryl ethers and water as the solute, largely one row
set, all $13$ aryl-ether rows and $18$ of the $30$ halogenated being water-as-solute rows
from one publication---are the cells the rule excludes. The block split is therefore the
whole of the evidence for the reversal being chemical.

The floor checks are same-database, same-latent \emph{and} same-convention. The
\emph{UD-native} check on the smaller set, the one commensurable with the broad set, is at
zero under the deployed rule and $+0.69$ under the full convention. Under the 2010 kernel's
own typed latent, $306$-dimensional and so several times worse resolved, the ordering is
\emph{lost} on the smaller set ($-0.77$) and holds on the broad set ($+1.29$)---both
aggregates, and read as such. That is loss of resolution, not a demonstrated flip to the
inputs, which would need a \emph{lower} bound on $\Binsuf$ (\S\ref{sec:decomp}); the
arithmetic under both conventions and all three clusterings is in \S\ref{sec:si-tables}.

We know of no large-scale infinite-dilution benchmark of the $2002\to2010$ step, and our
search was not exhaustive, so this is an absence of evidence rather than evidence of
absence; the broad-set null lies within the range of magnitudes the comprehensive
assessment~\cite{fingerhut2017} reports. We claim only that \textbf{the fidelity lever we
can demonstrate is confined to the smaller set's chemistry}, and that null is additionally a
small-molecule and glycol-heavy one (composition audit, \S\ref{sec:si-repro}), so on
drug-like solids the lever is untested rather than absent.

\subsubsection{Correctability, and where the closure fails chemically}
\label{sec:badclosure}
Where $\Bclos$ dominates---on this set, the one row set the rule establishes---the ceiling
is in the map; that does not say the map is \emph{fixably} wrong. A small
structure-preserving residual on the fixed COSMO-SAC exchange-energy kernel, fit on the
reference VT-2005 profiles, removes $46\pm5\%$ of the reference-input error under random
folds but $-5\pm16\%$ once folds are grouped so that no solvent is shared. Only the grouped
design is free of leakage, the closure error being predominantly a between-solvent
systematic. Stated as a bound rather than a null, that design excludes a cross-solvent
reduction above about $30\%$. $\Bclos$ is therefore a defect a handful of kernel parameters
can absorb \emph{within} a solvent and that we have not shown to be correctable across
solvents---weaker than a general repair.

Where it fails is chemically placed, and placed differently on the two sets. On the
VT-2005-matched set $90\%$ of the squared error falls on strong hydrogen-bond-donor solvents
and the closure is near-exact on acceptor-only ones, putting that set's $\Bclos$ in
COSMO-SAC's documented single-parameter H-bond kernel---the specific defect the
donor/acceptor-typed 2010 closure was built to fix. On the broad set that reading does not
reproduce, and neither does its replacement, an absent dispersion term: which of the two the
broad set points at is decided in either direction by the same publication the map already
declines to state, so we state neither, and the H-bond reading remains the VT-2005-matched
set's, neither corroborated nor replaced by the broad set. That arithmetic is in
\S\ref{sec:si-map}. The residual's fold design, rank ordering and the scope of its standard
errors, its failure to gain over the free $2002\to2010$ step above, the NIST reproduction
that rules out an implementation artifact, and the Staverman--Guggenheim counter-test in
which the physically \emph{more} complete combinatorial term \emph{increases} error on
size-asymmetric pairs are in \S\ref{sec:si-tables}.

That COSMO-SAC-2002 is a low-fidelity closure is the contribution rather than a
qualification of the result: it is what a practitioner adopts by default, a closure's
fidelity is \emph{not knowable in advance} on a new system, and the field routinely composes
an expressive encoder with a fixed mechanistic map and assumes the map is good enough. What
follows when it is not is that the learned latent silently departs from the quantity it is
named after (\S\ref{sec:surrogate}), invisibly without an external oracle. Outside that one
row set the map reports cells it cannot resolve, not cells in which the closure is sound.

\paragraph{What $\Bclos$ is a property of.} The external ground truth used to charge
$\Bclos$ against $\Binsuf$ is quantum chemistry, not experiment: the VT-2005 profiles are
computed for a single reference conformer and protonation state (\S\ref{sec:si-repro}), and
the successor Delaware database revises those conformations for about a third of its
compounds~\cite{fingerhut2017}. That gap between $\zstar$ and the state actually populated
under the measurement does not load on $\Binsuf$ alone: if the reference is displaced, even a
perfectly specified $g$ carries a term in $\Bclos$, whose share we do not bound. $\Bclos$ as
measured here is therefore the misspecification of a composite object, the kernel applied to
one fixed computed conformer, and not of the kernel at the true conformational
ensemble---but the deployed pipeline is that same composite, so this is the quantity a
practitioner faces. Why the superpopulation has a non-degenerate $\Binsuf$ at all, and the
conformer term's own arithmetic, are in \S\ref{sec:si-repro}.

\input{sections/compensation-surrogate}

\subsection{A downstream correction on the output}
\label{sec:gateb}
% Artifacts: results/e5_sigma_grounding/seed_42/grounded_a_truetrain_residual{,_v2}_predictions.summary.json (Gate B v1 gated-shut / v2 forced-open); model.py correction_force_open_gate.
If the closure binding were merely an inaccurate point estimate, a learned output-space
correction on top of the physics prediction should recover accuracy. It does not: an
additive bounded residual with a learned confidence gate leaves the gate effectively shut,
and forcing it open removes the systematic \emph{bias} ($+0.45\to+0.06$) but recovers little
\emph{accuracy} ($R^2$ $0.236\to0.274$ on the same rows), as expected if the closure, not
the estimate, is the binding constraint. Both arms are seed $42$ only, so this fixes a sign
and not an effect size, and it is a statement about the one correction tested: an additive
bounded residual on the output, gated, on this closure and this corpus. Other downstream
corrections, and this one at other seeds, are untested here.

\subsection{A second chemical closure: pKa through a fixed Hammett relation}
\label{sec:pka-main}
The instrument is not specific to COSMO-SAC, and a second domain runs it where the sign of
grounding can be predicted before the measurement. There the fixed closure is a Hammett
linear free-energy relationship in a substituent constant, the learned intermediate is that
constant predicted from structure, the external reference is the Hansch--Leo table, and the
data are the OPERA experimental compilation, split within scaffold $K{=}20$ times. On the
meta- and para-substituted aromatics the relation was built for ($n{=}158$), substituting
the tabulated constant for the learned one \emph{improves} accuracy---MAE $0.48$ against
$0.315$, the reference better in $19$ of $20$ splits and in three of the four reaction
series; on the ortho-substituted and heteroaromatic ones it does not describe ($n{=}846$),
the same substitution \emph{degrades} accuracy---$0.83$ against $1.62$, the reference worse
in $20$ of $20$ splits and in all four series. The sign of grounding therefore reverses
inside one closure, in the direction the penalty rule (Proposition~\ref{prop:tax}) gives a
priori and on the channel it names: the ortho and steric information the tabulated constant
omits, which is input insufficiency, rather than the closure-error cancellation of
\S\ref{sec:surrogate}. Only $G{=}4$ Hammett reaction series exist, so the bootstrap resolves
nothing finer than series-level sign agreement, read as a sign test on four units
($p{=}1/16$ on the pole where grounding hurts); the construction, the estimator validation,
the measured fidelity sweep and the crossover law that predicts both signs are in
\S\ref{sec:pka}.

\subsection{Scope of the results}
\label{sec:discussion}
The result is a cautionary measurement about one of the two operations the word
``grounding'' names. Supervising a learned physical latent on reference values during
training helps; \emph{supplying} those values as an input at inference hurts, and it hurts
because the fixed map that consumes them is misspecified. When that map is of unknown
fidelity---and the standard route does not improve it---better inputs surface its bias
rather than help. Both sets are scoped by the reference database that defines them, and
neither is the deployment regime (composition audit, \S\ref{sec:si-repro}), so the fidelity
null is a null on small-molecule chemistry and the lever remains untested on drug-like
solids. The useful form of the finding is one-sided and narrow: a place where grounding
effort is \emph{not} the remedy, on one row set of the map, rather than an allocation across
chemistries---and an average over a set says only what that set happened to contain.

\input{sections/blackbox-probe}

\paragraph{The contribution.} The unit of contribution is the measurement, not any
single number it returned. Its two ingredients are individually standard---the
closure--insufficiency split is a law-of-total-variance identity, and the sign of
grounding's effect follows the model-discrepancy principle
\cite{kennedy2001,brynjarsdottir2014}---and what is new is their combination into a usable
instrument: an identity on a \emph{fixed} operator, with the insufficiency side bounded
rather than point-identified (\S\ref{sec:decomp}), whose sign rule can be tested \emph{a
priori} and \emph{bidirectionally}. The pKa domain supplies that test as an in-distribution
sign flip (\S\ref{sec:pka-main}), at the resolution four series allow and no better---an
oracle-on-a-fixed-operator affordance the concept-leakage setting lacks, its intermediate
having no ground truth \cite{parisini2025,espinosa2025leakage}. The solubility results are
the instrument's first application, and their transfer out of the matched regime is not
established.

\paragraph{The accuracy confound, and what survives it.} The physics and control arms were
tuned separately---the settings differ by up to an order of magnitude and are listed in
\S\ref{sec:si-methods}---and no hyperparameter-matched control was run. A global
$\Delta\mathrm{MAE}$ of $+0.14$ between pipelines whose learning rates differ by an order of
magnitude carries no information about the thermodynamic bottleneck, and neither do the
per-solvent-class map, the per-solute-class map, the novelty inversion or the
data-efficiency trend, all of which resolve the \emph{same} difference along chemical axes.
We therefore claim no accuracy result in either direction; those strata are reported in
\S\ref{sec:map} and \S\ref{sec:data-efficiency} as hypothesis-generating descriptions of
where two particular pipelines differ, each carrying that caveat, and no conclusion rests on
them. Running the matched control is the direct repair and we have not run it. What survives
the confound is every \emph{within-checkpoint} contrast, where the two sides share one
trained model and one tuning: \S\ref{sec:paradox}, \S\ref{sec:measure} (which uses no
trained model at all), \S\ref{sec:surrogate} and \S\ref{sec:gateb}. Separately, and not as
an accuracy claim about the bottleneck: we did not find a configuration in which the physics
path attains a lower error than the control. External state-of-the-art models (FastSolv,
SolProp) do no better on a by-solute split, which bounds the task's difficulty and does not
rank the models against one another (\S\ref{sec:si-baselines}), and grounding the crystal
factor on external labels did not move the ceiling (\S\ref{sec:e2}). The one place the
physical \emph{structure} helps is temperature extrapolation, as a hypothesis-class property
the trained models do not exploit (\S\ref{sec:supporting}).

\paragraph{The sign rule's own prediction.} The penalty rule (Proposition~\ref{prop:tax})
predicts \emph{when} to expect these negatives: a prior is predicted to hurt precisely in the
mature-data, low-noise, well-covered regime. The synthetic sweep (Fig.~\ref{fig:dial};
\S\ref{sec:dial}) is true by construction there, so it verifies the instrument rather than
the hypothesis, while the pKa closure (\S\ref{sec:pka-main}) exercises the sufficiency
channel on real data. Whether grounding helps is shaped by \emph{two} axes, closure fidelity
and how the latent is trained; characterising that plane on real systems is future work. The
chemical strata form a pattern in the direction the sign rule would give; separately tuned
pipelines can produce it for reasons unrelated to the prior and there is no multiplicity
control, so \S\ref{sec:map} records it as the experiment a matched control should run, not
as evidence, and the diagnostic is \emph{proposed} rather than validated.

% Limitation (i) enlarged-set numbers: run_b_insuff_decomposition.{match_pairs,evaluate_closure,
% lotv_binning} (res convention, 8 equal-count bins) on notebooks/data/raw/idac_expanded{,_raw}.csv
% against results/sigma_profile_artifact/sigma_profiles.csv; the same call reproduces the deployed
% n=60 set (MSE 1.4723, B_insuff 0.6180, margin +0.2362) exactly. Expanded: n=115, MSE 1.2553,
% B_insuff 0.4545, margin +0.3463; gas-chromatography-only subset n=100, MSE 0.9250, B_insuff 0.5274,
% margin -0.1298. Provenance of the n=60 pairs: notebooks/data/raw/idac.csv DOI counts 33/20/7.
% Limitation (vi) scaffold geometry: Murcko scaffolds over the 147 solutes of the has_solubility
% test rows -> 124 scaffolds, 11 multi-member holding 34 solutes and 492 of 5608 rows (8.8%).
\paragraph{Limitations: what the sets can carry.} (i)~The phenomenon and its causal attribution lie
in \emph{different regimes}: the paradox is measured where the model operates (finite composition,
all $\gamma$, $n{=}5608$), the attribution and the latent-departure result only on infinite-dilution
activity data ($n{=}477$ across temperatures, and the $298$\,K, low-$\gamma$, VT-2005-matched
$n{=}60$ and $n{=}44$ sets). Transfer back to the paradox's own regime, argued from the in-regime
signs, is a conjecture and is the principal open question here. The $n{=}60$ set is narrowly
sourced---all by gas chromatography and all from three publications, $33$ pairs from one---so
neither a method-stratified nor a source-clustered re-estimate is available on it, and its margin is
extraction-sensitive: a broader ThermoML pull at $298$\,K widens it but \emph{inverts} it on the
gas-chromatography subset, a sensitivity we leave unresolved and do not read as a verdict on the
extraction. The broad set does not move the same way under the same cut (\S\ref{sec:broad}) and is
not the set the paradox's own oracle is defined on; the arithmetic, the enlarged-pull values and the
matching-rule alternatives are in \S\ref{sec:si-repro}. (ii)~Under the deployed convention no
\emph{aggregate} margin is resolved on either set and only the literature-standard convention
resolves one, while the headline convention is the deployed one (Table~\ref{tab:claims}); the
aggregate falls for the stronger reason of \S\ref{sec:broad} rather than for want of resolution.
(iii)~Of the map's six admissible cells the glycol ethers are the only row set also positive and not
contained in another, and not the only cell whose deletions all run; the halogenated solvents and
the mono-alcohols keep their sign under the one deletion each admits, so they are unverified rather
than shown to fail.

\paragraph{Limitations: what the estimator can deliver.} (iv)~$\Binsuf$ is upper-bounded, not
point-identified (no replicates at fixed $\zstar$); the smoothness-free Jensen bound assumes $m$ and
$g$ share the $\lngi$ reference state, established by the closure validation of \S\ref{sec:model},
so the mean offset is decoder bias and not a units artifact. (v)~The constant-offset margin is
convention-specific and inverts under the deployed residual-only default on both sets; there the
separation rests on the law-of-total-variance bound and the more-physics-worse effect. That bound is
not itself convention-independent---only the estimand $\Binsuf$ is (\S\ref{sec:decomp})---so the
deployed claim rests on it \emph{computed under the deployed convention}. (vi)~The stratification of
\S\ref{sec:estimator} reads structure alone, so no cell was selected on its outcome, and it is
exhaustive and disjoint \emph{within} an axis but not across the two axes or their granularities:
$362$ of the $1711$ stratum pairs share rows, all $37$ water-as-solute rows lie inside three solvent
classes, and $108$ of the $129$ alkane-solute rows are glycol-ether rows, so cells from different
axes are not independent evidence and those rows are counted once (Table~\ref{tab:map-b}). Granularity
is an analyst choice, which is why three solvent levels and two solute levels are all reported.
There is no multiplicity control on the per-cell bootstrap frequencies, so one cell's $P$ describes
that cell alone; the admissibility rule is the only protection in place, six of the nine solvent
classes are not boundable here and three carry no estimate at all.
The rule is one-sided in what it can deliver: because $\Binsuf$ is bounded only from above, no cell
can ever return ``better inputs would help here'', so the map only ever withholds a chemistry from
an allocation of grounding effort.

\paragraph{Limitations: what is untested, and what is not auditable.} (vii)~Absolute scaffold
accuracy is encoder-bound but well \emph{above} the inter-lab noise floor ($0.15$--$0.31$ in $\lnx$;
\S\ref{sec:supporting}), so label noise does not explain the ceiling. (viii)~The latent-departure
result is three-seed mean$\pm$sd ($n{=}44$; \S\ref{sec:surrogate}): what it establishes is the
\emph{sign} and a transferable shared direction, the magnitudes
($51\pm2\%$/$72\pm1\%$/$4.1\pm0.5\times$) being reported but not stable across analysis
configurations. The two top-two shares in play are distinct quantities: the learned-versus-reference
deviation clears a smoothness-matched phase-randomised null but not a wrong-molecule one, and is
measured on one grounded checkpoint rather than three seeds, whereas the isolation drift has no
matched null of its own. (ix)~The probe result on the unconstrained control is bounded by the same
reference scarcity: its crystal-term and temperature tests rest on $13$ held-out solutes and cannot
be enlarged, so the negative is an upper bound at $R^{2}\approx0.30$ and not a demonstration that no
crystal information is present; the crystal-slope test is additionally limited by the corpus, whose
empirical slope carries $-\Delta H_{\text{fus}}/R$ at $r=+0.24$. The scaffold-organisation result
needs no external reference, but its two partitions nearly coincide by construction, so a ratio near
one is weak evidence of within-scaffold indistinguishability (\S\ref{sec:si-blackbox}). (x)~The
closure-fidelity contrast compares \emph{oracle-input} activity error across kernels, so $\Ggap$
itself is not measured under a modern closure on either axis, and whether the \emph{solubility}
paradox survives a $2010$/dsp closure is untested; both need a typed (donor/acceptor)
$\sigma$-profile head trained end to end through the 2010 kernel, and the solubility test a
UD$\,\cap\,$BigSolDB matched set. (xi)~No difference in accuracy between the physics and control
arms is attributable to the thermodynamic bottleneck; the confound and what survives it are stated
above. (xii)~The scaffold-leak guard is a property of the released stream builder and not a
certified property of these runs: one $\sigma$-stream build on disk carries $91$ held-out solutes,
and per-run stream provenance was not logged (\S\ref{sec:si-repro}). The paradox contrast is one
checkpoint evaluated two ways and so cannot be generated by such a leak, and DirectGNN uses no
stream, but auditability is not restored by those arguments. (xiii)~Several of the numbers the
verdict rests on are checkable only by re-running the released code, whose input artifacts are
larger than the repository carries (\S\ref{sec:si-repro} names which); the two decomposition sets
are therefore deposited as row-level tables (Data Sets~S1 and~S2), so the sets and their margins can
be recomputed without code, which does not deliver the provenance of the closure evaluations in them
or of the trained arms.

% CONCLUSIONS: two paragraphs, no numbers, and not a restatement of the abstract -- the
% consequence and the design rule, touching a finding only to carry the "therefore".
\section{Conclusions}
\label{sec:conclusion}
Grounding a learned physical intermediate in reference values is two operations, not one,
and only one of them helped here. Training the intermediate against reference values
improved the model; supplying those values at prediction time did not. On the activity data
where the two terms can be separated, they do not separate the same way in every chemistry:
on one class of solvent, under a rule applied to every class alike, the bound on the fixed
thermodynamic model's own error lies above the bound on the contribution of its inputs,
which is the condition under which more accurate inputs expose a model's bias rather than
remove it; on every other row set the separation is not established, at the sample sizes and
the number of laboratories available. As a single number for a whole data set the comparison
is an average over that set's composition, a property of the set and not of the model. And
the intermediate trained through that model stopped reporting the physical quantity it is
named after, on the molecules where it can be held against its reference, which is the price
of reading a grounded latent as a measurement.

The general statement is a design rule with a test attached. Wherever a predictor composes a
fixed physical model over a learned intermediate for which an external reference exists,
that reference measures which of the two limits accuracy, and where it answers, the answer
decides the remedy: a more faithful physical model, or better inputs. The answer comes
chemistry by chemistry rather than once for a field, and it comes one-sided, so what the
measurement returns is the chemistry on which better inputs are not the remedy---here one
class of solvent, on which the standard route to a more faithful model did not clear the bar
either. Whether a latent trained through an imperfect model becomes, in general, a surrogate
for that model's error is the prediction this work leaves testable.

\section*{Author contributions}
\noindent
%% PROVISIONAL, for both authors to confirm before submission: the roles below are inferred from the
%% work as recorded and are not an agreed statement.
N.~L. Polomoshnov: Conceptualization, Methodology, Software, Formal analysis, Investigation,
Data curation, Visualization, Writing -- original draft.
A.~V. Rudik: Writing -- review \& editing.
%% CRediT vocabulary (use only these 14 names): Conceptualization; Data curation; Formal analysis;
%% Funding acquisition; Investigation; Methodology; Project administration; Resources; Software;
%% Supervision; Validation; Visualization; Writing -- original draft; Writing -- review \& editing.

\section*{Conflicts of interest}
\noindent There are no conflicts to declare.

\section*{Data availability}
The analysis code, and those intermediate artifacts small enough to version, are available at
\url{https://github.com/doctawho42/tgnn-solv} (commit
\pending[hash to be supplied at submission]) under the MIT licence, that commit being the one
the reported numbers were produced from; the model checkpoints, the processed split, the full
per-arm prediction files and the larger analysis artifacts---among them the two audit files
\S\ref{sec:si-repro} names---will be deposited in a Zenodo archive,
\pending[whose DOI is registered at submission]. The data sources are public: BigSolDB~2.0
\cite{krasnov2025bigsoldb} for solubility, the VT-2005 database for the reference
$\sigma$-profiles, Ref.~\cite{fingerhut2017} as redistributed with Ref.~\cite{bell2020cosmosac}
for the UD profiles, ThermoML for the infinite-dilution activity coefficients, and the OPERA
compilation \cite{mansouri2019opera} for the second-domain pKa data. Most analysis scripts
carry their run commands in their docstrings, and the compute ledger (\S\ref{sec:si-repro})
names the script and artifact file behind each audit. Four deposited artifacts carry disclosed
defects, recorded in \S\ref{sec:si-repro} and \S\ref{sec:si-tables}: the seed-44 oracle per-row
file is truncated, the three-seed surrogate run's per-run split provenance was not logged, one
$\sigma$-stream build on disk was written against the wrong split files, and the two
VT-2005-matched audit artifacts disagree on one out-of-fold estimate.

\begin{acknowledgement}
This research received no external funding.
Computations were carried out on commercial cloud services (Google Colab Pro+, Modal, and Google Cloud Platform) using NVIDIA T4 and L4 GPUs.
% AI-use disclosure removed at the author's request; to be rewritten before submission. ACS asks for
% one, so this must not be forgotten -- it is item 5 of the pre-submission checklist above.
\end{acknowledgement}

% The Supporting Information is a separate document, grounding_paradox_si.tex. This paragraph is what
% ACS prints as the "Supporting Information Available" note, and it is also where this article tells
% its reader how to read an S-number, since every pointer from here into that document resolves to one.
\begin{suppinfo}
The Supporting Information is a separate document. Its sections, tables, figures and equations are
numbered S1, S2, \ldots, and every reference above that carries an S is to it. It contains
supplementary methods; proofs; per-arm tables and decomposition robustness; the map's remaining
$44$ strata; external baselines and ablations; supplementary mechanism results; supplementary
diagnostics and broad negatives; the second-domain pKa analysis; data provenance and
reproducibility; the black-box probe tables; the synthetic closure-fidelity family; and the
positioning against adjacent literatures. Two machine-readable tables are deposited with it: the
$477$-row broad IDAC set (Data Set~S1, \texttt{broad\_idac\_set\_477.csv}) and the $60$-row
VT-2005-matched set (Data Set~S2, \texttt{vt2005\_matched\_set\_60.csv}); their columns are listed
in \S\ref{sec:si-repro}.
\end{suppinfo}

% DO NOT \input sections/SI here. It is the SI document's body now, it declares S-numbered
% floats, and pulling it back in would restore the reading-order defect (the reference list
% printing forty pages after the text that cites it) and give this document a second,
% conflicting set of table and figure numbers.

% achemso selects achemso.bst automatically; no \bibliographystyle is given.
\bibliography{references_verified}

\end{document}
