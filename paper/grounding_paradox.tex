% =====================================================================
% GROUNDING-PARADOX PAPER — reframed submission draft (2026-07-08)
% Supersedes conditional_optimality_skeleton.tex (theory-first, headlined the
% REFUTED Thm3 "conditional optimality iff"). That file is kept for reference /
% reusable Fisher+T-extrap numbers, NOT the thesis.
%
% THESIS PIVOT: from "physics helps iff (conditions)" -> the GROUNDING PARADOX:
%   feeding the TRUE external physical latent (VT-2005 sigma-profiles) through the
%   differentiable COSMO-SAC closure makes activity/solubility prediction WORSE,
%   and we MEASURE why: B = B_closure + B_insuff, closure dominates.
%
% SECTION MAP (general-theory reframe, 2026-07-11):
%   Abstract + Contributions ...... general composed-predictor method (paradox + decomposition
%                                   instrument + Gamma-symptom + physics-tax) ; solubility = instance
%   S2 Setup ...................... SLE eq + sigma-profile closure + z* external latent   [reuse eq:sle]
%   S3 Framework (general g(h(x))). Lemmas 1-3 (non-identifiability/efficient-info/B-decomposition);
%                                   Theorem 1 grounding-gap sandwich 0<=Gamma<=B_clos (thm:gap, sec:gap);
%                                   Proposition physics-tax sign rule (prop:tax, sec:tax);
%                                   proofs in SI. Gamma = SYMPTOM, decomposition = certificate.
%   S4 Results .................... R1 paradox; R2 B measurement (keystone); R3 Gate B null;
%                                   synthetic dial (sec:dial) + pKa second closure (sec:pka); data-eff; maps
%   S5 Discussion ................. phase picture / when a physics prior must lose + 4 hedges + scope
%   S6 Conclusion
%
% NUMBER PROVENANCE: Fig-paradox / chemistry-map numbers regenerate from
% results/e5_sigma_grounding/ via scripts/analysis/run_e5_comparison.py
% (3-seed aggregate: results/e5_sigma_grounding/THREE_SEED_SUMMARY.md).
% Table-2 B-decomposition numbers regenerate from
% scripts/analysis/run_b_insuff_decomposition.py -> results/b_insuff/decomposition.json.
% BIB: murphy1973 / degroot1983 (calibration refinement) and white1982 (QMLE under
% misspecification) are present in references_verified.bib and cited.
% =====================================================================
\documentclass[11pt]{article}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{hyperref}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\newtheorem{assumption}{Assumption}
\newtheorem{definition}{Definition}

\newcommand{\pending}[1][?]{{\color{red}\texttt{[#1]}}}
\newcommand{\lnx}{\ensuremath{\ln x_2}}
\newcommand{\Phicry}{\ensuremath{\Phi}}
\newcommand{\lng}{\ensuremath{\ln\gamma_2}}
\newcommand{\lngi}{\ensuremath{\ln\gamma_2^\infty}}
\newcommand{\Tm}{\ensuremath{T_m}}
\newcommand{\dHfus}{\ensuremath{\Delta H_{\mathrm{fus}}}}
\newcommand{\Rgas}{\ensuremath{R}}
\newcommand{\zstar}{\ensuremath{z^\star}}
\newcommand{\Bclos}{\ensuremath{B_{\mathrm{closure}}}}
\newcommand{\Binsuf}{\ensuremath{B_{\mathrm{insuff}}}}
\newcommand{\Ex}{\ensuremath{\mathbb{E}}}
\newcommand{\Var}{\ensuremath{\mathrm{Var}}}

\title{When True Physical Inputs Help or Hurt a Learned Solubility Model:\\
Closure Fidelity and Latent Supervision Govern the Sign of Grounding}
\author{N.~L. Polomoshnov \and \pending[co-authors]}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Physics-informed models increasingly route a prediction through a fixed, differentiable
physical \emph{closure}---a mechanistic map with no fitted parameters---fed by a learned
intermediate. For solid--liquid solubility the closure is COSMO-SAC, which turns a molecule's
surface-charge $\sigma$-profile into an activity coefficient; the $\sigma$-profile is predicted
by a network. The premise is that grounding the closure on truer physical inputs improves it.
We show its sign is not fixed. Replacing our model's learned $\sigma$-profiles with the true,
externally measured ones (VT-2005) makes prediction \emph{worse} than a free learned head---yet a
recent \emph{supervised}-$\sigma$ model reports the opposite. We resolve this by mapping the grounding
effect onto two governing axes---how the latent is trained (supervised toward truth vs.\ shaped
end-to-end through the fixed closure) and the closure's fidelity---anchored by three real systems and a
synthetic fidelity dial. On the end-to-end, low-fidelity anchor (ours), grounding hurts because the
learned $\sigma$-profile is not physical: on a confound-free one-model control it is a systematic,
low-rank, out-of-sample-transferable closure-compensating surrogate (converged departure $53\%$,
transfer $3.3\times$ over identity). An externally-grounded decomposition measures \emph{where} the
ceiling sits, and finds it is the closure, not the inputs, for the deployed COSMO-SAC-2002 map (our
differentiable closure reproduces the NIST reference COSMO-SAC to RMSE $0.003$, so this is genuine
misspecification, not an implementation artifact). Raising closure fidelity moves the ceiling: a
standard modern closure (COSMO-SAC-2010/dsp) removes ${\sim}70\%$ of the closure error on the same set
and \emph{flips} the binding term from the closure to the inputs. The practical map: if the closure is
misspecified and the latent is end-to-end, grounding hurts and the latent is not physical---supervise
the latent, or raise closure fidelity. We give the supporting composed-predictor theory (an exact
error decomposition, a crystal/activity non-identifiability result, and a physics-tax sign rule); we
make no accuracy claim.
\end{abstract}

\section{Introduction}
\label{sec:intro}
% Frame: the field's default is "add physics / ground on real inputs -> better OOD".
% We show a clean counterexample and a measurement that says why.
Coupling a graph encoder to an explicit thermodynamic bottleneck is a common
recipe for out-of-distribution solubility prediction: the model must route its
prediction through interpretable physical factors, and each factor can in
principle be grounded on abundant single-component data that a black-box
predictor cannot exploit. The expectation is that the closer the model's physical
inputs are to the truth, the better it should predict. On the activity branch
this expectation fails on our model---yet a recent supervised-$\sigma$ model reports the
\emph{opposite}, so the sign of grounding is not a fixed fact. The reason we dwell on our failure is
what its \emph{cause} reveals. Having the latent's true value externally is a double lever: it lets us
\emph{measure} where the ceiling lies and attribute it to the closure rather than the inputs (the only
setting in which that attribution is possible, since a fitted latent has no external oracle), and it
lets us hold the learned latent against its truth---which shows that, trained end-to-end through a
misspecified closure, the latent is driven off the physical quantity into a systematic, low-rank,
out-of-sample-transferable compensating surrogate (confound-free control, \S\ref{sec:surrogate}).
Both organize into one map: whether grounding helps or hurts is governed by two axes---how the latent
is trained (supervised vs.\ end-to-end) and the closure's fidelity---which we anchor with three real
systems and a synthetic dial, and a standard higher-fidelity closure (COSMO-SAC-2010/dsp) moves the
ceiling and \emph{flips} the binding term (\S\ref{sec:measure}). The composed-predictor framing
(\S\ref{sec:framework}) and the physics-tax sign rule give the formal account of the sign; the
synthetic dial (\S\ref{sec:dial}) and the pKa closure (\S\ref{sec:pka}) trace the same two axes. We
make no accuracy claim.

\paragraph{Contributions.}
Our thesis is that whether grounding a learned latent in true physics helps or hurts is governed by
two axes---how the latent is trained (supervised toward truth vs.\ shaped end-to-end through the fixed
closure) and the closure's fidelity. We map this, give the mechanism on the corner where grounding
hurts, a measurement that locates the ceiling, and the supporting theory and negatives.
\begin{itemize}
  \item \textbf{The grounding paradox (empirical).}
  Substituting true VT-2005 $\sigma$-profiles into the differentiable COSMO-SAC
  closure degrades activity ($\lngi$) and solubility ($\lnx$) prediction relative
  to a free learned activity head, the opposite of the intended benefit
  (\S\ref{sec:paradox}).
  \item \textbf{A measurement instrument: the closure/insufficiency decomposition.} Because the
  physical latent $\zstar$ (the true $\sigma$-profile) has external ground truth, the
  true-input excess risk splits \emph{exactly} as $B=\Bclos+\Binsuf$ with no model
  fit (Lemma~\ref{prop:decomp}). The decomposition and its estimators are textbook;
  the new element is exploiting an externally measured latent to bound the split
  empirically. On the convention-robust evidence---a bound that holds however the
  COSMO-SAC closure is configured, plus the more-physics-worse effect---$\Bclos$
  (misspecified decoder) exceeds $\Binsuf$ (input insufficiency) on the low-$\gamma$ subset we
  can measure, robustly across the $k$-NN, binning, and random-forest insufficiency estimators
  (only the most conservative ridge does not certify) and, under the law-of-total-variance bound,
  to leaving out any solvent: there,
  the binding term is \emph{likely} the closure (\S\ref{sec:measure}).
  \item \textbf{The mechanism where grounding hurts: a compensating-surrogate latent.} On the
  end-to-end, low-fidelity anchor the network's ``physical'' input does not stay physical: on a
  confound-free one-model control the predicted $\sigma$-profile departs from the true one by $53\%$ in
  a low-rank (top-two modes $73\%$), out-of-sample-transferable ($3.3\times$) pattern---a systematic
  closure-compensating surrogate, not the physical quantity (a $\sigma$-grounded model \emph{can}
  recover the true profile; the objective pulls it away). This is why grounding hurts here, and why a
  \emph{supervised}-$\sigma$ model finds the opposite (\S\ref{sec:surrogate}).
  \item \textbf{The closure-fidelity axis: a modern closure flips the verdict.} On the same matched
  set, the standard better-parameterized closures (COSMO-SAC-2010/dsp, reference implementation) cut
  the closure error by ${\sim}70\%$ and \emph{flip} the binding term from the closure to the
  inputs---so the 2002 closure-dominance is a documented H-bond-kernel inadequacy, and the actionable
  lesson is to raise closure fidelity, not to enrich the inputs (\S\ref{sec:measure}).
  \item \textbf{A negative actionability result (Gate B).} A post-hoc
  output-space residual placed on the physics prediction recovers calibration but
  not accuracy; the closure binding cannot be undone downstream
  (\S\ref{sec:gateb}).
  \item \textbf{A broad, explained negative.} The physics prior does not beat a
  matched black box on accuracy at any training-data budget---including the low-data
  regime where an inductive bias should help most (\S\ref{sec:data-efficiency})---nor, as a
  trained model, on temperature (though the physical hypothesis \emph{class} does extrapolate
  there, \S\ref{sec:supporting}); read against the lemmas and the closure measurement, this
  points to a \emph{structural} ceiling within this closure family rather than an incidental
  one (\S\ref{sec:discussion}). We make no accuracy claim.
  \item \textbf{A general account, and honest scoping.} The method is not tied to
  COSMO-SAC: it is stated for any composed predictor $\hat y=g(h(x))$ with a fixed closure
  $g$ over a learned intermediate. We prove three structural lemmas
  (Lemmas~\ref{prop:ident}--\ref{prop:decomp}), a grounding-gap identity
  (Theorem~\ref{thm:gap}), and a sign rule for the physics tax (Prop.~\ref{prop:tax}).
  The grounding-gap identity bounds the gap by the closure bias, but only under assumptions
  that fail here; we therefore read the gap as a \emph{symptom} and let the assumption-light
  decomposition do the certifying. The sign rule's critical-budget and aleatoric-floor
  corollaries predict the two negatives above. The account transfers to synthetic closure
  families (\S\ref{sec:dial}) and a second chemical closure (pKa through a Hammett relationship,
  \S\ref{sec:pka}, where a trained physics arm does beat its control---though a stratified check
  shows the gain is not cleanly a closure-fidelity effect); all proofs are elementary
  (App.~\ref{sec:si-proofs}).
\end{itemize}

\input{sections/related-work}

\section{Problem setup and notation}
\label{sec:setup}
For a solute~$s$ in solvent~$v$ at temperature~$T$, the target is $y=\lnx$, the
log saturated mole fraction. Dissolving a solid trades two costs against each
other---breaking the crystal lattice ($\Phicry$) and mixing the freed molecules
against non-ideal solute--solvent interactions ($\lng$)---and solid--liquid
equilibrium (SLE) sets the log-solubility to their negative sum:
\begin{equation}
  \lnx = -\underbrace{\frac{\dHfus}{\Rgas}\!\Big(\tfrac1T-\tfrac1\Tm\Big)}_{\Phicry(T)\ \text{(crystal, depends on $s$)}}
         \;-\;\underbrace{\lng(x_2,T)}_{\text{activity, depends on }(s,v)} ,
  \label{eq:sle}
\end{equation}
with the constant-$\Delta C_p$ (heat-capacity-of-fusion) correction omitted for
clarity. The activity term is produced by a \emph{differentiable closure} $g$:
a graph encoder predicts a $\sigma$-profile---the distribution of screening
(surface) charge density over a molecule, the histogram a COSMO model
consumes---for each molecule, and COSMO-SAC (the COnductor-like Screening
Model--Segment Activity Coefficient \cite{lin2002}) maps the pair of profiles to
$\lng$. Write $\zstar$ for the \emph{true} physical latent, the externally measured
$\sigma$-profile from the VT-2005 database (the Virginia Tech 2005 compilation of
quantum-chemically computed screening-charge profiles for ${\sim}1{,}400$
compounds \cite{mullins2006}), and $g(\zstar)$ for the closure output when fed the
true profile. The model's own head instead predicts a \emph{learned} profile
$\hat z$ and evaluates $g(\hat z)$; a matched \emph{DirectGNN} control shares the
encoder but emits $\lnx$ with no closure, and is the reference for ``what the
physics bottleneck costs'' (Fig.~\ref{fig:arch}).

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_architecture.pdf}
\caption{The model and the grounding intervention. Solute and solvent graphs pass
through a shared encoder to a crystal head (the ideal term $\Phi(T)$) and a
$\sigma$-profile head; the predicted profile $\hat z$ feeds the fixed COSMO-SAC closure
$g$, which returns $\ln\gamma_2$, and an SLE solver combines the two into $\lnx$. Input
insufficiency $\Binsuf$ enters at the intermediate $\hat z$ and closure misspecification
$\Bclos$ at $g$. The $\sigma$-oracle replaces $\hat z$ with the true VT-2005 profile
$\zstar$ (salmon). DirectGNN (grey) is the matched control---it shares the encoder but
drops the closure, so the gap to the physics path is the physics tax
$R_{\mathrm{phys}}-R_{\mathrm{direct}}$.}
\label{fig:arch}
\end{figure}

\paragraph{The grounding intervention.} Feeding $\zstar$ in place of $\hat z$
(a $\sigma$-oracle) isolates the closure from encoder error: if the closure were
well specified, $g(\zstar)$ should be the best available activity estimate. The
paradox is that it is not.

\paragraph{Data.} Solubility measurements are a Bemis--Murcko scaffold split of
BigSolDB \cite{krasnov2025bigsoldb} (grouping molecules by ring-system-plus-linker
scaffold, side chains stripped), so test solutes share no scaffold with training;
the activity labels $m$ are experimental infinite-dilution activity coefficients
($\lngi$, IDAC) compiled from ThermoML (the NIST/IUPAC archive of experimental
thermophysical data \cite{frenkel2011thermoml}), and the true $\sigma$-profiles
$\zstar$ are the VT-2005 database.

\begin{table}[t]
\centering
\caption{Notation and coined terms used throughout.}
\label{tab:notation}
\begin{tabular}{ll}
\toprule
Symbol / term & Meaning \\
\midrule
$g$ (closure)          & fixed, non-fitted physical map (COSMO-SAC; a Hammett LFER for pKa) \\
$h$ (encoder)          & learned graph network, $x\mapsto z$ \\
$\zstar$, $\hat z$     & true (oracle) vs.\ learned physical intermediate (the $\sigma$-profile) \\
$m$                    & activity target (experimental $\lngi$) \\
$\Bclos$               & closure misspecification, $\Ex[(\Ex[m\mid\zstar]-g(\zstar))^2]$ \\
$\Binsuf$              & input insufficiency, $\Ex[\Var(m\mid\zstar)]$ \\
$\Gamma$ (grounding gap) & $R_{\mathrm{orc}}-R_{\mathrm{e2e}}$: oracle risk minus best end-to-end risk \\
$\Delta_\infty$ (physics tax) & asymptotic risk of the physics arm minus the matched control \\
full / res             & COSMO-SAC with / without the Staverman--Guggenheim combinatorial term \\
Tier~1 / Tier~2        & convention-independent (load-bearing) / convention-robust corroboration \\
\bottomrule
\end{tabular}
\end{table}

\section{Theory: locating a physics prior's ceiling in a composed predictor}
\label{sec:framework}

The results below are stated for a general \emph{composed predictor} $\hat y=g(h(x))$
in which a fixed, non-fitted map $g$---the \emph{closure}---consumes a learned physical
intermediate $z=h(x)$, and are compared against a matched free predictor that shares the
encoder but drops $g$. Solubility, with $g$ the COSMO-SAC closure and $z$ the
$\sigma$-profile, is one instance; \S\ref{sec:pka} gives a second (pKa through a fixed
Hammett closure), and \S\ref{sec:dial} a controlled synthetic family. Nothing in
Lemmas~\ref{prop:ident}--\ref{prop:decomp}, Theorem~\ref{thm:gap}, or
Proposition~\ref{prop:tax} is specific to COSMO-SAC. Two quantities organize the section:
the closure/insufficiency split $B=\Bclos+\Binsuf$, which is the assumption-light
\emph{instrument}, and the observable grounding gap $\Gamma$, which is a \emph{symptom} of
closure misspecification whose validity we characterize (Fig.~\ref{fig:composed}).

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_composed.pdf}
\caption{The general composed predictor $\hat y=g(h(x))$ and its matched control. A learned
encoder $h$ produces a physical intermediate $z$; a \emph{fixed} closure $g$ maps it to the
prediction. Input insufficiency $\Binsuf$ lives in $z$ (what the intermediate cannot resolve)
and closure misspecification $\Bclos$ in $g$ (where the fixed map is wrong); an external oracle
$\zstar$ lets us measure the split. The matched control (grey) shares $h$ but drops $g$, so the
gap between them is the ``physics tax.'' The same abstraction instantiates as solubility, pKa,
and the synthetic dial (table).}
\label{fig:composed}
\end{figure}

\subsection{Why a free head can compensate: structural non-identifiability}
The crystal/activity split is not identifiable from solubility data alone, which
is precisely why an unconstrained head can silently absorb crystal error into the
activity term (Fig.~\ref{fig:ident}).

\begin{figure}[t]
\centering
\includegraphics[width=0.94\linewidth]{figs/fig_ident.pdf}
\caption{Why the crystal/activity split is non-identifiable from solubility.
\emph{(a)}~As functions of $1/T$ the crystal direction $\Phi$ and the activity direction
$\ln\gamma^\infty$ are nearly collinear, so a whole band of crystal/activity splits fits the
data equally well (the profiled Fisher information for $\dHfus$ is zero, Lemma~\ref{prop:ident}).
\emph{(b)}~An external single-component label points off that axis and collapses the band to a
point, pinning the split---the mechanism behind the aux-stream grounding, with the measured
Cram\'er--Rao drop on $\dHfus$.}
\label{fig:ident}
\end{figure}
\begin{lemma}[Structural non-identifiability of the SLE split]\label{prop:ident}
Consider the infinite-dilution SLE observable $\mu(T)=-\Phicry(T)-\lngi(T)$ with
$\Delta C_p=0$, so that $\Phicry(T)=(\dHfus/\Rgas)(1/T-1/\Tm)$ is affine in $u=1/T$,
and a symmetric two-suffix activity class ($\lng=\lngi\,(1-x_2)^2$) in which
$\lngi(T)$ is affine in $u$. Then, for any design with at least two distinct
temperatures, $\mu$ depends on the parameters only through the two-dimensional
statistic (its slope and intercept in $u$), and the activity intercept and slope
already span that plane;
hence the crystal parameters $(\dHfus,\Tm)$ are unidentified, the profiled Fisher
information for $\dHfus$ is exactly zero, and the indeterminacy set is
two-dimensional. The deficiency is closed only by a rank-completing external
constraint (a direct crystal label, or a fixed activity slope). At finite dilution
the composition factor $(1-x_2)^2$ breaks the exact affinity, leaving a non-zero but
near-degenerate information of order $x_2^2$ (numerically, condition number
$2.3\times10^{5}$; \S\ref{sec:ident}).
\end{lemma}
\noindent This is the standard structural-identifiability picture for a
collinear design \cite{raue2009,villaverde2016}; we prove it in the SLE form
(App.~\ref{sec:si-proofs}) and audit it numerically in \S\ref{sec:ident}. It is
\emph{enabling background},
not our headline: it explains why grounding an under-determined factor on
external single-component data is necessary, and why compensation between the
factors is expected. By itself it says nothing about whether grounding helps.
That depends on whether the closure that consumes the grounded inputs is
correctly specified, which is the question we make measurable next.

The non-identifiability is not binary but graded by how much of the crystal
direction the activity class can absorb.
\begin{lemma}[Class-dependent efficient information]\label{prop:semiparam}
Let the activity nuisance lie in a class $\mathcal H$ with tangent projection
$\Pi_{\mathcal H}$, and let $v=\partial\mu/\partial\dHfus$ be the crystal score.
The efficient information for $\dHfus$ is
$\mathcal I_{\mathrm{eff}}(\dHfus)=\sigma^{-2}\,\lVert v-\Pi_{\mathcal H}v\rVert^2$.
If $\mathcal H$ contains the crystal score's span $\{1,1/T\}$ (a free intercept and slope) then
$\Pi_{\mathcal H}v=v$ and $\mathcal I_{\mathrm{eff}}=0$, recovering
Lemma~\ref{prop:ident}; if $\mathcal H$ is restricted (the slope is known or
constrained) then $\mathcal I_{\mathrm{eff}}>0$ and the Cram\'er--Rao lower bound (CRLB) on
$\dHfus$ is finite. (The melting point $\Tm$ is likewise an unknown crystal parameter, but is
profiled jointly: its score $\partial\mu/\partial\Tm$ lies in $\mathrm{span}\{1\}\subset
\mathrm{span}\{1,1/T\}$, already inside the nuisance tangent whenever the slope is free, so the
dichotomy is unaffected by treating $\Tm$ as free.)
\end{lemma}
\noindent In words: the efficient information is what the data can still pin down
about the crystal term \emph{after} the activity model has absorbed everything
within its reach. If that model can fit a free intercept and slope in $1/T$, it
absorbs the entire crystal direction and nothing identifies $\dHfus$
($\mathcal I_{\mathrm{eff}}=0$); constraining the slope leaves a little information
behind. The proof is the efficient-score construction (App.~\ref{sec:si-proofs}):
project the crystal score onto the orthogonal complement of the nuisance tangent
space; the squared norm of the residual is the efficient information. The numerical
audit of \S\ref{sec:ident} quantifies both regimes, and shows the honest corollary:
merely \emph{restricting} the activity slope leaves $\mathrm{sd}(\dHfus)\approx8{,}251$\,J/mol
--- finite but unusable --- whereas \emph{external} crystal labels ($\Tm$ and $\dHfus$ together), which
contribute a score direction outside the activity tangent space, close it to
$\approx1{,}689$\,J/mol.

\subsection{The closure--insufficiency decomposition}
\label{sec:decomp}
Let $m$ be the activity target (experimental $\lngi$) and $g(\zstar)$ the closure
evaluated on the true latent. The excess risk of the true-input path over the
best-possible predictor $\Ex[m\mid\cdot]$ (the Bayes predictor) decomposes
exactly---into a part the intermediate cannot resolve and a part the fixed map gets
wrong (Fig.~\ref{fig:decomp-concept}).

\begin{figure}[t]
\centering
\includegraphics[width=0.82\linewidth]{figs/fig_decomp_concept.pdf}
\caption{What the closure/insufficiency split means. The activity target $m$ is scattered
against a one-dimensional summary of the physical intermediate $\zstar$. The vertical spread
of the conditional cloud is the input insufficiency $\Binsuf=\Ex\,\Var(m\mid\zstar)$---what no
map of $\zstar$ can resolve---while the gap between the conditional mean $\Ex[m\mid\zstar]$ (the
best any map reaches) and the fixed closure $g(\zstar)$ is the closure misspecification $\Bclos$.
Feeding the true $\zstar$ through $g$ lands on the lower curve, off the conditional mean; that
displacement is the paradox. Schematic; the measured bounds are Fig.~\ref{fig:decomp}.}
\label{fig:decomp-concept}
\end{figure}

\begin{lemma}[Exact closure--insufficiency decomposition and its one-sided bounds]\label{prop:decomp}
For a \emph{fixed} (non-fitted) closure $g$,
\begin{equation}
  \underbrace{\Ex\big[(m-g(\zstar))^2\big]}_{\text{MSE of the true-input path}}
  \;=\;
  \underbrace{\Ex\big[\Var(m\mid \zstar)\big]}_{\Binsuf\ \text{(inputs cannot resolve)}}
  \;+\;
  \underbrace{\Ex\big[(\Ex[m\mid\zstar]-g(\zstar))^2\big]}_{\Bclos\ \text{(decoder mis-map)}} .
  \label{eq:decomp}
\end{equation}
The cross term vanishes identically because $\Ex[m\mid\zstar]-g(\zstar)$ is
$\zstar$-measurable and $\Ex[(m-\Ex[m\mid\zstar])\mid\zstar]=0$. The split is exact with no fitting of the closure $g$---the cross term vanishes because $g$ is
fixed and $\zstar$ is externally observed---so the total and $\Bclos$'s Jensen lower bound are
fit-free; $\Binsuf=\Ex[\Var(m\mid\zstar)]$, however, must be estimated with a conditional-variance
smoother whose choice affects the verdict (\S\ref{sec:measure}).
\end{lemma}
\noindent Equation~\eqref{eq:decomp} is a bias--variance split of model
discrepancy in the sense of Kennedy--O'Hagan \cite{kennedy2001} and
Brynjarsd\'ottir--O'Hagan \cite{brynjarsdottir2014}: $\Bclos$ is the systematic
discrepancy of a structurally fixed physical map, and its dominance over
$\Binsuf$ is the empirical content of ``the ceiling is the closure, not the
inputs.'' We do not claim \eqref{eq:decomp} as new; the affordance is that the
external groundedness of $\zstar$ lets us \emph{measure} the split rather than
assume it. This is the move's distinction from model-discrepancy calibration and auxiliary-data
uncertainty quantification, which use held-out physical observations to \emph{fit} a correction to a
discrepancy: here the closure is fixed and the latent itself is externally observed, so the map's
error is \emph{attributed} with no correction fitted---at the cost that the affordance runs only
where such an external latent oracle exists (fixed-closure predictors with a tabulated or measured
latent: LFERs, COSMO / group-contribution closures, thermodynamic cycles), not in gray-box or PINN
hybrids whose internal fields have no external ground truth. \textbf{The exactness is contingent on
$g$ being fixed}: for a
\emph{fitted} decoder the cross term does not vanish and a plug-in point
decomposition is invalid (we observe a spurious $-0.36$ cross term when
$\Ex[m\mid\zstar]$ is replaced by a fitted estimate). We therefore report
\emph{one-sided bounds}, not a point split (each proved in App.~\ref{sec:si-proofs}):
\begin{itemize}
  \item $\Bclos \ge (\Ex[m]-\Ex[g])^2$ by Jensen: a constant offset between the
  decoder's mean output and the target is misspecification, not input insufficiency, and
  this bound needs \emph{no smoothness assumption}. Its one requirement is that $m$ and $g$
  share the $\lngi$ reference state, which we check directly (\S\ref{sec:measure}).
  \item $\Binsuf \le \Ex[\Var(m\mid \mathrm{bin}(g(\zstar)))]$, a valid
  \emph{upper} bound (law of total variance, since a binning of $g(\zstar)$
  coarsens $\zstar$).
\end{itemize}
Because $\Binsuf=\Ex[\Var(m\mid\zstar)]$ depends only on $(m,\zstar)$, it is
\emph{independent of the closure convention}: any disagreement between closure
variants is decoder disagreement and lands in $\Bclos$.

\subsection{The grounding gap: a symptom of closure misspecification}
\label{sec:gap}
The paradox---true inputs hurting---is a measurable quantity, not a curiosity. Let
$\mathcal E$ be the encoder class ($h:x\mapsto z$) and define three population risks of
the activity target $m$: the oracle risk $R_{\mathrm{orc}}=\Ex[(m-g(\zstar))^2]$, the best
end-to-end risk $R_{\mathrm{e2e}}=\inf_{h\in\mathcal E}\Ex[(m-g(h(x)))^2]$, and the
free-predictor (black-box) risk $R_{\mathrm{free}}=\Ex[(m-\Ex[m\mid x])^2]$. The
\emph{grounding gap} is $\Gamma:=R_{\mathrm{orc}}-R_{\mathrm{e2e}}$; the paradox is
$\Gamma>0$ (a free learned head beats the true input pushed through the closure).
\begin{theorem}[Grounding-gap sandwich]\label{thm:gap}
Assume all risks are finite ($m,g(\zstar)\in L^2$; $g$ and each $h\in\mathcal E$
measurable), (A1) the true encoder is representable, $\phi^\star\in\mathcal E$ with
$\zstar=\phi^\star(x)$, and (A2) the intermediate is a lossless bottleneck,
$\Ex[m\mid x]=\Ex[m\mid\zstar]$. Then
\begin{equation}
0\;\le\;\Gamma\;\le\;\Bclos,\qquad
\Gamma=\Bclos-\big(R_{\mathrm{e2e}}-R_{\mathrm{free}}\big),
\label{eq:gap}
\end{equation}
with $\Gamma=\Bclos$ iff $R_{\mathrm{e2e}}=R_{\mathrm{free}}$ (the composed class realizes
the Bayes predictor by distorting the intermediate). In particular a well-specified closure
($\Bclos=0$) gives $\Gamma=0$; hence \emph{under A1--A2}, $\Gamma>0$ certifies $\Bclos>0$.
\end{theorem}
\noindent The proof is elementary (App.~\ref{sec:si-proofs}): $\Gamma\ge0$ because
$\phi^\star$ is a feasible encoder; $R_{\mathrm{e2e}}\ge R_{\mathrm{free}}$ because every
$g(h(x))$ is $\sigma(x)$-measurable; and $R_{\mathrm{free}}=\Binsuf$ under A2. Thus
$\Gamma$ is exactly the closure bias the encoder can \emph{absorb} by feeding $g$ a
wrong-but-useful intermediate---the formal content of Lemma~\ref{prop:ident}'s ``a free
head silently compensates.''

\paragraph{$\Gamma$ is a symptom, not a certificate.} Both assumptions are load-bearing and
neither holds cleanly here. A1 is \emph{empirically doubtful}: the encoder is transfer-limited (a linear probe onto
descriptors drops from train $R^2\approx0.76$ to test $R^2\approx0.45$ out of distribution, a proxy
for---not a direct measure of---representability of $\zstar$), making $\phi^\star\in\mathcal E$
unlikely across scaffolds, so effectively $\phi^\star\notin\mathcal E$ and even $\Gamma\ge0$
is not guaranteed. A2 is unverifiable without the oracle and physically dubious (the
$\sigma$-profile is a lossy summary of the graph). When A2 fails, $\Gamma>0$ \emph{conflates}
two causes: closure misspecification ($\Bclos>0$) and input insufficiency (a correct closure
driven off-support to recover information the intermediate discarded). A controlled construction
with a \emph{well-specified} closure and a lossy intermediate gives $\Bclos=0$ yet $\Gamma>0$
(\S\ref{sec:dial}, and the pKa ortho sweep of \S\ref{sec:pka}). Certification of
closure-dominance therefore rests on the convention-independent \emph{decomposition}
(Lemma~\ref{prop:decomp}), which needs neither A1 nor A2: in solubility the Jensen bound and
the insufficiency estimates place $\Bclos$ above $\Binsuf$ \emph{directly}
(\S\ref{sec:measure}), and the grounding gap only corroborates it. This is the paper's method:
\emph{the decomposition is the instrument, $\Gamma$ is the cheap symptom that motivates it, and
separating its two causes is what an externally measured latent buys.}

\subsection{The physics tax and the critical data budget}
\label{sec:tax}
The gap is a population statement; the deployed question is whether the \emph{trained} physics
model beats the \emph{trained} black box at sample size $n$. Writing each expected risk as an
asymptote plus estimation variance, $\Ex[\hat R_{\mathrm{phys}}(n)]\approx
R_{\mathrm{e2e}}+V_{\mathrm{phys}}(n)$ and $\Ex[\hat R_{\mathrm{direct}}(n)]\approx
R_{\mathrm{direct},\infty}+V_{\mathrm{direct}}(n)$ with $V(n)\downarrow0$, the \emph{physics tax}
is
\begin{equation}
\mathcal{T}(n)\approx\Delta_\infty+\big[V_{\mathrm{phys}}(n)-V_{\mathrm{direct}}(n)\big],\qquad
\Delta_\infty:=R_{\mathrm{e2e}}-R_{\mathrm{direct},\infty}.
\end{equation}
\begin{proposition}[Sign of the physics tax]\label{prop:tax}
(i)~$\mathcal{T}(n)<0$ (physics helps) iff $V_{\mathrm{direct}}(n)-V_{\mathrm{phys}}(n)>\Delta_\infty$:
the variance a constrained prior saves must exceed its asymptotic bias. (ii)~If
$\Delta_\infty>0$ and the variance gap vanishes ($V_{\mathrm{direct}}-V_{\mathrm{phys}}\to0$),
there is a critical budget $n^\star$ beyond which $\mathcal{T}(n)>0$ for all $n>n^\star$. (iii)~Since
$V_{\mathrm{direct}}-V_{\mathrm{phys}}\le V_{\mathrm{direct}}$, as the black box nears the
aleatoric floor ($V_{\mathrm{direct}}\to0$) the tax tends to $\Delta_\infty$; this limit is
$\ge0$ when the control is asymptotically Bayes ($R_{\mathrm{direct},\infty}=R_{\mathrm{free}}$),
not merely from $V_{\mathrm{direct}}\to0$.
\end{proposition}
\noindent When the control is asymptotically Bayes and A2 holds, $\Delta_\infty=\Bclos-\Gamma$,
tying the asymptotic tax to the \emph{un-compensatable} part of the closure bias; in general
$\Delta_\infty$ is read off the large-$n$ asymptote. Both corollaries are \emph{loosely consistent} with
the data---the data-efficiency null (a positive tax at every budget, \S\ref{sec:data-efficiency},
single-seed) and the novelty inversion (the tax heaviest on the \emph{best-covered} solutes,
\S\ref{sec:map}, exploratory) point in directions the proposition allows---but neither is a
controlled test: in the data-efficiency sweep the physics arm is far more under-converged than its
control at the shared light budget (its $R^2$ falls to $-0.79$ where the control holds near $+0.22$),
so the within-budget gap conflates inductive bias with optimization difficulty and we do not map it
to a specific case. We prove the structural parts (existence of $n^\star$ given
$\Delta_\infty>0$ and a vanishing variance gap; the floor inequality); the shapes of $V(n)$ are
empirical.

\subsection{Supporting structural facts}
Three further consequences of the same collinear geometry are used informally and
are not stated as theorems. \emph{(i)~Temperature extrapolation.} Within the
affine-in-$1/T$ activity family, same-pair prediction at a temperature $T^\star$
outside the training range is an affine extrapolation whose error is controlled by
the fitted-slope variance times $|1/T^\star-\overline{1/T}|$, whereas an
unstructured temperature encoder is unconstrained beyond its support. This is the
one place the physical \emph{structure} is expected to help; \S\ref{sec:supporting}
reports the class-level margin, which is a property of the hypothesis class and not
an accuracy claim for the trained model (indeed the trained models trail the
van't~Hoff baseline). \emph{(ii)~Geometry of compensation.} As the training window
$\Delta T\to0$, $1/T$ becomes nearly constant, the crystal and activity design
columns become collinear, and the smallest Fisher eigenvalue tends to zero (the
$\Delta T$ sweep of \S\ref{sec:ident} reaches conditioning $10^{16}$ at
$\Delta T=10$\,K), so an optimizer prefers to split error between the factors rather
than fit either correctly. \emph{(iii)~Grounding design.} Each single-component
stream rank-completes a different deficient direction of Lemma~\ref{prop:ident};
this is the design rule behind the auxiliary streams, though (as \S\ref{sec:e2}
shows for a dose-limited crystal run) it does not by itself promise an end-task gain.

\subsection{What we prove, and what we do not claim}
We prove the three structural lemmas the measurement rests on --- the split's
non-identifiability (Lemma~\ref{prop:ident}), its class-dependence
(Lemma~\ref{prop:semiparam}), and the exact closure/insufficiency decomposition with
its one-sided bounds (Lemma~\ref{prop:decomp}) --- together with the grounding-gap
sandwich (Theorem~\ref{thm:gap}) and the sign rule for the physics tax
(Prop.~\ref{prop:tax}); all proofs are elementary (App.~\ref{sec:si-proofs}). We are
deliberate about their reach. Theorem~\ref{thm:gap}
certifies closure misspecification \emph{only} under A1--A2, and both fail or are
unverifiable in our setting (A1 empirically, A2 in principle); we therefore rest
closure-dominance on the assumption-light decomposition and use $\Gamma$ as a corroborating
symptom, not a certificate. Proposition~\ref{prop:tax} predicts the \emph{sign} of the tax
from measurable quantities, not an optimum. In particular there is no ``optimal grounding''
theorem and no necessary-and-sufficient condition for the decomposition to improve
out-of-distribution accuracy --- the honest empirical answer (\S\ref{sec:results}) is that
it does not. The lemmas are read
against, not in competition with, known results on model discrepancy
\cite{kennedy2001,brynjarsdottir2014}, parameter sloppiness
\cite{transtrum2015,gutenkunst2007}, structural and practical identifiability
\cite{raue2009,villaverde2016}, estimation under misspecification \cite{white1982},
and underspecification \cite{damour2022}; the calibration-versus-accuracy separation
of \S\ref{sec:gateb} is the classical refinement decomposition of a probability
forecast \cite{murphy1973,degroot1983}.

\input{sections/methods}

\section{Results}
\label{sec:results}

Results build the map. The paradox (\S\ref{sec:paradox}) motivates; the decomposition then
\emph{measures} where the ceiling sits and a standard higher-fidelity closure moves it (the
closure-fidelity axis, \S\ref{sec:measure}); the compensating-surrogate mechanism
(\S\ref{sec:surrogate}) explains the end-to-end/low-fidelity corner where grounding hurts. The
remaining sections---the
synthetic dial (\S\ref{sec:dial}), the output-residual control (\S\ref{sec:gateb}), the pKa closure
(\S\ref{sec:pka}), and the data-efficiency and chemistry maps---stress-test and generalize the
account.

\subsection{The grounding paradox}
\label{sec:paradox}
% Artifacts: results/e5_sigma_grounding/seed_{42,43,44}/comparison.json (3-seed e5 lock, n=5608); aggregate THREE_SEED_SUMMARY.md via run_e5_comparison.py.
% Triple confirmation: eval-oracle / true-at-train / channel-swap.
Feeding true $\sigma$-profiles into the closure degrades it rather than improving it,
in both the activity and the solubility target, and it fails the way a \emph{misspecified
map} does---not the way missing information would. In the solubility target the effect
erases any positive $R^2$ (Fig.~\ref{fig:paradox}): the model predicts solubility better
\emph{from its own learned $\sigma$-profile} than when \emph{given the true measured one}
(MAE $1.85$ vs $2.25$, in $\lnx$ units). Two facts pin the cause on the map. First,
restoring the physically \emph{more} complete Staverman--Guggenheim combinatorial term
(the entropic contribution from solute--solvent size and shape mismatch) \emph{increases}
error---what a wrong map does, not what a missing input would. Second, on the activity
target ($\lngi$, $n{=}60$ matched pairs) the fixed true-input closure sits about $4\times$
above the irreducible-variance floor a flexible regressor reaches (\S\ref{sec:measure}),
under-predicting $\lngi$ with a systematic bias (Fig.~\ref{fig:parity}). The paradox survives three controls---the evaluation-time
oracle, injection of the true profile \emph{at training} (MAE $1.98$), and a channel-swap
($2.08$), all worse than the learned-$\sigma$ arm---ruling out a wiring artifact.
The $\sigma$-oracle per-arm values are in \S\ref{sec:si-tables}; the true-at-train and
channel-swap figures are the inline values above.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_paradox.pdf}
\caption{Controlled comparison on the scaffold split (3 seeds, mean$\pm$sd,
$n{=}5608$; metrics are the 3-seed mean of the cross-arm intersection lock --- rows
supervised and finite in every arm --- from \texttt{run\_e5\_comparison.py}).
DirectGNN shares the encoder but has no thermodynamic closure; the
physics closures trail it (the ``physics tax''). Replacing the model's learned
$\sigma$-profile with the \emph{true} VT-2005 profile (the $\sigma$-oracle,
salmon) gives the \emph{worst} arm, collapsing $R^2$ to $\approx0$ ($-0.03\pm0.04$):
by MAE the model predicts solubility distinctly better from its invented $\sigma$
($1.85$) than from the measured one ($2.25$). Per-arm exact values are transcribed
in the SI.}
\label{fig:paradox}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.72\linewidth]{figs/fig_parity.pdf}
\caption{Activity-coefficient parity on the $n{=}60$ matched pairs. The true-$\sigma$
COSMO-SAC closure $g(\zstar)$ (salmon) falls systematically below the diagonal at
larger $\lngi$, under-predicting the activity coefficient (MSE $1.80$, full
convention). This systematic downward bias is the visible form of the decoder
misspecification $\Bclos$; for reference a flexible regressor of $\lngi$ on the same
true profiles reaches the irreducible-variance floor (MSE $\approx0.40$, our $\Binsuf$
estimator; \S\ref{sec:measure}), so the closure's excess over that floor is $\Bclos$.}
\label{fig:parity}
\end{figure}

\subsection{Measuring the ceiling: the closure dominates}
\label{sec:measure}
% Artifact: results/b_insuff/decomposition.json via scripts/analysis/run_b_insuff_decomposition.py.
Because the true latent is externally observed, we can locate the prior's ceiling and attribute it to
the map rather than the inputs---the measurement instrument at the core of the map. We apply the
decomposition of \S\ref{sec:decomp} to the $n{=}60$ matchable activity pairs (298\,K, infinite
dilution), with $\zstar$ the true concatenated solute/solvent
$\sigma$-profiles and $m$ the experimental $\lngi$; Table~\ref{tab:decomp} reports the
bounds. The set spans 41 solutes and 17 solvents in $102$ profile dimensions, with two
formamide pairs dropped on a canonical-SMILES tautomer mismatch.

\begin{table}[t]
\centering
\caption{Closure--insufficiency decomposition on the true-input activity path.
Bounds, not a point split (Lemma~\ref{prop:decomp}). Convention ``full'' =
COSMO-SAC with the combinatorial term (size factor $r_i=V_i/r_0$ on the true
COSMO cavity volume, VT-2005); ``res'' = residual-only (the deployed default).
$\Binsuf$ upper bounds are convention-independent estimates of
$\Ex[\Var(m\mid\zstar)]$; ``k\textsc{nn}'' is a direct high-dimensional estimate
(biased upward), the others are law-of-total-variance / out-of-fold upper bounds.
The \emph{load-bearing} separation is the convention-independent
$\Bclos\ge\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ row, which exceeds $\Binsuf$ under
\emph{both} conventions ($1.24$, $0.91>0.62$); the assumption-free constant-offset
(Jensen) bound separates only under ``full'' ($0.72$) and inverts under the
deployed ``res'' ($0.43<0.62$).}
\label{tab:decomp}
\begin{tabular}{lrr}
\toprule
Quantity & full & res \\
\midrule
MSE$_{\text{total}}=\Bclos+\Binsuf$ (exact)      & $1.80$ & $1.47$ \\
$\Bclos$ lower via MSE$-\Binsuf^{\text{up}}$ (RF; \emph{$\Binsuf$ conv.-indep., load-bearing}) & $1.24$ & $0.91$ \\
$\Bclos$ lower bound (Jensen, offset$^2$; assumption-free; \emph{full-only}) & $0.72$ & $0.43$ \\
$\Binsuf$ upper (bin $g$; law of total variance)  & $0.53$ & $0.62$ \\
$\Binsuf$ upper (RF / Ridge out-of-fold)          & $0.56/0.62$ & same \\
$\Binsuf$ (k\textsc{nn}-in-$\zstar$, convention-indep., biased up) & $0.40$ & $0.40$ \\
label noise $\sigma_\eta^2$ (IDAC, unestimable here) & \multicolumn{2}{c}{assumed $0$ (no replicates in matched set)} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_decomposition.pdf}
\caption{One-sided bounds on the closure--insufficiency split under the \emph{deployed}
residual-only COSMO-SAC convention---the configuration the model actually runs. The
load-bearing, convention-independent bound $\Bclos\ge\mathrm{MSE}-\Binsuf^{\mathrm{LOTV}}\approx0.85$
exceeds every valid $\Binsuf$ upper bound (salmon region); the white gap is a separation that needs
no smoothness assumption, and the teal region is the certified closure share of the total error
(MSE $=1.47$). We headline the law-of-total-variance (LOTV) binning bound because it conditions on
the scalar $g(\zstar)$ and is immune to the near-duplicate leakage that deflates the
$z^\ast$-neighbour estimators ($k$-NN, RF; \S\ref{sec:measure}). The assumption-free Jensen
constant-offset bound is convention-specific and \emph{inverts} here ($0.43<0.62$, dotted), which is
why we do not lean on it.}
\label{fig:decomp}
\end{figure}

We grade the evidence in two tiers: \emph{Tier~1} claims hold for \emph{any} COSMO-SAC
configuration and are the load-bearing ones, while \emph{Tier~2} claims are
configuration-robust corroboration that is not strictly independent of the model fit.

\paragraph{The load-bearing separation (convention-independent, Tier~1).} Because
$\Binsuf=\Ex[\Var(m\mid\zstar)]$ depends only on $(m,\zstar)$, its upper bound is the
same in every closure convention; subtracting it from the total therefore lower-bounds
$\Bclos$ even for the model's \emph{deployed} residual-only convention, giving
$\Bclos\gtrsim0.91$, well above $\Binsuf\lesssim0.62$ (Table~\ref{tab:decomp}). This is the claim we lean
on, and it needs no smoothness assumption beyond the validity of the $\Binsuf$ upper
bound. We stress-test the separation directly (App.~\ref{sec:si-tables}), and doing so exposes an
identification hazard that dictates \emph{which} estimator to trust. The matched set is a crossed
$41$-solute$\times17$-solvent design, so any two pairs sharing a solute or a solvent share $51$ of
$102$ $\zstar$ coordinates exactly; the raw nearest neighbour of a pair is such a near-duplicate
for $98\%$ of pairs, which deflates the $\zstar$-neighbour estimators anti-conservatively---the
$k$-NN bound reads $0.40$, but forbidding shared-solute/solvent neighbours \emph{inflates} it to
$1.83$ as the neighbour gap takes over. Local conditional-variance estimators therefore
\emph{bracket} $\Binsuf$ across $\mathrm{MSE}/2$ and cannot pin it, so we do not lean on the $k$-NN
or random-forest out-of-fold values. We lean instead on the law-of-total-variance binning bound,
which conditions $m$ on the coarsened scalar $g(\zstar)$ rather than on $\zstar$-neighbours: it is a
valid upper bound on $\Binsuf$ (a binning of $g(\zstar)$ is a function of $\zstar$) and is
\emph{immune} to the shared-coordinate leakage. That bound is stable---it survives
leave-one-solvent-out ($17/17$ folds), leave-one-solute-out ($40/41$), leave-one-pair-out
($60/60$), and adversarial deletion of the eight highest-error pairs. For confidence, a single-axis
solvent-clustered bootstrap gives $P(\Bclos>\Binsuf)\approx0.89$; a two-way (solute$\times$solvent)
cluster bootstrap that also charges for repeated solutes lowers this to $P\approx0.78$ with a $90\%$
margin spanning zero---so we present the separation as a \emph{supporting} measurement, likely but
not certified at conventional confidence, not a stand-alone certificate. Stratifying the matched set by $\lngi$ under the same leakage-immune binning bound, the verdict
holds in every stratum and the closure lower bound still increases with $\gamma$ ($0.95\to2.69$);
but the higher-$\gamma$ margin is thin and estimator-dependent (the random-forest bound flips
there), so we read the shared cause with the all-$\gamma$ paradox as a conjecture supported by the
\emph{direction} of the in-regime signs, not a measured magnitude (App.~\ref{sec:si-tables}). Under the full COSMO-SAC convention the separation \emph{sharpens} to an
assumption-free constant-offset bound: a constant decoder offset alone accounts for
$\Bclos\ge0.72$ (Fig.~\ref{fig:decomp}), exceeding every $\Binsuf$ upper bound
($\le0.62$), and subtracting the insufficiency bound gives $\Bclos\ge1.24>0.62$
($\sim2\times$). That constant-offset bound is convention-specific and \emph{inverts}
under the deployed residual-only default ($0.43<0.62$; hedge~1), which is exactly why
we headline the convention-independent bound above rather than the constant offset.
Label noise is unestimable in this replicate-free set (the matched IDAC pairs carry no repeat
measurements at $298$\,K, so an inter-source floor cannot be formed as it was for solubility) and
assumed zero; the bounds we rely on are invariant to zero-mean noise. Because a replicate floor is
unavailable, we test exposure to the highest-scatter data directly: the separation is unchanged by
deleting any single pair ($60/60$) and by trimming the eight highest-error pairs---which include the
physically suspect associating outliers (e.g.\ $1$-hexanamine in methanol, $\ln\gamma^\infty{=}{+}2.62$
against its reverse $-0.97$)---so the ceiling is carried by the bulk of the associating systems, not
a few leverage points.

\paragraph{The closure defect is low-rank and correctable.} That $\Bclos$ dominates locates the
ceiling in the map; it does not say the map is \emph{fixably} wrong. It is. Fitting a small
structure-preserving residual to the fixed COSMO-SAC exchange-energy kernel---%
$\Delta=B\,\mathrm{diag}(a)\,B^{\!\top}$ over the $\sigma$-grid, symmetric and zero-initialised, the
same object a learned closure-correction (Arm~C) would carry---on the \emph{true} VT-2005 profiles
removes $56\%$ of the closure error out-of-sample (five-fold cross-validated, activity MSE
$1.47\!\to\!0.64$ held-out), and does so at \emph{rank one} ($52$ parameters): rank~2 adds nothing,
rank~3 begins to over-fit. (At $52$ parameters against $n{=}60$ this held-out reduction is
provisional pending a larger matched set, and we have not yet separated the kernel correction from a
per-solvent mean-offset removal.) So $\Bclos$ is not diffuse model error but a low-dimensional, correctable
defect of the map---addressable with a handful of parameters placed on the closure, whereas the same
error is only made worse by feeding the inputs truthfully (\S\ref{sec:paradox}) and untouched by an
output residual (\S\ref{sec:gateb}). This is the constructive complement to the paradox, on the
true-input set where the closure error lives; whether an end-to-end trained model realises the same
gain is a compute-gated question we leave open. (The map correction and the input compensation of
\S\ref{sec:surrogate} occupy different $\sigma$-grid footprints---they are two routes to the same
defect, not one.)

\paragraph{Convention-robust corroboration (Tier~2).} The genuinely
convention-robust fact is that adding the physically \emph{more} complete
Staverman--Guggenheim combinatorial term \emph{increases} error (activity MSE
$1.47\to1.80$)---what a misspecified map does, not what missing information would.
This is robust to the size-factor's volume basis: recomputing the combinatorial term
on the true COSMO cavity volumes (VT-2005 $V_{\mathrm{cosmo}}$) rather than an RDKit
molar volume leaves it essentially unchanged (Jensen $0.714\to0.717$, MSE
$1.793\to1.800$), because the term depends on molecular-size \emph{ratios} in which a
near-uniform volume rescaling cancels. The increase is, however, concentrated on
size-asymmetric pairs (its magnitude correlates with $|\ln(V_2/V_1)|$ at $r{=}0.33$ and is
$11\times$ larger in the high-asymmetry half), i.e.\ it is the documented miscalibration of the
\emph{unmodified} combinatorial term for size-asymmetric mixtures---the reason
modified-Staverman--Guggenheim and free-volume corrections exist---so we read it as corroboration of
closure misspecification, not an independent second effect. The residual (deployed) error is itself
localized, and it is not an implementation artifact: our differentiable layer reproduces the NIST
benchmark COSMO-SAC-2002~\cite{bell2020cosmosac} to RMSE $0.003$~$\ln\gamma$ on identical VT-2005
profiles ($0.005$ even in the associating regime, max $0.02$), and self-solvation
$\ln\gamma^\infty(i,i)$ is exactly zero. Yet $90\%$ of the squared error falls on strong hydrogen-bond-donor
solvents while acceptor-only solvents are near-exact (mean squared error $0.09$)---placing $\Bclos$
in COSMO-SAC's documented single-parameter hydrogen-bonding kernel, a real but \emph{known}
limitation rather than an idiosyncrasy of our differentiable layer. A second observation is consistent but is
\emph{not} independent of the decomposition: a flexible regressor of $m$ on $\zstar$
fits the activity target $\sim$4$\times$ better than the fixed true-input closure---but
that regressor is exactly our convention-independent $\Binsuf$ estimator
($\approx0.4$), so ``$4\times$ better'' restates that the closure sits far above the
irreducible-variance floor (that $\Bclos$ is large) rather than corroborating it by an
independent route. We therefore lean on the dimension-robust random-forest / ridge
out-of-fold and binning bounds and on the combinatorial effect, which agree that
$\Bclos$ exceeds $\Binsuf$.

\paragraph{The ceiling moves with closure fidelity: a standard-closure control.} The 2002
closure-dominance above is not a general fact about physical inputs; it is specific to the deployed
map's single-parameter hydrogen-bonding kernel, and the field already has better-parameterized
closures built for exactly that defect. Running the NIST reference implementation of COSMO-SAC-2010
(donor/acceptor-typed HB) and -dsp on the \emph{same} matched pairs (true $3$-profile inputs) cuts the
closure error from $\mathrm{MSE}=1.26$ to $0.77$/$0.79$---a ${\sim}70\%$ reduction of $\Bclos$ against
the unchanged $\Binsuf$ floor---and \emph{flips} the binding term: under 2010/dsp $\Bclos\approx0.20<
\Binsuf$, so the \emph{inputs}, not the closure, bind. (Our differentiable layer reproduces the
reference COSMO-SAC-2002 to RMSE $0.003$ $\ln\gamma$ on identical VT-2005 profiles, so the 2002 error
is genuine misspecification, not an implementation artifact.) The decision-relevant reading is thus not
``the closure always dominates'' but ``on a low-fidelity closure it does, and the fix is a better
closure, not truer inputs''---closure fidelity is the second axis of the map (\S\ref{sec:discussion}).

\paragraph{Scope and hedges.} An adversarial audit sharpened the scope without
inverting the verdict. The constant-offset bound is convention-specific---it inverts
under the deployed residual-only default, where the convention-independent bound
($\Bclos\ge0.91>0.62$) and the combinatorial fact carry the claim instead; the closure
error is chiefly a between-solvent bias (a solvent-conditional lower bound
$\Bclos\ge0.89$); the solvent-clustered confidence on the constant-offset separation is $P(\Bclos{>}\Binsuf)\approx0.85$ ($0.83$ vs the random-forest bound, $0.89$ vs the law-of-total-variance bound);
and the measurement is confined to the low-$\gamma$, $298$\,K, VT-2005-matchable corner,
so the numeric ceiling is not claimed to transfer to the finite-composition regime. The
four hedges are stated in full in App.~\ref{sec:si-tables}.

\input{sections/compensation-surrogate}

\input{sections/synthetic-dial}

\begin{figure}[t]
\centering
\includegraphics[width=0.78\linewidth]{figs/fig_fidelity_dial.pdf}
\caption{Reproducible closure-fidelity dial (\texttt{run\_fidelity\_dial.py}). As the
closure is made more misspecified (fidelity $F$ decreasing from $1$), the true-input
penalty $\Delta R^2=R^2_{\text{oracle}}-R^2_{\text{head}}$ grows monotonically, and the
same ordering holds across four non-chemistry teacher families (linear, monotone,
kinetics-exponential, PDE-field) and six misspecification functional forms. The effect is
a property of the fidelity magnitude, not of any one domain or deformation. The real
COSMO-SAC closure sits well inside the misspecified regime ($F\lesssim0.4$)---the sign and
rough magnitude of the empirical paradox, not a precise fidelity value.}
\label{fig:dial}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figs/fig_grounding_conflation.pdf}
\caption{The grounding gap $\Gamma$ is a symptom, not a certificate (\S\ref{sec:gap};
\texttt{run\_fidelity\_dial.py}). \emph{Left:} when the intermediate is sufficient (A2
holds), $\Gamma$ tracks the closure bias $\Bclos$ as fidelity varies, so
Theorem~\ref{thm:gap}'s proxy is valid. \emph{Right:} with a \emph{well-specified} closure
($\Bclos=0$) but a lossy intermediate (A2 fails), $\Gamma>0$ and grows with the discarded
variance---a positive grounding gap from pure input insufficiency (the worked
counterexample in the proof of Theorem~\ref{thm:gap}, App.~\ref{sec:si-proofs}). Hence certification of closure-dominance
rests on the decomposition, which needs neither assumption.}
\label{fig:conflation}
\end{figure}

\subsection{The ceiling is not fixable downstream (Gate B)}
\label{sec:gateb}
% Artifacts: results/e5_sigma_grounding/seed_42/grounded_a_truetrain_residual{,_v2}_predictions.summary.json (Gate B v1 gated-shut / v2 forced-open); model.py correction_force_open_gate.
If the closure binding were merely a bad point estimate, a learned output-space
correction on top of the physics prediction should recover accuracy. It does not.
An additive bounded residual with a learned confidence gate
($\hat y=\hat y_{\text{phys}}+(1-c)\,r$, with $y=\lnx$) leaves the valve
effectively shut (applied
residual $\approx0$). Forcing the gate open removes the systematic
\emph{bias} ($+0.45\to+0.06$) but recovers little \emph{accuracy} ($R^2$
$0.236\to0.274$ on the same rows, a marginal rise that leaves the fit far short of usable). This is the classical calibration-versus-accuracy
separation: when the upstream map is misspecified, a monotone or affine post-hoc
map can fix reliability but not resolution. Recalibration buys calibration
without buying accuracy, which is what we expect if the closure, rather than the
estimate, is the binding constraint.

\input{sections/pka-domain}

\input{sections/ident-compensation}

\input{sections/crystal-null}

\input{sections/data-efficiency}

\input{sections/chemistry-map}

\input{sections/supporting}

\section{Discussion}
\label{sec:discussion}
The result is a cautionary measurement for physics-informed modeling: grounding a
model on truer physical inputs is only as good as the closure that consumes them,
and when that closure is a fixed, mildly misspecified map, better inputs surface
its bias rather than help. The external latent does not make the physics path win. What it buys is
\emph{attribution}: on the infinite-dilution, low-$\gamma$ subset we can measure, the
ceiling sits in the decoder ($\Bclos$), with the inputs ($\Binsuf$) the less likely
source and label noise unestimable but assumed zero (and unable to manufacture the
constant offset the bounds rest on). We do not claim this numeric attribution transfers
unchanged to the finite-composition, high-$\gamma$ regime the solubility paradox itself
spans; that the solubility paradox shares the same closure-misspecification cause is a
conjecture supported by in-regime signs---the more-physics-worse effect on the full
$n{=}5608$ set and the chemistry map---not a measured fact. For practice this relocates the lever. Within this closure family, effort
spent on richer inputs or a better encoder is mis-targeted; the payoff is in the
closure itself, for example a corrected or learned residual on the map rather
than on its output (which \S\ref{sec:gateb} shows is inert).

\paragraph{The accuracy case is broad, with an explained boundary.} That the physics prior does
not improve accuracy is a pattern with no confirmed exception \emph{on accuracy}: the grounded
model trails the matched black box on the full corpus (and the external state-of-the-art (SOTA),
FastSolv and SolProp retrained on our corpus, does no better---though on a by-solute split that
bounds task difficulty rather than ranking head-to-head; App.~\ref{sec:si-baselines}), a
(dose-limited) attempt to ground the crystal factor on external labels did not move the ceiling
(\S\ref{sec:e2}, inconclusive), and---in the case built to favour a physics prior---it loses at
\emph{every} training-data fraction, including the low-data regime where an inductive bias should
help most (\S\ref{sec:data-efficiency}). The one axis on which the physical \emph{structure} does
help is temperature extrapolation---but as a hypothesis-class property that the trained models do
not exploit (\S\ref{sec:supporting}), a partial counterexample we flag rather than hide. Read
together these point to a \emph{structural} ceiling: the crystal/activity split is
non-identifiable from solubility alone (Lemma~\ref{prop:ident}), and the activity closure is
misspecified where we can measure it (\S\ref{sec:measure})---a measurement that feeds true inputs
through the fixed closure and so cannot be a training artifact---so within this family better
inputs, more data, and auxiliary grounding do not recover an accuracy the factorization cannot
express. The contribution is accordingly not
an accuracy result but a \emph{map} of when grounding a learned latent in true physics helps or hurts:
the externally grounded decomposition \emph{locates} the ceiling (measured, not proved), a standard
higher-fidelity closure moves it and flips the binding term, and---where the closure binds and the
latent is end-to-end---the ``physical'' intermediate behaves as a compensating surrogate; the two
governing axes are closure fidelity and latent supervision. What a misspecified closure costs is thus
not only accuracy but the interpretability the bottleneck appeared to buy.

\paragraph{When a physics prior must lose.} The tax rule (Prop.~\ref{prop:tax}) turns these
negatives into a phase picture rather than a single verdict (Fig.~\ref{fig:phase}), and says
\emph{when} to expect them. A fixed-closure prior helps only when the estimation variance it saves exceeds its
asymptotic bias $\Delta_\infty$ (equal to $\Bclos-\Gamma$ only under A2 with an
asymptotically-Bayes control, otherwise read off the large-$n$ asymptote); both are measurable. The variance benefit is
largest at small $n$ and vanishes as data accumulate or as the black box nears the aleatoric
floor, whereas $\Delta_\infty$ is set by the closure and does not. A prior is therefore
predicted to hurt precisely in the mature-data, low-noise, well-covered regime---exactly where
our data-efficiency null (a positive tax at every budget, \S\ref{sec:data-efficiency}) and the
novelty inversion (the tax heaviest on the best-covered solutes, \S\ref{sec:map}) place this
system, and where much of modern machine learning operates. The synthetic dial (\S\ref{sec:dial})
and the pKa closure (\S\ref{sec:pka}) trace the two governing axes---closure fidelity and
intermediate sufficiency---directly. We claim no universal boundary; we \emph{propose}, but do not here cleanly validate, a diagnostic for the sign of the physics tax:
measure $\Bclos$ through the decomposition, read $\Delta_\infty$ off the large-$n$ asymptote, and
the sign should follow---a prediction our own tests (single-seed, under-converged; \S\ref{sec:data-efficiency}) support only weakly. In this sense the failure is not idiosyncratic to
solubility; it recurs whenever a misspecified closure is coupled to an expressive encoder on
abundant, low-noise data.

\begin{figure}[t]
\centering
\includegraphics[width=0.82\linewidth]{figs/fig_phase.pdf}
\caption{When a physics prior helps versus hurts. The physics tax has sign
$\mathcal{T}=\Delta_\infty-(\text{variance saved})$: a fixed-closure prior helps only below the $\mathcal{T}=0$
diagonal, where the estimation variance it saves exceeds its asymptotic closure bias
$\Delta_\infty$ (the large-$n$ asymptote; $=\Bclos-\Gamma$ under A2). The variance saving shrinks as $n$ grows, so systems drift
left: solubility/COSMO-SAC sits deep in the ``hurts'' region and moves further in with more
data, pKa through a Hammett closure lands on the ``physics helps'' side as a \emph{trained}
comparison (\S\ref{sec:pka})---though a stratified check shows the help is not cleanly a
closure-fidelity effect---and the synthetic dial climbs out of ``helps'' as closure fidelity
$F$ falls. Conceptual---only the
sign is measured, not the boundary's exact shape.}
\label{fig:phase}
\end{figure}

\paragraph{Limitations.} (i)~$\Binsuf$ is bounded, never point-identified: there
are no true replicate measurements at fixed $\zstar$, so the insufficiency estimates
lean on a smoothness assumption. The constant-offset (Jensen) bound is smoothness-free
but assumes $m$ and $g$ share the $\lngi$ (mole-fraction, Lewis--Randall) reference
state; we check this two ways---the closure is nearly exact within the largest
single-solvent stratum (pyridine, MSE $0.07$), which a uniform reference-state offset
could not produce, and an independent recomputation reproduces the closure output---so
the mean offset is genuine decoder bias, not a units artifact. We headline the
convention-independent bound (\S\ref{sec:measure}) as the load-bearing claim.
(ii)~The assumption-free constant-offset margin is convention-specific (full COSMO-SAC)
and \emph{inverts} under the deployed residual-only default; there the separation rests
on the convention-independent bound $\Bclos\ge\mathrm{MSE}-\Binsuf^{\mathrm{up}}$ and
the more-physics-worse effect (which we verified is robust to the molecular-volume
basis). (iii)~The measurement is at infinite
dilution and 298\,K on the VT-2005-matchable, low-$\gamma$ subset; the non-ideal
amide-solvent regime is untested. (iv)~Absolute scaffold $\lnx$ accuracy is
encoder-bound; it sits well \emph{above} the inter-lab replicate-reproducibility floor
($0.15$--$0.31$ in $\lnx$; \S\ref{sec:supporting}), so label noise does not explain the ceiling,
but it is near the practical difficulty of new-solute extrapolation set by the current
state of the art. (v)~The second-edge surrogate result is the paper's weakest link: its converged
magnitude and clean one-model transferability are pending---a short run reaches only ${\sim}16\%$ of
the departure and ${\sim}1.7\times$ transferability, and the strong, transferable form is measured
only in confounded/two-model analyses. This paper is not an accuracy claim.

\section{Conclusion}
\label{sec:conclusion}
Feeding a physics-informed solubility model its true physical inputs made it
worse. What the paper contributes is a way to measure that failure: with an
externally grounded latent, the true-input excess risk splits into a
decoder-misspecification term and an input-insufficiency term, and on the
infinite-dilution, low-$\gamma$ subset we can measure, the ceiling is most likely
the closure rather than the inputs ($P\approx0.78$ under two-way solute$\times$solvent clustering,
$0.89$ single-axis; not certified at conventional confidence;
label noise unestimable and assumed zero). The
practical implication is to fix the map itself, since within this closure truer
inputs surface its bias and an output-space correction is inert. The same grounding also indicates,
where the closure binds, that the learned $\sigma$-profile is a closure-compensating surrogate rather
than the physical quantity---an interpretability cost of physics-informed bottlenecks whose converged
magnitude is still pending, but whose direction the external latent already exposes.

\section*{Data and code availability}
All code and the intermediate result artifacts needed to regenerate every figure and
table are available at \pending[repository URL]\ (commit \pending[hash]), released
under \pending[license]. The solubility corpus is BigSolDB~2.0
\cite{krasnov2025bigsoldb}; the true $\sigma$-profiles are the VT-2005 database,
ingested to \texttt{results/sigma\_profile\_artifact/sigma\_profiles.csv}; the
experimental infinite-dilution activity coefficients are compiled from ThermoML
(\texttt{notebooks/data/raw/idac.csv}). The Bemis--Murcko scaffold split is produced
by \texttt{scripts/prepare\_data.py}. The controlled comparison
(Fig.~\ref{fig:paradox}, \S\ref{sec:map}) is produced by
\texttt{scripts/experiments/run\_e5\_sigma\_grounding.sh} and aggregated by
\texttt{scripts/analysis/run\_e5\_comparison.py} over
\texttt{results/e5\_sigma\_grounding/seed\_\{42,43,44\}/}; the 3-seed aggregate is
tabulated in \texttt{results/e5\_sigma\_grounding/THREE\_SEED\_SUMMARY.md}. The
closure--insufficiency decomposition (Table~\ref{tab:decomp},
Figs~\ref{fig:parity} and~\ref{fig:decomp}) is regenerated deterministically by
\texttt{scripts/analysis/run\_b\_insuff\_decomposition.py} $\rightarrow$
\texttt{results/b\_insuff/decomposition.json} from the committed matched-pair table
\texttt{results/b\_insuff/matched\_pairs.csv} (the $n{=}60$ VT-2005-matchable pairs).
The second-domain pKa measurement (\S\ref{sec:pka}) uses the OPERA experimental compilation
\cite{mansouri2019opera}: \texttt{scripts/data/build\_pka\_hammett\_subset.py} assigns the oracle
Hammett $\sigma$ and \texttt{scripts/analysis/run\_pka\_real\_decomposition.py} produces the
decomposition ($\rightarrow$\texttt{results/pka\_hammett/real\_decomposition.json}), with the
controlled semi-synthetic validation in \texttt{scripts/analysis/run\_pka\_hammett\_probe.py} and
the trained encoder-vs-DirectGNN comparison in \texttt{scripts/experiments/run\_pka\_trained\_comparison.py}.
The synthetic fidelity dial (Fig.~\ref{fig:dial}) is
\texttt{scripts/analysis/run\_fidelity\_dial.py}, and the conceptual schematics
(Figs~\ref{fig:composed}, \ref{fig:decomp-concept}, \ref{fig:ident}, \ref{fig:phase}) and the
architecture figure (Fig.~\ref{fig:arch}) are \texttt{scripts/analysis/make\_concept\_figures.py}.
The model checkpoints and the full per-arm prediction CSVs are large and are deposited
at \pending[Zenodo DOI] rather than in git; the named scripts regenerate them from the
raw sources. The manuscript compiles with \texttt{make} in \texttt{paper/}.

\input{sections/SI}

\bibliographystyle{plain}
\bibliography{references_verified}

\end{document}
